/* Placeholder for the vendored htmx.min.js. The wheel build
   (trade-trace-1kkv.2 + .5) drops the real htmx 1.9.x file here
   so the Console MVP can fetch partial updates without a Node
   build pipeline. The placeholder exists so the static asset
   tree is well-formed when the source tree is checked out and
   so a missing-htmx situation degrades gracefully: console.js's
   `refreshNow()` falls back to a full reload when `window.htmx`
   is undefined.

   Source pin: https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js
   (SHA-256 to be recorded at vendor time per
   docs/architecture/console.md §4).
*/
(function () { /* placeholder; real htmx is vendored at build time */ })();
