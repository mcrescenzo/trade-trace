# v0.0.2 Gate 7 live-adapter evidence — 2026-05-26

## Scope

Manual HITL/live-adapter Gate 7 from `docs/RELEASE_CHECKLIST.md` using a disposable Polygon RPC configuration and public Polymarket Gamma market IDs.

Sanitization: this artifact intentionally omits raw RPC URLs, keys, bearer tokens, raw request/response bodies, and private credential file contents. It records only command names, public market IDs, sanitized statuses, and non-secret latency/result summaries.

## Provisioned public inputs

- Open market Gamma id: `540844`
  - conditionId: `0xbb57ccf5853a85487bc3d83d04d669310d28c6c810758953b9d9b91d1aee89d2`
  - question: Will bitcoin hit $1m before GTA VI?
- Resolved market Gamma id: `240380`
  - conditionId: `0x41190eb9336ae73949c04f4900f9865092e69a57cf9c942a6157abf6ae8d16c6`
  - question: [Single Market] Will Donald J. Trump win the U.S. 2024 Republican presidential nomination?
- Local-only provisioning evidence: profile-home `.trade-trace-gate7/provisioning-evidence.md`.

## Initial failed attempt and repair

The first Gate 7 attempt failed at `market.bind` against open market `540844` with sanitized error:

- code: `ADAPTER_PROTOCOL_ERROR`
- message: `Polymarket adapter accepts only binary markets in v0.0.2`
- details: `external_id=540844`, `outcome_count=13`

Public Gamma evidence showed `outcomes` was a JSON-encoded string (`["Yes","No"]`), not a native JSON array. Public Gamma also returned `404` for `/markets/540844/book`. The repair in this run normalizes Gamma JSON-string list fields and changes `snapshot.fetch` to derive snapshots from `/markets/{id}` market payloads instead of `/markets/{id}/book`.

## Successful Gate 7 rerun

Throwaway `TRADE_TRACE_HOME`: profile-home `.trade-trace-gate7-preflight-v002/gate7-live-smoke-20260526T1559Z`.

Call cap: 8 calls used / 12 allowed. Wall-clock elapsed: under 1 second / 300 seconds allowed.

| Step | Tool | Public target | Result | Sanitized summary |
|---:|---|---|---|---|
| 1 | `journal.init` | throwaway home | PASS | initialized local journal |
| 2 | `journal.config_set` | `network.polymarket.enabled` | PASS | adapter enabled |
| 3 | `journal.config_set` | `network.polymarket.gamma_base_url` | PASS | Gamma endpoint configured; raw value omitted |
| 4 | `journal.config_set` | `network.polymarket.polygon_rpc_url` | PASS | Polygon RPC configured; raw value omitted |
| 5 | `market.bind` | open Gamma id `540844` | PASS | state=`open`, mechanism=`clob`, latency≈126.6ms |
| 6 | `snapshot.fetch` | open market from step 5 | PASS | bid=`0.492`, ask=`0.493`, mid=`0.4925`, implied_probability=`0.4925`, latency≈60.3ms |
| 7 | `market.bind` | resolved Gamma id `240380` | PASS | state=`closed_for_trading`, mechanism=`clob`, latency≈206.1ms |
| 8 | `outcome.fetch` | resolved market from step 7 | PASS | status=`resolved_final`, outcome_label=`unknown`, latency≈3.3ms |

Gate 7 result: PASS for the required `market.bind`, `snapshot.fetch`, and `outcome.fetch` live adapter smoke path.

## Verification commands

- `pytest tests/integration/test_market_fetch.py tests/integration/test_snapshot_fetch.py tests/integration/test_adapter_polymarket_no_rpc.py tests/integration/test_resolution_fetch.py` → 26 passed.
- `ruff check src/trade_trace/tools/adapter_polymarket.py tests/integration/test_market_fetch.py tests/integration/test_snapshot_fetch.py` → passed.
- `mypy src/trade_trace/tools/adapter_polymarket.py` → passed.
- `pytest -q` → 1853 passed, 24 skipped.
- Independent read-only QA review of the adapter diff → PASS; targeted tests 21 passed and `git diff --check` passed.
- Secret scan: exact raw RPC URL not found in tracked git files; local-only evidence does not contain the exact raw RPC URL; `.env.gate7.local` is git-ignored.

## Caveats

- `outcome.fetch` returned `outcome_label=unknown` because the selected public Gamma resolved market does not expose a `winningOutcome` field in the adapter-consumed payload. The status path is still `resolved_final` and the tool returned a successful envelope; if future evidence needs a concrete winning label, select a resolved market whose payload includes that field or extend outcome derivation from final `outcomePrices`.
- The local env file is owned by `michael` with mode `0660`; the controller could read/write through group permissions but could not chmod it to `0600`. Tightening the file mode is still recommended but was not needed for Gate 7 execution.
