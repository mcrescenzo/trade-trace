# Friction registry (rolling)

> The dedup memory for the AX dogfood loop. **Read this at the start of every
> run (Phase A)** so the bot does not re-report or re-fix something already
> handled, and **update it at the end of every run (Phase D)** with whatever was
> found this run.
>
> One row per distinct friction item. Keep `id` stable once assigned
> (`AX-NNN`, monotonically increasing). When an item's disposition changes
> (e.g. open → fixed), update its row in place rather than adding a new one.

## Status legend

- `open` — observed, not yet addressed (still reproducible).
- `fixed` — resolved by a direct commit this loop (see `ref` for the SHA).
- `filed` — handed to Beads as a feature / major rework / design question
  (see `ref` for the bead id).
- `intentional` — confirmed deliberate; promoted to
  [`intentional-design.md`](./intentional-design.md). Do not re-report.
- `wontfix` — decided against, with reason in `ref`.

## Surface legend

`tool` (MCP tool behavior) · `schema` (tool schema/description text) ·
`error` (error message / `next_actions` hint) · `doc` (docs drift) ·
`cli` (CLI surface) · `onboarding` (cold-start/first-run) · `report` (report output).

## Registry

| id | first-seen run | surface | description | status | ref |
|----|----------------|---------|-------------|--------|-----|
| AX-001 | 2026-06-03-01 | report | bootstrap caveat codes are cryptic; no inline gloss/glossary | filed | trade-trace-o1wr |
| AX-002 | 2026-06-03-01 | tool | no live market discovery surface; bot must curl Gamma out-of-band | filed | trade-trace-663l |
| AX-003 | 2026-06-03-01 | schema | market.bind `example_minimal` carries ~32 fields, obscures the 4 required | filed (deferred) | trade-trace-mpsu — entangled: example_minimal is the json_schema_derive source, so trimming it shrinks the schema; needs decoupling (repo-wide) |
| AX-004 | 2026-06-03-01 | error | market.bind missing `source` → bare error, no allowed values | fixed | 4e4ea9c |
| AX-005 | 2026-06-03-01 | error | decision.add missing `type` (not `decision_type`) → bare error, no allowed values | fixed | 8e974a5 |
| AX-006 | 2026-06-03-01 | tool | `market_id` vs `instrument_id` naming inconsistency across tools | fixed | ff641a6 (closed trade-trace-nqyv) |
| AX-007 | 2026-06-03-01 | tool | paper_enter needs `thesis_id` but forecast.add returns `forecast_id` | fixed | 68fb687 (closed trade-trace-4x1b) |
| AX-008 | 2026-06-03-02 | report | unrealized PnL for a `no`-side position marks NO entry (0.875) vs YES price (0.12), no side conversion → phantom +$75.50 on a flat position; `decision.add` price convention for `no` undocumented | filed | trade-trace-ctvb (P1) |
| AX-009 | 2026-06-03-02 | tool | snapshot.fetch/fetch_series/market.refresh build the Gamma URL from `external_id`, ignoring `gamma_market_id`; namespaced external_id (per AGENT_GUIDE example) → HTTP 422 | fixed | 1ea628d |
| AX-010 | 2026-06-03-02 | schema | forecast.add `confidence_label` enum not in tool.schema; invalid value leaked raw SQLite CHECK error | fixed | 4bd01ce |
| AX-011 | 2026-06-03-02 | schema | snapshot.fetch/outcome.fetch advertise `idempotency_key` as optional/runtime-defaulted but reject calls that omit it (`auto_derivation_available:false`) | filed | trade-trace-2cmb (P2) |
| AX-012 | 2026-06-03-02 | report | open forecast with `resolution_at:null` never surfaces in report.work_queue as resolve_due (only report.lifecycle shows it) | filed | trade-trace-ptyi (design Q) |
| AX-013 | 2026-06-03-02 | report | skip/watch/hold decisions can't link `forecast_id`, so report.coach flags a forecasted-but-skipped market as unforecasted | filed | trade-trace-t9n5 (design Q) — fix verified live in run 2026-06-03-03: skip links forecast_id, coach no longer false-flags |
| AX-014 | 2026-06-03-03 | doc | AGENT_GUIDE `market.bind` example omitted now-required `state`+`mechanism`; a cold bot copying it gets VALIDATION_ERROR | fixed | c9383ff (+regression test) |
| AX-015 | 2026-06-03-03 | schema | `market.refresh` advertises only `market_id` required but rejects calls without `idempotency_key` (auto_derivation_available:false) — AX-011 class, tool the 2cmb fix missed | filed | trade-trace-4z2t |
| AX-016 | 2026-06-03-03 | tool | `market.refresh` with `idempotency_key` throws a RAW (non-envelope) error: event_type `market.refreshed` unregistered in events_semantic_keys; tool 100% unusable | filed | trade-trace-4z2t (P1) |
| AX-017 | 2026-06-03-03 | tool | no agent-surface way to retrieve a market's resolution_rule/description text (market.search/bind/snapshot.fetch don't expose it); can't responsibly forecast ambiguous markets | filed | trade-trace-n33z (P2) |
| AX-018 | 2026-06-03-03 | report | `report.work_queue` flags resolve_due_forecast on a still-live market (mid 0.505, accepting_orders:true) solely from resolution_at_missing (AX-012 inverse) | filed | trade-trace-fe2f (design Q) — re-confirmed run 2026-06-03-04 (fc_CheQKZ still flagged while mid 0.505) |
| AX-019 | 2026-06-03-04 | tool | `market.search` `query` is a silent no-op: routed to Gamma `/markets?q=` which has no free-text search and ignores unknown params; any query → identical default list. Discoverability broken + misleading. Real endpoint is `/public-search` (event-nested shape) | fixed | 782d4ec (+regression tests; closed trade-trace-yz3q) |
| AX-020 | 2026-06-03-04 | tool/error | `resolve_due_forecast` obligation has no in-surface path to outcome evidence when Polygon RPC is unset: `outcome.fetch` fails closed (CONFIG_REQUIRED), nothing points to `snapshot.fetch` (Gamma) as the no-RPC alternative; feeder dead-ends, forecasts never score | filed | trade-trace-isqo (design Q) |
| AX-021 | 2026-06-03-05 | tool/schema | `market.bind` first-bind Gamma `/markets/{id}` lookup is built from `external_id`, ignoring the caller's `gamma_market_id`; the AGENT_GUIDE's own bind example (`external_id:"polymarket:<id>"` + bare `gamma_market_id`) → ADAPTER_PROTOCOL_ERROR 422. AX-009 class on the path that fix missed. Also: schema/docstring claimed "Manual/local only: no network" but the adapter-enabled path makes a read-only Gamma call (`bound_via:adapter`) | fixed | f2313e1 (+regression test; corrected stale description) |
| AX-022 | 2026-06-03-05 | report/onboarding | cold `report.bootstrap` on an empty journal surfaces only read/continuity `suggested_process_calls` (work_queue/next_actions/recall_receipts/strategy.show); no first-run entry-point hint (`market.search`→bind→forecast), so a bootstrap-first cold bot hits empty queues with no forward action | filed | trade-trace-xqjv (design Q) — re-confirmed run 2026-06-03-06 (cold probe) |
| AX-023 | 2026-06-03-06 | tool/error | `market.bind` adapter path (`bound_via=adapter`) created only the `markets` row, not the compatibility `instruments` row, so `forecast.add`/`decision.add` on the freshly-returned `market_id` failed `NOT_FOUND "instrument_id not found"` until a `snapshot.fetch` lazily materialized it (`_ensure_market_instrument`). Misleading error + contradicts the bind docstring's "prerequisite for forecast.add" promise. Manual path already did this via `_ensure_market_bind_prerequisites`. | fixed | 9b05e73 (+regression test; reverted-test confirms repro) |
| AX-024 | 2026-06-03-06 | tool | `memory.recall` BM25/FTS5 multi-word queries are conjunctive (implicit AND): every token must co-occur in one node or BM25 yields nothing; dotted/underscored tokens split oddly (`NOT_FOUND`→`not_found`). Fused mode masks it (temporal/graph still return results), so natural-language recall silently degrades to recency-only; LIKE fallback only fires on malformed MATCH, never on a valid zero-result query. Verified live (bm25-only: `bitcoin`✓, `snapshot`✓, `forecast.add instrument_id not found market.bind`→0, `instrument snapshot ordering trap`→0). | filed | trade-trace-95ry (P2) |
| AX-025 | 2026-06-04-01 | report | `report.open_positions`/`report.current_exposure` row-level `unrealized_pnl` stayed `null` and kept the `open_no_mark` caveat ("no current mark") even when `mark_state:"available"` + a populated `latest_mark` were on the same row. Root cause: the positions projection only marks `unrealized_pnl` at rebuild time, so a position opened before its first snapshot is never re-marked when a later snapshot lands. Cross-cutting follow-up (other PnL readers still stale): `trade-trace-pr2j`. | fixed | 2315e62 (+regression test) |
| AX-026 | 2026-06-04-01 | tool/schema | `market.search` query is matched conjunctively by Gamma `/public-search` (ALL terms must co-occur in one market): `bitcoin ethereum price`→0 while `bitcoin`→6 and `bitcoin price`→6. A bot's natural multi-keyword query silently dead-ends with no narrow-your-query signal. Fix adds a point-of-failure `search_hint` on zero-result multi-word queries + documents the behavior; matching is Gamma-side and unchanged. | fixed | 7341ab9 (+regression test) |
| AX-027 | 2026-06-04-02 | tool/report | `snapshot.fetch` derived `snapshots.price` (the canonical YES mark the positions projection + PnL reports value open positions against) from Gamma `lastTradePrice` over the book mid. A live ETH market printed lastTrade=0.49 while the two-sided book was 0.41/0.44, so a 0.44 YES paper entry reported `unrealized_pnl=+$1.00` while the same snapshot's `mid`/`implied_probability` (0.425) said it was underwater — and 0.49 was above the current ask (un-executable). Fix anchors `price` to the mid (`mid` already falls back to last/raw price when no two-sided book exists, so no-book markets are unchanged). Distinct axis from AX-025 (when marked) and pr2j (which readers): this is *which price*. | fixed | 752a4bf (+2 regression tests) |
| AX-018 (dup) | 2026-06-04-02 | report/error | Re-observed via a **second** trigger code `resolution_at_due_without_score` (not only `resolution_at_missing`): `fc_rHrzE9WuUh5qmILR`/`mkt_39bP49PZ8zJ2t5Jx` flagged resolve_due while live (mid 0.395); `report.coach` also nags "resolve or supersede" with no true-close / no "market still live" state. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-017 (dup) | 2026-06-04-02 | tool/schema | Fresh instance: bound `mkt_oMvnHMf7r4kB1Fn4` (ETH ≥ $2,000) — Gamma `description` (exact Binance touch rule) present in `venue_metadata_json` but structured `resolution_rule.text` is null. Minimal fix candidate (map description→resolution_rule.text) appended to n33z. Re-confirmed run 2026-06-04-03 on `mkt_JnHO0PZg7Q9_kLc0` (CPI ≤3.3%; full BLS rule in description, `resolution_rule.text` null). | filed | trade-trace-n33z |
| AX-028 | 2026-06-04-03 | report | `report.open_positions`/`report.current_exposure` left an open row's stale **non-null** `unrealized_pnl` in place while attaching a fresher `latest_mark` — intra-row contradiction (live: ETH row `latest_mark.price=0.415` but `unrealized_pnl=+$1.00`, marked against the prior 0.49 snapshot). The AX-025 fix only re-marked the null-PnL case. Fix: shared `_position_row_payload` now always re-marks side-aware from the latest mark's YES-contract price. Distinct from AX-025 (null case) and AX-027 (which *price*); this is *staleness of a non-null stored mark*. `report.pnl` remains under pr2j. | fixed | 3824370 (+regression test; reverted-fix confirms repro) — re-verified live run 2026-06-04-04 (fresh probe `pos_CwabrU-E9BKf00jk` marked -$0.01 against mid 0.4335) |
| AX-029 | 2026-06-04-04 | tool | `report.lifecycle` has no pagination (only `filter`/`states`; `truncated:False` hardcoded) and double-serializes every case (`groups` + `lifecycle_cases`), so its full output (~51KB at just 22 cases) exceeds the MCP token cap and fails at the transport layer with no partial result and no narrowing hint. Unlike `report.open_positions`/`report.work_queue` it lacks `limit`/`cursor`. No honest small fix (result too large for an in-result caveat to surface); real fix is pagination + de-dup, a design-level envelope rework. | filed | trade-trace-hv19 (P2) |
| AX-030 | 2026-06-04-05 | tool/schema | `resolution.add`/`outcome.add` auto-derived MCP schema omits `confidence` (and `settlement_price`/`metadata_json`/`resolution_source_url` shown in `example_rich`), yet auto-scoring **requires** `confidence>=0.9` + a binary label. A bot calling with only the advertised/required fields gets `auto_scoreable:false`, **no `forecast_score`, and no error** — the run-fatal silent trap (intentional-design #4 documents the *gate*, not that the field is hidden). Confirmed live: only the call carrying the unadvertised `confidence=0.95` scored fc_rHrzE9WuUh5qmILR (Brier 0.3969). Fix: point-of-failure `auto_score_skipped_reason` on the result + `confidence` documented in `example_rich`. | fixed | d03fd22 (+3 regression assertions) |
| AX-031 | 2026-06-04-05 | tool/adapter | `snapshot.fetch` `_snapshot_from_raw` built depth as `... or raw`, so a market lacking `book`/`liquidity`/`orderBook`/`depth` (sports + closed/resolved markets) dumped the entire ~5KB raw Gamma payload (conditionId, clobTokenIds, full description, outcomePrices…) into `liquidity_depth_json`. Live markets with a numeric `liquidity` were clean, so it only surfaced on closed markets (observed on the resolved Knicks/Spurs market). Pollutes a depth column + bloats snapshot-embedding reports. Fix: drop the `or raw` fallback; store `{}` when no depth field exists. | fixed | 9e10fc2 (+regression test) — re-verified clean run 2026-06-04-06 (live Knicks-finals + Fed binds both had numeric `liquidity_depth_json`, no raw dump) |
| AX-032 | 2026-06-04-06 | error | `report.lifecycle` `states` rejected an unsupported value (`"active"`) with `VALIDATION_ERROR "unsupported lifecycle state(s): ['active']"` — echoed the offending value but did **not** list the allowed enum, so a bot must discover valid states by trial-and-error. Same self-documenting-error class as the fixed AX-004/005 bind/decision enum errors. `allowed_states` was already in scope; fix appends `allowed states are [...]` to the message. | fixed | 02150f3 (+regression test) |
| AX-033 | 2026-06-04-07 | tool/report | `decision.add(paper_enter)` always mints a fresh `position_id` (`decision.py:217-231`) and never looks up an existing open position on the same instrument+side; no paper `add` increases an existing paper position. Live: a 20-unit YES probe (`pos_dTqU6DR07HnbZHvd` @ 0.37) opened on `mkt_7tACn4aqhw6y8BwN` while a prior-run 100-unit YES position (`pos_EmNSXb2bDMWpFdub` @ 0.54) was already open, with **no** `already_exposed` signal at decision time — though the decision matrix defines `already_exposed` as a materiality reason. Exposure nets correctly in reports (120 YES, side-aware to mid), so not a marking bug (distinct from AX-025/027/028); the gap is autonomous-feeder safety/ergonomics. Filed as a design question, not a direct fix (position lifecycle semantics). | filed | trade-trace-scx8 (design Q, P3) |
| AX-034 | 2026-06-04-08 | tool/report | `report.current_exposure` (docstring's *"recommended trader-agent entry point for open trades/current exposure"*) and the unbounded `report.open_positions` default both exceed the MCP token cap once positions accumulate. Live with **9** open paper positions: `current_exposure`→~69KB (fails; schema has **no `limit`/`cursor`** for exposure buckets → unrecoverable), `open_positions` default→~61KB (fails; but it accepts `limit`/`cursor`, so `limit=3`+page works). Root cause (`portfolio_exposure.py`): triple serialization — `groups`==`event_exposure_sets` same object (open_positions 311-312, current_exposure 787-788) + the `open_positions` array; each row carries two 78-digit CLOB token IDs. Extra hazard: `current_exposure` passes `limit` through (728) but **not** `cursor` and returns **no `truncated`/`next_cursor`** (764-799) → a limited call silently drops exposure (correctness risk). Same token-cap envelope class as AX-029/hv19 (report.lifecycle), different surfaces; no honest small fix (real fix = pagination + envelope de-dup, design-level). | filed | trade-trace-lszg (P2); cross-linked trade-trace-hv19 |
| AX-017 (dup) | 2026-06-04-09 | tool/schema | New market category: bound US recession (`mkt_33hPArFcrg_A1Hbn`) and UK recession (`mkt_VMoCTL05SJkKDfWJ`) — both `resolution_rule.text` null while full BEA/NBER (US) and ONS (UK) two-consecutive-negative-quarter rules sat in `venue_metadata_json.description`. Rule-sensitive macro markets where the gap is most costly (US "advance estimate counts" nuance unreadable from structured fields). Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-018 (dup) | 2026-06-04-10 | report | **Strengthened.** `report.bootstrap`/`report.work_queue` surfaced THREE resolve_due_forecast obligations, all from `resolution_at_missing`; two are 2027-close recession markets (`mkt_33hPArFcrg_A1Hbn`, `mkt_VMoCTL05SJkKDfWJ`) flagged "due" at 2026-06-04T09:06Z — ~1.5y before resolvable. Clearest evidence the trigger ignores `close_at`. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-017 (dup) | 2026-06-04-10 | tool/schema | **Broadened.** `resolution_rule.text` null on two NEW structures: (1) subjective behavioral market `mkt_jxG6JnrreGXqPnae` (Trump-insult-Netanyahu) — the load-bearing personal-vs-professional carve-out lives only in `description`; for a subjective market the rule IS the question; (2) conditional negRisk bucket `mkt_0vS_cIjkn7xaJm5l` (OpenAI <$500B IPO) — `description` carries resolution TIMING + "No IPO by Dec 31" fallback while structured `close_at=2026-06-30` understates the true horizon. Gap = rule text + timing + conditional outcomes. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-017 (dup) | 2026-06-04-11 | tool/schema | FOMC meeting-outcome negRisk bucket set (June 2026: `mkt_F3k2AAPzU_S54ID7` no-change ext 906974, `mkt_mIfccQafP7CI2cgk` cut-25 ext 906973): both `resolution_rule.text` null; full FOMC rule + a **conditional timing fallback** ("resolves to No-change if no statement by next meeting") description-only. Same conditional-outcome class as the OpenAI instance; FOMC is the most common recurring structure a live bot hits. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-018 (dup) | 2026-06-04-11 | report | **Positive control.** Same bootstrap flagged 3 resolve_due forecasts, all `resolution_at_missing`; two new forecasts WITH explicit `resolution_at` (`fc_SuRobY9RNNSA4Dh6`/`fc_-PEfsbGq7RMxmZZJ`, FOMC June, resolution_at 2026-06-17T18:00Z) did **not** flag → the suppression path already exists and works; the bug is purely that absent `resolution_at` is treated as "due now" regardless of `close_at`. Validates option (b)/(a). Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-035 | 2026-06-04-12 | tool/schema | `market.search` single-term zero-result query returned `search_hint:null` (`NBA`→0, `temperature`→0). The AX-026 conjunctive-relaxation hint only fires when `len(query.split()) > 1`, so a single-term no-match gives a bot no signal — indistinguishable from a silent search failure (the AX-019 `/markets` no-op trap it was meant to prevent). Fix adds a distinct single-term hint: the search ran, the term just matches no live market text — try a different/broader keyword or `closed=true`. | fixed | 583fe6f (+updated AX-026 regression test) |
| AX-017 (dup) | 2026-06-04-12 | tool/schema | **Strongest instance — compound/conjunctive market.** `mkt_VUbLYr9ZGwekTeaQ` (ext 902977, "shutdown by Jan 31 AND Dem wins House 2026", negRisk grid): `resolution_rule.text` null; description encodes a logical-AND of two sub-events, EACH with its own resolution source (OPM Operating Status page; election certification/credible-reporting) + a Speaker-selection conditional fallback. "Yes"/"No" labels reveal none of it; one leg (shutdown-by-Jan-31) is already date-settled so live 0.81 prices only the surviving House-control leg. Tariff bind `mkt_HiVD4Ohg3wgF0VdH` same class. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-018 (dup) | 2026-06-04-12 | report | **Positive control re-confirmed.** work_queue flagged exactly the 3 `resolution_at_missing` forecasts; this run's two new forecasts with future `resolution_at` (`fc_-M0_BxRmayARddSR` 2026-11-10, `fc_kUWs9IDO1A54_qCn` 2026-07-01) did **not** flag. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-036 | 2026-06-04-13 | report | `report.lifecycle` `summary.metrics.due_count` counted any case that merely *had* a `due_at`, never comparing it to `as_of`: a forecast due 2026-06-04T16:00Z was counted "due" at 12:05Z (4h early), while the `resolution_at_missing` (due_at null) forecasts were NOT counted — the exact inverse of `report.work_queue`'s "due" set. Fix counts only `due_at <= as_of` via the existing `_review_overdue` predicate (matches `watchlist.overdue_count`). Narrow report-metric fix; does NOT touch the null/`resolution_at_missing` "due now" question (fe2f scope). | fixed | 48d5d3a (+regression test; reverted-fix confirms repro) |
| AX-018 (dup) | 2026-06-04-13 | report | **Cleanest control pair yet (one journal) + sibling fix.** 3 `resolution_at_missing` forecasts flagged resolve_due (incl. US-recession `fc_uywJQC8os4NsVqIP`, confirmed live mid 0.185); 2 new forecasts w/ future `resolution_at` (16:00Z, `fc_GE5gU0KrpenWWkoF`/`fc_ton8dJ8ZKz8-w0nY`) did **not** flag. `report.lifecycle` shows the future-due one as state=`open`, due_at=16:00Z, reason `forecast_pending` — the correct "awaiting resolution" state the missing ones should map to. AX-036 (above) is a sibling surface, fixed. Evidence + cross-link appended to fe2f. | filed | trade-trace-fe2f |
| AX-017 (dup) | 2026-06-04-13 | tool/schema | New instance — intraday **point-in-time** crypto strike markets (`mkt_qTAY1Ii6bpofLPSz` BTC>$62k ext 2379477; `mkt_bjUQDcLcegfQFj3r` $70k ext 2379494): both `resolution_rule.text` null; the Binance noon-ET *single-candle* settle rule — which is what distinguishes point-in-time from window-touch markets, a different probability model — lives only in `venue_metadata_json.description`. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-017 (dup) | 2026-06-04-14 | tool/schema | **ETH point-in-time ladder.** Bound the $1,700/$1,800/$2,000 ETH "above $X on June 4" strikes (`mkt_BAtc1d8DOVp4G3hw`/`mkt_lDGbhDtTiS5l9RDa`/`mkt_UsoYE8KN0eFkrdE6`, ext 2379363/2379366/2379374): all three `resolution_rule.text` null, Binance noon-ET single-candle rule only in `venue_metadata_json.description`. Broadens the run-13 BTC point-in-time evidence to ETH. Note: bind/forecast now accept `resolution_rule_text` (agent can *supply*), but the *retrieval* gap is unchanged. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-018 (dup) | 2026-06-04-14 | report | **Positive control re-confirmed.** work_queue/bootstrap flagged exactly the 3 `resolution_at_missing` forecasts (US/UK-recession + `fc_CheQKZ`); 2 new ETH-strike forecasts with future `resolution_at`=16:00Z (`fc__FHwOmnhZmK7FEcR`/`fc_zo3TquA7-zhMMubX`) did **not** flag, same journal. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-027/031/013 (re-verify) | 2026-06-04-14 | tool/report | Live re-confirmation, no change: `snapshot.fetch` `price` anchored to **mid** (0.3055, not lastTradePrice 0.329) on `mkt_lDGbhDtTiS5l9RDa`; `liquidity_depth_json` numeric (8805.65, no raw dump); `report.coach` did not false-flag the two skip+forecast decisions as unforecasted. | fixed | (prior SHAs 752a4bf / 9e10fc2 / fix verified) |
| AX-037 | 2026-06-04-15 | tool/adapter | `market.bind` adapter path (`bound_via=adapter`) silently discards a caller-supplied `resolution_rule_text` / `resolution_rule.text`. `_polymarket_identity_metadata` builds `resolution_rule.text` only from Gamma keys (`resolutionCriteria`/`rules`); Polymarket leaves those empty (rule prose is in `description`), so text=null — and the caller arg is never consulted, with no error/caveat. The **manual** path preserves it. Verified live: bind `mkt 2416222` (`resolution_rule_text`, adapter) → `text:null`; `2416223` (`bound_via=manual`) → preserved, provenance `caller_supplied`. Defeats the n33z supply-side workaround on the path a live bot uses. Fix: bind-time fill-when-null from caller text (venue text still wins; refresh untouched). | fixed | e9892cd (+2 regression tests; effective next fresh session) — cross-linked trade-trace-n33z |
| AX-018 (dup) | 2026-06-04-15 | report | fe2f re-confirmed: bootstrap/work_queue flagged exactly 3 resolve_due, all `resolution_at_missing` (fc_CheQKZ + US/UK-recession). `mkt_pwuUfbLMcvhkia50` proven **live** via snapshot.fetch (mid 0.505, accepting_orders:true) while flagged "due". Positive control held: new forecast `fc_Dl0Xp2CQf2e1EhDu` w/ explicit `resolution_at`=2026-07-01 did NOT flag. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-017 (dup) | 2026-06-04-15 | tool/schema | New **month-window touch** instance: `mkt_JU-hFNkB7qNd-y4Z` (ETH $2,500 in June, ext 2410584) — `resolution_rule.text` null; the "any Binance 1m candle ≥ $X during the month" window-touch rule (distinct probability model from single-candle point-in-time strikes) only in description. Cross-linked AX-037 (supply-side workaround now fixed; retrieval gap remains). Evidence appended to n33z. | filed | trade-trace-n33z |
| (obs) search past-close | 2026-06-04-15 | tool | `market.search closed:false` returned Beijing temp markets whose `close_at` (12:00Z) was ~2h past, mixed with future-close markets, all `closed:false`/`accepting_orders:true` (Gamma keeps a market "open" until settlement). `close_at` is present per-candidate, so a careful bot can detect it. Low severity; not filed (data present; filing = backlog noise). | observation | — |
| AX-038 | 2026-06-04-16 | report | `report.bootstrap` truncation reported `active_ideas` as `returned_count:0, available_count:51, reason:"max_items"` — self-contradictory (a >0 `max_items` budget can never reduce a section to 0). Root cause (`_apply_budget`, bootstrap.py:521-543): per-bucket trim to `max_items`, then if the trimmed section still exceeds `max_chars` the **whole** section is nuked to `{}` (`_empty_section`) — but `reason = reason or "max_chars"` kept the stale `max_items`, so a consumer reads "some rows omitted" when ALL rows were dropped by the char cap. Fix reports the controlling `max_chars`. The *substantive* half (active_ideas is effectively always empty in a mature journal because the char guard nukes rather than trims-to-fit → a bot never sees its open ideas in orientation) is filed design-level. | **fixed** (a0d28a2, +unit regression test; reverted-fix confirms repro) · design half filed trade-trace-dlup (P2) |
| AX-037 (re-verify) | 2026-06-04-16 | tool/adapter | Live re-confirm, no change: adapter-path `market.bind` with caller `resolution_rule_text` **preserved** it (`provenance=caller_supplied`) on both BTC binds (`mkt_1I__rxv9-y3mh65W` $60k, `mkt_cDjxbYAhIPlhUhdb` $64k) — fix e9892cd effective in-session. AX-027 also re-verified (`snapshot.fetch price`=mid 0.3875, not lastTradePrice 0.438); AX-031 clean (numeric `liquidity_depth_json`). | fixed (prior SHAs verified) |
| AX-018 (dup) | 2026-06-04-16 | report | **Cleanest single-journal control pair (re-confirm).** bootstrap + work_queue flagged exactly 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK-recession 2027-close); 2 new BTC point-in-time forecasts with explicit `resolution_at`=16:00Z (`fc_LdbfXCZrqu8esJhw`/`fc_uhCM0iYQxlSAI7Zu`) did NOT flag. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-017 (dup) | 2026-06-04-16 | tool/schema | Supply-side now verified live (AX-037 fix effective): adapter bind preserved caller `resolution_rule_text`. Retrieval gap unchanged — Gamma `description` (Binance noon-ET single-candle settle rule) still not auto-mapped to `resolution_rule.text` on either BTC bind. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-039 | 2026-06-04-17 | tool (report) | `report.work_queue` — bootstrap's #1 suggested orientation call (`suggested_process_calls[0]`) — and its documented "safe alias" `agent.next_actions` both exceed the MCP token cap (~60.5KB at 13 obligations) and fail at the transport layer with no partial result. A `kind=resolve_due_forecast` filter does NOT shrink it (60.8KB — per-item bloat, not item count). Root cause (`work_queue.py:133-167,270-285`): every obligation serialized **3×** — `groups[]` (each group also re-echoing the full normalized `filter_view`, ~30 fields) + `extra.work_queue` (verbose: allowed/forbidden_actions, closure_condition, trigger_evidence, source_refs…) + `extra.next_actions` (re-projects nearly every field again) — plus `truncated:False` hardcoded per group and **no `limit`/`cursor` param at all**. Same token-cap envelope-bloat class as AX-029 (lifecycle) / AX-034 (current_exposure) but on the PRIMARY orientation surface; a bot cannot follow bootstrap's own suggested sequence on a populated journal. Disproves hv19's premise that work_queue is paginated. No honest small fix (whole result over cap → no in-result hint possible; trimming the triple-serialization is an envelope-shape change). | filed | trade-trace-1y9s (P2); cross-linked trade-trace-hv19 + trade-trace-lszg |
| AX-038 (dup/strengthen) | 2026-06-04-17 | report | The per-section char cap nukes MORE than `active_ideas`: the same bootstrap call (as_of 16:05Z) also emptied the **obligations** section (`returned_count:0`, `available_count:13`, `reason:max_chars`). A bootstrap-first bot sees ZERO obligations AND zero ideas at orientation, then is routed to `report.work_queue` (which overflows — AX-039). Broadens the substantive harm to the two most decision-relevant sections. Evidence appended to dlup. | filed | trade-trace-dlup |
| AX-030 (re-verify) | 2026-06-04-17 | tool/schema | Live re-confirm + **fix verified working**: a schema-faithful `resolution.add` (MCP schema still omits `confidence`) wrote a non-scoring `resolved_final` (out_D-Ae); the live `auto_score_skipped_reason` named the cause + recovery; retry with explicit `confidence=0.97` auto-scored both forecasts on the instrument. Wart: recovery leaves a duplicate dead outcome row. AX-030 remediation (skip-reason, not schema field) functioning as intended. | fixed (skip-reason verified live) |
| AX-020 (re-confirm) | 2026-06-04-17 | tool/error | Resolution evidence path: point-in-time markets stay `accepting_orders:true` past their settle moment (16:00Z) and `snapshot.fetch` exposes no `winningOutcome`, so the surface alone cannot confirm settlement — resolution needed out-of-band web ground-truth (BTC≈$63.6k, ETH≈$1778 on 2026-06-04). The unsignposted-outcome-path gap. Evidence is the isqo case. | filed | trade-trace-isqo |
| AX-018 (dup) | 2026-06-04-17 | report | **Resolution loop exercised at volume.** Resolved 6 point-in-time strike markets → auto-scored **8 forecasts** (calibration N 1→9, Brier 0.135, skill +0.452). The 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession) remained flagged resolve_due (unresolvable, 2027-close); all 8 newly-scored had explicit `resolution_at` and resolved cleanly. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-039 (refine) | 2026-06-04-18 | tool (report) | **N-proportionality proven.** `report.work_queue` SUCCEEDED this run at 5 obligations (~no overflow) after last run's resolution loop shrank the open queue 13→5; last run it FAILED at 13 (~60.5KB). So the overflow is item-count-proportional (triple-serialization × N), latent below a threshold (between 5 and 13) and re-triggering as obligations accumulate — refines 1y9s's "unrecoverable" framing to "latent until N grows." A bootstrap-first bot works on a small/freshly-resolved journal and silently breaks as its own backlog grows. Evidence appended to 1y9s. | filed | trade-trace-1y9s |
| AX-038 (dup) | 2026-06-04-18 | report | Re-confirmed: `report.bootstrap` char cap nuked **both** `obligations` (returned 0/available 5, reason max_chars) AND `active_ideas` (0/40, max_chars) to empty — a bootstrap-first bot sees zero obligations and zero ideas at orientation. Counts shifted (was 13/51 last run) but the nuke persists. Evidence is the dlup case. | filed | trade-trace-dlup |
| AX-018 (dup) | 2026-06-04-18 | report | **Positive control + deferred resolution.** Resolved the 2 point-in-time strikes left open last run once their orderbooks converged (Bxq→0.9995 YES, Soh→0.002 NO) → auto-scored 2 forecasts (N 9→11). The 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession 2027-close) remained flagged resolve_due (unresolvable); the resolved point-in-time ones had explicit `resolution_at` and did not re-flag. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-030 (re-verify) | 2026-06-04-18 | tool/schema | Live re-confirm: first `resolution.add` on mkt_Bxq without `confidence` returned `auto_scoreable:false` + `auto_score_skipped_reason` naming the cause; confidence-bearing retry auto-scored. Wart recurred: non-scoring dead `resolved_final` row left behind (out_WFzwYXG79vpWZTBn). Skip-reason remediation working as intended. | fixed (skip-reason verified live) |
| AX-017 (dup) | 2026-06-04-18 | tool/schema | **Novel touch-BELOW structure + new asset (SOL).** Bound mkt_1E2FEVMTTXg3fUIq (SOL reach $160, ext 2410600) + mkt_DgqiOewVZWjWXC5f (SOL **dip to $10**, ext 2410615). The dip market resolves YES on any Binance SOL/USDT 1m candle **Low** ≤ strike — a touch-below mechanic distinct from every prior touch-above ("reach $X") instance, with an inverted (downside-tail) probability model. Venue rule null; caller `resolution_rule_text` preserved (AX-037 holds). Retrieval gap unchanged. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-037 (re-verify) | 2026-06-04-18 | tool/adapter | Adapter-path `market.bind` preserved caller `resolution_rule_text` (provenance=caller_supplied) on both SOL binds — fix e9892cd effective in-session. | fixed (prior SHA verified) |
| AX-027/031/035/013/022 (re-verify) | 2026-06-04-18 | tool/report | Live re-confirm bundle, no change: `snapshot.fetch` price=mid (0.0025 not lastTradePrice 0.005 on SOL dip); numeric `liquidity_depth_json` (no raw dump); `market.search` single-term zero-result `search_hint` fired ("temperature"→0); `report.coach` 0 unscored false-flags on the skip+forecast_id link; cold throwaway bootstrap still surfaces only read/continuity tools (no first-run entry point). | fixed/filed (prior SHAs / xqjv) |
| AX-017 (dup) | 2026-06-04-19 | tool/schema | **NEW ASSET CLASSES — traditional-asset "closes above $X" point-in-time markets.** Bound SPY/S&P500 (mkt_z1Z4v0I8kDzor_gU, ext 2435397, close 20:00Z=4pm ET) + WTI Crude (mkt_oO0CUlqgOB34f5Qi, ext 2435446, close 21:00Z=NYMEX). Both `resolution_rule.text` would be null; full rule only in `description`. Three modeling differences from crypto Binance strikes: (1) **Pyth "Close"** of the 1m candle at end of regular trading (not Binance); (2) explicit **50-50 fallback** on a non-trading day / no-trade session; (3) WTI **active-month futures-roll** (CME last-trading-day rule) so "$92" tracks whichever contract is active month at settle. Most complex rule structure yet. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-039 (refine) | 2026-06-04-19 | tool (report) | **N-proportionality curve completed.** `report.work_queue` SUCCEEDED at N=3 obligations this run — after fail@13 (run 17) and ok@5 (run 18). Sequence fail@13 / ok@5 / ok@3 fixes the overflow threshold between 5 and 13, confirming triple-serialization × N. Evidence appended to 1y9s. | filed | trade-trace-1y9s |
| AX-018 (dup) | 2026-06-04-19 | report | **Clean control pair across new asset classes.** work_queue flagged exactly the 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession); 3 NEW forecasts with explicit `resolution_at` — SPY $750 fc_A8wbW92 (20:00Z), SOL $60 fc_e0_3Gf8 + SOL $80 fc_lfC-Bti3 (16:00Z, June 5) — did NOT flag. Suppression path now confirmed across equities/commodity/crypto strikes. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-038 (re-verify) | 2026-06-04-19 | report | Fix a0d28a2 **verified live**: at default budget (max_chars_total 24000) bootstrap nuked `active_ideas` (0/40) and correctly reported `reason:max_chars` (not stale max_items). This run `obligations` **survived** (3/3) at small N while `active_ideas` (40 items) still nuked — confirms the char-cap nuke is per-section and N/size-proportional; the 40-item ideas section nukes consistently, obligations only at larger N. Substantive trim-to-fit half remains design (dlup). | fixed (reason) · design half filed dlup |
| AX-037/027/031/013 (re-verify) | 2026-06-04-19 | tool/adapter/report | Live re-confirm bundle, no change: **all 4 adapter binds** preserved caller `resolution_rule_text` (provenance=caller_supplied); `snapshot.fetch` price=mid on all 3 (SOL$60 0.975 NOT lastTradePrice 0.988); numeric `liquidity_depth_json`; `report.coach` 0 unscored false-flags on the 3 skip+forecast_id links. | fixed (prior SHAs) |
| AX-024 (re-confirm) | 2026-06-04-19 | tool | `memory.recall` multi-word query ("SPY WTI traditional asset closes above Pyth 50-50 fallback futures roll") ranked the brand-new memory containing **all** those terms BELOW older temporal/graph hits; single distinctive term "Pyth" → rank #1 (bm25:[1]). Re-confirms conjunctive-BM25 / RRF-fusion degradation on natural-language recall. | filed | trade-trace-95ry |
| (obs) strike-ladder gap | 2026-06-04-19 | tool | No near-the-money SOL June-5 strike: ladder jumps $60 (impl 0.975 ITM) → $80 (0.005 OTM) with SOL spot ~$68–72 in an unstruck gap, so only deep-tail SOL forecasts are available. Also WTI $92 effectively unpriced (liq $18, outcomePrices placeholder 0.5/0.5, spread 0.86) — untradeable. Data present per-candidate; low severity, not filed. | observation | — |
| AX-040 | 2026-06-04-20 | report/integrity metric | **NEW.** `abstention_coverage` integrity control (report.calibration `data.integrity_diagnostics` + report.coach) reported count:0 / share 0% / total:42 despite **32** `decision.add(type=skip)` "considered-and-passed" rows (2 with NO linked forecast — the cleanest abstention case). Root cause `reports/integrity.py:225-254` `_abstention_coverage()` reads ONLY `SELECT id FROM abstentions`; skip-type material-non-action decisions are never counted. Two overlapping "I passed on this market" surfaces (`abstention.record` vs `decision.add(skip)`) and the anti-survivorship control credits only one, with no signpost skip→abstention — a maximally-cautious bot reads as "never abstains / survivorship-biased denominator" when it diligently recorded 32 passes. Design-level (denominator semantics + surface reconciliation; risk of double-count) → filed, not fixed. Dedup: `bd search abstention/survivorship/"skip coverage"` all empty. | filed | trade-trace-s7nn (P3 design Q) |
| AX-037/027/031 (re-verify) | 2026-06-04-20 | tool/adapter/report | Live re-confirm on a **NEW structure** (Elon Musk tweet-COUNT bucket market `mkt_LvM6eoXxSu9yt6eh`, ext 2358280, "200-219 tweets May 29–Jun 5", close 16:00Z): adapter bind preserved caller `resolution_rule_text` (provenance=caller_supplied); `snapshot.fetch` price=mid 0.175 (NOT lastTradePrice 0.17); numeric `liquidity_depth_json` (24490.42, no raw dump). Liquid market (spread 0.01, vol $246k) unlike recent thin SOL strikes. | fixed (prior SHAs) |
| AX-017 (dup) | 2026-06-04-20 | tool/schema | **New structure class — numeric count bucket.** Same tweet-bucket bind: `resolution_rule.text` would be null; full counting methodology (xtracker.polymarket.com Post Counter; main+quote+reposts count, replies excluded, deleted-post ~5min capture rule, X as secondary source) lives only in `description`. Distinct from all prior price-strike/touch instances. Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-038 (re-verify) | 2026-06-04-20 | report | bootstrap char cap nuked `active_ideas` (0/46, reason=`max_chars`) while `obligations` survived (3/3) at small N — consistent per-section N/size-proportional nuke. | fixed (reason a0d28a2) · design half dlup |
| AX-039 (re-verify) | 2026-06-04-20 | tool (report) | `report.work_queue` SUCCEEDED at N=3 obligations — curve now fail@13 / ok@5 / ok@3 / ok@3. | filed | trade-trace-1y9s |
| AX-018 (dup) | 2026-06-04-20 | report | fe2f positive control held: bootstrap/work_queue flagged exactly the 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession); last run's 3 June-5 seeds (SPY/SOL) with explicit `resolution_at` did NOT flag. No resolvable inventory this run; seeds settle June 5. | filed | trade-trace-fe2f |
| AX-024 (re-confirm) | 2026-06-04-20 | tool | `memory.recall` natural-language multi-word query returned only temporally-recent items (ETH ladder, adapter rule_text, exposure), none on the queried abstention/denominator topic, all at RRF floor ~0.03. | filed | trade-trace-95ry |
| (obs) process_quality | 2026-06-04-20 | report | `report.process_quality` flagged 3/8 sized decisions `direction_consistent:false`, all `stated_edge ≤ 0` (e.g. price 0.93 vs win_prob 0.92, edge −0.01) — control working as intended (penalizes zero/neg-edge paper entries). Surface not previously in registry; behaves correctly. Not filed. | observation | — |
| AX-040 (positive control) | 2026-06-04-21 | report/integrity metric | **Clean before/after positive control.** Recorded a genuine first-class `abstention.record` (`abst_N3H5Vwop0Xoz3r7A`, China-GDP bucket `mkt_a4N9aOP9wsixBZ7Q`); `report.calibration` `integrity_diagnostics.abstention_coverage` moved count **0→1**, total **43→44**, share **0%→2.27%**, `sample_ids.abstentions` populated — verified live. Proves the `abstention.record` path IS credited, isolating the s7nn gap precisely to the 32 ignored `decision.add(type=skip,insufficient_edge)` rows. `report.coach` printed "1 of 44 considered markets were recorded as no-bet (2.3%)" while 32 documented skips exist → the callout actively undercounts the true considered-and-passed rate (~33/44). Strengthens the fix direction (count skips too + reconcile the two surfaces, avoid double-count). Evidence appended to s7nn. | filed | trade-trace-s7nn |
| AX-017 (dup) | 2026-06-04-21 | tool/schema | **New structure class — numeric RANGE bucket macro market.** Bound China GDP "between 4.6% and 4.9%" Q2-2026 (`mkt_a4N9aOP9wsixBZ7Q`, ext 2009765, NBS-settled, negRisk grid): `resolution_rule.text` null; load-bearing nuances description-only — **initial-release-only / revisions-ignored**, **bracket tie-break "resolves to the HIGHER bracket"**, and a no-data fallback (use last available quarter). Distinct from count-buckets (tweet count) and single-threshold price strikes: a two-sided numeric range with bracket-boundary + revision-exclusion semantics that change which side wins. AX-037 still holds (caller rule text preserved). Evidence appended to n33z. | filed | trade-trace-n33z |
| AX-037/027/031 (re-verify) | 2026-06-04-21 | tool/adapter/report | Live re-confirm on the China-GDP range-bucket bind: adapter `market.bind` preserved caller `resolution_rule_text` (`provenance=caller_supplied`); `snapshot.fetch` price=mid 0.715 (NOT lastTradePrice 0.71); numeric `liquidity_depth_json` (3712.25, no raw dump). | fixed (prior SHAs) |
| AX-038 (re-verify) | 2026-06-04-21 | report | bootstrap char cap nuked `active_ideas` (0/48, reason=`max_chars`) while `obligations` survived (3/3) at small N — consistent per-section N/size-proportional nuke. | fixed (reason a0d28a2) · design half dlup |
| AX-039 (re-verify) | 2026-06-04-21 | tool (report) | `report.work_queue` SUCCEEDED at N=3 obligations — curve now fail@13 / ok@5 / ok@3 / ok@3 / ok@3. | filed | trade-trace-1y9s |
| AX-018 (dup) | 2026-06-04-21 | report | fe2f positive control held: bootstrap/work_queue flagged exactly the 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession); last run's June-5 seeds with explicit `resolution_at` did NOT flag (SPY `fc_A8wbW92` lifecycle state `open`/`forecast_pending`, due_at 2026-06-05T20:00Z). No resolvable inventory this run; seeds settle June 5. | filed | trade-trace-fe2f |
| AX-024 (re-confirm) | 2026-06-04-21 | tool | `memory.recall` natural-language multi-word query (China-GDP topic) returned only temporally-recent items at RRF floor ~0.03, none on-topic; distinctive-term recall ("abstention_coverage positive control") ranked the new memory #1 (`bm25:[1]`). | filed | trade-trace-95ry |
| AX-022 (re-confirm) | 2026-06-04-21 | tool/onboarding | Cold throwaway-journal bootstrap suggested only read/continuity tools (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`); no first-run `market.search→bind→forecast` entry point; 0 obligations. | filed | trade-trace-xqjv |
| (obs) null close_at | 2026-06-04-21 | tool | Brazil Q2-2026 GDP market (ext 2428140) returned `close_at:null` while `active:true`/`closed:false` from `market.search`. Low severity, data present per-candidate; not filed. | observation | — |
| AX-041 | 2026-06-04-22 | report | **NEW.** `report.audit_readiness` (surface not previously in the registry) returns `ready:false` with 24 `blocking` `missing_resolution_rule_provenance` issues and **no in-surface remediation**. The check (`audit_readiness.py:94-108`) is per-forecast and requires BOTH `instrument.resolution_criteria_text` AND `forecast.resolution_rule_text` non-empty (`has_instrument_criteria` ∧ `has_forecast_rule`); the AX-037 instrument-level caller rule does NOT satisfy it. Proved the remedy live: a forecast created via `forecast.add(resolution_rule_text=…)` (fc_k8JRbxD7k3j-RIce) is NOT flagged while two siblings on the same instrument (mkt_7tACn4aqhw6y8BwN) lacking it stay blocking. Also `missing_snapshot` is `blocking` for 5 decisions with no `snapshot_id` though that field is optional in the decision matrix. Same self-documenting-output class as fixed AX-004/005/010/026/030/032/035. Fix: per-issue `remediation` string on all 9 checks (forecast-rule blocker names `forecast.add(resolution_rule_text)`; snapshot blocker notes the optional-but-blocking semantics); additive only, flagged-row set unchanged. | **fixed** 3524e67 (+regression assertions; effective next fresh session) |
| AX-040 (re-verify) | 2026-06-04-22 | report/integrity metric | s7nn re-confirm + **widen**: `abstention_coverage` count 1 / total 45 / 2.22% (the single run-21 `abstention.record`); `report.coach` prints "1 of 45 considered markets were recorded as no-bet (2.2%)" while skip-type considered-and-passed rows now total ~33 (this run's already_exposed skip on BTC $70k added one). Undercount scales monotonically with run count. Evidence appended to s7nn (cross-linked the AX-041 remediation pattern as fix-direction precedent). | filed | trade-trace-s7nn |
| AX-037/027/031 (re-verify) | 2026-06-04-22 | tool/adapter/report | Live re-confirm on BTC $70k month-window touch bind (mkt_7tACn4aqhw6y8BwN, ext 2420093): adapter `market.bind` preserved caller `resolution_rule_text` (`provenance=caller_supplied`); `snapshot.fetch` price=mid 0.335 (NOT lastTradePrice 0.34); numeric `liquidity_depth_json` (42978.75, no raw dump). Venue Binance month-window rule still description-only (n33z). | fixed (prior SHAs) / filed n33z |
| AX-033/scx8 (re-confirm) | 2026-06-04-22 | tool/report | Memory (mem_4zRTSDdMFuzcfAJs) flagged I already hold **120 YES** on mkt_7tACn4aqhw6y8BwN from prior runs before sizing; forecasted (fc_k8JRbxD7k3j-RIce p_yes=0.30) then **skipped** (already_exposed) → opened no new exposure. The cross-run accumulation hazard remains (decision time still gives no `already_exposed` signal), mitigated only by manual open-positions/memory checks. | filed | trade-trace-scx8 |
| AX-038 (re-verify) | 2026-06-04-22 | report | bootstrap char cap nuked `active_ideas` (0/48, reason=`max_chars`) while `obligations` survived (3/3) at small N — consistent per-section N/size-proportional nuke. | fixed (reason a0d28a2) · design half dlup |
| AX-039 (re-verify) | 2026-06-04-22 | tool (report) | `report.work_queue` SUCCEEDED at N=3 obligations — curve fail@13 / ok@5 / ok@3 ×4. `report.lifecycle` (open+pending_review) still overflowed (~112KB, AX-029/hv19). | filed | trade-trace-1y9s |
| AX-018 (dup) | 2026-06-04-22 | report | fe2f positive control held: bootstrap/work_queue flagged exactly the 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession); last run's June-5 seeds with explicit `resolution_at` did NOT flag. No resolvable inventory this run; seeds settle June 5. | filed | trade-trace-fe2f |
| AX-024 (re-confirm) | 2026-06-04-22 | tool | `memory.recall` multi-word natural-language query ("Bitcoin month window touch reach strike Binance candle") returned items only at RRF floor ~0.031 with no bm25 provenance; the distinctive-term query ("audit_readiness missing_resolution_rule_provenance has_forecast_rule") ranked the new memory #1 (`bm25:[1]`). | filed | trade-trace-95ry |
| AX-022 (re-confirm) | 2026-06-04-22 | tool/onboarding | Cold throwaway-journal bootstrap suggested only read/continuity tools (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`); no first-run `market.search→bind→forecast` entry point; 0 obligations. | filed | trade-trace-xqjv |
| (obs) resolution_misreads | 2026-06-04-22 | report | `report.resolution_misreads` returned 1 `unresolved` interpretation (fc_5B3pYiZvVlg0kfHS, interpreted oracle_feed vs actual market_contract), 0 contract_misreads — control behaves correctly (a still-open market can't yet be a misread). Surface not previously probed; behaves as designed. Not filed. | observation | — |
| AX-042 | 2026-06-04-23 | report | **NEW.** `report.opportunity` (surface not previously in the registry) `LEFT JOIN`ed `outcomes` AND `positions` on `instrument_id` alone (`opportunity.py:196-197`), so one decision was emitted once per `(resolved_final outcome row × position row)` on its instrument. Duplicate outcomes come from `resolution.add` retries (AX-030 wart: mkt_39bP49 had 3, mkt_1I__rxv9/mkt_Bxq had 2 each); duplicate positions from paper-entry fragmentation (scx8: mkt_7tACn4/mkt_pwuU had 2 each). Live journal: **46 distinct decisions reported as `sample_size:55`**; `good_skip`/`missed_positive_edge` buckets each `count:3` for the single skip `dec_2Z665` tripled by 3 outcome rows; the `mean_*` metrics and the "only N decisions in bucket" reliability warning are all distorted. Distinct from AX-029/034/039/hv19/lszg (those are token-cap envelope-bloat; this is a correctness/de-dup query bug). Fix: correlated subquery picks one canonical (latest) `resolved_final` outcome per instrument; positions aggregated to one summed `realized_pnl` row per instrument; each decision → exactly one record. Classification/labels/metrics logic unchanged. | **fixed** 9d13a5d (+regression test: 1 decision w/ 2 outcomes + 2 positions, repro 1→4 then assert 1; effective next fresh session) |
| AX-043 | 2026-06-04-23 | tool | **NEW.** `market.find_similar` (surface not previously in the registry) `score = matched_dimensions / comparable` (`market_similarity.py:80`), where `comparable` counts only dims both markets have non-null. A snapshot-less candidate is comparable on just 3 snapshot-independent dims (mechanism/resolution_source/ambiguous) and, matching all 3, ties `score:1.0` with a true 6-of-6 analogue; the tie-break was arbitrary `instrument_id`, so a 2-comparable Beijing-temp market outranked a 6-comparable Elon-tweet market — all reporting `1.0`. A bot ranking by `score` cannot tell a sparse coincidence from a full structural match. Fix (additive, score definition unchanged): expose `comparable_dimensions` (the denominator) per match + tie-break ranking by `-comparable_dimensions` so the fuller, more-corroborated analogue ranks first within a score band. Residual semantic Q (should score penalize missing dims — `matched/total`?) now mitigated + self-documenting; not filed (backlog noise), noted in run report. | **fixed** beb385c (+regression test; effective next fresh session) |
| AX-041 (re-verify) | 2026-06-04-23 | report | Fix 3524e67 **verified live** this fresh session: `report.audit_readiness` carries a per-issue `remediation` string on all checks (`missing_resolution_rule_provenance` names both `market.bind` + `forecast.add(resolution_rule_text)` and the `has_instrument_criteria ∧ has_forecast_rule` rule; `missing_snapshot` notes optional-but-blocking; `missing_agent_metadata` info-level). Handoff prediction held: `fc_k8JRbxD7k3j-RIce` (forecast-level rule) NOT in the 24-forecast blocking list; rule-less siblings on the same instrument stay blocking. | **fixed** (verified live) |
| AX-037/027/031 (re-verify) | 2026-06-04-23 | tool/adapter/report | Live re-confirm on a fresh BTC ≥ $90k month-window touch bind (`mkt_Weg9U9Ul6g19DJuO`, ext 2410562): adapter `market.bind` preserved caller `resolution_rule_text` (`provenance=caller_supplied`); `snapshot.fetch price`=mid 0.011 (NOT lastTradePrice 0.01); numeric `liquidity_depth_json` (81560.14, no raw dump). Venue Binance month-window rule still description-only (n33z). | fixed (prior SHAs) / filed n33z |
| AX-038/039/018/024/022 (re-verify) | 2026-06-04-23 | report/tool/onboarding | Standing re-confirms, no change: bootstrap `active_ideas` nuked 0/50 `max_chars` while `obligations` survived 3/3 (AX-038); `report.work_queue` fine at N=3 (AX-039, curve fail@13 / ok@5 / ok@3 ×5); exactly 3 `resolution_at_missing` flagged, new explicit-`resolution_at` fc did not flag (AX-018); `memory.recall` natural-language query returned only RRF-floor temporal hits, just-written on-topic memory absent (AX-024); cold throwaway bootstrap only read/continuity tools (AX-022). | filed dlup/1y9s/fe2f/95ry/xqjv |
| (obs) strategy_health/independence | 2026-06-04-23 | report/tool | Two surfaces probed, behave as designed (not filed): `report.strategy_health(all)` → `sample_size:0` / `no_strategies` (journal has ~71 forecasts but no `strategy.upsert` entity); `forecast.independence(fc_CheQKZ)` → clean `no_blind_commit` / `independence_proven:false`. | observation | — |
| AX-044 | 2026-06-04-24 | report | **NEW.** `report.exposure_anomalies` (surface not previously in the registry) returned `count:0` / `codes:[]` while the journal held TWO open same-side paper positions on each of two instruments (`report.pnl`: `mkt_7tACn4aqhw6y8BwN` open_count:2 YES, `mkt_pwuUfbLMcvhkia50` open_count:2 NO — the scx8 fragmentation). Root cause `portfolio_exposure.py:390-400`: the `DUPLICATE_DECISIONS` query `GROUP BY (instrument_id,type,side,quantity,price,run_id) HAVING COUNT(*)>1` catches only exact-replay dupes; fragments differing in qty/price across runs land in separate buckets. Cleanest proof: `mkt_pwuU`'s two NO `paper_enter`s share instrument/type/side/price(0.49)/run_id(NULL), differ ONLY in quantity (100 vs 10), both open → not flagged purely because `quantity` is in the GROUP BY. No same-side-fragmentation code exists, so a bot using exposure_anomalies as the post-hoc detector gets a clean bill while fragmented exposure sits open. Detection-side half of scx8; design-level (new `FRAGMENTED_SAME_SIDE_EXPOSURE` code and/or relax grouping; threshold + double-count semantics) → appended to scx8, not direct-fixed. Lesson retained `mem_hg3_nNifTi4u93ly`. | filed | trade-trace-scx8 |
| AX-018/fe2f (strengthen) | 2026-06-04-24 | report | **Cross-surface positive control.** `report.unscored_forecasts` → `count:0` while `report.work_queue`/bootstrap flagged exactly 3 `resolve_due_forecast` (all `resolution_at_missing`: fc_CheQKZ/UK-recession fc_I0VaLn1/US-recession fc_uywJQC8). `unscored_forecasts` uses the correct close-aware predicate (PAST `resolution_at`), correctly excluding NULL-`resolution_at` rows — isolates `work_queue`'s `resolution_at_missing="due now"` branch as the outlier. Evidence appended to fe2f. | filed | trade-trace-fe2f |
| AX-042 (verify) | 2026-06-04-24 | report | Fix 9d13a5d **verified fixed live** this fresh session: `report.opportunity` `sample_size:46`==`decision_count:46` (was 55→46 distorted); `good_skip`/`missed_positive_edge` buckets each `count:1` (was 3; `dec_2Z665` no longer tripled). Each decision → one record. | fixed (verified live) |
| AX-043 (verify) | 2026-06-04-24 | tool | Fix beb385c **verified fixed live**: `market.find_similar` exposes `comparable_dimensions` per match (6,6,5,5,3,3,3,3) and ties rank by `-comparable_dimensions` — full 6-dim analogues (US recession, Elon tweet) above sparse 3-dim coincidences within the `score:1.0` band. | fixed (verified live) |
| AX-037/027/031/038/018/024/040/022 (re-verify) | 2026-06-04-24 | tool/adapter/report/onboarding | Standing re-confirms, no change: fresh ETH-$2,500 June-5 point-in-time bind (`mkt_JuC0S5NAj4toCrVm`, ext 2386745) preserved caller `resolution_rule_text` (provenance=caller_supplied, AX-037); `snapshot.fetch` price=mid 0.004 one-sided-book fallback (AX-027), numeric `liquidity_depth_json` 54761 (AX-031); bootstrap `active_ideas` nuked 0/51 `max_chars`, obligations 3/3 (AX-038); 3 `resolution_at_missing` flagged, new explicit-`resolution_at` fc_CqkS2nRVlmIqDiBJ did not (AX-018); `memory.recall` only RRF-floor hits (AX-024); `abstention_coverage` 1/47 2.13% vs ~34 skips (AX-040/s7nn); cold bootstrap read/continuity tools only (AX-022). | filed dlup/1y9s/fe2f/95ry/xqjv/s7nn/n33z |
| AX-045 | 2026-06-05-01 | tool/report | **NEW.** `report.risk` (surface not previously in the registry) returned `sample_size:0` / `n_closed_total:0` / `missing_risk_count:0` AND **empty `caveats`** on a journal with 49 decisions + ~13 open paper positions, despite its docstring promising decisions without declared risk are "counted in caveats so the agent can chase the gap." Two-layer root cause: (1) its missing-count is over **closed** decisions only (`risk.py:157`) and no paper position is ever closed (intentional-design #1), so the gap is invisible on this surface — while `report.open_positions` DOES flag each row `missing_risk_budget` ("Record the risk you accepted at entry to recover this metric"); (2) the field that advice points at — `declared_risk_amount`/`declared_risk_unit` — is **accepted** by `decision.add` (`decision.py:91`, threaded into the position `initial_risk_amount` at `:228`) and depended on by `report.risk`/`report.opportunity`/the open-positions caveat, yet was **absent from every advertised surface**: not in `x-decision-matrix` optional lists, not in `_DECISION_ADD_SCHEMA.properties`, not in the examples. So `report.risk` is structurally always-empty for an MCP-only bot. Same self-documenting-schema class as AX-030/032/035. Fix: mark both `O` on `paper_enter`/`actual_enter`/`add`; add to schema properties (description points at report.risk/opportunity) + `actual_enter` example. Residual (not filed, backlog noise): report.risk still silent on an all-open risk-less journal since R-multiples need closes; open_positions already serves the per-row signal. | **fixed (verified live 2026-06-05-02)** 903f9f4 (+2 regression tests) |
| AX-046 | 2026-06-05-01 | tool (test/gate) | **NEW (surfaced at the full-suite gate, not as a bot).** `tests/integration/test_market_scan_guided_e2e.py` failed `source_attached 'weak'=='ok'` — a pre-existing time-bomb, NOT this run's change (proven: reverting both touched src files to HEAD~1 reproduced the identical failure). Root cause: `market.scan.promote` builds `final_check` via `journal.bundle.status` (`market_scan.py:443`) **without forwarding a deterministic clock or `stale_source_days`**; `_stale_sources` (`journal_bundle_status.py:379`) compares source `captured_at` to `datetime('now','-14 days')`. The test hardcoded source `captured_at=2026-05-21T12:05Z`, which crossed the 14-day threshold at 2026-06-04T12:05Z, so it began failing on a clock tick. Product behavior is correct (a 14-day source IS stale); the test's fixed timestamp was wrong. Test fixed (run-relative timestamps, stable across dry_run+double-promote replay); product non-determinism filed (promote should thread `current_time`/`stale_source_days` like `dry_run` already does). | test **fixed** 791f307 · product **filed** trade-trace-efmq (P3) |
| AX-037/027/031/025/028/018/039/038/013/040/024/022 (re-verify) | 2026-06-05-01 | tool/adapter/report/onboarding | Standing re-confirms, no change: ETH-$1,800 June-5 strike (`mkt_xKHM1hI9Jmbd4Vx5`) + China-GDP 4.0–4.3% (`mkt_HkRsvvwUHPcKU71Z`) adapter binds both preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid (ETH 0.2315 not lastTrade 0.259; China 0.0105 not 0.02) (AX-027); numeric `liquidity_depth_json` (AX-031); new NO position marked side-aware +0.02 (1−0.2315=0.7685), `mark_state:available` (AX-025/027/028); 3 `resolution_at_missing` flagged, 2 new explicit-`resolution_at` fc did not (AX-018); `report.work_queue` ok@N=3 (AX-039, curve fail@13/ok@5/ok@3×6); bootstrap `active_ideas` nuked 0/52 `max_chars`, obligations 3/3 (AX-038); `report.coach` 0 unscored false-flags on skip+forecast link (AX-013); `abstention_coverage` 1/49 2.04% vs ~35 skips (AX-040/s7nn); `memory.recall` only RRF-floor hits (AX-024); cold bootstrap read/continuity tools only (AX-022). | filed dlup/1y9s/fe2f/95ry/xqjv/s7nn/n33z |
| AX-045 (verify live) | 2026-06-05-02 | tool/report | **AX-045 fix 903f9f4 VERIFIED LIVE END-TO-END this fresh session.** `tool.schema decision.add` advertises `declared_risk_amount`/`declared_risk_unit` in `json_schema.properties` + the `paper_enter`/`actual_enter`/`add` `x-decision-matrix` optional lists + the `actual_enter` example (38). Supplied `declared_risk_amount=0.94` on a `paper_enter` (BTC-$80k YES 20@0.047, `pos_QE8HbdO-_dDXV875`): (1) `report.open_positions` row carries `initial_risk_amount:0.94` and **no longer emits `missing_risk_budget`** (only `no_strategy` info); (2) `report.risk` — previously structurally always-empty for an MCP-only bot — now shows `n_decisions_with_risk:1`/`n_pending_with_risk:1`/`pending_risk_count:1` + caveat "1 decision(s) carry declared risk but have no closed/resolved position P&L yet." R-multiple aggregate stays null (correct: needs closes; paper positions never close per intentional-design #1) — this also closes the 903f9f4 "report.risk still silent" residual (it surfaces pending risk now). Minor obs (not filed): `report.risk` advertises `filter` but `supported_filter_paths:[]`, rejecting every field with a self-documenting VALIDATION_ERROR — global aggregate by design. | **fixed (verified live)** 903f9f4 |
| AX-029/lszg (re-confirm) | 2026-06-05-02 | report | `report.open_positions` at default paging overflowed the MCP token cap at **74,618 chars** (transport-layer failure, no partial result); it honors `limit`/`instrument_id` so single-instrument scoping worked. Live re-confirm of the unbounded-default token-cap bug. | filed trade-trace-lszg |
| AX-040/s7nn (re-confirm) | 2026-06-05-02 | report | `abstention_coverage` count:1/total:51/1.96% (single `abstention.record`); ~33 `decision.add(skip)` rows still uncounted; `report.coach` callout "1 of 51 considered markets … no-bet (2.0%)" undercounts. Monotonic with run count. | filed trade-trace-s7nn |
| AX-017/n33z (re-confirm) | 2026-06-05-02 | tool/schema | Both BTC June touch-strike adapter binds (`mkt_TSGgmvasj9wBFVg-` $80k ext 2410566, `mkt_Weg9U9Ul6g19DJuO` $90k ext 2410562) returned `resolution_rule.text:null`; full Binance any-1m-candle-High rule only in Gamma `description`. AX-037 supply-side held: persisted my rule reading via `forecast.add(resolution_rule_text=…)` on both. `snapshot.fetch` price=mid ($80k 0.0465 not lastTrade 0.049; $90k 0.0105 not 0.01) (AX-027); numeric `liquidity_depth_json` (AX-031). | filed trade-trace-n33z |
| AX-018/fe2f (positive control) | 2026-06-05-02 | report | My 2 new forecasts with explicit `resolution_at:2026-07-01` were NOT flagged unscored (`report.coach` `unscored_forecasts.count:0`, `report.unscored_forecasts` sample_size:0); the 3 standing `resolution_at_missing` forecasts stay flagged. Suppression path confirmed. | filed trade-trace-fe2f |
| AX-024/95ry (re-confirm) | 2026-06-05-02 | tool | Distinctive single-term recall (`declared_risk_amount`) returned the freshly-retained lesson at bm25 rank #1, paired with the original AX-045 problem memory at #2 — consistent with conjunctive-BM25 degradation on multi-word natural-language queries (distinctive single term works). | filed trade-trace-95ry |
| AX-031/xqjv (re-confirm) | 2026-06-05-02 | onboarding | Cold throwaway journal bootstrap suggested only read/continuity tools (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`) — no first-run `market.search`→`bind`→`forecast.add` entry point. | filed trade-trace-xqjv |
| AX-047 | 2026-06-05-03 | report | **NEW.** `report.calibration_advisory` (surface not previously in the registry) `suggested_adjustment` was the RAW band gap (`observed_frequency - mean_probability`, `calibration.py:275`) while `suggested_probability` is clamped to [0,1] (`:250`). For a 0.97 candidate in band 0.9–1.0 (observed 1.0, mean 0.953, gap +0.0467): `suggested_probability` clamped to 1.0 but `suggested_adjustment` reported +0.0467, so a consumer doing `probability + suggested_adjustment` gets 1.0167 — overshooting the report's own suggested value (an invalid probability). Only diverges at the [0,1] boundary (the high/low-confidence bands where the advisory matters most). Same output-correctness class as AX-030/032/035/041/042/043. Fix: `suggested_adjustment = suggested_probability - probability` (effective post-clamp delta, invariant `probability + suggested_adjustment == suggested_probability`); `calibration_gap` still carries the raw band gap. | **fixed** 0502e41 (+regression test; effective next fresh session) |
| AX-048 | 2026-06-05-03 | report | **NEW.** `report.mistakes` + `report.strengths` (surfaces not previously in the registry) surfaced tags whose forecasts are not yet scored (`scored_forecast_count:0`, `mean_brier:null`) as BOTH "recurring mistakes" AND "recurring strengths" — the same tag double-listed under contradictory labels with zero Brier evidence to attribute (`_tag_ranked_report` in `tag_aggregates.py` appended every tag, including `sample_size==0`). Triple-corroborated against sibling surfaces that DO gate on scored evidence: `report.mistake_tripwire` (min_sample=10, `continue` below threshold) did NOT fire on the same null-Brier tags; `report.coach._top_tag_summary` (filters `scored_forecast_count>0`) returned `top_mistakes:[]`/`top_strengths:[]`; only the standalone reports leaked them. Fix: skip groups where `sample_size==0` in `_tag_ranked_report` (matches the module's own "no Brier to attribute" exclusion and the two sibling gates); `tag_count` now reflects surfaced groups. Scored tags + ranking unchanged. | **fixed** 6d84dd5 (+regression test; effective next fresh session) |
| AX-039/037/027/031/013/018/024/040/045/029/022 (re-verify) | 2026-06-05-03 | tool/adapter/report/onboarding | Standing re-confirms, no change: `report.work_queue` ok@N=3 (AX-039, curve fail@13/ok@5/ok@3×8); FOMC-no-change adapter bind (`mkt_F3k2AAPzU_S54ID7`) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.9825 (AX-027); numeric `liquidity_depth_json` 401301 (AX-031); skip linked `forecast_id`, `coach.unscored_forecasts.count:0` (AX-013); new fc w/ explicit `resolution_at:2026-06-17` did NOT flag, 3 `resolution_at_missing` stay flagged (AX-018/fe2f); multi-word recall → RRF-floor only, distinctive term → rank #1 bm25:[1] (AX-024); `abstention_coverage` 1/52/1.92% vs ~39 skips (AX-040/s7nn); BTC-$80k `pos_QE8HbdO` keeps `initial_risk_amount:0.94`, no `missing_risk_budget` (AX-045); `report.open_positions` default overflows token cap, honors `limit` (AX-029/lszg); cold bootstrap read/continuity tools only (AX-022/xqjv). | filed 1y9s/fe2f/95ry/s7nn/lszg/xqjv/n33z |
| (obs) new report surfaces | 2026-06-05-03 | report | Five surfaces probed for the first time and behave as designed (not filed): `report.forecast_diagnostics` (N=11, Brier 0.163, skill +0.343); `report.time_decay_sharpening` (single `0_24h` bucket — all scored forecasts intraday strikes, no decay curve); `report.source_quality` (`no_data`; minor doc drift — emits official/resolution_rule/redacted diagnostics beyond docstring's 5); `report.watchlist` (0 watches; loop uses skip); `report.process_analytics` (tag-frequency MVP, proper low-sample warnings). | observation | — |
| AX-049 | 2026-06-05-04 | report (schema/error) | **NEW.** `report.compare` (surface not previously in the registry) had two coupled honesty defects. (1) An unsupported `group_by` (e.g. `tag`) was rejected with `VALIDATION_ERROR "unsupported group_by for {calibration\|pnl} compare: 'tag'"` that echoed only the offending value and listed NO allowed set (`compare.py:98,149`) — the AX-004/005/032/035 self-documenting-error class. (2) The advertised `group_by` schema description (`tool_schemas.py:343`) listed `tag` as a "common value", but `tag` is in NEITHER `CALIBRATION_GROUP_SQL` nor `PNL_GROUP_SQL` (confirmed live: rejected for BOTH base_reports) — advertising-vs-runtime drift, the same `trade-trace-cs0r` class a prior fix already hit on this tool's `DOCUMENTED_GROUP_BY` set; the schema description was the third copy that fix missed. Positive control: `report.compare(calibration, instrument_id)` works (9 groups, full metrics). Fix: both rejections append the per-base allowed set; schema description enumerates the real per-`base_report` allowlists + states `tag` is unsupported; `venue_id` added to the reviewed mhy1 allowlist entry for `tool_schemas.py` (schema text now legitimately names it). | **fixed** 5da4c4c (+2 regression tests; effective next fresh session) |
| AX-047 (verify live) | 2026-06-05-04 | report | **AX-047 fix 0502e41 VERIFIED LIVE this fresh session, BOTH [0,1] boundaries.** `report.calibration_advisory(0.97)` clamp case: `suggested_probability=1.0`, `suggested_adjustment=0.03`, `calibration_gap=0.0467` (raw, preserved) → invariant `0.97+0.03==1.0` (was 1.0167 overshoot). `report.calibration_advisory(0.03)` no-clamp case: `suggested_probability=0.025`, `suggested_adjustment=-0.005==calibration_gap` → `0.03+(-0.005)==0.025`. Invariant `probability+suggested_adjustment==suggested_probability` holds with or without clamp. | **fixed (verified live)** 0502e41 |
| AX-048 (verify live) | 2026-06-05-04 | report | **AX-048 fix 6d84dd5 VERIFIED LIVE this fresh session.** `report.mistakes` and `report.strengths` both return `tag_count:0`/`groups:[]` with `summary.sample_size:5` in scope — the 5 tagged-but-unscored (null-Brier) decision groups are correctly skipped from both, instead of double-listing under contradictory labels. Agrees with sibling controls (`report.coach.top_*` `[]`, `mistake_tripwire` not fired). | **fixed (verified live)** 6d84dd5 |
| (obs) policy_candidates/playbook_adherence | 2026-06-05-04 | report | Two surfaces probed for the first time and behave as designed (not filed): `report.policy_candidates` (`sample_size:0`/`no_policy_candidates`, fully caveated — journal has no `policy_candidate` reflections); `report.playbook_adherence` (`sample_size:0`, structured all-zero `predicate_audit` + caveats — loop never records adherence rows). Both need specific seeded journal state to exercise the populated path. | observation | — |
| AX-039/037/027/031/013/018/024/022/038 (re-verify) | 2026-06-05-04 | tool/adapter/report/onboarding | Standing re-confirms, no change: `report.work_queue` ok@N=3 (AX-039, curve fail@13/ok@5/ok@3×9); `market.search` 8 live BTC candidates, non-empty (AX-019/035); BTC-$82.5k adapter bind (`mkt_HtqukmoKiJY4Ly71`) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.0265 not lastTrade 0.03 (AX-027); numeric `liquidity_depth_json` 33076.88 (AX-031); skip linked `forecast_id` (AX-013); new fc `fc_o0yrpd3` w/ explicit `resolution_at:2026-07-01` did NOT flag, exactly 3 `resolution_at_missing` stay flagged (AX-018/fe2f); multi-word recall → RRF-floor ~0.03 only (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv); bootstrap `active_ideas` nuked 0/60 `max_chars`, obligations 3/3 (AX-038). | filed 1y9s/fe2f/95ry/xqjv/n33z/dlup |
| AX-050 | 2026-06-05-05 | report (schema) | **NEW.** `report.compare` residual of the AX-049 fix (`5da4c4c`). `CALIBRATION_GROUP_SQL` (`compare.py:46-47`) contains BOTH `status` and `outcome_status` → `o.status` (aliases; verified live both return `ok`, identical `group_value=resolved_final`), and the runtime VALIDATION_ERROR for an unsupported `group_by` lists `outcome_status` among the allowed set. But the `tool.schema` `group_by` description (the AX-049 enumeration, `tool_schemas.py:343`) listed only `status`, NOT `outcome_status` — so a bot reading the schema never learns `outcome_status` exists while a bot reading the rejection error is offered it. Same advertising-vs-runtime drift class (`trade-trace-cs0r`/AX-049) on the one value `5da4c4c` missed: the enumeration-into-a-description pattern *moved* the drift instead of closing it. Fix: schema description names `outcome_status` (+ notes status alias); regression test strengthened to assert the description names EVERY allowlist value, not just a sample (the gap that let it slip). | **fixed** 2571022 (+1 regression test; effective next fresh session) |
| AX-049 (verify live) | 2026-06-05-05 | report | **AX-049 fix 5da4c4c VERIFIED LIVE this fresh session.** `report.compare(calibration, group_by=tag)` rejects with `VALIDATION_ERROR` listing the full allowed set; `tool.schema report.compare` `group_by` description enumerates the real per-`base_report` allowlists + states `tag` unsupported. Positive control `report.compare(calibration, instrument_id)` works. (Verifying it surfaced AX-050 above.) | **fixed (verified live)** 5da4c4c |
| (obs) calibration_integrity/filter_schema | 2026-06-05-05 | report | Last two genuinely-unprobed standalone surfaces probed, behave as designed (not filed): `report.calibration_integrity` standalone matches the embedded `integrity_diagnostics` block (six anti-goodhart diagnostics + `abstention_coverage`); minor doc drift — docstring enumerates 6 diagnostics, payload returns a 7th (`abstention_coverage`), additive/harmless. `report.filter_schema` returns the full canonical `ReportFilter` JSON Schema (8 sub-filters, `additionalProperties:false`, `__none__` sentinel documented inline) — clean. Report-surface catalog now fully probed across runs 01–05. | observation | — |
| AX-039/037/027/031/013/018/024/022/038/040/048 (re-verify) | 2026-06-05-05 | tool/adapter/report/onboarding | Standing re-confirms, no change: `report.work_queue` ok@N=3 (AX-039, curve fail@13/ok@5/ok@3×10); FOMC-no-change adapter bind (`mkt_F3k2AAPzU_S54ID7`) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.9825 (AX-027); numeric `liquidity_depth_json` 389670.77 (AX-031); skip linked `forecast_id`, `coach.unscored_forecasts.count:0` (AX-013); new fc `fc_H0rg8oEJJBehNAsZ` w/ explicit `resolution_at:2026-06-17` did NOT flag, exactly 3 `resolution_at_missing` stay flagged (AX-018/fe2f); multi-word recall (all 4 query tokens present in target memory) → RRF-floor ~0.03 only, distinctive term `AX-050`→rank #1 bm25:[1] (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv); bootstrap `active_ideas` nuked 0/61 `max_chars`, obligations 3/3 (AX-038); `abstention_coverage` 1/54/1.85% vs ~34 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048). | filed 1y9s/fe2f/95ry/xqjv/n33z/dlup/s7nn |
| AX-051 | 2026-06-05-06 | schema (`memory.link`) | **NEW (write-tool probing pivot — report catalog exhausted runs 01–05).** `memory.link` description promises `source_kind`/`target_kind`/`edge_type` are "validated against the documented enums", and runtime DOES reject bad values with self-documenting errors (verified live: bad `edge_type`→lists the 7 `about/supports/contradicts/supersedes/derived_from/violates/follows`; bad `source_kind`→lists the 13 endpoint kinds). But `tool.schema memory.link` advertised all three as bare `string` with no enum — registered with NO explicit `json_schema` (`memory.py:1337`), so the MCP schema auto-derived from `example_minimal` (only `memory_node/memory_node/supports`). A bot reading the schema couldn't discover the 13 source/target kinds or 7 edge types without first triggering a VALIDATION_ERROR; its sibling `memory.reflect` already advertises the identical 13-kind enum via `_MEMORY_REFLECT_SCHEMA`. Same self-documenting-schema class as AX-010 (enum absent from advertised schema), milder (error recovers). Fix: added `_MEMORY_LINK_SCHEMA` (enums sourced from `VALID_MEMORY_ENDPOINTS`/`EDGE_TYPES`) + registered it; additive, runtime unchanged. | **fixed** 2f1e170 (+1 regression test; effective next fresh session) |
| AX-038/018/027/031/037/024/022/040 (re-verify) | 2026-06-05-06 | tool/adapter/report/onboarding | Standing re-confirms, no change: bootstrap `active_ideas` nuked 0/62 `max_chars`, obligations 3/3 (AX-038); exactly 3 `resolution_at_missing` flagged (`fc_CheQKZ` live + US/UK recession), no new explicit-`resolution_at` fc this run (AX-018/fe2f); BTC ≥ $64k adapter bind (`mkt_hqqeWn4fuZvHCQt_`) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.2825 NOT lastTradePrice 0.269 (AX-027); numeric `liquidity_depth_json` 19374.03 (AX-031); multi-word recall (`blind commit independence proof reveal snapshot ordering`) → RRF-floor ~0.03 only, on-topic new reflection `mem_SLt1` absent from top-5; distinctive `blind_commit`→rank #1 bm25:[1] (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv); 2nd `abstention.record` recorded, `decision.add(skip)` undercount persists (AX-040/s7nn). | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| (obs) blind-commit independence + abstention + memory.link round-trips | 2026-06-05-06 | tool | Write veins probed, behave as designed (not filed): `forecast.commit_blind`→`reveal_snapshot`→`independence` proved order end-to-end (reveal_seq 434 > blind_commit_seq 432, `independence_proven:true`); BOTH refusal paths self-documenting (`reveal_snapshot` on non-blind → "call forecast.commit_blind first"; `commit_blind` on snapshot-anchored → "already has a market snapshot anchor"). `abstention.record`/`list`(by instrument+run)/`get` consistent round-trip. `memory.reflect` (+about-edge) and `memory.link` (`supports` edge `edg_ujtWHYv462RLZjtx`) round-trip into `memory.recall` provenance. | observation | — |
| AX-052 | 2026-06-05-07 | tool (`forecast.interpret_resolution`) | **NEW (write-vein probing).** Tool enforces "one interpretation per forecast" by SILENT first-wins. A SECOND call with a DIFFERENT idempotency_key AND a DIFFERENT payload (`market_contract` vs my original `oracle_feed`, different `interpreted_yes_condition`) returned `ok:true`, `idempotency_source:caller`, but echoed the ORIGINAL record (`resint_Fui3v1xyrNsr1kLV`) unchanged with NO caveat — a bot revising its resolution reading gets a false success and its revision is silently dropped. The forecast_id uniqueness guard (`resolution_interpretation.py:100-105`) is reached when the idempotency replay check (97-99) misses (different key). Distinct from intentional-design #7 (same key + diff payload → IDEMPOTENCY_CONFLICT). Self-documenting-output class. Fix: collision response carries `already_interpreted=true` + a caveat that the supplied values were NOT applied when they diverge; additive, append-only/idempotency-key enforcement untouched. | **fixed** d91e3c5 (+1 regression assertion; effective next fresh session) |
| AX-053 | 2026-06-05-07 | tool (`replay.evaluate_output`) | **NEW (write-vein probing).** The `candidate_metadata` criterion read metadata only from `candidate_output.metadata` (`replay_evaluate.py:205`), while the module is alias-tolerant for case_id/citations. All 8 required fields supplied under the obvious `candidate_metadata` key (mirroring the bundle's own `candidate_metadata_required`) reported every field "Missing required candidate metadata fields" — and there's NO published `replay.candidate_output.v0` schema for a bot to discover the right key. Error can't distinguish "absent" from "wrong key". Isolated with a valid single-case bundle (`bundle_contract`/`case_coverage`/`citation_coverage` all pass). Fix: accept `candidate_metadata` as a `metadata` alias (matches the module's existing alias-tolerance). | **fixed** 3a87db7 (+1 regression test; effective next fresh session) |
| AX-054 | 2026-06-05-07 | schema (`outcome.add`/`resolve.record`/`resolution.add`) | **NEW (AX-051-style audit lead paid off).** Registered with `**examples_for("outcome.add")` and no explicit `json_schema` (`outcome.py:212-219`), so the MCP schema auto-derived from `example_minimal`: `status` advertised as a bare `string` though the runtime rejects out-of-enum values with a self-documenting error (10-value `_OUTCOME_STATUSES`), and the auto-score-gating `confidence` field appeared only in `example_rich`, not `properties`. A schema-reading bot couldn't discover the status enum or that `confidence>=0.9` is required to auto-score. AX-051 (memory.link) class; also closes the AX-030 confidence-discoverability residual. Fix: `_OUTCOME_ADD_SCHEMA` (status enum from `_OUTCOME_STATUSES`, confidence documented w/ the auto-score gate) on both registrations; `resolution.add` alias inherits it. Additive; runtime unchanged. | **fixed** 1d86933 (+1 parity regression test across all 3 names; effective next fresh session) |
| AX-038/018/037/027/031/045/040/048/013/024/022 (re-verify) | 2026-06-05-07 | tool/adapter/report/onboarding | Standing re-confirms, no change: bootstrap `active_ideas` nuked 0/64 `max_chars`, obligations 3/3 (AX-038); exactly 3 `resolution_at_missing` flagged (`fc_CheQKZ` live + US/UK recession), new fc `fc_EDl7QETV9uG7ORu9` w/ explicit `resolution_at`=2026-06-05T16:00Z did NOT flag, `coach.unscored_forecasts.count:0` (AX-018/fe2f); both BTC-strike adapter binds (`mkt_hqqeWn4fuZvHCQt_` $64k, `mkt_vKG4bFEKOmOARW50` $70k) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.192 NOT lastTradePrice 0.194 (AX-027); numeric `liquidity_depth_json` 17307.06/100764.01 (AX-031); BTC-$64k NO `paper_enter` carried `declared_risk_amount=8.08` (AX-045); `abstention_coverage` 2/58/3.45% vs ~34 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048); skip `dec_B5MIlYq7Uc_Gkxrt` flagged no-forecast correctly (AX-013); distinctive multi-word recall → RRF-floor ~0.031, on-topic new memory absent from top-k (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv). | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| (obs) replay.* read/artifact veins + interpret_resolution happy path | 2026-06-05-07 | tool | Write/read veins probed, behave as designed (not filed): `replay.case_bundle` (blind_decision) clean — original artifacts + evaluation labels withheld, leakage protections enumerated, deterministic bundle hash; `replay.artifact_record`/`list`/`get` clean round-trip with self-documenting caveats (`small_sample_size`, `evidence_mode_paper`); `forecast.interpret_resolution` happy path + `forecast.resolution_interpretation` read-back clean (oracle_feed reading on `fc_EDl7QETV9uG7ORu9`, the exact axis `report.resolution_misreads` measures). | observation | — |
| AX-055 | 2026-06-05-08 | schema (`strategy.upsert`) | **NEW (write-vein audit, AX-051/054 continuation).** `strategy.create` (the public `strategy.upsert` create-mode) registered with `**_examples_for(...)` and NO explicit `json_schema`, so the MCP schema auto-derived from `example_minimal` advertised ONLY `name/slug/idempotency_key`. The runtime ALSO enum-validates `status` ∈ {active,archived} (verified live: bad status → self-doc error `must be one of ('active','archived')` w/ `details.allowed`) and accepts `description`/`hypothesis`/`meta_json` (shown only in `example_rich`). A schema-reading bot couldn't discover the status enum or the optional fields without triggering a VALIDATION_ERROR. Fix: `_STRATEGY_CREATE_SCHEMA` mirroring the runtime (status enum + optional fields); additive, handler unchanged. | **fixed** 69f66fd (+1 parity test strategy.create/upsert; effective next fresh session) |
| AX-056 | 2026-06-05-08 | schema (`import.commit`) | **NEW (write-vein audit).** `import.commit` registered with `**_examples_for(...)` and no explicit `json_schema`; `transaction_mode` advertised as a bare `string` (auto-derived from example `"single"`) though the runtime enum-validates it ∈ {single, per_row} (verified live: bad value → self-doc error `transaction_mode must be 'single' or 'per_row'`). Validation order: path-existence is checked before transaction_mode, so the enum surfaces only with a real path. AX-051 class (self-doc error, milder). Fix: `_IMPORT_COMMIT_SCHEMA` w/ the transaction_mode enum. | **fixed** 69f66fd (+1 parity test; effective next fresh session) |
| AX-057 | 2026-06-05-08 | schema/tool (`review.bundle`) | **NEW (write-vein audit).** `review.bundle` registered with NEITHER `json_schema` NOR an `example_minimal`, so `tool.schema` returned `json_schema:null` and the MCP `input_schema` had `properties:{}` — ZERO advertised params — even though the runtime `ReviewBundleInput` (extra=forbid) accepts a full ReportFilter `filter` plus `max_records`/`include_*`/`redaction_profile`/`max_examples_per_record`. Proven live: the output echoes a complete `filter` object (implying it's accepted) AND passing `{"filter":{...}}` failed `VALIDATION_ERROR dict_type loc=('filter',)` because the undeclared param was stringified by the MCP bridge. An MCP bot literally could not scope a reviewer bundle. Distinct from AX-055/056 (those hide an enum on an otherwise-advertised tool; this hides the ENTIRE input surface + breaks passthrough). Fix: `_REVIEW_BUNDLE_SCHEMA` mirroring `ReviewBundleInput` with `filter` as a typed object (so the bridge passes a dict) + all knobs + `redaction_profile` enum. Populated path otherwise behaves as designed (deterministic `bundle_hash`, `redaction_profile:audit_export`, 0 secret-pattern replacements, bodies/strategy_ids REDACTED). | **fixed** 69f66fd (+1 parity test; effective next fresh session — discoverability half advertised this session via the constant; live passthrough fix needs fresh server) |
| AX-058 | 2026-06-05-08 | tool/catalog (`playbook.*`) | **NEW (write-vein audit).** `report.playbook_adherence` POPULATED path is structurally unreachable from the v0.0.2 MCP catalog. `playbook.record_adherence` (catalog-visible) hard-requires a `playbook_version_id` (verified live: `pbk_` id → NOT_FOUND `playbook_version not found`); the only version-minting tool `playbook.propose_version` is folded into `V002_FOLDED_OR_REMOVED` (core.py:94, `catalog_visibility=legacy`, `redirect='playbook.upsert'`) — hidden from the catalog AND not dispatchable via MCP (verified: ToolSearch 'playbook propose version' returns upsert/record_adherence/report_playbook_adherence, NOT propose_version). The redirect target `playbook.upsert` runs `_playbook_create`, which creates only a playbook row, never a version. Net: an MCP-only bot can create a playbook + has a catalog-visible record_adherence but cannot mint the version it requires → `report.playbook_adherence` permanently `sample_size:0`. Secondary: `playbook.upsert` `status` is accepted as UNVALIDATED free-text (`_playbook_create` `status=args.get('status')`, no enum/default — stored `'flibbertigibbet'` verbatim live), asymmetric vs strategy.upsert's enum, unadvertised. Catalog-shape/design Q → filed, not fixed. Dedup: `bd search` playbook version/adherence/propose_version all empty. | filed | trade-trace-47tp (P2) |
| AX-037/027/031/045/038/018/040/048/013/024/022 (re-verify) | 2026-06-05-08 | tool/adapter/report/onboarding | Standing re-confirms, no change: BTC-$64k adapter bind (`mkt_hqqeWn4fuZvHCQt_`) preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037); `snapshot.fetch` price=mid 0.1235 NOT lastTradePrice 0.123 (AX-027); numeric `liquidity_depth_json` 26545.81 (AX-031); BTC-$64k NO `paper_enter` carried `declared_risk_amount=8.77`, `pos_IpXVdVjL6bZQrYDj` (AX-045); bootstrap `active_ideas` nuked 0/65 `max_chars`, obligations 3/3 (AX-038); exactly 3 `resolution_at_missing` flagged, new `fc_uHB-Ep0Of0b_mOgd` w/ explicit `resolution_at`=16:00Z did NOT flag, `coach.unscored_forecasts.count:0` (AX-018/fe2f); `abstention_coverage` 2/59/3.39% vs ~34 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048); skip-no-forecast flagged (AX-013); multi-word recall → RRF-floor ~0.031 (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv). NEW: `report.strategy_health` POPULATED path reached for the first time (runs 01–08) by seeding `strat_5im_MRMng09AggDU` — `strategy_count:1`, group `low_n:true`/`review_priority:2`, behaves as designed. | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| AX-055/056/057 (verify live) | 2026-06-05-09 | schema/tool | **Run-08 `69f66fd` VERIFIED LIVE on a fresh server.** strategy.upsert advertises `status` enum [active,archived] + optional fields; import.commit advertises `transaction_mode` enum [single,per_row]; review.bundle BOTH halves — discoverability (advertises `filter` object + knobs, was `properties:{}`) AND **passthrough** (`filter={"instrument":{"symbol":[...]}}` → self-doc VALIDATION_ERROR listing `supported_filter_paths`; `filter={"actors":{"actor_id":["agent:ax-dogfood"]}}` → full scoped bundle, deterministic `bundle_hash`, redaction_profile audit_export, 3 profile redactions, 0 secret-pattern). The bridge no longer stringifies the dict — an MCP bot can finally scope a reviewer bundle. | fixed (verified live) 69f66fd |
| AX-059 | 2026-06-05-09 | schema (`journal.fixture_seed`) | **NEW (write-vein audit).** Registered with `**_examples_for(...)` and no explicit `json_schema`; `target` auto-derived as a bare *required* string though the runtime enum-validates it against `FIXTURE_TARGETS` (proven live: bad target → self-doc error `must be one of ('mvp-eval-pm','forecast-only-pm','mvp-eval-rich','agent-continuity-loop')`, `details.allowed`) AND *defaults* it to `mvp-eval-pm` (so it is actually optional — second, defaulted-args drift). AX-055/056 class. Fix: `_FIXTURE_SEED_SCHEMA` (target enum, optional; required=[idempotency_key]). | **fixed** 9f3f80e (+1 parity test; effective next fresh session) |
| AX-060 | 2026-06-05-09 | schema (`memory.retain`) | **NEW (write-vein audit — highest-value instance).** Registered with `**_examples_for(...)` and no explicit `json_schema`; `node_type` auto-derived as a bare string though the runtime enum-validates it against `NODE_TYPES` {observation,reflection,playbook_rule} (memory.py:365, self-doc error). A bot calls `memory.retain` EVERY loop and could not discover the 3 valid node kinds (nor the importance/confidence_base/validity/meta_json knobs) without triggering a VALIDATION_ERROR. AX-051/054 class. Fix: `_MEMORY_RETAIN_SCHEMA` mirroring the runtime (node_type enum + optional knobs). | **fixed** 9f3f80e (+1 parity test; effective next fresh session) |
| AX-061 | 2026-06-05-09 | schema (`strategy.update`) | **NEW (write-vein audit, AX-055 sibling).** Registered with `**_examples_for(...)` and no explicit `json_schema`; auto-derived schema advertised only `strategy_id/description/idempotency_key` — HID the `status` enum {active,archived} (runtime enum-validates via `_STATUS_VALUES`, self-doc error, strategy.py:18) plus the `hypothesis`/`meta_json` update fields, AND falsely marked `description` REQUIRED on a partial-update tool (a bot updating only `status=archived` was wrongly blocked). Worse than AX-055 (hides the whole update surface + a false-required). Fix: `_STRATEGY_UPDATE_SCHEMA` (status enum, all update fields optional; required=[strategy_id,idempotency_key]). | **fixed** 9f3f80e (+1 parity test; effective next fresh session) |
| (obs) forecast.supersede | 2026-06-05-09 | schema | Next instance of the auto-derived class on a bot-facing tool: `kind` advertised as a bare string though the runtime accepts only `binary` (`_reject_non_binary_kind`), and the `outcomes` array structure (per-outcome `probability`) is undocumented. Milder (one valid `kind`); candidate for the next write-vein pass. Remaining no-`json_schema` registrations are enum-free (import.validate, journal.backup/restore), documented-in-description (journal.config_set), already filed (playbook.create → 47tp), or admin-only out of the trading surface. Not filed (backlog-noise avoidance). | observation | — |
| AX-037/027/031/045/013/018/040/048/038/024/022 (re-verify) | 2026-06-05-09 | tool/adapter/report/onboarding | Standing re-confirms, no change: 5 ETH-strike adapter binds preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037); `snapshot.fetch` price=mid (ETH-$1,700 0.2805 NOT lastTradePrice 0.346) (AX-027); numeric `liquidity_depth_json` (AX-031); ETH-$1,800 NO `paper_enter` carried `declared_risk_amount=9.85`, `pos_pNGilyAWk6l7zskZ`, no `missing_risk_budget` (AX-045); skip `dec_9uPhJZ3rBogGVo7b` linked forecast_id → `coach.unscored_forecasts.count:0` (AX-013); exactly 3 `resolution_at_missing` flagged, 2 new ETH forecasts w/ explicit `resolution_at`=16:00Z did NOT flag (AX-018/fe2f); `abstention_coverage` 2/61/3.28% vs ~34 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048); bootstrap `active_ideas` nuked 0/66 `max_chars`, obligations 3/3 (AX-038); multi-word recall → RRF-floor ~0.032 (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv); scx8 cross-run fragmentation re-confirmed (2 open NO rows on ETH-$1,800). | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| AX-062 | 2026-06-05-10 | tool/catalog/doc (`forecast.supersede`) | **NEW (write-vein pass on the run-09 `forecast.supersede` lead).** The tool has a real, distinct registered handler (`_forecast_supersede`, `ledger/forecast.py:552/770`) that performs the full append-only supersede flow (inserts the replacement forecast + outcomes, emits `forecast.created`/`supersedes` edge/`forecast.superseded`, drives the prior row → `scoring_state='superseded'`), but `core.py:77` places it in `V002_FOLDED_OR_REMOVED → redirect 'forecast.add'`, so the MCP catalog EXCLUDES it (verified live: no `mcp__trade-trace__forecast_supersede`; `tool.schema forecast.supersede` → `catalog_visibility:legacy`/`removed_in:0.0.2`/`redirect:forecast.add`). Two coupled defects: (1) the SHIPPED doc `scoring.md` (:105-107,:239-240,:193-224) AND the loop's pinned `intentional-design.md` item #6 both name `forecast.supersede` as THE recovery for `yes_label_ambiguous`/`label_mismatch`, but an MCP-only bot has no such catalog tool; (2) the redirect is semantically WRONG — `forecast.add` takes no `prior_forecast_id` and performs no supersede, so following it mints a SECOND live forecast (double-counts calibration) while leaving the prior un-superseded. Same doc/catalog-drift family as AX-058/`47tp`. Filed not fixed: catalog-shape/contract decision touching append-only forecast semantics (contract-firewall-adjacent); editing `scoring.md` to name `forecast.add` would be incorrect. Also: its auto-derived schema hides `kind=binary`-only + per-outcome structure (run-09 obs) — but fixing the schema alone wouldn't help a bot since the tool is uncatalogued. Dedup: `bd search` forecast.supersede/supersede/yes_label/'label correction'/'folded catalog' all empty. | filed | trade-trace-5z6r (P2) |
| AX-037/027/031/045/025/028/013/018/040/048/038/024/022/039/033 (re-verify) | 2026-06-05-10 | tool/adapter/report/onboarding | Standing re-confirms, no change: both BTC June-5 strike adapter binds (`mkt_hqqeWn4fuZvHCQt_` $64k, `mkt_ECzZqQcKrsA23gGg` $58k) preserved caller `resolution_rule_text` (AX-037); `snapshot.fetch` price=mid 0.056 NOT lastTradePrice 0.06 (AX-027); numeric `liquidity_depth_json` 31579.79 (AX-031); BTC-$64k NO `paper_enter` carried `declared_risk_amount=9.48`, `pos_iYQc6NTMc8WqGAwr`, no `missing_risk_budget`, marked side-aware NO −0.04 (AX-045); skip `dec_2djgByzgiGkGqPIS` linked forecast_id → `coach.unscored_forecasts.count:0` (AX-013); exactly 3 `resolution_at_missing` flagged, 2 new BTC forecasts w/ explicit `resolution_at`=16:00Z did NOT flag (AX-018/fe2f); `abstention_coverage` 2/65/3.08% vs ~34 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048); bootstrap `active_ideas` nuked 0/71 `max_chars`, obligations 3/3 (AX-038); multi-word recall (exact-term query) → RRF-floor ~0.033, just-written memory absent from top-5 (AX-024); cold bootstrap read/continuity tools only (AX-022/xqjv); scx8 cross-run fragmentation re-confirmed (4 open NO rows on BTC-$64k). NEW verify: `memory.retain` advertises `node_type` enum live (AX-060/`9f3f80e`). | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| AX-037/027/031/045/025/028/013/018/040/048/038/024/022/039/033 (re-verify) | 2026-06-05-11 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction.** Drove the full loop on a live BTC June-5 16:00Z strike ladder; triangulated spot ~$62k from a clean 3-strike sweep ($60k YES 0.939 / $62k YES 0.495 at-the-money competitive 0.9999 / $64k YES 0.049). All 3 adapter binds preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037); `snapshot.fetch` price=mid on all 3 ($62k 0.495 NOT lastTrade 0.56; $64k 0.049 NOT 0.056; $60k 0.939 NOT 0.927) (AX-027); numeric `liquidity_depth_json` 4497/10144/19333 (AX-031); BTC-$60k YES `paper_enter` 20@0.944 carried `declared_risk_amount=18.88`, `pos_UYjtjP8-EZLzhvN8`, no `missing_risk_budget` (AX-045); 4 open NO rows on $64k all marked side-aware vs mid 0.049 (NO complement 0.951), mark_state available (AX-025/028); both skips linked forecast_id → `coach.unscored_forecasts.count:0` (AX-013); exactly 3 `resolution_at_missing` flagged, 3 new forecasts w/ explicit `resolution_at`=16:00Z did NOT flag (AX-018/fe2f); `abstention_coverage` 2/68/2.94% vs ~36 skips (AX-040/s7nn); `coach.top_mistakes/strengths` `[]` (AX-048); bootstrap `active_ideas` nuked 0/73 `max_chars`, obligations 3/3 (AX-038); multi-word recall (exact terms from just-written `mem_SBTYpRr4wWrEpne1`) → RRF-floor ~0.034, memory ABSENT from top-3, distinctive `competitive 0.9999`→rank #1 bm25:[1] (AX-024); `report.work_queue` ok@N=3 (AX-039); cold bootstrap read/continuity tools only (AX-022/xqjv); scx8 re-confirmed (4 open NO rows on $64k, skipped a 5th fragmenting row via already_exposed). NEW verify (run-09 fixes, this fresh server): `journal.fixture_seed` advertises `target` enum (AX-059/`9f3f80e`); `strategy.update` advertises `status` enum + optional update fields, no false-required (AX-061/`9f3f80e`). | filed dlup/fe2f/95ry/xqjv/s7nn/n33z |
| AX-063 | 2026-06-05-12 | tool/contract (`journal.config_set`/`backup`/`restore`/`model.import`) | **NEW (admin/journal write-vein — a genuinely-unaudited surface).** Every write tool advertises `supports_dry_run=true` via `tool.schema` (`required_metadata`, computed `reg.is_write`), and the dispatcher honors it generically by setting `DRY_RUN_FLAG` which `UnitOfWork._commit` reads and rolls back (`core.py:450-457`, `unit_of_work.py:76-82`). But the raw-IO admin writers bypass `UnitOfWork` — `config_set` commits via a direct sqlite/`open_database` connection (`admin.py:603-627`), `backup` copies files, `restore` overwrites the live home, `model.import` stages a model dir — and gate only on `_confirm`, ignoring `DRY_RUN_FLAG`. So `_dry_run` was silently ignored and a "dry-run" mutated the live store. **Confirmed live:** `config_set` with `_confirm+_dry_run` returned `preview_only:false` and persisted `some.unknown.key=arbitrary` into the live axloop config table (probe row deleted afterward). Fix adds `_dry_run_active()` reading `DRY_RUN_FLAG` and short-circuits all four handlers to their existing would-write/would-copy/would-restore preview; +4 regression tests. Dry-run *contract* unchanged (not a firewall edit). Effective next fresh session. | **fixed** | 5aed8eb (+4 regression tests; live-confirmed repro on config_set) |
| AX-037/027/031/013/018/040/048/038/024/022 (re-verify) | 2026-06-05-12 | tool/adapter/report/onboarding | Standing re-confirms, no change: ETH-$1,800 adapter bind (`mkt_xKHM1hI9Jmbd4Vx5`) preserved caller `resolution_rule_text`; `snapshot.fetch price`=mid 0.003, numeric `liquidity_depth_json`; bootstrap `active_ideas` nuked 0/78 `max_chars`, obligations 3/3 (the 3 unresolvable `resolution_at_missing`); new ETH forecast w/ explicit `resolution_at=16:00Z` did NOT flag (fe2f positive control); skip linked `forecast_id` → coach `unscored_forecasts:0`; distinctive-term recall ranked new memory #1 `bm25:[1]`; cold throwaway bootstrap → read/continuity tools only. | (standing) | — |
| AX-064 | 2026-06-05-13 | schema (`market.bind`) | **NEW (write-vein audit — the highest-traffic instance of the auto-derived-schema class).** `market.bind`, the PRIMARY public entry tool (`catalog_visibility:public`), enum-validates 5 fields at runtime — `source`/`state`/`mechanism` (required, `_required_enum`, `market_bind.py:58-73`) and `resolution_source`/`ambiguity_kind` (optional, `_optional_enum`, :45-55) — each with a self-documenting `VALIDATION_ERROR` listing `sorted(allowed)`. But the registration passed no explicit `json_schema`, so the MCP schema was auto-derived from `example_minimal` and advertised all five as bare `{"type":"string"}` with NO `enum`. A bot reading `tool.schema` upfront cannot discover that e.g. `state ∈ {ambiguous,closed_for_trading,open,resolved,resolving,voided}` or `mechanism ∈ {amm,clob,hybrid,scalar}` without first triggering a failed write. Same class as AX-051(memory.link)/054(outcome.add)/055(strategy.upsert)/059(fixture_seed)/060(memory.retain), on the most-called tool. Fix refactors `register_market_bind_tool` to `derive_schema(example_minimal, optional_keys=…)` then injects `enum` (sorted, matching the runtime error order) on the 5 props and registers it explicitly — drift-proof (derives from the same example) and runtime-unchanged (discoverability only). +1 parity regression test mirroring the AX-055/060 tests. Dedup: `bd search "market.bind enum schema"`/`"schema enum discoverability"` both empty. Effective next fresh session. | **fixed** | db45fb6 (+1 parity test; effective next fresh session) |
| (env anomaly) | 2026-06-05-13 | infra/git | **Operational, not a product finding.** Working tree under concurrent modification during the run: `run.sh` stashed pre-existing debris to `stash@{1}` (axloop-debris, 28 files) at start; a separate pr2j WIP (17 files, open-position re-marking, `trade-trace-pr2j`) was in the tree by Phase D; a `stash push`/`pop` to isolate-verify conflicted and the tree file-set changed between commands (new files appeared) — concurrent-writer signature. Per playbook "git broken state → STOP, do not improvise": no checkout/reset/stash-drop, WIP preserved in `stash@{0}`+`stash@{1}`, only the isolated AX-064 commit + this report pushed. `origin/ax-dogfood@5bbbc13` is self-consistent. Needs a human to land/discard the WIP + clean the tree before the next mutating run. | flagged (handoff) | run report 2026-06-05-13 |
| AX-020/isqo (strengthen) + MILESTONE | 2026-06-05-14 | tool/error + calibration | **No new friction; first conclusive calibration panel.** `report.coach` flagged 21 forecasts past `resolution_at` (today's just-settled June-5 noon strikes). `outcome.fetch` dead-ended on all 10 instruments (`CONFIG_REQUIRED`, no Polygon RPC — AX-020). The no-RPC `snapshot.fetch`-mid path worked end-to-end: `price`/`mid` is the YES-contract price (AX-027), settled strikes read ≤0.002 (→NO) / ≥0.996 (→YES), unambiguous; recorded 10 `resolution.add` (`resolved_final`, confidence 0.9 per playbook) → **21 forecasts auto-scored, no inversions**. Crossed the `report.calibration` N≥20 floor (scored N 11→32, `sample_warning` now null; Brier 0.089, skill +0.583). Reliability bins reveal a genuine **overconfident-YES** signal at near-money bands (0.56–0.66 mean 0.597 & 0.20–0.30 mean 0.26 both observed 0). Evidence appended to isqo (the dead-end is pure signposting; the working route exists). | filed isqo (evidence appended) | — |
| AX-037/027/031/025/028/045/013/038/039/029/022 (re-verify) | 2026-06-05-14 | tool/adapter/report/onboarding | Standing re-confirms, no change: 4 BTC adapter binds (June-5 touch ladder `$64k/$65k/$66k` + monthly `dip-$55k`) preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037); `snapshot.fetch price`=mid on all (`$64k` 0.0305 NOT lastTrade 0.049; `dip-$55k` 0.472) (AX-027); numeric `liquidity_depth_json` (AX-031); `dip-$55k` YES `paper_enter` 25@0.473 carried `declared_risk_amount=11.83`, `pos_Vxe1iw_l7dUvAxMf`, no `missing_risk_budget`, marked side-aware YES −0.025 vs mark mid 0.472 (AX-025/028/045); both touch skips linked `forecast_id` (AX-013); bootstrap obligations 0/24 **and** active_ideas 0/82 both nuked `max_chars` (AX-038/dlup); `report.work_queue`/`report.lifecycle` over the MCP token cap, used jq-on-saved-file (AX-039/1y9s, AX-029/hv19); cold throwaway bootstrap → read/continuity tools only (AX-022/xqjv). | (standing) | — |
| AX-065 | 2026-06-05-14 | infra/test | **NEW (red suite at branch HEAD — contaminated prior commit).** `db45fb6` (run-13 AX-064 market.bind enum fix) bundled a stray one-line edit to `test_report_schemas_advertise_defaulted_args_as_optional`, adding `"limit","cursor"` to `report.lifecycle`'s expected-optional tuple. `report.lifecycle` has NO pagination params (intentionally-deferred AX-029/hv19: `truncated:False` hardcoded, no limit/cursor), so the assertion was false-by-construction and HEAD (`4f6c471`) was red. Never caught because run-13 couldn't run a clean Phase-D gate (its flagged env anomaly). Fix reverts the stray line → restores the pre-`db45fb6` correct assertion (NOT a weakening: still checks lifecycle's 5 real optional args). Loop lesson: a run that cannot complete its gate must not push. | **fixed** | e34d13a (gate-verified 2302p/7s green) |
| AX-066 / lszg | 2026-06-05-15 | tool/report (`open_positions`/`current_exposure`) | **NEW (closed-but-unlanded fix; partial direct fix this run).** Exercising the exposure surface: `report.open_positions` at `limit=10` (limit HONORED — 10 rows, `truncated:true`, `next_cursor` present, AX-029/hv19 pagination works) STILL overflowed the MCP token cap at 56,996 chars. Decomposition: `open_positions` rows 12,583 (bounded ✓) but `groups` 21,571 == `event_exposure_sets` 21,571 — `groups` is a **full duplicate** of `event_exposure_sets` (src lines 311-312 / 787-788: `"groups": event_exposure_sets`), 3.4× the bounded rows. `trade-trace-lszg` was CLOSED 2026-06-05 claiming this exact fix (`groups → {ref,count}` + current_exposure pagination + token-id trim), but the central drain-lane "did not commit/push" (its own contract note) so NOTHING landed: no commit in `git log -- portfolio_exposure.py`, and the claimed regression test is absent from the branch. **Direct fix this run:** e8e3005 replaces the re-embedded `groups` with `{ref:"event_exposure_sets",count}` at both sites (no consumer reads exposure-report `groups`; verified across src/tests), +1 regression test, gates green. **lszg REOPENED** for the residual design-level pieces it itself flagged as no-honest-small-fix: `current_exposure` still lacks `truncated`/`next_cursor`/`cursor` (silent-truncation hazard), `event_exposure_sets` unbounded (~21.5KB regardless of limit → ~35KB at scale post-fix), token-id trim unconfirmed. | **fixed (partial)** + **reopened** | e8e3005; trade-trace-lszg (reopened) |
| AX-024/95ry (re-confirm, sharpest) | 2026-06-05-15 | tool (`memory.recall`) | Immediately after `memory.reflect` wrote `mem_hI2EMse09wA1cKAf`, a recall for a phrase appearing VERBATIM in only that node missed it from top-5 (top-5 = older observations); a query with the guaranteed-unique body token `pos_7UesaoYvZwuuK` ranked it 9th/10. No returned item carried a `bm25` provenance rank in ANY recall this run — BM25/FTS5 contributes nothing to fused ranking (conjunctive-AND root cause). New angle: reproduces on `node_type=reflection` + a unique body token (rules out "term not distinctive"). | filed (evidence appended) | trade-trace-95ry (in_progress) |
| AX-020/isqo + fe2f + xqjv + AX-038 (re-verify) | 2026-06-05-15 | tool/report/onboarding | Standing re-confirms, no change: `outcome.fetch` dead-ends `CONFIG_REQUIRED` on 3 markets but now **signposts the no-RPC `snapshot.fetch` route** clearly (isqo fix live, no longer friction); 3 `resolve_due_forecast` obligations were false-positives on still-live markets (mids 0.505/0.175/0.385, none settled) flagged from `resolution_at_missing` only (fe2f); cold throwaway bootstrap → read/continuity tools only (`report.work_queue`/`agent.next_actions`/`report.recall_receipts`/`strategy.show`, xqjv); bootstrap `active_ideas` 0/59 nuked `max_chars` + `memory_context` not included (AX-038). Calibration N=32 conclusive, `sample_warning:null`, reaffirms overconfident near-money YES — acted on it (forecast $62k YES 0.27 below mid 0.305). | (standing) | filed fe2f/xqjv/95ry/isqo |
| AX-066 / lszg (residual fixed) | 2026-06-05-16 | tool/report (`current_exposure`) | **Silent-truncation CORRECTNESS residual FIXED.** `report.current_exposure` composed `report.open_positions` but DISCARDED its `truncated`/`next_cursor` (lines 762/789) and whitelisted `limit` without `cursor` (line 728), so a `limit`-bounded exposure read dropped rows beyond the page with NO signal — a bot could under-read its own exposure and believe it saw everything. Fix 543143f propagates `truncated`/`next_cursor` into the packet, whitelists `cursor` into the open_positions passthrough so paging works, and advertises `limit`+`cursor` in the schema (mirrors `report.open_positions`). Verified live that e8e3005 (`groups→{ref,count}`) is effective and `event_exposure_sets` track the limited page (limit=3 → 3 sets), so the prior "unbounded regardless of limit" worry is NOT current behavior. +2 regression tests; full gate green (2308p/7s). lszg remaining is design-level/lower-severity only: default no-limit call still defaults to 100 rows (high position counts can still exceed cap on the documented default); per-event-set identity still embeds two 78-digit CLOB token IDs (size opt, not correctness). Acute hazard gone → recommend downgrade/rescope. | **fixed (residual)** + lszg still open (design-level) | 543143f; trade-trace-lszg |
| AX-024/95ry (re-confirm, refined) | 2026-06-05-16 | tool (`memory.recall`) | Refined round-trip evidence. Reflection `mem_79LgYyGt7NvRzOzX` carried unique token `ZEBRAFINCH-LADDER-J6`. (1) recall `query=ZEBRAFINCH-LADDER-J6` + `strategies=['bm25']` → surfaced at **rank 1 WITH bm25 provenance** (round-trips fine in isolation). (2) Same token inside a multi-word **fused** query → node surfaced at rank 2 via **temporal/graph only, bm25 provenance ABSENT**, despite the query containing a token unique to that one node. So the node IS findable by bm25 in isolation; what fails is bm25 ranking WITHIN multi-word fused RRF (consistent with AX-024 conjunctive-AND + dotted/hyphenated split). Sharpens 95ry's failure mode. Minor (not friction): `mode` rejects `bm25` with a self-documenting error (allowed fused/per_strategy); isolate via `strategies=[...]`. | filed (evidence appended) | trade-trace-95ry (in_progress) |
| fe2f + n33z + xqjv (re-verify) | 2026-06-05-16 | report/tool/onboarding | Standing re-confirms, no change: 3 `resolve_due` obligations all `resolution_at_missing`, `fc_CheQKZ`/`mkt_pwuUfbLMcvhkia50` proven **live** (snapshot mid 0.505, accepting_orders:true) while flagged "due" (fe2f); both June-6 BTC strike binds had `resolution_rule.text` null with the Binance noon-ET rule only in `venue_metadata_json.description` (n33z); cold throwaway bootstrap → read/continuity suggested calls only, no first-run entry-point (xqjv). Calibration N=32 conclusive, `sample_warning:null`, reaffirmed near-money-YES overconfidence — acted on it (skip + tail-YES shade). | (standing) | filed fe2f/n33z/xqjv |
| AX-067 | 2026-06-05-17 | report (`report.resolution_misreads`) | **NEW (less-traveled-surface probe).** The contract-misread classifier scores `interpreted_resolution_source` against `markets.resolution_source` (`resolution_misreads.py:36`), but for the only live venue that field is a coarse adapter default: `adapter_polymarket.py:252` stamps `market_contract` (unless `disputed`) because Gamma exposes no enum-mappable resolution category. So a defensible `oracle_feed` reading of a UMA-over-Binance crypto strike → `contract_misread` against a default, and the report can never confirm an `oracle_feed`/`manual_review`/`arbitration` reading for Polymarket. Confirmed live (prior interp `fc_EDl7QETV9uG7ORu9`/`mkt_hqqeWn4fuZvHCQt_`). Ergonomic half fixed: surface per-group `actual_source_provenance` (=`bound_via`) + summary `contract_misread_adapter_bound_count` + caveat so a consumer can discount adapter-defaulted misreads. Design half (faithful mechanism mapping) filed. | **fixed** `81345c8` (+2 regression tests; effective next fresh session) · design half filed `trade-trace-v5va` (P2) |
| (obs) time_decay single-bucket | 2026-06-05-17 | report (`report.time_decay_sharpening`) | `report.time_decay_sharpening` collapses all 32 scored forecasts into one `0_24h` bucket (avg 8.15h to resolution) because the loop only ever scores same-day strikes → the time-decay curve can never populate. Data-shape of the hourly loop, not a product defect. Low severity; not filed. | observation |
| AX-037/027/031/013/018/038/024/022/s7nn (re-verify) | 2026-06-05-17 | tool/adapter/report/onboarding | Standing re-confirms, no change: 3 BTC month-window-touch binds ($70k/$77.5k reach, $57.5k dip) preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037), venue structured rule null (n33z); `snapshot.fetch price`=mid (0.145/0.748/0.505 NOT lastTrade) (AX-027), numeric `liquidity_depth_json` (AX-031); coach did not false-flag the 2 forecast-linked skips (AX-013); 3 `resolve_due` all `resolution_at_missing`, `fc_CheQKZ`/`mkt_pwuUfbLMcvhkia50` proven live mid 0.505 (fe2f/AX-018); bootstrap `active_ideas` nuked 0/65 `max_chars`, obligations 3/3 (AX-038); abstention_coverage 2/80 — skips not counted (s7nn); cold bootstrap read/continuity tools only (AX-022/xqjv). | re-verified |
| AX-024/95ry (re-confirm, sharpest) | 2026-06-05-17 | tool (`memory.recall`) | Unique token `QUOKKA-MISREAD-J6-1900` in the just-written reflection `mem_nJuuFZClr1yz8TeG`: (1) `strategies=['bm25']` recall → rank **1 with bm25 provenance** (round-trips in isolation); (2) a **fused** multi-word query *containing that verbatim token* buried the node entirely OUT of top-5 (top-5 = older temporal/graph observations). RRF lets temporal/graph swamp an exact unique-token bm25 hit — natural-language recall silently degrades to recency. | filed `trade-trace-95ry` |
| AX-SPY750 (live event) | 2026-06-05-18 | tool/report (resolution) | **First genuine same-day EQUITY resolution + an evidence CONFLICT.** Bootstrap resolve_due included real past-due `fc_A8wbW92` (SPY-$750 "closes above on June 5", `resolution_at_due_without_score`, due 20:00Z=4pm-ET Pyth close), alongside the 3 standing `resolution_at_missing` false-positives. The obligation gave `forecast_id`+`instrument_id` but no readable question/rule → had to re-`market.bind` idempotently to read the description (n33z, live at settlement). Light web research CONFLICTED (LLM summary claimed SPY ~$754 ⇒ YES) with the live orderbook (YES 0.0005, `bestAsk 0.001`, NO 0.9995, 8 min pre-close); the arbitrage-free near-settlement orderbook is authoritative (a liq-$2.1k market can't leave a ~1000× arb), web summary confabulated. Resolved **NO** conf 0.9 (playbook's unambiguous-outcomePrices path; not 0.99, no UMA post yet) → auto-scored Brier **0.7225** (`p_yes=0.85` overconfident-YES miss). Not a product defect (product has no web surface; snapshot/outcomePrices path worked). Lesson `mem_rzdYmGk2Hoi8B48K`. | observation (n33z/isqo/fe2f standing) |
| AX-067 (verify live) | 2026-06-05-18 | report (`report.resolution_misreads`) | **Run-17 fix `81345c8` VERIFIED LIVE on a fresh cron-booted server.** `report.resolution_misreads` now carries per-group `actual_source_provenance: "adapter"`, summary `contract_misread_adapter_bound_count: 1`, and the adapter-default coarseness caveat → the `fc_EDl7QETV9uG7ORu9` `oracle_feed`-vs-`market_contract` misread is now discountable. Design half remains `trade-trace-v5va`. | fixed (verified live) |
| AX-024/95ry (re-confirm) | 2026-06-05-18 | tool (`memory.recall`) | Same pattern as run-17 (not re-filed, backlog-noise): unique token `PANGOLIN-SPY750-J5-2001` in just-written `mem_rzdYmGk2Hoi8B48K` → bm25-only rank 1 w/ provenance, but a **fused** query containing the verbatim token buried it entirely OUT of top-5 (top-5 = older temporal/graph at RRF floor ~0.030, no bm25 provenance). | filed `trade-trace-95ry` (in_progress) |
| AX-037/027/031/013/018/048/038/022/s7nn (re-verify) | 2026-06-05-18 | tool/adapter/report/onboarding | Standing re-confirms, no change: 2 BTC binds ($70k reach, $55k dip) preserved caller `resolution_rule_text` (AX-037), venue rule null (n33z); `snapshot.fetch price`=mid ($70k 0.165, $55k 0.4755, SPY 0.01 — not lastTrade) (AX-027); numeric `liquidity_depth_json` (AX-031); both skips linked `forecast_id`, `coach.unscored_forecasts:0` (AX-013); 3 `resolution_at_missing` false-positives, positive control held (`fc_A8wbW92` flagged via `resolution_at_due_without_score`; 2 new forecasts w/ explicit `resolution_at`=2026-07-01 did NOT flag) (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); bootstrap `active_ideas` nuked 0/68 `max_chars`, obligations 4/4 (AX-038); abstention 2/82, skips uncounted (s7nn); cold bootstrap read/continuity tools only (AX-022/xqjv). Calibration N 32→33, the SPY miss sharpens overconfident-YES (bin 0.57-0.85 gap +0.693). | re-verified |
| AX-037/027/031/045/025/028/013/018/048/038/024/022/s7nn (re-verify) | 2026-06-05-19 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction.** Drove the full loop on the live BTC June month-window touch ladder (close 2026-07-01); BTC sold off intraday (reach-$70k YES oneDayPriceChange −0.16, dip-$55k +0.15) triangulating spot ~$60k. All 3 adapter binds ($70k/$77.5k reach, $55k dip) preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037), venue rule null (n33z); `snapshot.fetch price`=mid ($70k 0.175 not lastTrade 0.18; $77.5k 0.054 not 0.058; dip-$55k 0.3995 not 0.41) (AX-027); numeric `liquidity_depth_json` 39652/50504/34401 (AX-031); new dip-$55k NO `paper_enter` 20@0.6005 carried `declared_risk_amount=12.01`, `pos_Wyx2AotA`, no `missing_risk_budget`, marked side-aware flat `unrealized_pnl:0` (=1−mid 0.3995) (AX-045/025/028); both forecast-linked skips → `coach.unscored_forecasts:0` (AX-013); exactly 3 `resolution_at_missing` false-positives, 3 new forecasts w/ explicit `resolution_at`=2026-07-01 did NOT flag, `report.unscored_forecasts` count:0 positive control (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); bootstrap `active_ideas` nuked 0/70 `max_chars` (AX-038); multi-word fused recall containing verbatim unique token `MARMOT-DIP55K-J5-2100` buried the just-written node OUT of top-5 (RRF floor ~0.031, no bm25 provenance), bm25-only ranked it #1 w/ provenance (AX-024); abstention 2/85, ~34 skips uncounted (s7nn); cold bootstrap read/continuity tools only (AX-022/xqjv). NEW (not friction): now hold 25 YES (run-14) + 20 NO (this run) on dip-$55k — a legit two-sided HEDGE, not scx8 same-side fragmentation; `groups:{ref,count}` confirms e8e3005 live. Calibration N=33 unchanged (month-window forecasts settle July 1), overconfident-YES (0.57-0.85 observed 0) standing. | re-verified |
| AX-068 | 2026-06-05-2200 | tool (`market.refresh`) | **NEW (headline; novel surface — first time market.refresh exercised on a stale market).** `market.refresh` is **completely non-functional** on any cache-miss. The handler emits `event_type="market.refreshed"` when re-syncing a bound market row from live Gamma (`adapter_polymarket.py:361`), but that event type was never registered in the default-deny `SEMANTIC_KEYS` registry, so `events/log.py:224` hard-raises `KeyError: "event_type 'market.refreshed' is not registered in events_semantic_keys; refusing to write…"` and the whole call fails. Confirmed live on all 3 stale standing forecasts' markets (`mkt_pwu…`/`mkt_33h…`/`mkt_VMo…`, each with idempotency_key). A freshly-bound market masks it — the warm open-market 1h adapter cache short-circuits (`cache_hit:true`) before the emit path — which is why it survived 67 prior friction items: every prior run only ever refreshed via `market.bind` re-enrich or `snapshot.fetch`, never `market.refresh` on an aged row. A bot that calls `market.refresh` to detect resolution (open→resolved) is dead in the water; the no-RPC resolution path must use `snapshot.fetch` + `market.bind` re-enrich. **Fixed:** register `market.refreshed` mirroring `market.bound`; classify bucket-D (diagnostic, not import-replayable — re-fetches live) in jsonl-replay-taxonomy.md + `_DIAGNOSTIC_EVENT_TOOLS`; 3 regression tests incl. a **source-wide guard** asserting every `event_type="x.y"` literal in src is registered (the existing alignment lints only walked the exporter map, not emit sites). | **fixed** `4cb1307` (+3 regression tests; gate 2313p/7s; effective next fresh session) |
| AX-068 (verify live) | 2026-06-05-21 | tool (`market.refresh`) | **Run-20/22 fix `4cb1307` VERIFIED LIVE on this fresh cron-booted server.** `market.refresh` on the **stale** US-recession market `mkt_33hPArFcrg_A1Hbn` (aged adapter row, with `idempotency_key`) returned **`ok:true`** — `event_id:708`, `state_changed:false`, the row re-synced from live Gamma (outcomePrices [0.185,0.815], state still `open`) — instead of the prior hard `KeyError: event_type 'market.refreshed' is not registered`. `market.refresh` is now a usable open→resolved lifecycle-detection route alongside `snapshot.fetch`. The cache-miss/aged-row path (the exact trigger that masked the bug for 67 items) is the one now exercised green. | **fixed (verified live)** `4cb1307` |
| AX-037/027/031/045/025/028/013/018/048/024/038/022/s7nn/scx8 (re-verify) | 2026-06-05-21 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction.** Drove the full loop on the live **BTC** June month-window touch ladder (settle 2026-07-01). Intraday BTC selloff (reach-$70k YES mid 0.22 oneDayPriceChange −0.105; dip-$57.5k YES 0.561 +0.16; dip-$52.5k YES 0.2355 +0.09) ⇒ coherent ladder, edge ~0. 3 adapter binds (reach-$70k `mkt_7tACn4aqhw6y8BwN`, dip-$57.5k `mkt_2T1L59jjnoYXgS39`, dip-$52.5k `mkt_e86jIXc4vcxi7NZn`) preserved caller `resolution_rule_text` provenance=caller_supplied (AX-037), venue rule null (n33z); `snapshot.fetch price`=mid on all (0.22/0.561/0.2355 not lastTrade 0.23/0.564/0.23) (AX-027); numeric `liquidity_depth_json` 20297/18299/27138 (AX-031); thin 15u YES `paper_enter` dip-$57.5k @0.561 carried `declared_risk_amount=8.42`, `pos_WyFm4hU95R--dsYz`, no `missing_risk_budget`, marked side-aware unrealized ~0 (=entered at mid) (AX-045/025/028); both tail skips forecast-linked → `coach.unscored_forecasts:0` (AX-013); 3 standing `resolution_at_missing` false-positives — markets verified live (mkt_pwu mid 0.505 accepting_orders; mkt_33h open via `market.refresh`), NOT fabricated; 3 new forecasts w/ explicit `resolution_at`=2026-07-01 did NOT flag, `report.unscored_forecasts` count 0 (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); bm25-only recall of verbatim token `PLATYPUS-DIP575-J5-2300` → rank 1 w/ provenance, fused multi-word query containing it buried the node OUT of top-5 (AX-024/95ry); bootstrap `active_ideas` 0/78 nuked `max_chars` (AX-038); abstention 2/91 (2.2%), ~34 skips uncounted (s7nn); cold bootstrap read/continuity tools only (AX-022/xqjv). **scx8 re-confirmed:** dip-$57.5k YES paper_enter fragmented a 2nd row while prior June-4 YES (`pos_CwabrU` 20@0.434) open on same instrument+side, no `already_exposed` signal; nets correctly 35 YES side-aware. **Self-check:** dip-$57.5k YES forecast at **0.56** lands in WELL-calibrated bin 6 (0.49-0.56, observed 0.5, gap 0.01), deliberately NOT in the overconfident-YES bin 7 (0.57-0.85, observed 0, gap 0.693). Calibration N=33 Brier 0.109 skill 0.485. Gate 2313p/7s green. | re-verified |
| AX-027/045/024/048/018/013/s7nn/xqjv (re-verify) | 2026-06-05-2200 | tool/adapter/report/onboarding | **Standing-confirmation, NEW asset/ladder.** Drove the full loop on the live **ETH** June month-window touch ladder (close 2026-07-01) — first non-BTC dogfood. Intraday ETH selloff (dip-$1500 YES oneDayPriceChange **+0.35**); coherent ladder (reach-$2400 YES 0.0175, reach-$2600 0.009, dip-$1500 0.655) ⇒ genuine edge ~0. `snapshot.fetch price`=mid (0.0175/0.009/0.655, not lastTrade) (AX-027); thin 15-unit YES `paper_enter` on dip-$1500 @0.66 carried `declared_risk_amount=9.90`, `pos_4k3uu8d0NOlMXFWh`, no `missing_risk_budget`, marked side-aware (unrealized −0.075 = paid 0.66 vs mark 0.655) (AX-045); both reach-tail skips forecast-linked → `coach.unscored_forecasts:0` (AX-013); 3 standing `resolution_at_missing` false-positives — markets verified still-live (mids 0.505/0.19/0.34, all `accepting_orders`), NOT fabricated resolutions; 3 new forecasts w/ explicit `resolution_at`=2026-07-01 did NOT flag (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); bm25-only recall of verbatim token `OTTER-ETHDIP1500-J5-2200` ranked the just-written node #1 w/ provenance (AX-024); abstention 2/88, skips uncounted (s7nn); cold bootstrap read/continuity tools only (AX-022/xqjv). Calibration N=33 unchanged. **Self-check:** the new dip-$1500 YES 0.67 lands in reliability bin 7 (0.57-0.85, 3/3 observed_freq 0) — the standing overconfident-YES band; +1.5pt-over-mid restraint was the right discipline. | re-verified |
| AX-069 | 2026-06-06-01 | tool schema (`market.refresh`) | **NEW (headline; cold-bot Phase B, first action after bootstrap).** `market.refresh` is a retryable write (`is_write=True`) that is NOT in the auto-derivation registry (`TOOL_PRIMARY_EVENT_TYPE`) — so the dispatcher cannot synthesize an `idempotency_key` and the caller MUST supply it — but its MCP schema was auto-derived from `example_minimal={"market_id":"mkt_..."}` and advertised `required:["market_id"]` only. A schema-following bot's first call (mine, to check whether the 3 "due" forecasts' markets had closed) `VALIDATION_ERROR`s with `details.field="idempotency_key"`, `auto_derivation_available:false`. Identical class to `trade-trace-2cmb` (which fixed exactly this for the sibling fetch writes `snapshot.fetch`/`outcome.fetch`) — `market.refresh` was simply omitted from that fix. **Fixed:** add `idempotency_key` to `market.refresh`'s `example_minimal` so the derived schema marks it required, fold it into the shared 2cmb comment, and extend `test_fetch_idempotency_schema_parity.py` to cover `market.refresh` (now in `_FETCH_WRITE_TOOLS`). Not a contract-firewall item (schema honesty, not idempotency enforcement). | **fixed** `a501ca5` (+regression; gate 2313p/7s; effective next fresh session) |
| fe2f / xqjv (re-verify) | 2026-06-06-01 | report (work_queue) / onboarding (bootstrap) | **Standing design-Qs reconfirmed, fresh evidence appended (not re-filed).** **fe2f:** bootstrap+work_queue flagged 3 forecasts `resolve_due_forecast` urgency=due purely on `resolution_at_missing`; `market.refresh` proved all 3 markets **live** (Rihanna/GTA close 2026-07-31, US-recession 2027-01-31, UK-recession 2027-03-31) — left all open, fabricated nothing. **xqjv:** cold throwaway-journal bootstrap `suggested_process_calls` all read/continuity tools, `obligations:0`, no `market.search→bind→forecast` first-run breadcrumb. Loop forecast 2 BTC June strikes (reach-$70k YES 0.17, dip-$52.5k YES 0.27), both skipped insufficient_edge; both land in overconfident-YES reliability bins 3 (gap 0.165) & 4 (gap 0.28) at N=33 → shade-down validated. AX-068 (`market.refresh`) confirmed still live (3× `ok:true` with key). Calibration N=33 Brier 0.109 skill 0.485 unchanged. | re-verified |
| AX-068/069/064 + AX-027/031/037/045/025/028/013/018/048/040/024/038/022/066 (verify+re-verify) | 2026-06-06-0100 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction.** Drove the full loop on the live BTC June month-window touch ladder (settle 2026-07-01); ladder (reach-77.5k YES 0.044, dip-55k YES 0.3985 oneDayPriceChange +0.175, dip-50k YES 0.1465) triangulated spot ~$59-60k. **Verified live on this fresh server:** AX-068 (`4cb1307`) `market.refresh` functional on 3 stale/aged rows (3× `ok:true` w/ key, re-synced from live Gamma, all `open`, no `KeyError`); AX-069 (`a501ca5`) schema advertises `required:[market_id,idempotency_key]` (first schema-following call succeeded); AX-064 (`db45fb6`) `market.bind` advertises source/state/mechanism/resolution_source/ambiguity_kind enums. **Re-confirmed (no change):** `snapshot.fetch price`=mid on all 3 (AX-027); numeric `liquidity_depth_json` (AX-031); 3 adapter binds preserved caller `resolution_rule_text`, venue rule null (AX-037/n33z); dip-50k NO `paper_enter` 15@0.8535 `pos_7Cvu2i8Qd_eFs_5r` carried `declared_risk_amount=12.8`, no `missing_risk_budget`, side-aware NO mark `unrealized_pnl:0` (AX-045/025/028); `groups:{ref,count}` no dup (AX-066); both skips forecast-linked → `coach.unscored_forecasts:0` (AX-013); 3 `resolution_at_missing` flagged + proven live via `market.refresh`, 3 new fc w/ explicit `resolution_at`=2026-07-01 did NOT flag (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/96, 2 new skips uncounted (s7nn); bm25-only recall of `WOMBAT-DIP50K-J6-0100` → rank 1 w/ `bm25:[1]`, fused query containing the verbatim token surfaced it #2 via temporal/graph only, no bm25 provenance (AX-024/95ry); bootstrap `active_ideas` nuked 0/87 `max_chars` (AX-038); cold bootstrap read/continuity tools only (AX-022/xqjv). **Self-check:** dip-50k YES 0.12 lands in overconfident-YES bin 3 (0.12-0.20, observed 0, gap +0.165) — shaded below mid AND took NO side, both aligned. Calibration N=33 Brier 0.1087 skill 0.485. Gate 2313p/7s green. | re-verified |
| AX-064/027/031/013 + AX-018/017/024/040/048/022 (verify+re-verify) | 2026-06-06-0300 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction — FIRST full forecast loop on a SPORTS CHAMPIONSHIP FUTURE** (head-to-head 2026 NHL Stanley Cup Final; prior sports bind was the resolved Knicks/Spurs single game, AX-031 evidence only). Bound both "win the Cup" markets: Carolina (`mkt_r_dMck6c9wtEO3IK` YES mid **0.555**, $1.65M vol, oneDay +0.165 — took series lead) + Vegas (`mkt_nq9_ZKyQh8QDheM6` YES mid 0.4465, $2.98M vol, oneDay −0.1535). **KEY STRUCTURE:** a two-team final is a coherent COMPLEMENTARY PAIR — YES mids sum to **1.0015** (~0.15% vig), the head-to-head analogue of the crypto strike ladder ⇒ genuine edge ~0. Forecast both **at mid** (`fc_AWRqGmqUC9yPdfYL` 0.555 / `fc_eZbgvQVi5ecQqEAv` 0.4465, anchored to `snp_TlATCbVnxVYRjz8f`/`snp_qNi3UaIG74zCdurf`, explicit `resolution_at`=2026-06-30), did NOT shade (no edge ⇒ mid IS my estimate). **SKIPPED both** insufficient_edge (`dec_mCaR1-xCKE01wQFG`/`dec_wyw7opvgEtrkNsFi`) — 3% sports taker fee (`sports_fees_v2`) swamps a near-coinflip; paper_enter NOT forced. **Re-confirmed:** both binds used advertised enums, `bound_via:adapter` (AX-064); `snapshot.fetch price`=mid (0.555/0.4465) (AX-027); numeric `liquidity_depth_json` 143221.26/121366.96, no raw dump (AX-031); both skips forecast-linked → `coach.unscored_forecasts:0` (AX-013); 3 `resolution_at_missing` flagged (all 2027-close recession/long-horizon, fabricated nothing), 2 new fc w/ explicit `resolution_at`=2026-06-30 did NOT flag (AX-018/fe2f); `resolution_rule.text` **null** on both NHL rows, full rule description-only — new sports-futures category (AX-017/n33z); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/100, 2 new skips uncounted (s7nn/AX-040); bm25 recall of `PRONGHORN` → rank 1 w/ `bm25:[1]`+`temporal:[1]`, fused multi-word query w/ the verbatim hyphenated token dropped it from top-3 (AX-024/95ry); cold bootstrap (schema v29) read/continuity tools only, 0 obligations, no first-run breadcrumb (AX-022/xqjv). **Self-check:** both forecasts land in WELL-calibrated bin 6 (0.49-0.56, obs 0.5, gap 0.01) — forecasting AT mid on a genuine coinflip is calibration-sound, distinct from the shaded near-money-YES overconfidence (bins 3/4/7 obs 0). Calibration N=33 Brier 0.1087 skill 0.485. Gate 2313p/7s green. | re-verified |
| AX-068/069/064 + AX-027/031/017/013/018/048/040/024/022 (verify+re-verify) | 2026-06-06-0200 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction — FIRST full forecast loop on a FOMC rate-decision consensus market** (prior FOMC binds were AX-017 evidence only). Bound the June-2026 FOMC negRisk bucket: no-change (`mkt_F3k2AAPzU_S54ID7` YES mid **0.9865**, $10.3M vol, 0.001 spread, $1.3M liq) + cut-25 (`mkt_mIfccQafP7CI2cgk` YES mid 0.0065). Overwhelming no-change consensus = deep ITM/OTM (well-calibrated zone), NOT a near-money ladder. Forecast no-change YES 0.987 (`fc_NaR79mWzxh61Yei8`, at mid) + cut-25 YES 0.006 (`fc_my_FbT_dhwPEY0xe`, at mid), both anchored + explicit `resolution_at`=2026-06-17T18:00Z. **SKIPPED both** insufficient_edge (`dec_YBBuCzdzFK-ehYHf`/`dec_Ujw7n_AVoXdDdv6d`) — all mass at extremes ⇒ poor R/R (risk ~0.99 to make ~0.01) swamped by 5% economics fee; paper_enter NOT forced. **Verified live (fresh cron server):** AX-068 (`4cb1307`) `market.refresh` functional on aged Rihanna row (`ok:true` w/ key, re-synced, `open`); AX-069 (`a501ca5`) schema advertises `required:[market_id,idempotency_key]`, first call succeeded; AX-064 (`db45fb6`) both binds used advertised enums. **Re-confirmed:** `snapshot.fetch price`=mid (0.9865/0.0065) (AX-027); numeric `liquidity_depth_json` 1319055.86/620015.59 no raw dump (AX-031); `resolution_rule.text` **null** on both FOMC rows, full FOMC rule + no-statement conditional fallback description-only (AX-017/n33z, on the most common recurring macro structure); both skips forecast-linked → `coach.unscored_forecasts:0` (AX-013); 3 `resolution_at_missing` flagged resolve_due (the one closable proven live via `market.refresh` mid 0.505), 2 new fc w/ explicit `resolution_at` did NOT flag (AX-018/fe2f); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/98, 2 new skips uncounted (s7nn/AX-040); bm25-only recall of `PANGOLIN-FOMC-J6-0200` → rank 1 w/ `bm25:[1]`, fused multi-word query dropped it from top-3 entirely (AX-024/95ry); cold bootstrap (schema v29) read/continuity tools only, no first-run breadcrumb (AX-022/xqjv). **Self-check:** both forecasts land in WELL-calibrated extreme bins (no-change 0.987→bin 9 obs 1.0 gap −0.029; cut-25 0.006→bin 0); standing near-money-YES overconfidence (bins 3/4/7 obs 0) unchanged. Calibration N=33 Brier 0.1087 skill 0.485. Gate 2313p/7s green. | re-verified |
| AX-018/068/069/027/031/013/017/024/022/048/040 (settle+verify) | 2026-06-06-0400 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction — but a genuine behavioral FIRST: the loop's first full forecast→resolution→auto-score SETTLEMENT CYCLE on the intraday TOUCH ("reach $X on <date>") daily BTC ladder** (distinct from the noon-ET "above $X" point-in-time strikes settled in run-16). `work_queue` flagged 5 resolve_due; only 2 genuinely due — the June-5 touch pair reach-$64k (`mkt_Wxkna_mpchBYWIDh`/`fc_2jfW_utpl18Ps3om`) + reach-$65k (`mkt_6Bpdgm1VCoNBURgx`/`fc_GJSlnFhzth-6H_16`), both `resolution_at_due_without_score` due 04:00Z (just passed). `report.unscored_forecasts` returned EXACTLY those 2 (clean counter-surface to the 3 `resolution_at_missing` recession false-positives). `market.refresh` both (key, `ok:true`, AX-068/069): terminal `outcomePrices=["0.0005","0.9995"]` (BTC didn't touch June 5) ⇒ `resolution.add status=resolved_final outcome_label=no confidence=0.95` from one-sided outcomePrices; both **auto-scored** (intentional-design #4 end-to-end): Brier **0.000625** (p_yes≈0.025) + **0.000036** (p_yes≈0.006), deep-OTM bins 0/1. **Scan/forecast:** dedup via `memory.recall` (ZEBRAFINCH-LADDER-J6/run-18 already forecast June-6 $64k/$66k noon strikes — both binds `state_changed:false`, in `forecast_pending`, correctly NOT flagged due = AX-018 positive control); only the **$58k** June-6 noon strike was a fresh bind (`mkt_Q-w3HIxGx4SgvobY` YES mid 0.9425, `state_changed:true`) → forecast YES **0.93** (`fc_LHIDb7fc29SHqHK8`, anchored `snp_zbPGhtzejnfRSG39`, `resolution_at`=16:00Z), shaded just below mid per downside-momentum/overconfident-YES lesson; **SKIPPED** insufficient_edge (`dec_Je8nRXES_pj6xLDF`, forecast-linked → `coach.unscored_forecasts:0`, AX-013). **Re-confirmed:** `snapshot.fetch price`=mid on 3 binds (AX-027); numeric `liquidity_depth_json` (AX-031); `resolution_rule.text` null, full Binance touch/close rule description-only on both ladder types (AX-017/n33z); 3 recession `resolution_at_missing` false-positives (AX-018/fe2f); single-token "SANDPIPER" recall → rank 1 `bm25:[1]`, 6-token query (hyphenated token) dropped it from top-3 despite indexing (total_in_scope 58→59) (AX-024/95ry); cold bootstrap read/continuity tools only (AX-022/xqjv); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/101 (s7nn/AX-040). **Self-check:** both settlements near-perfect (bins 0/1); standing near-money-YES overconfidence persists (bin 7 0.56-0.85 mean 0.66 obs 0 gap +0.66). Calibration N 33→**35**, Brier 0.109→**0.102**, skill **0.498**. Gate 2313p/7s green. | re-verified |
| AX-017/064/069/027/031/013/018/024/022/038/048/040 (verify+re-verify) | 2026-06-06-0500 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction — FIRST full forecast loop on a GEOGRAPHIC mutually-exclusive negRisk grid** (Trump–Putin "meet next in &lt;location&gt;?"; prior negRisk were FOMC outcomes / China-GDP brackets / shutdown grid — this is a geography outcome-space, structurally new). Nothing resolvable (RUN_ID 05:01Z; June-6 noon ladder settles 16:00Z, ~11h out). `work_queue` flagged exactly 3 `resolution_at_missing` false-positives (Rihanna/GTA `fc_CheQKZ` + US/UK recession); `report.unscored_forecasts` **count:0** clean counter-surface — fabricated nothing. **Scan/forecast:** bound 2 grid legs US (`mkt_iLg0UkO4Ul6CU3VI` YES mid **0.0035**) + Russia (`mkt_Emu_2zPZxqGxyox_` YES mid **0.009**), both deep-OTM long-shots dominated by the "No meeting by June 30" residual; coherence US 0.0035 + Russia 0.009 = 0.0125 of grid YES mass. Forecast BOTH **at mid** (no edge on a coherent low-liq geopolitical grid): `fc_vXuo7cuJjydF7PyS` 0.0035 (anchored `snp_CalrdL7IhMtggcBy`) + `fc_KiD8l4xsizj5o8u3` 0.009 (anchored `snp_CwDK_TK84r46PkCY`), `resolution_at`=2026-06-30. **SKIPPED both** insufficient_edge (`dec_1RqgbcS_KXl22JzK`/`dec_lstCUfbS8b_RzInj`, forecast-linked → `coach.unscored_forecasts:0`, AX-013) — sub-cent prices, taker fee swamps. **Re-confirmed:** both binds used advertised enums `bound_via:adapter` (AX-064); `market.refresh` schema advertises `required:[market_id,idempotency_key]` (AX-069, observed in loaded schema, not exercised — nothing due); `snapshot.fetch price`=mid (US 0.0035 not lastTrade 0.005; Russia 0.009 not 0.01) (AX-027); numeric `liquidity_depth_json` 29448/44717 no raw dump (AX-031); `resolution_rule.text` **null** on both grid legs, full rule (meeting definition + no-meeting residual + Gulf-states def) description-only — NEW structural category, concise note appended to n33z (AX-017/n33z); bm25-only recall of `AARDVARK-TPGRID-J6-0500` → rank 1 `bm25:[1]`, fused multi-word query w/ the verbatim token ranked NONE of the just-written node (top-5 older temporal/graph RRF floor ~0.030, no bm25 provenance), indexed total_in_scope 59→60 (AX-024/95ry); bootstrap `active_ideas` nuked 0/96 `max_chars`, obligations 3/3 (AX-038); cold bootstrap (schema v29) read/continuity tools only, 0 obligations, no first-run breadcrumb (AX-022/xqjv); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/103 (1.94%), ~34 skips uncounted (s7nn/AX-040). **Self-check:** both forecasts land in well-calibrated deep-OTM extreme bins 0/1 (obs 0, tiny gap) — deliberately NOT the standing overconfident-YES bands (bin 7 0.56-0.85 mean 0.66 obs 0 gap +0.66; bins 3/4). Calibration N=35 (new forecasts pending; settle 2026-06-30) Brier 0.1025 skill 0.498. Gate 2313p/7s green. | re-verified |
| AX-064/027/031/037/013/018/024/022/038/048/040/017 (verify+re-verify) | 2026-06-06-0600 | tool/adapter/report/onboarding | **Standing-confirmation run, NO new friction — FIRST full forecast loop on a real-world ORGANIZATIONAL/POLITICAL ELECTION** (2026 Real Madrid presidential election; prior election-adjacent binds were subjective/conditional — Trump-insult, OpenAI IPO — or the geographic Trump–Putin grid, none a clean candidate-wins-the-office vote). Nothing resolvable (RUN_ID 06:01Z; June-6 noon ladder settles 16:00Z, ~10h out); `work_queue` flagged exactly the 3 standing `resolution_at_missing` false-positives (Rihanna/GTA `fc_CheQKZ` + US/UK recession), `report.unscored_forecasts` **count:0** clean counter-surface — fabricated nothing. **Scan/forecast:** searched live (`temperature`/`NBA`→0 w/ single-term `search_hint`, AX-035; `election`→15); bound 2 active legs — incumbent **Florentino Pérez** (`mkt_2JyUlhTJWtADIOH0` YES mid **0.965**, $25.7k vol) + only active challenger **Enrique Riquelme** (`mkt_jnjx8xc1DkIAgdGJ` YES mid **0.0355**); negRisk grid but other candidate legs `active:false` ⇒ coherent COMPLEMENTARY PAIR, the two YES mids sum to **1.0005** (~0.05% vig). No private info on a Real Madrid election (Pérez long-dominant incumbent since 2009) ⇒ genuine edge ~0. Forecast BOTH **at mid** (`fc_6oac5wDvKvBN9e0o` 0.965 anchored `snp_B5AHhLy8G5KMNy83` + `fc_qBq-mC4eU8KSAfSY` 0.0355 anchored `snp_rm6D49a-10b8kvT9`, `resolution_at`=2026-06-08). **SKIPPED both** insufficient_edge (`dec_CfJtjeeZPmDVydoW` Pérez deep-ITM poor R/R; `dec_YAR36hgsqQ5FS41-` Riquelme deep-OTM tail; forecast-linked → `coach.unscored_forecasts:0`, AX-013) — `sports_fees_v2` (3%) swamps. **Re-confirmed:** both binds used advertised enums `bound_via:adapter` (AX-064); `snapshot.fetch price`=mid (Pérez 0.965 not lastTrade 0.959; Riquelme 0.0355 not 0.095) (AX-027); numeric `liquidity_depth_json` 7457.97/11376.94 no raw dump (AX-031); caller `resolution_rule_text` preserved (AX-037); `resolution_rule.text` **null** on both legs, full rule (credible-reporting consensus + official-Real-Madrid-sources fallback + "resolve to Other if undetermined", source desc has a "June 31" typo) description-only — NEW category instance (AX-017/n33z); bm25-only recall of `CAPYBARA-RMADRID-J6-0600` → rank 1 `bm25:[1]` (in_scope 60→61), fused multi-word query w/ the verbatim token ranked NONE of the just-written node (top-5 older temporal/graph RRF floor ~0.028-0.030, no bm25 provenance) (AX-024/95ry); bootstrap `active_ideas` nuked 0/100 `max_chars`, obligations 3/3 (AX-038); cold bootstrap (schema v29) read/continuity tools only, 0 obligations, no first-run breadcrumb (AX-022/xqjv); `coach.top_mistakes/strengths` `[]` (AX-048); `abstention_coverage` 2/105 (1.9%), ~34 skips uncounted (s7nn/AX-040). **Self-check:** Pérez 0.965 → well-calibrated bin 9 (0.95-0.99, obs 1.0, gap -0.029); Riquelme 0.0355 → bin 2 (0.035-0.1, obs 0, gap +0.057) — both extreme well-calibrated bins, NOT the overconfident-YES bands (bins 3/4/7). Calibration N=35 (new forecasts pending; settle ~June 7-8) Brier 0.1025 skill 0.498. Gate 2313p/7s green. | re-verified |
