# AX dogfood run report — 2026-06-06-12

- **RUN_ID:** `2026-06-06-1100`
- **Branch:** `ax-dogfood` (up to date with `origin/main`; rebase clean, no-op)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma)
- **Disposition:** standing-confirmation pass — **NO new friction (11th consecutive)**. First forecast in a **new market category**: corporate quarterly-KPI / Tesla Q2-2026 vehicle-deliveries negRisk grid.

## Summary (as a bot)

The loop ran clean end to end. `report.bootstrap` → `report.unscored_forecasts`
(`count:0`, fabricated nothing) → scan/bind/snapshot/forecast/skip → review →
remember all behaved exactly as the prior 10 runs taught. Nothing was genuinely
resolvable this cycle (it is 11:00Z; the June-6 noon "above $X" BTC ladder
settles ~16:00Z, ~5h out; Real Madrid election June 7-8; ECB June 11; World Cup
July 19). The `work_queue`/`bootstrap` surfaced exactly the 3 standing
`resolution_at_missing` false-positives tracked in **fe2f** (Rihanna/GTA
`fc_CheQKZ`, US/UK recession `fc_uywJQC8os4NsVqIP` / `fc_I0VaLn1GHFFYpFYZ`); I did
not act on them.

Breadth came from a genuinely new domain. Prior categories exhausted: crypto
ladders, FOMC, NHL future, NBA, Real Madrid election, Trump–Putin geographic
grid, recession, OpenAI IPO, US×China trade-policy, box-office, awards/film-noms,
IPO-valuation cap, ECB monetary policy, FIFA World Cup. **This run: corporate
quarterly-KPI (Tesla Q2-2026 vehicle deliveries).**

## What I exercised

- `report.bootstrap` (cold continuity), `report.unscored_forecasts` (clean 0),
  `report.work_queue` (via bootstrap obligations), `report.calibration`,
  `report.coach`.
- `market.search` (probed Wimbledon→0, Nvidia→0, **Tesla→15**), `market.bind` ×6
  (located the distribution mode across the grid), `snapshot.fetch` ×2,
  `forecast.add` ×2, `decision.add(skip)` ×2 (forecast-linked),
  `memory.recall` (bm25) + `memory.retain` + recall round-trip.
- Cold-start throwaway-journal probe (`tt journal init` + `tt report bootstrap`).

## Forecasts (both at-mid, both skipped — insufficient_edge)

The Tesla Q2-2026 deliveries grid is a 9-bracket mutually-exclusive negRisk
partition (`negRisk=true`, close 2026-06-30, delivery report early July; source
`ir.tesla.com/press`). I bound 6 brackets to locate the mass and read a
**monotone-rising distribution toward the top end**:

| bracket | gamma id | YES mid | trend |
|---|---|---|---|
| <300k | 1846793 | ~0.004 | flat |
| 350-375k | 1846796 | ~0.087 | falling (-0.09/mo) |
| 375-400k | 1846797 | ~0.080 | falling (-0.15/mo) |
| 400-425k | 1846798 | ~0.1195 | falling (-0.065/mo) |
| **475k+** | 1846801 | **~0.2245** | **rising (+0.137 1wk)** — modal bin |

Forecast the two liquid top legs as a coherent pair:

1. **Tesla ≥475k deliveries Q2-2026** (`mkt_VW5lVi484LFBYlYH`, negRisk, the modal
   open-ended bin) — YES mid **0.2245** (bid 0.201/ask 0.248, $5.4k vol).
   `fc_0QU8-V8XZ3LBiIpp` at mid, anchored `snp_fXiqp2r69NpsbpGM`, resolution_at
   **2026-06-30**. Skipped `insufficient_edge` (`dec_Lk7kZ4ioQGoySdkr`).
2. **Tesla 400-425k deliveries Q2-2026** (`mkt_bG5D4ESwd5WUp7JN`, negRisk, the
   most-traded bin, $10.7k vol) — YES mid **0.1195** (bid 0.081/ask 0.158, wide
   0.077 spread). `fc_plVMGtUVNVpx4oOW` at mid, anchored `snp_krZfsrM1uUb3oaBf`,
   resolution_at **2026-06-30**. Skipped `insufficient_edge`
   (`dec__zz2mQ4HPo68TeHU`).

**Coherence:** 400-425k (0.1195) + 475k+ (0.2245) = 0.344 of the partition, with
the unbound 425-450k/450-475k brackets carrying much of the remaining high-end
mass — internally consistent (mutually-exclusive brackets, sum ≤ 1, monotone
toward the top). The grid's collective read is a strong Q2 delivery print.

**No-edge posture:** my knowledge cutoff predates Q1-2026 actuals and the
April/May-2026 production/registration data needed to beat a liquid consensus
3.5 weeks before quarter-end ⇒ anchor both at mid, skip both. Self-check: both
legs land in standing-overconfident low-prob bins (bin3 0.10-0.17 gap +0.14;
bin4 0.20-0.29 gap +0.247; obs 0 in each) — anchoring **to** mid (not above) is
the disciplined response.

## Friction found

**None new (11th consecutive clean run).** Re-verified live, all known/intentional:

- **AX-027** (snapshot `price`=mid, not `lastTradePrice`): 475k+ price 0.2245 vs
  lastTrade 0.20; 400-425k price 0.1195 vs lastTrade 0.081. ✓
- **AX-031** (numeric `liquidity_depth_json`): "1546.90146" / "1437.63865" on the
  two snapshots — no raw-payload dump. ✓
- **AX-013** (forecast-linked skip → coach not false-flagged): both skips carry
  `forecast_id`; `report.coach` `unscored_forecasts.count:0`. ✓
- **AX-017 / n33z** (resolution_rule.text null): all 6 Tesla binds had structured
  `resolution_rule.text=null` while the description carried a **consequential**
  multi-clause rule (default-to-lowest-bracket if unpublished by 2026-08-31;
  boundary value rounds to the higher bracket). Fresh new-domain evidence
  appended to **n33z**.
- **AX-024 / 95ry** (bm25-only recall): `Tesla deliveries` → `mem_TrvjFm…` rank 1
  `bm25:[1]`; `total_in_scope` 65→66. ✓
- **AX-022 / xqjv** (cold bootstrap entry-point gap): throwaway journal, schema
  v29, `suggested_process_calls` = [work_queue, agent.next_actions,
  recall_receipts, strategy.show] — read/continuity only, no
  market.search→bind→forecast first-run hint. ✓
- **fe2f** (resolve_due false-positives): the 3 standing `resolution_at_missing`
  obligations, all on live/non-due markets. ✓
- **s7nn** (abstention_coverage undercount): integrity reports 2/115 = 1.74%
  abstention share — counts only first-class `abstention.record`, not the
  ~38 `decision.add(type=skip)` considered-and-passed rows. ✓

## Fixes made

None (no new friction → no code change this run).

## Beads filed / updated

- **Updated:** `trade-trace-n33z` — appended fresh corporate-KPI-domain evidence
  (Tesla Q2-2026 deliveries grid; consequential default-to-lowest +
  boundary-rounds-up rule, null in `resolution_rule.text`).
- **Filed:** none (no genuinely new friction).
- Open ax-dogfood queue unchanged (8): `v5va`, `1y9s`, `lszg`, `n33z`, `s7nn`,
  `scx8`, `xqjv`, `fe2f`.

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2313 passed, 7 skipped** (113.37s). 7 skips are opt-in
  perf baselines.

## Calibration snapshot

N=35 scored (unchanged; the two new forecasts are pending until 2026-06-30).
Brier **0.102478**, skill **0.497859**, ECE 0.154, baseline 0.2857. Reliability
bins show low-prob bands (bins 0-4) still all-NO so far (overconfident shoulders),
consistent with anchoring my tail-bin Tesla legs to mid rather than above it.

## Handoff — next run

**Settlement opportunities (climb N past 35):**

1. **June-6 NOON "above $X" BTC ladder settles 16:00Z.** First cron after 16:00Z:
   resolve run-05 $58k (`mkt_Q-w3HIxGx4SgvobY`, `fc_LHIDb7fc29SHqHK8`, YES 0.93)
   + run-18 $64k (`mkt_mMrI9fVsyQDM0xrQ`) + $66k (`mkt_DaGR4nGM18wtBnBC`):
   `market.refresh` each, snapshot mid (≈0.999⇒yes ≈0.001⇒no), `resolution.add`
   conf≥0.9. BTC ~$59-60k ⇒ $58k likely YES, $64k/$66k NO.
2. **Real Madrid election** (`fc_6oac5wDvKvBN9e0o` Pérez 0.965 /
   `fc_qBq-mC4eU8KSAfSY` Riquelme 0.0355, resolution_at 2026-06-08) settles
   ~June 7-8. `market.refresh` both, snapshot mid, `resolution.add` conf≥0.9.
3. **ECB June-11** (`fc_j1u6uRp7ZTcjuCAf` 25bps-increase YES 0.9795 /
   `fc_sEhjgByFXdnZoAsa` no-change YES 0.0215, resolution_at 2026-06-11). After
   the ECB release: refresh both, snapshot mid, `resolution.add` conf≥0.9.
4. **US×China tariff** (`fc_-6WE3NqCmMuBEzaD` YES 0.08, resolution_at 2026-06-30).
5. **World Cup continent grid** (`fc_ssLiJq5aNIkjflfM` Europe YES 0.705 /
   `fc_F73MktmmX1SjMwiS` SAmerica YES 0.225, resolution_at 2026-07-19).
6. **Tesla Q2-2026 deliveries** (THIS run): `fc_0QU8-V8XZ3LBiIpp` 475k+ YES 0.2245
   / `fc_plVMGtUVNVpx4oOW` 400-425k YES 0.1195, resolution_at **2026-06-30**.
   After the early-July delivery report: `market.refresh` both, snapshot mid,
   `resolution.add` binary conf≥0.9 — winning bracket=YES, the other=NO.

**Longer-horizon pending:** 2 FOMC (after June-17), NHL Final pair (~late June),
Trump-Putin grid (June 30), BTC/ETH June month-window (July 1), box-office Wicked
(`fc_9iuBZBPGA1YqU1A1`, Dec 31), Oscars PHM (`fc_8z58I6-HwcgNB8IW`, 2027-02-28),
SpaceX IPO >$1T (`fc_71GDDR9PVjyp5m6g`, 2027-12-31).

**Novelty status:** report-surface + write-vein catalogs remain EXHAUSTED; novelty
now = settlement cycles + new market CATEGORIES. This run added corporate
quarterly-KPI (Tesla deliveries). Untried categories still available for future
breadth: tennis/golf majors (no live market this run), single-equity stock price
targets (none live), government-shutdown/legislative, Time Person of the Year,
weather/climate records.
