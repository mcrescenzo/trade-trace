# AX dogfood run 2026-06-04-03

- **RUN_ID:** `2026-06-04-0200`
- **Branch:** `ax-dogfood` (verified; never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma)
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (how it felt as a bot)

Drove the full MCP loop on live Polymarket. Orientation via `report.bootstrap`
→ `report.work_queue`/`report.lifecycle` was coherent; the two `resolve_due`
obligations were legible but **both markets are still actively trading** (fetched
live snapshots: `mkt_pwuUfbLMcvhkia50` mid 0.505, `mkt_39bP49PZ8zJ2t5Jx` mid
0.225, both `accepting_orders:true`), so I left both forecasts open rather than
invent outcomes. This is the standing AX-018 friction (filed `fe2f`). The big
positive: **AX-027 is now live this fresh session** — every fresh
`snapshot.fetch` returned `price == mid` (e.g. ETH 0.415, CPI 0.001), confirming
last run's mid-anchoring fix took effect on a new server boot.

The single real bug found this run came straight out of verifying AX-027:
after a fresh ETH snapshot landed (mid 0.415), `report.open_positions` updated
the row's `latest_mark` to 0.415 **but kept `unrealized_pnl=+$1.00`** — the value
implied by the *prior* 0.49 mark. A single row advertised `latest_mark.price=0.415`
and a PnL marked at 0.49 simultaneously. That is the exact "contradictory signal"
the AX-025 fix set out to kill, surviving for the non-null-stored case the AX-025
fix didn't cover. Fixed directly (AX-028). `memory.recall` again pulled the
directly-relevant prior lesson (`mem_KrD5guOtsKXrhkGE`: don't trust the PnL sign
on a fresh probe), which is exactly what flagged the contradiction.

## What I exercised

`report.bootstrap` → `report.work_queue` → `report.lifecycle` →
`snapshot.fetch` (×2 liveness) → `report.open_positions` → `memory.recall` →
`snapshot.fetch` (ETH, AX-027 verify) → `report.open_positions` (re-mark check)
→ `market.search` → `tool.schema` → `market.bind` (adapter) → `snapshot.fetch`
→ `forecast.add` (folded public path, snapshot-anchored) → `decision.add`
(`skip`, forecast-linked) → `report.calibration` → `report.coach` →
`memory.reflect` → `memory.recall` (round-trip) → cold-start probe (throwaway
journal init + bootstrap, destroyed).

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-028** | report | **med (correctness)** | `report.open_positions` / `report.current_exposure` left an open row's `unrealized_pnl` at the stale projection value while attaching a fresher `latest_mark`. Live: ETH YES position (entry 0.44) showed `latest_mark.price=0.415` (mid, post-AX-027) yet `unrealized_pnl=+$1.00` — i.e. still marked against the prior 0.49 snapshot. Intra-row contradiction; the AX-025 fix only re-marked the **null-PnL** case. | **fixed** (3824370) |
| AX-018 | report/error | low-med | Both `resolve_due` obligations (`fc_CheQKZ-UOTWPVUPs` / `mkt_pwuUfbLMcvhkia50`, reason `resolution_at_missing`; `fc_rHrzE9WuUh5qmILR` / `mkt_39bP49PZ8zJ2t5Jx`, reason `resolution_at_due_without_score`) are on **live** markets (verified mid 0.505 / 0.225, accepting_orders). `coach` re-nags "resolve or supersede" with no "market still live" state. | **not re-filed** (`trade-trace-fe2f`) |
| AX-017 | tool/schema | low-med | Re-confirmed on a fresh market: bound `mkt_JnHO0PZg7Q9_kLc0` (CPI ≤3.3%) — the full BLS resolution rule (12-month CPI to one decimal, release-date contingency) is in `venue_metadata_json.description` but structured `resolution_rule.text` is `null`. | **not re-filed** (`trade-trace-n33z`) |
| AX-024 | tool | low | `memory.recall "CPI inflation bucket ladder tail calibration probe"` ranked the only literally-CPI memory **#3**, behind two unrelated touch-market reflections; my new node surfaced only via temporal rank. Conjunctive BM25 degrading multi-word recall to recency, as filed. | **not re-filed** (`trade-trace-95ry`) |
| AX-022 | onboarding | (known) | Cold-start probe: clean init (29 migrations, schema_version 29); empty-journal `report.bootstrap` still surfaces only read/continuity guidance, no first-run entry-point. | **not re-filed** (`trade-trace-xqjv`) |

### Positive signals (system behaved well)

- **AX-027 confirmed live (fresh session):** every fresh `snapshot.fetch`
  returned `price == mid` — ETH `snp_YGESau96v2DK5SIT` price/mid 0.415; CPI
  one-sided book `snp_d58uQHQUTq07_CQN` price/mid 0.001 (no-book fallback path
  intact); liveness snapshots 0.505/0.225 likewise.
- **AX-025 base behavior holds:** marked rows carry populated `unrealized_pnl`
  + `mark_state:"available"` with no false `open_no_mark` (the value was wrong
  on the stale-stored axis — that is AX-028).
- **AX-026 / AX-023 again:** single-term `market.search` returned candidates
  with `search_hint:null`; `forecast.add` on a freshly adapter-bound market
  (snapshot-anchored) worked end-to-end.
- `memory.recall` surfaced the directly-applicable prior lesson that caught the
  AX-028 contradiction; the new reflection round-tripped (top-3, about-edge intact).

## Fixes made

- **3824370** — `fix(report): re-mark open positions from the live mark even
  when stored PnL is non-null (AX-028)`. In the shared `_position_row_payload`
  (`src/trade_trace/reports/tool_handlers/common.py`), dropped the
  `unrealized_pnl is None` gate so an open row is **always** re-marked side-aware
  from the latest mark's YES-contract price (canonical `trade-trace-ctvb`
  convention) whenever a live mark + entry/quantity inputs are present.
  `latest_mark` and `unrealized_pnl` can no longer disagree on a row; bucket/event
  totals read the same payload, so they stay consistent. +1 regression test
  (`test_report_open_positions_remarks_when_stored_pnl_predates_latest_mark`;
  reverted-fix confirms repro: stale 3.25 vs re-marked −0.26). Both
  `report.open_positions` and `report.current_exposure` go through this payload;
  **effective immediately for reads in any session** (it's a read-layer fix, no
  server-state dependency — though the live MCP server in *this* session still
  served the pre-fix path when the bug was observed).

## Beads filed / updated

- `trade-trace-pr2j` — appended AX-028 evidence: the
  `open_positions`/`current_exposure` slice of this cross-cutting inconsistency
  is now fixed (3824370). **Remaining pr2j scope:** `report.pnl` is a separate
  reader (does **not** use `_position_row_payload`; verified) and still reads the
  stale projection `unrealized_pnl`; the positions projection itself still only
  marks at rebuild time. Decision needed: read-layer re-mark in `report.pnl` vs.
  re-mark the projection on snapshot landing.
- No new beads filed (AX-028 fixed directly; the rest dedup to open beads).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2273 passed, 7 skipped** (+1 vs prior run = the new AX-028
  regression test; the 7 skips are the opt-in perf baselines).

## Records created this run

- Market `mkt_JnHO0PZg7Q9_kLc0` (Polymarket 2242373, "Will annual inflation be 3.3% or less in May?"), bound via adapter; resolves on BLS May CPI ~2026-06-10.
- Snapshots: `snp_69LsVqwlMA8KAFI2` (`mkt_pwuUfbLMcvhkia50` liveness), `snp_FO1nVgepAeFcEh1m` (`mkt_39bP49PZ8zJ2t5Jx` liveness), `snp_YGESau96v2DK5SIT` (ETH, AX-027 verify), `snp_d58uQHQUTq07_CQN` (CPI ≤3.3%).
- Forecast `fc_Uj2BX_otzxGZm0hg` (YES 0.015 tail-calibration vs market 0.001; resolution_at 2026-06-10T08:00Z), thesis `th_9OZwVl6L7Hh1iCZv`, anchored to `snp_d58uQHQUTq07_CQN` (market_implied 0.001).
- Decision `dec_K203xHa-f69wkRSF` (`skip`, `insufficient_edge`, forecast-linked — un-actionable 0.001-tick one-sided book).
- Reflection `mem_FvbyLGhXVjHJXz8b` (CPI bucket-ladder structure: tails un-actionable at the 0.001 tick, forecast near-mode for real calibration signal).

## Handoff notes for next run

- **AX-028 verification:** re-`snapshot.fetch` a market with an open position
  whose prior mark differs from the new mid, then `report.open_positions` — the
  row's `unrealized_pnl` must now match the new `latest_mark.price`. The ETH
  position `pos_WJcHuU_6umr7H0HN` will show the corrected (negative) PnL once a
  fresh post-fix read runs (the live server this session still served the old
  path). `report.pnl` is **not** fixed (pr2j remainder) — expect it to still lag.
- **Calibration is starved:** 0 of 14 forecasts scored — every market is still
  live / long-dated. Earliest genuine resolution is `fc_uZ-WmwfdrsEtISlE` (BTC
  <$64k, resolves **2026-06-04T16:00Z**) — the next run after 16:00Z should be
  able to score it (first real calibration data point). Then the CPI buckets
  (~2026-06-10).
- **Due forecasts still open (not resolvable):** `fc_CheQKZ-UOTWPVUPs`
  (`mkt_pwuUfbLMcvhkia50`) and `fc_rHrzE9WuUh5qmILR` (`mkt_39bP49PZ8zJ2t5Jx`) —
  both live. `fc_rHrzE9WuUh5qmILR`'s resolution_at (00:30Z) was set too early by a
  prior run; consider `forecast.supersede` with a corrected close once the true
  close is known (I did not, to avoid guessing the close time).
- **CPI ladder opportunity:** to get genuine near-term calibration signal,
  forecast a *near-mode* CPI bucket (3.6–4.0%) where you actually disagree with
  the consensus distribution — the tails (≤3.3% at 0.001) are un-actionable
  calibration-only probes. See `mem_FvbyLGhXVjHJXz8b`.
- **Tooling note (still true):** `gh pr edit` fails on this repo (GitHub
  "Projects (classic)" GraphQL deprecation); `git push` updates PR #2 fine, body
  can't be edited via `gh`. `bd dolt push` has no remote configured in this
  checkout (bd note appends persist only in the local embedded Dolt DB).
