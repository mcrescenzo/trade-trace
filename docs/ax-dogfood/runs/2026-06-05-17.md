# AX dogfood run 2026-06-05-17

- **RUN_ID:** 2026-06-05-1900
- **Branch:** ax-dogfood (verified)
- **Actor:** agent:ax-dogfood · journal home `~/.trade-trace-axloop` · live Polymarket
- **Rebase:** `git rebase origin/main` → already up to date.
- **Tree at start:** clean. Stashes `stash@{0}`/`stash@{1}` = human-owned axloop-debris (1500/1600), untouched throughout.

## One-paragraph summary

Drove the full canonical loop on the live June BTC **month-window touch** ladder
during an active intraday selloff. Bootstrap surfaced the standing 3
`resolve_due_forecast` false-positives (all `resolution_at_missing`; `fc_CheQKZ`
proven live via snapshot mid 0.505, accepting_orders:true) — recorded **zero**
resolutions (nothing genuinely settled; did not fabricate). Bound three BTC
strikes ($70k/$77.5k reach, $57.5k dip), all `resolution_rule.text` preserved
`caller_supplied` (AX-037), venue structured rule null (n33z). The ladder was
internally coherent (dip-$57.5k 0.748 surged +0.39 on the day; reach-$70k 0.145
−0.21) so per documented coherent-ladder discipline I claimed no edge: forecast
$70k YES 0.12 / $57.5k-dip YES 0.75 and **skipped both** (`insufficient_edge`,
forecast-linked — AX-013 held, coach did not false-flag). Calibration N=32
unchanged (Brier 0.089, skill +0.583; near-money-YES overconfidence reaffirmed —
validated the shade-down). **New finding (AX-067):** probing the less-traveled
`report.resolution_misreads` surface revealed its ground truth
(`actual_resolution_source`) is `market.bind`'s coarse adapter default
(`market_contract` for every non-disputed Polymarket market), so a defensible
`oracle_feed` reading of a UMA-over-Binance crypto strike is hard-classified
`contract_misread`. Fixed the ergonomic half directly (surface provenance + a
discountable count); filed the design half.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-067** | report (`report.resolution_misreads`) | **med (new)** | The misread classifier scores `interpreted_resolution_source` against `markets.resolution_source` (resolution_misreads.py:36), but for the only live venue that field is the adapter default: `adapter_polymarket.py:252` sets `market_contract` (unless `disputed`) because Gamma exposes no enum-mappable resolution category. A defensible `oracle_feed` reading of a UMA-over-Binance crypto strike → `contract_misread` against a coarse default; the report can never confirm an oracle/manual/arbitration reading for Polymarket. Confirmed live (prior interp `fc_EDl7QETV9uG7ORu9`/`mkt_hqqeWn4fuZvHCQt_` flagged). | **fixed** (ergonomic half, `81345c8`) **+ filed** (design half, `trade-trace-v5va`) |
| (obs) time_decay_sharpening single bucket | report | low | `report.time_decay_sharpening` collapses all 32 scored forecasts into one `0_24h` bucket (avg 8.15h to resolution) because the loop only ever scores same-day strikes — the time-decay curve can never populate. Data-shape of the loop, not a defect. | observation (not filed) |

### Standing re-confirmations (no change)

- **fe2f / AX-018:** bootstrap/work_queue flagged exactly 3 `resolve_due_forecast`, all `resolution_at_missing` (`fc_CheQKZ` + US/UK-recession 2027-close). `fc_CheQKZ`/`mkt_pwuUfbLMcvhkia50` proven **live** (snapshot mid 0.505, accepting_orders:true) while flagged "due". No genuine settlement → recorded 0 resolutions.
- **AX-037:** all 3 adapter binds preserved caller `resolution_rule_text` (`provenance=caller_supplied`). **n33z:** structured venue `resolution_rule.text` null (Binance month-window-touch rule only in `venue_metadata_json.description`).
- **AX-027:** `snapshot.fetch price`=mid (0.145 / 0.748 / 0.505), not lastTradePrice. **AX-031:** numeric `liquidity_depth_json` (41113 / 34988 / 29878), no raw dump.
- **AX-013:** `report.coach` did not false-flag the two forecast-linked skips as unforecasted. **s7nn:** abstention_coverage still 2/80 — `decision.add(type=skip)` rows are not counted as abstentions.
- **AX-038:** bootstrap `active_ideas` nuked 0/65 by `max_chars`, obligations 3/3 intact.
- **AX-024/95ry (sharpest):** unique token `QUOKKA-MISREAD-J6-1900` → bm25-only recall ranks the new node **1st with bm25 provenance**; the **fused** multi-word query *containing that verbatim token* buried it entirely out of top-5 (temporal/graph dominate the RRF). Natural-language recall silently degrades to recency.
- **AX-022/xqjv:** cold-start throwaway journal — bootstrap `suggested_process_calls` = read/continuity tools only (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`); no first-run `market.search→bind→forecast` entry point.

## Fixes made (atomic commits)

- **`81345c8`** — `fix(reports): surface actual_source_provenance in report.resolution_misreads …` (AX-067 ergonomic half). Adds per-group `actual_source_provenance` (= `markets.bound_via`), summary `contract_misread_adapter_bound_count`, and a caveat so a consumer can discount a misread that is only against an adapter default. +2 regression tests (`test_misread_surfaces_manual_source_provenance`, `test_misread_against_adapter_default_source_is_flagged_low_confidence`).

## Beads filed

- **`trade-trace-v5va`** (P2, task, ax-dogfood) — Design Q: `report.resolution_misreads` ground truth is the adapter-default `resolution_source`; decide how the adapter should faithfully map Polymarket's resolution mechanism (UMA-oracle → `oracle_feed`, subjective → `manual_review`) or whether the report should exclude/down-weight adapter-defaulted sources. (AX-067 design half.)

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2310 passed, 7 skipped** (skips = perf-baseline opt-ins) in ~90s.

## Loop artifacts (this run)

- Binds: `mkt_7tACn4aqhw6y8BwN` ($70k reach), `mkt_Vh36actmypkVNBiq` ($77.5k reach), `mkt_2T1L59jjnoYXgS39` ($57.5k dip).
- Forecasts: `fc_floGYHZX3d495wsb` ($70k YES 0.12), `fc_ifp9a3gWpu6_h2xT` ($57.5k-dip YES 0.75) — both `resolution_at` 2026-07-01T04:00:00Z (month-window; will not score until July).
- Skips: `dec_sE_howD-Oc5nOCOR`, `dec_kRbcolKnvIu1TCQw` (both `insufficient_edge`, forecast-linked).
- Interpretation: `resint_jt7WfNVMfFcpxpsk` (`fc_ifp9a3gWpu6_h2xT`, `market_contract`, classified `unresolved` — correct, still pending).
- Reflection: `mem_nJuuFZClr1yz8TeG`.

## Handoff notes for next run

- This run's two forecasts are **month-window touch** (close 2026-07-01); unlike same-day noon strikes they will **not** score this cycle — calibration N stays 32 until they settle in July. The loop's N-growth still depends on same-day point-in-time strikes (mem_8AkXjiw_OEDBBPHc).
- AX-067 fix (`81345c8`) is **effective next fresh session** — the live MCP `report.resolution_misreads` won't show `actual_source_provenance` until a new server boots. Verify via the unit test this session.
- `trade-trace-v5va` is the substantive AX-067 follow-up; a future write-vein/run could also probe whether `forecast.interpret_resolution` for *subjective/manual* markets (n33z compound/behavioral instances) compounds the misread unreliability.
- Standing live false-positive `fc_CheQKZ`/`mkt_pwuUfbLMcvhkia50` (fe2f) remains the canonical fe2f witness — still live mid 0.505.
