# AX dogfood run 2026-06-04-19

- **RUN_ID:** 2026-06-04-1800
- **Branch:** ax-dogfood (verified; rebase: up to date with origin/main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** evidence-gathering + N-seeding run, **no code shipped**.

## Summary (as a bot)

Bootstrapped cleanly. The 3 open obligations were the known unresolvable
`resolution_at_missing` set (fc_CheQKZ live + US/UK recession 2027-close) — no
resolvable inventory this run (last run resolved the deferred point-in-time
strikes; today's noon-ET strikes settled at 16:00Z, 2h before this run).

The handoff predicted "tomorrow's strikes not posted" — that turned out **only
partly true**: tomorrow's (June 5) point-in-time strikes ARE posted now for SOL
and, notably, for **two new asset classes I had never dogfooded — SPY (S&P 500)
and WTI Crude "closes above $X"**. These resolve at a known future time (SPY
20:00Z, WTI 21:00Z, SOL 16:00Z on June 5), so I seeded **3 genuine pre-settle
forecasts** that will resolve next-day and push calibration N (11→14 after
tomorrow's settle): SPY $750 YES 0.85, SOL $60 YES 0.95 (ITM tail), SOL $80 YES
0.02 (OTM tail; a consistent bracket on SOL spot ~$68–72). All forecast-only,
**no exposure** (wide/empty books → skip insufficient_edge).

It felt like a normal, smooth bot loop: orient → (nothing to resolve) → scan →
forecast → review → remember. The friction was all in the *resolution-rule
retrieval* surface (n33z) and orientation-envelope size (1y9s/dlup), both
already filed.

## Resolution loop

No resolvable forecasts. The 3 work-queue obligations all require external
input that does not exist yet (recession markets close 2027; fc_CheQKZ market
mkt_pwuU still live). Left open, correctly.

## Forecasts seeded (all resolve June 5, grow N)

| forecast | market | p_yes | resolution_at | book | decision |
|----------|--------|-------|---------------|------|----------|
| fc_A8wbW92buXzfKgoA | SPY closes >$750 (mkt_z1Z4…) | 0.85 | 2026-06-05T20:00Z | liq ~$1006, spread 0.60/0.97, impl 0.785 | skip insufficient_edge |
| fc_e0_3Gf823Q_OTm-c | SOL >$60 (mkt_1kNF…) | 0.95 | 2026-06-05T16:00Z | impl 0.975 ITM | skip insufficient_edge |
| fc_lfC-Bti3X5MZOJyY | SOL >$80 (mkt_b9TT…) | 0.02 | 2026-06-05T16:00Z | impl 0.005 OTM | skip insufficient_edge |

WTI $92 (mkt_oO0CUlqgOB34f5Qi) bound but **not forecast**: effectively unpriced
(liq $18.39, outcomePrices placeholder 0.5/0.5, spread 0.86) — no genuine market
reference to forecast against.

## Friction found this run (all already filed/fixed — no new direct-fix bug)

| surface | finding | disposition |
|---------|---------|-------------|
| tool/schema (n33z) | **NEW asset classes**: SPY + WTI "closes above" point-in-time markets. Pyth-Close reference (not Binance candle), explicit **50-50 non-trading-day fallback**, and WTI **active-month futures-roll** logic — all only in `description`; structured `resolution_rule.text` null. Most complex rule structure yet. | appended trade-trace-n33z |
| tool/adapter (AX-037) | All **4** adapter binds preserved caller `resolution_rule_text` (provenance=caller_supplied). Fix e9892cd effective in-session. | verified (prior SHA) |
| tool/report (AX-027) | snapshot `price`=mid on all 3 snapshots: SPY 0.785, SOL$80 0.005, **SOL$60 0.975 NOT lastTradePrice 0.988** (un-executable). | verified (752a4bf) |
| tool/adapter (AX-031) | All snapshots numeric `liquidity_depth_json`, no raw dump. | verified (9e10fc2) |
| report (AX-013) | `report.coach` `unscored_forecasts=0`; the 3 new forecasts linked via skip `forecast_id`, no false unforecasted flag. | verified |
| report (fe2f) | Positive control: 3 new forecasts w/ explicit `resolution_at` did NOT flag; only the 3 `resolution_at_missing` did. Suppression works across new asset classes. | appended trade-trace-fe2f |
| tool (AX-039) | **N-proportionality curve completed**: work_queue SUCCEEDED at N=3 (after fail@13, ok@5). Threshold between 5 and 13. | appended trade-trace-1y9s |
| report (AX-038/dlup) | bootstrap at default budget nuked `active_ideas` (0/40, reason **max_chars** — fix a0d28a2 verified live) while `obligations` **survived** (3/3) at small N. The 40-item ideas section is consistently nuked; obligations nuke only at larger N. | dlup (design half) |
| tool (AX-024/95ry) | New memory containing exact query terms ranked **below** older temporal/graph hits on a multi-word recall ("SPY WTI…Pyth…futures roll"); single-term "Pyth" → rank #1. Conjunctive-BM25 degradation. | re-confirm trade-trace-95ry |
| onboarding (AX-022/xqjv) | Cold throwaway-journal bootstrap → only read/continuity tools (work_queue, next_actions, recall_receipts, strategy.show); no first-run entry point. | re-confirm trade-trace-xqjv |
| obs (market structure) | No near-the-money SOL June-5 strike: ladder jumps $60 (0.975 ITM) → $80 (0.005 OTM) with SOL spot ~$68–72 in an unstruck gap. Only deep-tail SOL forecasts available, not coin-flips. | observation (not filed) |

## Memory

Retained reflection mem_W0_PtHjX6okuW6z1 (traditional-asset Pyth structure +
50-50 fallback + futures roll + SOL ladder gap). Round-trip confirmed via
single-term "Pyth" → rank #1 (multi-word miss is the 95ry behavior).

## Fixes / beads / reverts

- **Fixed & gate-verified:** none (no code changed).
- **Beads filed:** none (all findings dedup to existing open beads).
- **Beads updated (evidence appended):** trade-trace-n33z (SPY/WTI new asset
  classes), trade-trace-1y9s (work_queue N=3 success completes proportionality
  curve), trade-trace-fe2f (clean control pair, new asset classes).
- **Attempted-then-reverted:** none.

## Gate result

- `ruff check src tests` → All checks passed
- `mypy src` → Success: no issues found in 178 source files
- `pytest -q` → **2279 passed, 7 skipped in 104.14s** (baseline; no code change)

## Handoff for next run

- **3 forecasts resolve June 5** (SPY $750 @20:00Z; SOL $60 & $80 @16:00Z).
  After 16:00Z June 5 the two SOL strikes are resolvable (web ground-truth SOL
  noon-ET Binance close); after 20:00Z SPY is resolvable (Pyth SPY close). A run
  in the post-settle window should resolve all 3 → calibration N 11→14, a real
  step toward the 20-floor. The SPY/SOL$60 are high-confidence (0.85/0.95) so
  correct resolution should improve the sparse upper reliability bins.
- **Tomorrow's (June 5) inventory is already posted** for SOL + SPY + WTI — so
  seeding can continue. If BTC/ETH June-5 strikes appear (only June-4 + month
  markets were visible this run), prefer near-the-money strikes for coin-flip
  calibration points; SOL has no near-money June-5 strike (gap $60→$80).
- **N still 11** (< 20 floor). Continue the resolve→score loop each run.
- No Dolt remote configured → `bd` notes persist **local only** (commit carries
  .beads/ JSONL).
