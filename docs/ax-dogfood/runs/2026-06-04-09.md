# AX dogfood run 2026-06-04-09 (RUN_ID 2026-06-04-0800)

Bounded AX dogfood pass on `ax-dogfood`. **No source code changed** — a clean
re-confirmation run on a fresh market category (macro GDP/recession markets).
No new friction surfaced; everything observed re-confirms already-filed items.
One bead updated with new-category evidence (n33z). Gates green.

## Summary (as a bot)

Drove the full canonical loop on two **fresh, non-crypto, longer-dated** binary
markets to diversify away from the accumulated 16:00Z BTC/ETH cluster and avoid
piling duplicate exposure (AX-033). The surface felt smooth end-to-end:
`market.search` (single-topic, clean), adapter `market.bind` (AX-021 fix
holding), `snapshot.fetch` (clean numeric `liquidity_depth_json` on both —
AX-031 holding on a new market type), the folded `forecast.add` path with
`_anchor_to_latest_snapshot`, `decision.add(skip)` carrying the
`material_non_action` taxonomy, and `report.calibration`/`report.coach` (honest,
actionable, correctly low-N). Memory retain→recall round-tripped. As a bot with
no genuine private edge over efficient macro markets, the disciplined play was
**forecast≈mid + skip(insufficient_edge)** on both, not paper entries.

## What I exercised

- **Orient:** `report.bootstrap` → `report.work_queue`. Single obligation, the
  known `fc_CheQKZ-UOTWPVUPs` resolve_due re-flag (AX-018, live mid ~0.505 in
  prior runs). At 08:00Z nothing is genuinely due (the four near-term BTC/ETH
  forecasts resolve at the 16:00Z noon-ET close). Did **not** fabricate a
  resolution.
- **Forecast (2):** bound US recession (`mkt_33hPArFcrg_A1Hbn`, ext 609655,
  mid 0.185, $1.5M vol, 1c spread) and UK recession (`mkt_VMoCTL05SJkKDfWJ`,
  ext 2061857, mid 0.38, ~$1.4K depth, 4c spread). Forecasts
  `fc_uywJQC8os4NsVqIP` (p_yes 0.17, low) and `fc_I0VaLn1GHFFYpFYZ` (p_yes 0.37,
  low), both anchored to their snapshot.
- **Decide (2):** `skip` both with `insufficient_edge`
  (`dec_6GBI-Um7LuweeUOJ`, `dec_bSOBmhhd9mAiDj70`), each linking `forecast_id`.
- **Review:** calibration still N=1 (Brier 0.3969); coach `unscored_forecasts=0`,
  no mistake false-flag.
- **Remember:** retained `mem__bP41TJATaVizIkc` (macro-market skip lesson),
  confirmed BM25 single-token round-trip.
- **Cold probe:** throwaway journal init + bootstrap, then destroyed.

## Friction found

No NEW friction. All observations re-confirm already-filed items:

| id | surface | severity | observation this run | disposition |
|----|---------|----------|----------------------|-------------|
| AX-017 | tool/schema | medium | `resolution_rule.text` null on BOTH recession binds; full BEA/NBER (US) and ONS (UK) two-consecutive-negative-quarter rules only in `venue_metadata_json.description`. New market category (rule-sensitive macro GDP markets) where the gap is most costly. | filed → n33z (evidence appended) |
| AX-024 | tool | medium | `recall("recession")` fused returned 5 recency nodes, **none** lexically matching "recession" (total_in_scope 14); after retaining a node containing the token, BM25 per_strategy returned exactly that one node. Reconfirms fused-recency-masks-conjunctive-BM25. | filed → 95ry |
| AX-022 | report/onboarding | medium | Cold-probe empty-journal bootstrap → only read/continuity `suggested_process_calls` (work_queue/next_actions/recall_receipts/strategy.show), 0 obligations/ideas, no first-run entry-point. | filed → xqjv |
| AX-013 | report | — | coach `unscored_forecasts=0`, no false-flag on the two forecasted-then-skipped markets. | holds (fix verified) |
| AX-018 | report | — | `fc_CheQKZ` still sole resolve_due via `resolution_at_missing` on a live market. Expected. | filed → fe2f |
| AX-031 | tool/adapter | — | clean numeric `liquidity_depth_json` (29176.8779, 1396.922) on both binds — no raw-dump on a fresh market type. | fixed, re-verified |
| AX-004/005 class | error | — | `memory.recall mode="bm25"` → self-documenting VALIDATION_ERROR listing allowed `[fused, per_strategy]`. | holds |

## Fixes made

None (no new bugs; no contract-firewall-safe small fix exists — all
re-confirmations are design-level, already filed).

## Beads filed

None new. **Updated:** `trade-trace-n33z` — appended macro-market (US/UK
recession) evidence: both binds had `resolution_rule.text=null` with the full
rule in `description`; reinforces the map-description→resolution_rule.text fix.

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2275 passed, 7 skipped** in 101.72s (unchanged — no code touched)

## This run's journal artifacts

- Forecasts: `fc_uywJQC8os4NsVqIP` (US recession, p_yes 0.17, low; skip
  `dec_6GBI-Um7LuweeUOJ` insufficient_edge) and `fc_I0VaLn1GHFFYpFYZ` (UK
  recession, p_yes 0.37, low; skip `dec_bSOBmhhd9mAiDj70` insufficient_edge).
  Both markets close 2027 — they do NOT contribute to near-term calibration.
- Memory: `mem__bP41TJATaVizIkc` (macro-market: liquidity tier gates whether the
  mid is a usable anchor; forecast≈mid + skip when no edge).
- No new exposure opened (both skipped); open paper positions unchanged at 9.

## Handoff notes for next run

- **Due 16:00Z today (2026-06-04):** FOUR near-term forecasts resolve at the
  Binance noon-ET (16:00Z) close — prior-run `fc_7p8h6j` (BTC $64k),
  `fc_0K8m09` (BTC $60k), ETH `fc_uZ-Wmwfd` (`mkt_BxqIYenOQjh6ta1l`) and
  `fc_1yoUooDa` (`mkt_SohHCLs3qtCWRWbT`, ETH>$1,800). If a run fires **after
  16:00Z**, resolve from the venue close and **pass `confidence` explicitly**
  (≥0.9 venue-unambiguous; AX-030 only surfaces `confidence` in `example_rich`).
  Don't fabricate. These finally lift calibration off N=1.
- **AX-034 workaround until lszg lands:** with 9 open positions, ALWAYS
  `report.open_positions limit=3` + page `next_cursor`; NEVER
  `report.current_exposure` or unbounded `open_positions` (both blow the token
  cap).
- **Open exposure = 9 paper positions** (unchanged this run; this run added
  none). Per AX-033, check `open_positions` before re-entering any market.
- `fc_CheQKZ` keeps re-flagging resolve_due (AX-018/fe2f) — expected, live
  market, don't fabricate.
- The two recession forecasts (`fc_uywJQC8os4NsVqIP`, `fc_I0VaLn1GHFFYpFYZ`) are
  open but won't be resolve_due for a long time (close 2027); no action needed.
- Highest-priority untouched bead: `trade-trace-4z2t` (market.refresh, P1).
