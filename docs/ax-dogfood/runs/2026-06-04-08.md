# AX dogfood run report — 2026-06-04-08

- **RUN_ID:** `2026-06-04-0700`
- **Branch:** `ax-dogfood` (never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma) enabled
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (how it felt as a bot)

Mostly smooth, with one genuinely sharp **new** finding that bit me mid-flow.
Bootstrap → work_queue → lifecycle oriented cleanly; the single derived
obligation was the known still-live false-positive (`fc_CheQKZ`), which I
declined to fabricate an outcome for after a fresh `snapshot.fetch` confirmed the
market is live (mid 0.505, accepting_orders:true, $800K vol). But when I tried to
do the responsible thing — check my open exposure before sizing a new probe (the
`mem_4zRTSDdMFuzcfAJs` lesson) — the **recommended** entry point
`report.current_exposure` **hard-failed at the MCP transport layer** (~69KB,
exceeded the token cap) with **no `limit`/`cursor` knob to recover**, and the
documented default `report.open_positions` call failed the same way (~61KB). I
could only read my own exposure by calling `report.open_positions` with an
explicit `limit=3` and paging the cursor by hand — and the journal turned out to
hold **9** open paper positions across runs. That's the headline (AX-034, filed).

Everything else behaved. I scanned the live BTC "above $X on June 4" daily ladder
(point-in-time noon-ET settlements, distinct from my multi-week touch exposure),
bound two strikes, and ran two honest **near-term** forecasts that resolve at
**16:00Z today** — exactly the calibration data the journal needs (stuck at N=1):
**$64k** YES p 0.57 (market-anchored, no edge → disciplined `skip(insufficient_edge)`
linking the forecast_id) and **$60k** YES p 0.92 (high-confidence bin → small
10-unit YES probe on a *fresh* instrument, deliberately not piling onto existing
exposure per AX-033/`mem_4zRTSDdMFuzcfAJs`). `memory.recall` round-tripped, coach
was honest and did **not** false-flag the linked skip (AX-013 holding), and all
the marking fixes held live (AX-025/027/028 side-aware to mid; NO-side complement
correct; AX-031 clean numeric `liquidity_depth_json` on both fresh binds).

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-034** | tool/report | medium-high | Following bootstrap → check-exposure with **9** accumulated open paper positions, **both** exposure reads failed at the MCP transport layer: `report.current_exposure` (the docstring's *"recommended trader-agent entry point for open trades/current exposure"*) returned ~69KB and exceeded the token cap, and its schema has **no `limit`/`cursor`** for the exposure buckets → unrecoverable; `report.open_positions` default/unbounded call returned ~61KB and also failed (it *does* accept `limit`/`cursor`, so `limit=3`+page works). Root cause (`portfolio_exposure.py`): triple serialization — `groups` and `event_exposure_sets` are the **same object** (`open_positions` 311-312, `current_exposure` 787-788) plus the `open_positions` array, each row carrying two 78-digit CLOB token IDs. Extra hazard: `current_exposure` passes `limit` through (728) but **not** `cursor`, and returns **no `truncated`/`next_cursor`** (764-799) → a limited call **silently drops** exposure with no signal (correctness risk). Same envelope class as AX-029/hv19 (report.lifecycle), different surfaces. | **filed** → `trade-trace-lszg` (P2); cross-linked `trade-trace-hv19` |
| AX-018 | report | (known) | `fc_CheQKZ-UOTWPVUPs` / `mkt_pwuUfbLMcvhkia50` still the sole `resolve_due_forecast` (reason `resolution_at_missing`) while `snapshot.fetch` shows mid 0.505, bid/ask 0.50/0.51, accepting_orders:true, $801K vol — genuinely live. Declined to fabricate. | re-confirmed → `trade-trace-fe2f` |
| AX-017 | tool/schema | (known) | Both fresh BTC daily binds (`mkt_cDjxbYAhIPlhUhdb` $64k, `mkt_1I__rxv9-y3mh65W` $60k) returned `resolution_rule.text=null` while `venue_metadata_json.description` carried the full Binance noon-ET close rule. | re-confirmed → `trade-trace-n33z` |
| AX-024 | tool | (known) | A brand-new memory node (`mem_FeRwPf9eHJvifqEr`) on exactly the queried topic did **not** surface in the top-3 of a fused multi-word `memory.recall` (`"current_exposure token cap transport limit cursor pagination"`) — out-ranked by older recency nodes — yet was the **#1** hit under a tight 2-token `per_strategy`/`bm25` query. Conjunctive BM25 masked by fused recency, exactly as filed. | re-confirmed → `trade-trace-95ry` |
| AX-022 | onboarding | (known) | Cold throwaway-journal `report.bootstrap` surfaced only read/continuity `suggested_process_calls` (`work_queue`, `agent.next_actions`, `recall_receipts`, `strategy.show`), 0 obligations, no first-run `market.search→bind→forecast` entry point. | re-confirmed → `trade-trace-xqjv` |

### Fixes holding live (verified, no action)

- **AX-025 / AX-027 / AX-028** — all 9 open rows marked side-aware to the latest
  **mid** (not lastTradePrice, not a stale non-null mark). NO-side positions
  complement correctly: e.g. US-Iran NO @ 0.875 vs yes-mid 0.12 → +$0.50;
  Rihanna NO @ 0.49 vs yes-mid 0.505 → +$0.05; Fed-no-change NO. Fresh BTC $70k
  probe −$0.20, carried 100-unit row −$18.00 against mid 0.36.
- **AX-031** — both fresh binds (`$64k`, `$60k`) and the `fc_CheQKZ` snapshot had
  clean numeric `liquidity_depth_json` (48048.7 / 36313.7 / 17457.9), no raw
  Gamma dump.
- **AX-013** — the linked `skip` on `mkt_cDjxbYAhIPlhUhdb` did **not** trip a
  coach false-flag (coverage callout only).
- **AX-004/005/032 class** — `memory.recall mode="bm25"` returned a clean
  self-documenting `VALIDATION_ERROR` listing the allowed values
  (`["fused","per_strategy"]`).

## Fixes made (direct commits)

None. The only new finding (AX-034) is a design-level envelope/pagination rework
that touches output shape — filed per the contract-firewall + "no honest small
fix" discipline (identical handling to its sibling AX-029/hv19).

## Beads filed

- **`trade-trace-lszg`** (P2, task, `ax-dogfood`) — AX-034, the exposure-report
  token-cap blowup. Cross-linked to `trade-trace-hv19` (note added to hv19
  correcting its claim that `report.open_positions` is the good paginated
  counterexample — it also fails on its unbounded default).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2275 passed, 7 skipped** (unchanged — no source touched this run)

## This run's journal artifacts

- Forecasts: `fc_7p8h6j_t-hhxZlBE` (BTC $64k June 4, p_yes 0.57, `confidence_label=medium`,
  resolution_at 16:00Z today; skip `dec_FPwY5ZLwIjUUNB4M` insufficient_edge) and
  `fc_0K8m09TrkWzssHvY` (BTC $60k June 4, p_yes 0.92, `confidence_label=high`;
  paper_enter `dec_IBYcdV4bM3fRlJCV` YES 10 @ 0.93 → `pos_e8h40wfAeag_gRts`).
- Binds: `mkt_cDjxbYAhIPlhUhdb` ($64k), `mkt_1I__rxv9-y3mh65W` ($60k).
- Snapshots: `snp_2O_O7K0kbqRVd0bJ`, `snp_sduA02AHtAdSuwyk`, `snp_jazU4jGB73yOnsiT`.
- Memory: `mem_FeRwPf9eHJvifqEr` (use `report.open_positions limit=N`; never
  `report.current_exposure` / unbounded open_positions when many positions open).

## Handoff notes for next run

- **Due 16:00Z today (2026-06-04):** several near-term forecasts now resolve at
  the Binance BTC/USDT **noon-ET (16:00Z)** close — this run's `fc_7p8h6j` ($64k)
  and `fc_0K8m09` ($60k), plus the prior ETH `fc_uZ-WmwfdrsEtISlE`
  (`mkt_BxqIYenOQjh6ta1l`) and `fc_1yoUooDa7wbjasEk` (`mkt_SohHCLs3qtCWRWbT`,
  ETH>$1,800). **If a run fires after 16:00Z, resolve from the venue close and
  pass `confidence` explicitly** (≥0.9 venue-unambiguous; AX-030 only surfaces
  the field in `example_rich`). Don't fabricate. These will finally lift
  calibration off N=1.
- **Exposure check workaround (AX-034):** until `trade-trace-lszg` lands, ALWAYS
  call `report.open_positions` with `limit=3` (or similar) and page `next_cursor`;
  do **not** call `report.current_exposure` or unbounded `report.open_positions`
  — both exceed the MCP token cap with 9 positions open. (Memory
  `mem_FeRwPf9eHJvifqEr`.)
- **Open exposure now (9 paper positions):** BTC $70k June (120 YES across
  `pos_EmNSXb2bDMWpFdub` 100 @ 0.54 + `pos_dTqU6DR07HnbZHvd` 20 @ 0.37, −$18.20
  at mid 0.36), BTC dip $57.5k (`pos_CwabrU` 20 YES), ETH $1,800 June 4
  (`pos_yEG4IYrG` 10 YES, resolves 16:00Z today), ETH $2,000 June
  (`pos_WJcHuU` 20 YES), Fed no-change June (`pos_dIKpqkuqggcQfwKz` 100 NO),
  Jesus/GTA-VI (`pos_NMrnt` 100 NO), US-Iran peace (`pos_R35Rl91` 100 NO),
  Rihanna/GTA-VI (`pos_PyKbHGC` 10 NO), and the new BTC $60k June 4
  (`pos_e8h40wfAeag_gRts` 10 YES). Per AX-033: check `report.open_positions`
  before re-entering any of these.
- `fc_CheQKZ` will keep re-flagging `resolve_due` (AX-018) — expected; it has no
  `resolution_at` and the market is still live.
- Highest-priority untouched bead: `trade-trace-4z2t` (market.refresh, P1).
