# AX dogfood run report — 2026-06-05-19

- **RUN_ID:** `2026-06-05-2100`
- **Branch:** `ax-dogfood` (confirmed; not `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop`
- **Mode:** standing-confirmation pass — **NO new friction found**
- **Rebase:** `git rebase origin/main` → "up to date" (no conflict)

## Summary (one paragraph)

Drove the full canonical journal loop as a cold bot on the live BTC June
**month-window touch** ladder (close 2026-07-01). BTC sold off intraday
(reach-$70k YES `oneDayPriceChange` −0.16; dip-$55k +0.15), triangulating spot
~$60k with elevated two-sided vol. Bound 3 binary markets, snapshotted, recalled
prior lessons, forecast each, and recorded decisions: skipped reach-$70k
(`already_exposed`, hold 120 YES from prior runs) and reach-$77.5k
(`insufficient_edge`, deep OTM), and took a thin 20-unit NO on dip-$55k
(forecast YES 0.38 vs mid 0.3995, ~0.02 edge). Reviewed calibration/coach/
open_positions, retained a lesson (round-trip confirmed), and ran the cold-start
probe. As a bot the surface felt coherent and predictable: every rough edge I
hit was an **already-filed/fixed** item behaving exactly as the registry
predicts. No genuinely new experience friction surfaced — the report catalog and
write-tool catalog have been exhaustively probed over runs 01–18, and this pass
(like runs 11 and 18) is a clean re-confirmation.

## Friction found

**None new.** All observations are standing re-confirmations:

| item | surface | disposition | evidence this run |
|------|---------|-------------|-------------------|
| AX-037 | tool/adapter | fixed (standing) | all 3 adapter binds preserved caller `resolution_rule_text` (`provenance=caller_supplied`) |
| AX-027 | tool/report | fixed (standing) | `snapshot.fetch price`=mid on all 3 ($70k 0.175 not lastTrade 0.18; $77.5k 0.054 not 0.058; dip-$55k 0.3995 not 0.41) |
| AX-031 | tool/adapter | fixed (standing) | numeric `liquidity_depth_json` (39652 / 50504 / 34401), no raw dump |
| AX-045/025/028 | report | fixed (standing) | new NO `pos_Wyx2AotA` carries `initial_risk_amount:12.01`, no `missing_risk_budget`, marked side-aware flat (`unrealized_pnl:0` = NO 0.6005 vs 1−mid 0.3995); prior YES `pos_Vxe1iw` marked −1.84 |
| AX-013 | report | fixed (standing) | both forecast-linked skips → `coach.unscored_forecasts.count:0` |
| AX-018/fe2f | report | filed (standing) | 3 `resolution_at_missing` false-positives (`fc_CheQKZ` live `mkt_pwuU` + US/UK recession); 3 new forecasts w/ explicit `resolution_at`=2026-07-01 did NOT flag; `report.unscored_forecasts` count:0 positive control |
| AX-038/dlup | report | fixed (reason) / design filed | bootstrap `active_ideas` nuked 0/70 `max_chars`; `memory_context` not included |
| AX-024/95ry | tool | filed (in_progress) | fused query containing verbatim unique token `MARMOT-DIP55K-J5-2100` buried the just-written node out of top-5 (RRF floor ~0.031, no bm25 provenance); bm25-only ranked it #1 w/ provenance |
| AX-022/xqjv | onboarding | filed (standing) | cold throwaway bootstrap suggested only read/continuity tools (`report.work_queue`/`agent.next_actions`/`report.recall_receipts`/`strategy.show`), 0 obligations, no first-run entry point |
| AX-048 | report | fixed (standing) | `coach.top_mistakes/strengths` `[]` |
| s7nn | report/integrity | filed (standing) | `abstention_coverage` 2/85 (2.35%); ~34 `decision.add(skip)` rows still uncounted |
| e8e3005 | report | fixed (standing) | `report.open_positions` `groups:{ref:"event_exposure_sets",count:1}` (no re-embedded duplicate) |

### Notable (not friction): two-sided hedge, not scx8 fragmentation

On dip-$55k (`mkt_NEPHuqH1YQ2Wq-8F`) I now hold **25 YES** (run-14, `pos_Vxe1iw`)
and **20 NO** (this run, `pos_Wyx2AotA`) — opposite sides, a legitimate
**hedge**, distinct from scx8 same-side fragmentation. `report.open_positions`
marked both side-aware (NO flat at entry; YES −1.84 = 25×(0.3995−0.473)), each
carrying its own `declared_risk_amount`. Correct behavior; no finding.

## Calibration

- `report.calibration` N=33 (unchanged — my 3 new forecasts settle 2026-07-01,
  won't score same-day, per the month-window data-shape). Brier 0.109, skill
  0.485, `sample_warning:null`.
- Reliability bins reaffirm the standing **overconfident-YES** signal: the
  0.57–0.85 band (mean 0.693, observed 0) carries the run-18 SPY-$750 miss
  (`fc_A8wbW92` p_yes=0.85, Brier 0.7225); the 0.16 and 0.275 bands also
  observed 0. My conservative shading (skips + tail-NO lean) is consistent with
  this.

## Fixes made

**None** (no new friction to fix). No source files changed this run.

## Beads filed

**None** (no new friction; all observations dedup to existing registry rows /
open beads: fe2f, 95ry, xqjv, s7nn, n33z, dlup, lszg, scx8, v5va, 1y9s).

## Attempted-then-reverted

**None.**

## Gate results

`ruff check src tests`, `mypy src`, `pytest -q` — run on an unchanged tree to
confirm HEAD is green before committing the docs. (See final session output for
the exact summary line.)

## Journal artifacts created this run

- Binds: `mkt_7tACn4aqhw6y8BwN` ($70k reach), `mkt_Vh36actmypkVNBiq` ($77.5k
  reach), `mkt_NEPHuqH1YQ2Wq-8F` ($55k dip) — all adapter, rule_text preserved.
- Forecasts: `fc_C4m_mLcBqyRVNfLx` ($70k YES 0.16), `fc_tUgL1mFDF3Fw6RQk`
  ($77.5k YES 0.05), `fc_lO8mYeWEJB8kbpXo` (dip-$55k YES 0.38) — all
  `resolution_at`=2026-07-01T04:00:00Z.
- Decisions: skip `dec_dRBskXnuTnFGni-0` ($70k, already_exposed), skip
  `dec_JImgNgg6BxeMUbX-` ($77.5k, insufficient_edge), paper_enter
  `dec_7bjVlE0mzqdNnEc_` → NO `pos_Wyx2AotA_7fuJwmw` (20@0.6005, risk 12.01).
- Memory: `mem_x3QAhS7RIbajzFNp` (run-19 lesson, round-trip confirmed).

## Handoff notes for the next run

- **Tree state:** clean at start; two human-owned debris stashes remain
  (`stash@{0}` axloop-debris-1600, `stash@{1}` axloop-debris-1500) — untouched,
  same as the run-18 handoff. A human should land/discard them when convenient;
  they do not block the loop.
- **Catalog exhaustion:** the report-surface and write-tool catalogs are fully
  probed (runs 01–18). Expect future runs to be predominantly
  standing-confirmation passes; new findings will be rare and likely require a
  genuinely novel market structure, a fresh-server fix verification, or a
  full-suite gate surprise (cf. AX-046/AX-065). Honest "no new friction" is the
  expected steady state.
- **Open design beads worth a human decision:** fe2f (resolution_at_missing
  "due now"), 1y9s/lszg (token-cap envelope bloat), n33z (resolution_rule
  retrieval), s7nn (skip→abstention coverage), v5va (resolution_misreads
  mechanism mapping), 95ry (memory.recall conjunctive-BM25). None are acute.
- **Scoring inventory:** no same-day-settleable inventory this run; the 3 new
  month-window forecasts settle 2026-07-01. The 3 standing `resolution_at_missing`
  forecasts (live `fc_CheQKZ` + US/UK recession 2027-close) remain unresolvable.
