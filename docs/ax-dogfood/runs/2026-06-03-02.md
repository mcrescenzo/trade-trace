# AX dogfood run — 2026-06-03-02

- **RUN_ID:** `2026-06-03-1644`
- **Branch:** `ax-dogfood` (verified; never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma) enabled
- **Phase A:** `git rebase origin/main` → already up to date. Read registry + intentional-design.

## Summary (as a bot)

Drove the full canonical loop on live Polymarket data: oriented via
`report.bootstrap` → `report.work_queue` / `report.lifecycle`, checked a
prior open forecast for due resolution (market still live → correctly left
open), scanned Gamma for active binary markets, bound 3, snapshotted them,
recalled prior lessons, recorded 3 forecasts + 3 decisions (1 `paper_enter`,
2 `skip`), reviewed calibration/coach/exposure, wrote a reflection and
confirmed it round-trips, and ran a cold-start probe. The loop is coherent
and the surfaces are mostly self-describing — but I hit one silent
correctness trap (phantom PnL on a `no` position) and two cases where the
schema/docs actively misled me (fetch by `external_id`; undocumented
`confidence_label` enum). Discipline held: skipped a tiny-edge market, only
took the larger-edge NO.

## Lifecycle artifacts created

- Markets bound (bare numeric `external_id`, fetch-capable): `mkt_5vB5azx8RhWc_2V8` (Iran peace by Jun 7), `mkt_dY-7NiQT9N6tCaRI` (Iran peace by Jun 15), `mkt_39bP49PZ8zJ2t5Jx` (Knicks vs Spurs, resolves tonight).
- Forecasts: `fc_OvsCxP-L3dWiRcGr` (Jun 7 YES 0.02), `fc_kQSCPEfjYkV6Hexc` (Jun 15 YES 0.05), `fc_rHrzE9WuUh5qmILR` (NBA YES=Knicks 0.37). All with `resolution_at` + `scoring_support=supported`.
- Decisions: `dec_MnaxcsDkxb-fkyDb` (paper_enter NO @ 0.875, opened `pos_R35Rl91Jz4FAkcS_`), `dec_RxGFrNfq06ACVCkj` (skip Jun 7, insufficient_edge), `dec_2Z665AAqRKKW9drW` (skip NBA, no edge).
- Reflection `mem_U1g6t8Ny1jsBQko5` (edge-discipline lesson), recall-confirmed.
- 3 orphan market rows bound with a namespaced `external_id` (`polymarket:NNNN`) before AX-009 was understood — harmless, unforecasted; left in place as evidence.

## Friction found

| id | surface | severity | finding | disposition |
|----|---------|----------|---------|-------------|
| AX-008 | report/tool | **high** | Unrealized PnL for a `no`-side position marks the NO entry price (0.875) against the YES implied price (0.12) with no side conversion → flat position shows phantom **+$75.50** (`pos_R35Rl91Jz4FAkcS_`; true ≈ 0). `decision.add price` convention for `no` undocumented. | filed `trade-trace-ctvb` |
| AX-009 | tool/doc | high | `snapshot.fetch` / `snapshot.fetch_series` / `market.refresh` build the Gamma `/markets/{id}` URL from `external_id`, ignoring the dedicated `gamma_market_id`; a namespaced `external_id` (per the AGENT_GUIDE's own example) → HTTP 422. Confirmed: bare numeric id works. | **fixed** `1ea628d` |
| AX-010 | schema/error | medium | `forecast.add` `confidence_label` enum `{very_low,low,medium,high,very_high}` not in `tool.schema` (bare `{type:string}`); an invalid value (`moderate`) leaked the raw SQLite CHECK constraint as the user-facing message. | **fixed** `4bd01ce` |
| AX-011 | schema | medium | `snapshot.fetch` / `outcome.fetch` schema text lists `idempotency_key` under "Runtime-defaulted keys are optional", but the dispatcher rejects calls that omit it (`auto_derivation_available:false`). Schema vs enforcement disagree. | filed `trade-trace-2cmb` |
| AX-012 | report | medium | An open forecast with `resolution_at:null` never appears in `report.work_queue` as `resolve_due` (only `report.lifecycle` shows it). A bot driving off the work queue never resolves it. | filed `trade-trace-ptyi` (design Q) |
| AX-013 | report | low | `skip`/`watch`/`hold` decisions can't carry `forecast_id`, so `report.coach` flags a forecasted-but-skipped market as "no linked forecast / backfill". | filed `trade-trace-t9n5` (design Q) |

Minor (not filed): a freshly `journal init`'d journal shows the Polymarket adapter `enabled:false` with no inline hint on how to enable live data — overlaps the already-filed AX-001/AX-002 onboarding gaps; low value to file separately.

## Fixes made (gate-verified)

- **AX-009** — `1ea628d` `fix(adapter): fetch Gamma markets by gamma_market_id, not external_id`. Added `_gamma_request_id()` (prefer `gamma_market_id`, fall back to `external_id`); routed all three fetch sites through it; fixed the AGENT_GUIDE `market.bind` example + documented the lookup rule. Unit tests for the helper.
- **AX-010** — `4bd01ce` `fix(forecast): validate confidence_label with clean error + schema enum`. Added shared `validate_confidence_label()`; called on the folded `forecast.add` path and legacy `thesis.add`; added `enum` to the `forecast.add` schema. 3 regression tests.

> Both fixes are tool-code; per the playbook the long-lived MCP server will not
> reflect them until a **fresh session** boots a new server. Verified by unit
> tests this session; effective next fresh session.

## Beads filed

- `trade-trace-ctvb` (bug, P1) — no-side PnL / `decision.add` price convention (AX-008).
- `trade-trace-2cmb` (bug, P2) — fetch tools advertise `idempotency_key` optional but reject its omission (AX-011).
- `trade-trace-ptyi` (task/design-Q, P2) — open forecast with null `resolution_at` invisible to work_queue (AX-012).
- `trade-trace-t9n5` (task/design-Q, P3) — skip/watch/hold can't link a forecast (AX-013).

## Attempted-then-reverted

- None. Both attempted fixes passed the gates and were kept.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2227 passed, 7 skipped in 96.06s** (7 skips are perf-baseline opt-ins).

## Handoff for next run

- **Near-term resolution to test:** the NBA forecast `fc_rHrzE9WuUh5qmILR` (instrument `mkt_39bP49PZ8zJ2t5Jx`) resolves tonight (2026-06-04 ~00:30Z), `resolution_at` set, `scoring_support=supported`, binary yes/no labels → it is auto-scoreable once a `resolution.add` with `confidence>=0.9` lands. Next run should resolve it (Knicks win → `yes`, Spurs win → `no`) and confirm the score row appears. The two Iran forecasts come due Jun 7 / Jun 15.
- **Resolution path caveat (environment):** `outcome.fetch` returns `CONFIG_REQUIRED` (`network.polymarket.polygon_rpc_url`) in this journal — the on-chain resolution path is not configured. `snapshot.fetch` (Gamma) works for prices. Determine true outcomes from Gamma `closed`/`winningOutcome`/`outcomePrices` (curl out-of-band per AX-002) and record via `resolution.add`. Do NOT fabricate `confidence` to satisfy the auto-score gate.
- **PnL caveat:** until `trade-trace-ctvb` is resolved, treat the +$75.50 unrealized PnL on `pos_R35Rl91Jz4FAkcS_` as a known artifact, not signal.
