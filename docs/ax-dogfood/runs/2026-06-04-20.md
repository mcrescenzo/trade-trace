# AX dogfood run 2026-06-04-20

- **RUN_ID:** `2026-06-04-1900`
- **Branch:** `ax-dogfood` (confirmed; never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket Gamma
- **Mode:** evidence-gathering + ONE genuine new finding filed. **No code shipped** (the new finding is design-level / contract-adjacent; firewall says file, not fix).
- **Phase A:** `git rebase origin/main` → already up to date. Read registry + intentional-design + open bead queue (11 open `ax-dogfood` beads) for dedup.

## Summary (one paragraph)

Drove the canonical bot loop cold via MCP only: `report.bootstrap` →
`report.work_queue` → resolve-due check → scan/bind/snapshot/forecast/skip →
Review (`report.calibration`, `report.process_quality`, `report.coach`,
`report.forecast_diagnostics`) → Remember (`memory.retain`/`memory.recall`) →
cold-start probe. No resolvable inventory again this run (the 3 open obligations
are the unresolvable `resolution_at_missing` set: fc_CheQKZ live + US/UK
recession 2027-close; last run's June-5 SPY/SOL seeds settle tomorrow, not yet).
Calibration N still 11. Instead of re-litigating the saturated known items, I
exercised the **Review** surfaces harder and found **one genuinely new friction
item**: the `abstention_coverage` survivorship-bias integrity control counts only
first-class `abstention.record` rows and ignores the 32 `decision.add(type=skip)`
"considered-and-passed" decisions — so a maximally-cautious bot reads as "never
abstains / denominator survivorship-biased" when it diligently recorded 32 passes.
Filed as a design question (trade-trace-s7nn). Also exercised the loop end-to-end
on a **new market structure** (Elon Musk tweet-count *bucket* market, resolves
June 5 → grows N next run), forecast-only at market with no edge (no exposure).

## Friction found

| Sev | Surface | Item | Disposition |
|---|---|---|---|
| **Medium (NEW)** | report / integrity metric | `abstention_coverage` (report.calibration `data.integrity_diagnostics` + report.coach) reported count:0 / share 0% despite 32 `decision.add(type=skip)` rows (2 with NO linked forecast — the cleanest "considered-and-passed without forecasting" = abstentions). Root cause `integrity.py:225-254` `_abstention_coverage()` reads ONLY `SELECT id FROM abstentions`; skip-type material-non-action decisions are never counted. Two overlapping "I passed" surfaces (`abstention.record` vs `decision.add(skip)`); the anti-survivorship control credits only one, no signpost. | **filed** trade-trace-s7nn (P3 design Q) |
| low (re-confirm) | tool/adapter | AX-037: adapter-path `market.bind` preserved caller `resolution_rule_text` (`provenance=caller_supplied`) on a NEW structure (Elon tweet-count bucket `mkt_LvM6eoXxSu9yt6eh`). | fixed (prior SHA e9892cd) |
| low (re-confirm) | tool/schema | n33z: rule text null — the full counting methodology (xtracker Post Counter; replies excluded; reposts/deleted-post handling) lives only in `venue_metadata_json.description` on the tweet-bucket market. New structure class (numeric count bucket). | filed trade-trace-n33z |
| low (re-confirm) | tool/report | AX-027 (`snapshot.fetch` price=mid 0.175, not lastTradePrice 0.17) + AX-031 (numeric `liquidity_depth_json` 24490.42, no raw dump) on the bucket market. | fixed (prior SHAs) |
| low (re-confirm) | report | AX-038/dlup: bootstrap char cap nuked `active_ideas` (0/46, reason=`max_chars`) while `obligations` survived (3/3) at small N. | filed trade-trace-dlup |
| low (re-confirm) | tool (report) | AX-039: `report.work_queue` SUCCEEDED at N=3 obligations (curve now fail@13 / ok@5 / ok@3 / ok@3). | filed trade-trace-1y9s |
| low (re-confirm) | report | fe2f positive control: 3 `resolution_at_missing` forecasts flagged resolve_due; last run's 3 June-5 seeds with explicit `resolution_at` did NOT flag. | filed trade-trace-fe2f |
| low (re-confirm) | tool | 95ry: natural-language multi-word `memory.recall` ("skip insufficient edge abstention calibration denominator survivorship") returned only temporally-recent items, none on-topic, all at the RRF floor (~0.03). | filed trade-trace-95ry |
| low (re-confirm) | tool | xqjv: cold throwaway-journal bootstrap suggested only read/continuity tools (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`); no first-run `market.search→bind→forecast` entry point; 0 obligations. | filed trade-trace-xqjv |

### Observation (not filed — tool working as intended)
`report.process_quality` flagged 3/8 sized decisions `direction_consistent:false`,
all with `stated_edge ≤ 0` (e.g. dec_IBYcdV4bM3fRlJCV: price 0.93 vs win_prob
0.92, edge −0.01). This is the process-quality control correctly penalizing
zero/negative-edge paper entries; not friction. Noted because the surface had not
appeared in prior registries — it behaves correctly.

## Fixes made

None. No source changed this run.

## Beads filed

- **trade-trace-s7nn** (P3, task/design-Q, label ax-dogfood) — `abstention_coverage`
  ignores `decision.add(type=skip)` considered-and-passed rows. Dedup: `bd search
  abstention|survivorship|"skip coverage"` all returned no matches; nothing in the
  11 open `ax-dogfood` beads covers the abstention/survivorship surface.

## Attempted-then-reverted

None.

## Lifecycle writes this run (journal `~/.trade-trace-axloop`)

- `market.bind` `mkt_LvM6eoXxSu9yt6eh` (Elon tweets 200-219, ext 2358280) — adapter, caller rule text preserved.
- `snapshot.fetch` `snp_0IDJxVj1MtO9kLgi` (price 0.175 = mid).
- `forecast.add` `fc_NvPsMr3TxLJRifY2` p_yes=0.17 (concur with liquid market, low confidence), resolution_at 2026-06-05T16:00:00Z → **resolves next run, grows N**.
- `decision.add` skip `dec_CWvIbtcvBX7VaMg-` (insufficient_edge, no exposure).
- `memory.retain` `mem_S45-odsmx52rbfGG` (abstention/skip survivorship gap lesson).
- No `paper_enter`, no exposure opened.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2279 passed, 7 skipped** (skips are opt-in perf baselines) in ~104s.

## Handoff notes for next run

- **Resolvable next run (June 5):** last run's seeds — SPY $750 `fc_A8wbW92` (20:00Z),
  SOL $60 `fc_e0_3Gf8` + SOL $80 `fc_lfC-Bti3` (16:00Z) — plus this run's Elon
  tweet-bucket `fc_NvPsMr3TxLJRifY2` (16:00Z). These should grow calibration N from
  11 toward the 20 floor once their windows close and outcomes are recorded.
  Resolution evidence is out-of-band (no `winningOutcome` pre-settle, isqo theme):
  SPY/SOL via web spot/Pyth close; Elon bucket via xtracker Post Counter.
- **The 3 standing obligations** (fc_CheQKZ live; US/UK recession 2027-close) remain
  unresolvable; expect them flagged resolve_due every run (fe2f).
- **New finding to watch:** trade-trace-s7nn (abstention_coverage). It is a design
  decision — do NOT ship a counting change before the denominator semantics are
  agreed (risk of double-count + changing a published integrity metric). A safe
  pre-decision step would be a doc/signpost from the skip path; deferred this run.
- **Calibration N still 11** — the loop keeps seeding near-term forecasts but the
  saturation is real: registry re-confirmations (n33z, fe2f, AX-037/027/031/038/039,
  95ry, xqjv) dominate. Genuinely-new findings now require pushing into less-trodden
  surfaces (this run: the integrity diagnostics panel).
