# AX dogfood run — 2026-06-04-13

- **RUN_ID:** 2026-06-04-1200
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** one new friction found & **fixed** (AX-036, `report.lifecycle`
  `due_count` counted future-due cases as due). Exercised a structurally distinct
  market type (intraday **point-in-time** single-candle settle) that resolves
  same-day. Two beads strengthened (fe2f cleanest control pair yet + sibling fix
  cross-link; n33z point-in-time instance). Gates green.

## Summary (as a bot)

Drove the full journal loop on **intraday point-in-time** Polymarket markets —
"Will Bitcoin be above $X on June 4?" — which settle on a *single* Binance
BTC/USDT noon-ET (16:00Z) 1-minute candle close. Memory recall was decisive
here: my prior BTC lessons (`mem_4MIojgkFHXh8GPHC`, `mem_KrD5guOtsKXrhkGE`) are
all about **window-touch** markets ("reach $X any time in June" — daily mid is a
*lower bound* on cumulative touch probability). Those lessons must NOT be
applied to a point-in-time settle, exactly the "verify the resolution criterion
first" warning in `mem_6wWb9uY0nF3qaJ29`. I confirmed the criterion from the
description, recognized I had no edge on a ~4h point-in-time BTC move, forecasted
at the market-implied probability, and skipped (no exposure). The operational
prize: these markets close **today at 16:00Z**, so a forecast recorded now with
`resolution_at=close_at` is the *only* category the hourly loop can actually
carry to auto-score within hours — the path to the `report.calibration` N≥20
floor that far-future (2026/2027-close) markets never reach.

The one genuinely new friction was found while verifying fe2f: `report.lifecycle`
`summary.metrics.due_count` counted every case that merely *had* a `due_at`,
never comparing it to `as_of`. My future-due forecast (due 16:00Z) was counted
as "due" 4h early — and it's the *inverse* of `report.work_queue`, which omits
future-due forecasts entirely. Small, self-contained, non-firewall → fixed
directly.

## What was exercised

- `report.bootstrap` → 3 obligations, all `resolve_due_forecast` /
  `resolution_at_missing` (the two 2027-close recession forecasts + fc_CheQKZ).
- `snapshot.fetch` ×2 on the flagged markets (`mkt_pwuUfbLMcvhkia50` mid 0.505;
  `mkt_33hPArFcrg_A1Hbn` US-recession mid 0.185) → both `accepting_orders:true`,
  live, not resolvable → did **not** fabricate resolutions.
- `market.search` ×2 (`bitcoin`, `ethereum`) → intraday strike ladders (both
  hint:null, populated results — correct).
- `market.bind` ×2 (2379477 → `mkt_qTAY1Ii6bpofLPSz` $62k; 2379494 →
  `mkt_bjUQDcLcegfQFj3r` $70k), both `bound_via:adapter`.
- `snapshot.fetch` ×2 → `price` anchored to **mid** (0.489 / 0.001); numeric
  `liquidity_depth_json` (15313.88 / 52137.86), no raw dump.
- `memory.recall` → surfaced the window-touch lessons + the verify-the-rule
  warning, which directly changed how I priced these.
- `forecast.add` ×2 (`fc_GE5gU0KrpenWWkoF` p_yes 0.49; `fc_ton8dJ8ZKz8-w0nY`
  p_yes 0.005), each anchored to its snapshot, with explicit
  `resolution_at=2026-06-04T16:00:00Z` + self-supplied `resolution_rule_text`.
- `decision.add(skip)` ×2 (`dec_X2f-xPJ54Fns1gSm`, `dec_d-fxXMVkiTd-K4jc`),
  linked `forecast_id`, materiality `insufficient_edge`.
- `report.work_queue` / `report.coach` / `report.lifecycle` (verification — see
  below).
- `memory.retain` (`mem_xGhjmg7UN7m3c94L`, point-in-time-vs-touch) +
  `memory.recall` round-trip.
- Cold-start probe: throwaway `tt journal init` + `tt report bootstrap`.

## Forecasts (both efficient/no-edge → forecast ≈ mid + skip, NO exposure)

| forecast | market | mid | p_yes | resolution_at | decision |
|----------|--------|-----|-------|---------------|----------|
| `fc_GE5gU0KrpenWWkoF` | `mkt_qTAY1Ii6bpofLPSz` BTC > $62k on Jun 4 (point-in-time, ext 2379477) | 0.489 | 0.49 (low) | 2026-06-04T16:00Z | skip `dec_X2f-xPJ54Fns1gSm` |
| `fc_ton8dJ8ZKz8-w0nY` | `mkt_bjUQDcLcegfQFj3r` BTC > $70k on Jun 4 (point-in-time, ext 2379494) | 0.001 | 0.005 (high, confident NO) | 2026-06-04T16:00Z | skip `dec_d-fxXMVkiTd-K4jc` |

Strike-ladder pricing is internally consistent ($62k YES≈0.49 vs $70k YES≈0.0005
⇒ BTC sits ~$62k); a >$70k close requires ~13% in ~4h ⇒ ~0. Memory
`mem_xGhjmg7UN7m3c94L` records the point-in-time-vs-window-touch distinction and
why same-day markets are the loop's only realistic calibration-N source.

## Friction found

| ref | surface | observation this run | disposition |
|-----|---------|----------------------|-------------|
| **AX-036** | report | `report.lifecycle` `summary.metrics.due_count` counted any case with a `due_at` set (never compared to `as_of`), so a forecast due at 16:00Z was counted "due" at 12:05Z — 4h early — while the `resolution_at_missing` (due_at null) forecasts were NOT counted, the exact inverse of `report.work_queue`. | **fixed** 48d5d3a |
| AX-018 / fe2f | report | **Cleanest control pair yet, in one journal.** 3 `resolution_at_missing` forecasts flagged resolve_due (incl. US-recession `fc_uywJQC8os4NsVqIP`, live mid 0.185); 2 new forecasts with future `resolution_at` did **not** flag. `report.lifecycle` shows the future-due one as state=`open`, due_at=16:00Z, reason `forecast_pending` — the correct "awaiting resolution" state the missing ones should map to. Appended to fe2f (+AX-036 sibling cross-link). | filed |
| AX-017 / n33z | tool/schema | New instance — intraday point-in-time crypto strike markets: `resolution_rule.text` null on both binds; the Binance noon-ET single-candle settle rule (which distinguishes point-in-time from window-touch) lives only in `venue_metadata_json.description`. Appended to n33z. | filed |
| AX-027 | tool/report | `snapshot.price` = mid (0.489 / 0.001), not lastTradePrice. | re-verified |
| AX-031 | tool/adapter | numeric `liquidity_depth_json` on both binds, no raw Gamma dump. | re-verified |
| AX-013 | report | coach `unscored_forecasts.count=0` — no false-flag on the two forecasted-then-skipped markets. | re-verified |
| AX-022 / xqjv | onboarding | cold-probe empty journal: `suggested_process_calls` all read/continuity (work_queue, agent.next_actions, recall_receipts, strategy.show); no first-run entry-point. | re-verified |

## Fixes made

- **AX-036** — `48d5d3a` `fix(report.lifecycle): due_count must exclude future
  due_at`. `due_count` now counts only cases due as of the read boundary
  (`due_at <= as_of`) via the existing `_review_overdue` predicate, matching
  `watchlist.overdue_count` semantics. Narrow, non-firewall report-metric fix;
  deliberately does NOT touch the `resolution_at_missing`/null "due now" question
  (that is fe2f's design scope). +regression test
  (`test_report_lifecycle_due_count_excludes_future_due_at`) confirmed to fail
  pre-fix and pass post-fix. ruff + mypy clean. **Effective next fresh MCP
  session** (the live server is long-lived this session); verified by unit test.

## Beads filed

None new. Evidence appended: **trade-trace-fe2f** (control pair + AX-036 sibling
cross-link), **trade-trace-n33z** (point-in-time intraday instance).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → All checks passed
- `mypy src` → Success, no issues in 178 source files
- `pytest -q` → **2276 passed, 7 skipped** in 104.52s (count +1: this run added a
  regression test rather than modifying an existing one)

## Handoff notes for next run

- **Now PAST 16:00Z?** A batch of forecasts resolve at today's Binance/Polymarket
  noon-ET (16:00Z) close. This run added two: `fc_GE5gU0KrpenWWkoF`
  (`mkt_qTAY1Ii6bpofLPSz`, BTC > $62k — the live ~50/50, the *informative* one)
  and `fc_ton8dJ8ZKz8-w0nY` (`mkt_bjUQDcLcegfQFj3r`, BTC > $70k — confident NO).
  Plus the prior run's set (`fc_7p8h6j` BTC $64k, `fc_0K8m09` BTC $60k, ETH
  `fc_uZ-Wmwfd`, `fc_1yoUooDa`). A run firing AFTER 16:00Z should `snapshot.fetch`
  each, read the resolved `outcomePrices`/`winningOutcome`, and `resolution.add`
  with **`confidence` passed explicitly** (≥0.9 venue-unambiguous; AX-030: the
  field is hidden from the MCP schema but the dispatcher accepts it) + a binary
  label. These are the loop's best shot at lifting calibration off N=1. Do NOT
  fabricate before the close.
- **fe2f false-flag set (NOT real):** `fc_uywJQC8os4NsVqIP` + `fc_I0VaLn1GHFFYpFYZ`
  (2027-close recessions) + `fc_CheQKZ` still flag resolve_due solely from
  `resolution_at_missing` (both recession markets confirmed live this run,
  accepting_orders:true). Don't fabricate.
- **AX-036 lands next fresh session:** `report.lifecycle` `due_count` will stop
  counting not-yet-due cases — expect it to read 0 when your only dated forecasts
  are still in the future; not a regression.
- **Calibration / process steer:** prefer **same-day-resolving intraday markets**
  (point-in-time "above $X on <date>" or short-window) when the goal is to build
  calibration N. Far-future markets forecasted-then-skipped never resolve within
  the loop's reach — they only accumulate fe2f false-flags. See
  `mem_xGhjmg7UN7m3c94L`.
- **AX-034 workaround until lszg lands:** with many open positions, ALWAYS
  `report.open_positions limit=3` + page `next_cursor`; NEVER
  `report.current_exposure` or unbounded `open_positions` (both blow the token
  cap). (No exposure added this run — both forecasts skipped.)
- Highest-priority untouched bead: `trade-trace-4z2t` (market.refresh, P1).
