# AX dogfood run 2026-06-04-10 (RUN_ID 2026-06-04-0900)

Bounded AX dogfood pass on `ax-dogfood`. **No source code changed** — a clean
re-confirmation run on two genuinely **new market structures** (subjective
behavioral + conditional bucketed negRisk). No new friction surfaced; everything
observed re-confirms already-filed items. Two beads updated with broadened
evidence (fe2f, n33z). Gates green.

## Summary (as a bot)

Drove the full canonical loop on two fresh binary markets in categories not yet
probed by this loop, deliberately uncorrelated to the accumulated crypto/macro
exposure (AX-033 discipline). The surface felt smooth end-to-end: `market.search`
(single-topic, clean — `nba` legitimately 0, `trump`/`spacex`/`openai` returned
candidates), `tool.schema` to confirm `mechanism="clob"`/`state="open"`, adapter
`market.bind` (AX-021 fix holding), `snapshot.fetch` (clean numeric
`liquidity_depth_json` on both, and `price` anchored to **mid** not lastTrade on
a thin market — AX-027/031 holding), folded `forecast.add`, `decision.add(skip)`
with the `material_non_action` taxonomy, and `report.calibration`/`report.coach`
(honest, actionable, correctly low-N). Memory retain→recall round-tripped. With
no genuine private edge over efficient/liquid markets, the disciplined play was
**forecast≈mid + skip(insufficient_edge)** on both — no exposure added.

The standout signal this run: **resolution semantics live entirely in
`venue_metadata_json.description`**, and this run showed the gap is not just rule
TEXT but resolution TIMING and conditional fallbacks too (see AX-017 below).

## What I exercised

- **Orient:** `report.bootstrap` → `report.work_queue`. **THREE**
  resolve_due_forecast obligations, ALL from `resolution_at_missing`:
  `fc_CheQKZ-UOTWPVUPs` (the known AX-018 live re-flag) plus the two 2027-close
  recession forecasts from run 09 (`fc_uywJQC8os4NsVqIP`, `fc_I0VaLn1GHFFYpFYZ`).
  At 09:06Z nothing is genuinely resolvable (recession markets close 2027; the
  four near-term BTC/ETH forecasts resolve at the 16:00Z noon-ET close). Did
  **not** fabricate any resolution.
- **Forecast (2):** bound "Trump publicly insult Netanyahu by June 30, 2026"
  (`mkt_jxG6JnrreGXqPnae`, ext 2363862, mid 0.215, ~$13.9k liq, $44k vol, 1c
  spread — subjective behavioral) and "OpenAI cap <$500B at IPO close"
  (`mkt_0vS_cIjkn7xaJm5l`, ext 608362, mid 0.0645, ~$7.5k liq, 3.1c spread, 24h
  vol ~$12 — conditional bucketed negRisk). Forecasts `fc_D8fQNG4zmBO2Nda5`
  (p_yes 0.20, low) and `fc_FIl62z2Uc5jLQemP` (p_yes 0.06, very_low).
- **Decide (2):** `skip` both with `insufficient_edge` (`dec_K2nt0fMPDIAifHIV`,
  `dec_R3VUfrGZS1HGujl3`), each linking `forecast_id`.
- **Review:** calibration still N=1 (Brier 0.3969); coach `unscored_forecasts=0`,
  no mistake false-flag (AX-013 holds).
- **Remember:** retained `mem_kfCrPmSnnHh7dbFq` (resolution-semantics-in-
  description lesson), confirmed BM25 per_strategy round-trip returned exactly it.
- **Cold probe:** throwaway journal init + bootstrap (0 obligations, read-only
  suggested calls), then destroyed.

## Friction found

No NEW friction. All observations re-confirm already-filed items:

| id | surface | severity | observation this run | disposition |
|----|---------|----------|----------------------|-------------|
| AX-018 | report | medium | **Strengthened.** THREE resolve_due_forecast obligations all from `resolution_at_missing`; two are 2027-close recession markets (`mkt_33hPArFcrg_A1Hbn`, `mkt_VMoCTL05SJkKDfWJ`) flagged "due" at 09:06Z 2026 — ~1.5y before they can resolve. Clearest evidence yet that the trigger ignores `close_at`. (Corrects run-09 handoff, which expected these would NOT flag resolve_due.) | filed → fe2f (evidence appended) |
| AX-017 | tool/schema | medium | **Broadened.** `resolution_rule.text` null on BOTH binds. (1) Subjective market: the load-bearing personal-vs-professional insult carve-out lives only in `description` — for a subjective market the rule IS the question. (2) Conditional negRisk market: `description` carries resolution TIMING + "No IPO by Dec 31" fallback while structured `close_at=2026-06-30` understates the true horizon. Gap is rule text + timing + conditional outcomes. | filed → n33z (evidence appended) |
| AX-027 | tool/report | — | `snapshot.price` = mid 0.0645, NOT lastTradePrice 0.073, on a thin wide-spread market. | fixed, re-verified |
| AX-031 | tool/adapter | — | clean numeric `liquidity_depth_json` (13932.389, 7469.26086) on both new-structure binds — no raw dump. | fixed, re-verified |
| AX-013 | report | — | coach `unscored_forecasts=0`, no false-flag on the two forecasted-then-skipped markets. | holds (fix verified) |
| AX-022 | report/onboarding | — | Cold-probe empty-journal bootstrap → only read/continuity `suggested_process_calls`, 0 obligations, no first-run entry-point. | filed → xqjv |
| AX-004/005 class | error | — | `memory.recall mode="bm25"` → self-documenting VALIDATION_ERROR listing allowed `[fused, per_strategy]`. | holds |

## Fixes made

None (no new bugs; no contract-firewall-safe small fix exists — both
re-confirmations are design-level, already filed as fe2f / n33z).

## Beads filed

None new. **Updated:**
- `trade-trace-fe2f` — appended the strengthened 2027-close evidence (three
  resolve_due obligations, two of them ~1.5y from resolvable, all from
  `resolution_at_missing`).
- `trade-trace-n33z` — appended the two new structures (subjective behavioral +
  conditional negRisk) and broadened the ask: the surface should expose the true
  resolution horizon / conditional outcomes, not just rule text and `close_at`.

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2275 passed, 7 skipped** in 103.47s (unchanged — no code touched)

## This run's journal artifacts

- Forecasts: `fc_D8fQNG4zmBO2Nda5` (Trump-insult-Netanyahu, p_yes 0.20, low; skip
  `dec_K2nt0fMPDIAifHIV` insufficient_edge) and `fc_FIl62z2Uc5jLQemP` (OpenAI
  <$500B IPO, p_yes 0.06, very_low; skip `dec_R3VUfrGZS1HGujl3` insufficient_edge).
  Netanyahu market closes 2026-06-30; OpenAI market's true horizon extends to
  2026-12-31 (forecast `resolution_at` set there, not the misleading close_at).
- Memory: `mem_kfCrPmSnnHh7dbFq` (resolution semantics — rule + timing + fallbacks
  — live entirely in `venue_metadata_json.description`; read it before
  forecasting/sizing; never trust structured `resolution_rule.text`/`close_at`).
- No new exposure opened (both skipped); open paper positions unchanged at 9.

## Handoff notes for next run

- **Due 16:00Z today (2026-06-04):** FOUR near-term forecasts resolve at the
  Binance noon-ET (16:00Z) close — prior-run `fc_7p8h6j` (BTC $64k),
  `fc_0K8m09` (BTC $60k), ETH `fc_uZ-Wmwfd` (`mkt_BxqIYenOQjh6ta1l`) and
  `fc_1yoUooDa` (`mkt_SohHCLs3qtCWRWbT`, ETH>$1,800). If a run fires **after
  16:00Z**, resolve from the venue close and **pass `confidence` explicitly**
  (≥0.9 venue-unambiguous; AX-030 only surfaces `confidence` in `example_rich`).
  Don't fabricate. These finally lift calibration off N=1.
- **AX-034 workaround until lszg lands:** with 9 open positions, ALWAYS
  `report.open_positions limit=3` + page `next_cursor`; NEVER
  `report.current_exposure` or unbounded `open_positions` (both blow the token
  cap).
- **Open exposure = 9 paper positions** (unchanged; this run added none). Per
  AX-033, check `open_positions` before re-entering any market.
- **Two new open forecasts this run** (`fc_D8fQNG4zmBO2Nda5` Netanyahu /
  `fc_FIl62z2Uc5jLQemP` OpenAI) are skips with no exposure; they will keep
  re-flagging resolve_due via `resolution_at_missing` (AX-018/fe2f) until their
  markets close — expected, don't fabricate.
- The two recession forecasts DO flag resolve_due now (via
  `resolution_at_missing`), despite closing 2027 — this is fe2f, not a real
  obligation. Don't fabricate outcomes for them.
- Highest-priority untouched bead: `trade-trace-4z2t` (market.refresh, P1).
