# AX dogfood run — 2026-06-06-04

- **RUN_ID:** 2026-06-06-0300
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** Standing-confirmation run — **NO new friction**. First time the
  loop drove a **sports championship future** (head-to-head Stanley Cup Final) as
  a full forecast loop — a genuinely new market *category*; ~6 standing items
  re-confirmed live.

## Summary (as a bot)

Drove the full canonical loop on the live **2026 NHL Stanley Cup Final**
(Carolina Hurricanes vs Vegas Golden Knights), a category the loop had never
forecast before (prior sports bind was the resolved Knicks/Spurs single game,
AX-031 evidence only). Bootstrap oriented cleanly to 3 obligations (all the
standing `resolution_at_missing` false-positives on 2027-close recession
markets), 1 active strategy, `active_ideas` nuked. Fabricated no resolutions:
the three "due" markets are all long-horizon recession/other markets that
cannot have settled. Bound both "win the Cup" markets, snapshotted each,
recalled prior lessons (no sports-futures lessons existed — confirmed novel),
forecast both **at mid**, **skipped both** with `insufficient_edge`, reviewed
calibration/coach, retained a reflection and round-tripped it. A friction-free
session: every surface behaved as designed or as an already-filed standing item.

### Market read

A two-team final is a coherent **complementary pair** — the head-to-head
analogue of the crypto strike ladder. The two YES mids sum to ~1.0, so the pair
is internally coherent and carries no edge to exploit:

| market | mkt_id | YES mid | note |
|---|---|---|---|
| Carolina wins Cup | mkt_r_dMck6c9wtEO3IK | **0.555** | $1.65M vol · 0.01 spread · oneDay **+0.165** (took series lead) |
| Vegas wins Cup | mkt_nq9_ZKyQh8QDheM6 | **0.4465** | $2.98M vol · 0.003 spread · oneDay **−0.1535** |

- Pair sum **0.555 + 0.4465 = 1.0015** (~0.15% vig) — coherent two-sided market.
  Series-score markets (`2438844` Hurricanes-lead-3-1, etc.) close 2026-06-18, so
  the Final is mid-series; Carolina's +0.165 daily move says it just took a lead.
- **Forecasts:** Carolina YES **0.555** (`fc_AWRqGmqUC9yPdfYL`, at mid);
  Vegas YES **0.4465** (`fc_eZbgvQVi5ecQqEAv`, at mid, the complement). Both
  anchored to their snapshots (`snp_TlATCbVnxVYRjz8f` / `snp_qNi3UaIG74zCdurf`),
  both with explicit `resolution_at`=2026-06-30T00:00:00Z. Did **not** shade —
  with no independent edge on the series state, the market mid IS my estimate.
- **Decision:** **SKIPPED both** with `insufficient_edge` (`dec_mCaR1-xCKE01wQFG`,
  `dec_wyw7opvgEtrkNsFi`). Near-coinflip with no edge; the 3% sports taker fee
  (`sports_fees_v2`, lower than the 7% crypto fee but still decisive on a
  coinflip) swamps any plausible edge — no leg offered a sensible entry, so
  `paper_enter` was **not** forced just to exercise it.
- **Self-check:** both forecasts land in reliability **bin 6 (0.49–0.56)** — the
  one well-calibrated near-money band (mean 0.51, obs_freq 0.5, gap 0.01). My
  standing weakness is *overconfident* near-money YES (bins 3/4/7 obs_freq 0),
  but those are shaded-away-from-mid forecasts; forecasting AT mid on a genuine
  coinflip is exactly where my calibration is sound. Calibration N=33, Brier
  0.1087, skill +0.485 (unchanged — the 2 new forecasts stay pending to June 30).

## Friction found

**None new.** Sixth standing-confirmation run (cf. runs 11, 19, 21, 02, 03).
Every observation maps to an already-open bead or already-fixed item — this time
on a fresh sports-championship-futures category, which exercises the same
surfaces without surfacing anything new.

## Fixes made

**None.** No code changed (deliberately — no new friction to fix; manufacturing
a change would be backlog noise).

## Beads filed

**None.** All Phase B observations dedup against the open queue
(fe2f/xqjv/s7nn/95ry/n33z/scx8/1y9s/lszg/v5va). The n33z gap recurred on this new
category (NHL futures `resolution_rule.text` null) but n33z already catalogs ~11
instances across categories — appending a 12th would be noise.

## Attempted-then-reverted

**None.**

## Verified live this run (fresh cron-booted server)

- **AX-064** (`db45fb6`) — both `market.bind` calls used the advertised
  `source`/`state`/`mechanism` enums and succeeded (`bound_via:adapter`,
  enriched from live Gamma).
- **AX-027** (`752a4bf`) — `snapshot.fetch price`=mid on both
  (0.555 / 0.4465), matching `implied_probability`.
- **AX-031** (`9e10fc2`) — numeric `liquidity_depth_json` (143221.26 / 121366.96),
  no raw Gamma dump on either row (both have two-sided books).
- **AX-013** (`t9n5`) — both skips linked `forecast_id`;
  `coach.unscored_forecasts.count:0`, no false unforecasted-market flag.

## Standing items re-confirmed (no change)

- **AX-018 / fe2f** — exactly 3 `resolution_at_missing` forecasts flagged
  resolve_due (all 2027-close recession/long-horizon markets, fabricated
  nothing); my 2 new forecasts with explicit `resolution_at`=2026-06-30 did
  **NOT** flag — positive control intact.
- **AX-017 / n33z** — `resolution_rule.text` **null** on both NHL markets while
  the full NHL rule ("resolves Yes if [team] wins the 2026 NHL Stanley Cup … No
  if it becomes impossible based off the rules of the NHL") sits only in
  `venue_metadata_json.description`. Reconfirmed on a new (sports-futures)
  category.
- **AX-024 / 95ry** — bm25 recall of the unique token `PRONGHORN` → rank 1 with
  `bm25:[1]` + `temporal:[1]` provenance (round-trips in isolation); the fused
  multi-word query *containing the verbatim hyphenated token plus other terms*
  (`PRONGHORN-SCF-J6 complementary pair sports futures`) dropped it from the
  top-3 entirely — the standing conjunctive-AND / hyphen-split degradation
  masked in fused RRF.
- **AX-040 / s7nn** — `abstention_coverage` 2/100 (2.0%); the 2 new skips this
  run remain uncounted (only first-class `abstention.record` credited).
- **AX-048** — `coach.top_mistakes/top_strengths` `[]`.
- **AX-022 / xqjv** — cold throwaway-journal bootstrap (schema v29) → only
  read/continuity `suggested_process_calls`
  (`report.work_queue`/`agent.next_actions`/`report.recall_receipts`/
  `strategy.show`), 0 obligations, no first-run `market.search→bind→forecast`
  breadcrumb.

## Gate result

`ruff check src tests` clean · `mypy src` clean (178 files) ·
`pytest -q` → **2313 passed, 7 skipped** (119.36s). Identical to the prior
baseline — confirms no code change and a healthy branch.

## Handoff for next run

- Branch HEAD healthy at 2313p/7s; no stranded WIP, no env anomaly.
- **Resolvable inventory growing.** This run's 2 NHL forecasts
  (`fc_AWRqGmqUC9yPdfYL` Carolina / `fc_eZbgvQVi5ecQqEAv` Vegas,
  `resolution_at`=2026-06-30) become resolvable when the Final ends (~mid/late
  June) — the loser's market settles ~0.01, the winner's ~0.99 (unambiguous).
  Resolve via `market.refresh`/`snapshot.fetch` mid (no Polygon RPC, AX-020) with
  a binary label + `confidence≥0.9`. The two FOMC forecasts (run 03) resolve
  after the 2026-06-17 statement; BTC/ETH June month-window forecasts settle
  July 1.
- Open design-Q queue unchanged (8 ax-dogfood beads). Report-surface and
  write-vein catalogs remain exhausted; remaining novelty is behavioral
  re-confirmation across new market *categories* (this run: sports championship
  futures / head-to-head) + waiting on settlements.
- Calibration N=33 stable; deep-ITM/OTM and near-money bin 6 well-calibrated,
  near-money YES shaded forecasts (bins 3/4/7 obs_freq 0) standing overconfidence
  unchanged. New data point: forecasting AT mid on a genuine coinflip lands in
  the well-calibrated bin 6 — the discipline of not shading when edge is truly
  zero is calibration-sound.
