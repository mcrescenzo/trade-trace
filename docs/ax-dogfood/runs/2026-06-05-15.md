# AX dogfood run report — 2026-06-05-15

- **RUN_ID:** 2026-06-05-1700
- **Branch:** ax-dogfood (main untouched)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop
- **Gate:** `ruff` clean · `mypy` clean (178 files) · `pytest` **2306 passed, 7 skipped**

## Summary (as a bot)

Drove the full canonical loop on live Polymarket. Orientation
(`report.bootstrap` → `report.lifecycle`) was legible: 3 `resolve_due_forecast`
obligations surfaced, but `outcome.fetch`/`snapshot.fetch` showed all three
markets are still **live and trading** (mids 0.505 / 0.175 / 0.385, none settled)
— so they are false-positive obligations flagged solely from `resolution_at_missing`
(known **fe2f**), and I correctly left the forecasts open rather than fabricate a
resolution. `outcome.fetch` dead-ended `CONFIG_REQUIRED` as expected (no Polygon
RPC) but now **clearly signposts the no-RPC snapshot.fetch route** — the isqo fix
is working live and that error is no longer friction. Forecast leg: bound two
near-money June-6 16:00Z BTC strikes ($60k, $62k), applied my own documented
calibration discipline (overconfident near-money YES → forecast at/below mid), and
opened one small NO paper test position. Calibration is now conclusive (N=32,
`sample_warning:null`) and strongly reaffirms the near-money-YES overconfidence
signal. `report.coach` was genuinely actionable (flagged reflection hygiene, which
I then cleared). The session's real find came from exercising the exposure surface.

## Friction found

| # | Surface | Severity | What happened | Disposition |
|---|---------|----------|---------------|-------------|
| 1 | report.open_positions / report.current_exposure | **high** | At `limit=10`, `groups` (21,571 chars) was a **full duplicate** of `event_exposure_sets` (21,571 chars) — 3.4× the bounded `open_positions` rows (12,583) — blowing the MCP token cap even with limit honored. lszg was CLOSED claiming this exact fix (`groups → {ref,count}`), but the central-lane fix **was never committed** (no commit in git log; its regression test absent from the branch). | **fixed** e8e3005 + **reopened lszg** for the remaining pieces |
| 2 | memory.recall | medium (filed) | A recall query containing a phrase that appears verbatim in ONLY my just-written reflection failed to surface it in top-5; a query with a guaranteed-unique body token ranked it 9th/10. No item in any recall carried a `bm25` provenance rank — BM25/FTS5 contributes nothing to fused ranking. | **append evidence** to 95ry (in_progress) |
| 3 | report.bootstrap / report.work_queue | low (intentional/filed) | 3 `resolve_due_forecast` obligations on still-live markets (flagged from `resolution_at_missing` only). | re-confirm **fe2f** |
| 4 | onboarding (cold start) | low (filed) | Throwaway-journal bootstrap suggested only read/continuity tools (`report.work_queue`, `agent.next_actions`, `report.recall_receipts`, `strategy.show`) — no first-run entry point. | re-confirm **xqjv** |
| 5 | report.bootstrap | low (filed) | `active_ideas` truncated 0/59 by `max_chars`; `memory_context` not included. | re-confirm (AX-038 class) |

## Fixes made (gate-verified)

- **e8e3005** `fix(reports): stop double-serializing event_exposure_sets as groups
  in exposure reports (trade-trace-lszg)` — replaces the re-embedded `groups` value
  with `{ref:"event_exposure_sets", count}` at both `_report_open_positions` and
  `_report_current_exposure`. +1 regression test
  (`test_exposure_reports_groups_reference_event_exposure_sets_not_reembedded`)
  asserting the reference shape on both surfaces. ruff/mypy/golden/full-suite green.
  Effective next fresh MCP session.

## Beads

- **trade-trace-lszg** — **REOPENED** (was prematurely closed on an uncommitted
  central-lane fix). Documented: groups-ref partial fix landed this run (e8e3005);
  remaining = current_exposure real pagination (`truncated`/`next_cursor`/`cursor`
  still absent → silent-truncation hazard), unbounded `event_exposure_sets`
  (~21.5KB regardless of limit, still ~35KB at scale post-fix), unconfirmed 78-digit
  token-id trimming.
- **trade-trace-95ry** — appended sharpest BM25-recall reproduction (reflection node
  + unique body token).

## Attempted-then-reverted

- None.

## Forecasts / positions this run

- `fc_a22ZUUF0PzfPy2Oy` — BTC > $60k June-6 16:00Z, YES **0.68** (≈mid 0.6855);
  decision: **skip** (no edge).
- `fc_zqwIgTpxcK6E8ml3` — BTC > $62k June-6 16:00Z, YES **0.27** (shaded *below*
  mid 0.305 to correct documented near-money-YES overconfidence); decision:
  **paper_enter NO** `pos_7UesaoYvZwuuK-2s` (20@0.695, declared risk $13.90).
- Reflection `mem_hI2EMse09wA1cKAf` records the deliberate forward calibration test;
  both score next loop after 2026-06-06T16:00Z.

## Handoff notes

- **Branch green & clean** after e8e3005; full suite 2306p/7s.
- **bd:** lszg reopened, 95ry annotated — both writes are to the local embedded-Dolt
  DB (no dolt remote configured; closures/edits are local-DB-only, shared across
  machine agents).
- **Carryable opens:** June-6 BTC $60k/$62k forecasts (close 06-06T04:00… resolution
  06-06T16:00Z); NO paper position `pos_7UesaoYvZwuuK-2s`; older June-3/4
  pending_review forecasts remain genuine fe2f false-positives on still-live markets.
- **For the human / next owner:** lszg is the live priority — the exposure surface is
  still over the token cap at scale until `event_exposure_sets` is bounded and
  `current_exposure` gets real pagination (the silent-truncation correctness hazard).
- **Stashes** `{0,1}` (axloop-debris 1500/1600) remain human-owned; untouched.
