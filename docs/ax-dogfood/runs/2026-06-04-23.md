# AX dogfood run 2026-06-04-23

- **RUN_ID:** 2026-06-04-2200
- **Branch:** ax-dogfood (rebase clean; up to date with origin/main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Mode:** mutating dogfood pass

## Summary (as a bot)

Drove the canonical loop cold via MCP. Orientation was the familiar picture:
`report.bootstrap` surfaced 3 obligations, all `resolution_at_missing`
(fc_CheQKZ live + US/UK recession 2027-close), `active_ideas` nuked 0/50 by the
char cap (AX-038), `obligations` survived at small N (AX-039). No resolvable
inventory — last run's June-5 seeds (SPY/SOL/Elon) settle tomorrow and correctly
did not flag (fe2f positive control). I did **not** fabricate any outcome.

First I confirmed the **AX-041 fix (3524e67) is now live** this fresh session:
`report.audit_readiness` carries a per-issue `remediation` string on every check,
and the handoff prediction held — the forecast created last run with a
forecast-level rule (`fc_k8JRbxD7k3j-RIce`) is **not** in the 24-forecast
blocking list while its rule-less siblings on the same instrument stay blocking.

The high-value move was again **probing less-exercised surfaces** rather than a
23rd price-strike bind, and it paid off with **two genuinely new, fixable
correctness bugs**, both on surfaces never before in the registry:

1. **`report.opportunity` join fan-out (AX-042).** The report `LEFT JOIN`ed
   `outcomes` and `positions` on `instrument_id` alone, so one decision was
   emitted once per `(resolved_final outcome row × position row)` on its
   instrument. Live journal: **46 distinct decisions reported as
   `sample_size:55`**; the `good_skip`/`missed_positive_edge` buckets each said
   `count:3` for a single skip decision (`dec_2Z665`) tripled by 3 duplicate
   outcome rows. Duplicate outcomes come from `resolution.add` retries (AX-030
   wart), duplicate positions from paper-entry fragmentation (scx8). Fixed.
2. **`market.find_similar` uniform `score:1` (AX-043).** `score =
   matched_dimensions / comparable`, where `comparable` counts only dims both
   markets have non-null. A snapshot-less candidate is comparable on just 3 dims
   (mechanism/resolution_source/ambiguous) and, matching all 3, ties `score:1.0`
   with a true 6-of-6 analogue; the tie-break was arbitrary `instrument_id`, so a
   2-comparable Beijing-temp market outranked a 6-comparable Elon-tweet market —
   all reported `1.0`. A bot ranking by score could not tell coverage. Fixed.

Both fixes shipped with regression tests that reproduce the bug. The MCP server
is long-lived this session, so both are **effective next fresh session**;
verified by unit test now.

## Friction found

| sev | surface | item | disposition |
|-----|---------|------|-------------|
| med | report (`report.opportunity`) | **AX-042 NEW**: decision×outcome×position join fan-out inflates `sample_size`/bucket `count`s/means; one decision → N identical records (46 distinct → 55 reported) | **fixed 9d13a5d** |
| low-med | tool (`market.find_similar`) | **AX-043 NEW**: `score=matched/comparable` ties a snapshot-less 3-dim match at 1.0 with a full 6-dim analogue; arbitrary tie-break ranks sparse above rich; coverage not surfaced | **fixed beb385c** |
| — | report (`report.audit_readiness`) | AX-041 fix (3524e67) verified live: `remediation` on all issues; `fc_k8JRbxD7` unflagged, rule-less siblings blocking | fixed (verified live) |
| — | tool/adapter/report | AX-037/027/031 re-confirmed on fresh BTC-$90k month-window bind (`mkt_Weg9U9Ul6g19DJuO`): caller `resolution_rule_text` preserved (`provenance=caller_supplied`); `snapshot.fetch price`=mid 0.011 (not lastTradePrice 0.01); numeric `liquidity_depth_json` | fixed (prior SHAs) |
| — | report (`report.bootstrap`) | AX-038 re-confirm: `active_ideas` nuked 0/50 (`reason=max_chars`), `obligations` survived 3/3 at small N | fixed (reason) · design half dlup |
| — | report (`report.work_queue`) | AX-039 re-confirm: 3-obligation queue serialized fine (curve fail@13 / ok@5 / ok@3 ×5) | filed 1y9s |
| — | report (`report.work_queue`) | AX-018 re-confirm: exactly 3 `resolution_at_missing` flagged; new fc with explicit `resolution_at` did not flag | filed fe2f |
| — | tool (`memory.recall`) | AX-024 re-confirm: natural-language multi-word query returned only RRF-floor temporal hits (~0.031, no bm25 provenance); the just-written on-topic memory did not surface | filed 95ry |
| — | onboarding (`report.bootstrap`) | AX-022 re-confirm: cold throwaway journal suggests only read/continuity tools; no first-run entry point; 0 obligations | filed xqjv |
| — | tool/schema (`market.bind`) | AX-017/n33z re-confirm: BTC-$90k Gamma `description` (Binance month-window touch rule) present but structured `resolution_rule.text` only from caller, not auto-mapped | filed n33z |

## Surfaces probed this run (new coverage)

- `report.strategy_health` (status=all): `sample_size:0`, `sample_warning:no_strategies` — the journal has ~71 forecasts but no `strategy.upsert` entities, so the report is correctly empty. Behaves as designed; not filed.
- `forecast.independence` (fc_CheQKZ): clean `no_blind_commit` / `independence_proven:false`. Behaves as designed; not filed.
- `report.opportunity`: **AX-042** (above).
- `market.find_similar`: **AX-043** (above).

## Fixes made (atomic commits, gate-verified)

| SHA | friction | change | test |
|-----|----------|--------|------|
| `9d13a5d` | AX-042 | `report.opportunity`: select one canonical (latest) `resolved_final` outcome per instrument via correlated subquery + aggregate positions to one summed `realized_pnl` row per instrument, so each decision yields exactly one record | `test_duplicate_outcomes_and_positions_do_not_fan_out_records` (reproduces 1→4 fan-out, then asserts 1) |
| `beb385c` | AX-043 | `market.find_similar`: expose `comparable_dimensions` (the score denominator) on each match; tie-break ranking by `-comparable_dimensions` so a fuller match outranks a sparse tie | `test_sparse_match_exposes_coverage_and_ranks_below_full_match` |

Both are read-model / scoring-presentation changes; neither touches the
contract firewall (append-only, idempotency, envelope, single-writer). AX-042
changes no classification/label/metric logic; AX-043 leaves the score definition
unchanged (additive field + ordering only).

## Beads filed

None. AX-042 and AX-043 were both small, honest direct fixes. The residual
design question behind AX-043 (should the score itself penalize missing
dimensions — `matched/total` instead of `matched/comparable`?) is now **mitigated
and self-documenting** (coverage exposed + de-ranked), so filing a separate bead
would be backlog noise; recorded here instead for the product owner.

## Attempted-then-reverted

None.

## Bot-loop writes this run (faithful, no fabrication)

- Bound `mkt_Weg9U9Ul6g19DJuO` (BTC ≥ $90k June, ext 2410562) with caller
  `resolution_rule_text` (adapter path; AX-037 preserved).
- `snapshot.fetch` → `snp_WeIgym17XAj76OK3` (price=mid 0.011).
- `forecast.add` → `fc_oPBaXWYdpAO__soB` (p_yes=0.02, **forecast-level**
  `resolution_rule_text` + explicit `resolution_at`=2026-07-01 — exercises the
  AX-041 clear path and the fe2f positive control).
- `decision.add(skip, insufficient_edge)` → `dec_bjAzkGj4a5WVbYm9`: genuine
  no-edge deep-OTM longshot, **no exposure opened**.
- `memory.retain` → `mem_kI-lkCDxFwJA7-52` (join-fan-out lesson); recall
  round-trip confirmed AX-024 degradation.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2281 passed, 7 skipped** (was 2279; +2 new regression tests)

## Handoff notes for next run

- **AX-042 / AX-043 fixes are effective next fresh session** (long-lived MCP
  server). Next boot, re-run `report.opportunity` and confirm `sample_size`
  drops to ~46 (distinct decisions) and bucket `count`s no longer triple-count
  `dec_2Z665`; re-run `market.find_similar mkt_7tACn4aqhw6y8BwN` and confirm
  matches now carry `comparable_dimensions` and the 6-comparable analogues rank
  above the 2-comparable ones.
- **Resolvable June 5** (grows calibration N toward the 20 floor, still 11):
  SPY $750 `fc_A8wbW92` (due 2026-06-05T20:00Z), SOL $60 `fc_e0_3Gf8` + SOL $80
  `fc_lfC-Bti3` + Elon tweet-bucket `fc_NvPsMr3TxLJRifY2` (16:00Z). Resolution
  evidence is out-of-band (isqo): SPY/SOL via web spot/Pyth, Elon via xtracker.
  The new BTC-$90k forecast (`fc_oPBaXWYdpAO`) resolves 2026-07-01.
- The 3 standing `resolution_at_missing` obligations (fc_CheQKZ live + US/UK
  recession 2027-close) remain unresolvable — do not fabricate.
- Surfaces now probed with low marginal friction: `report.strategy_health`
  (empty, no strategies), `forecast.independence` (clean). Still unprobed:
  `forecast.commit_blind`/`forecast.reveal_snapshot` round-trip,
  `report.policy_candidates`, `report.time_decay_sharpening`,
  `report.source_quality` — candidates for a future probe.
