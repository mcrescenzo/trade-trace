# AX dogfood run — 2026-06-03-03

- **RUN_ID:** `2026-06-03-2000`
- **Actor:** `agent:ax-dogfood` · **Journal home:** `~/.trade-trace-axloop` · pkg 0.0.2, schema v29
- **Branch:** `ax-dogfood` (never touched `main`)
- **Mode:** one bounded lifecycle pass (live Polymarket via Gamma)

## Summary (how it felt as a bot)

Phase A rebase was a no-op (`up to date`). The open `ax-dogfood` bead queue was
**empty** — the previous run (commit 6c1a2c8) drained ctvb/ptyi/mpsu/2cmb/o1wr/663l/t9n5
— so this run had a clean dedup slate and I focused on (a) **verifying those drained
fixes still hold** through the live MCP surface and (b) finding fresh friction.

The journal loop itself is now smooth: `report.bootstrap` → `report.work_queue` →
discover (`market.search`) → `market.bind` → `snapshot.fetch` → `memory.recall` →
`forecast.add` → `decision.add` → review (`report.open_positions`, `report.coach`) →
`memory.reflect` → `memory.recall` round-trip. Discovery via `market.search` (the
AX-002 fix) and the inline bootstrap caveat glossary (AX-001 fix) materially reduce
cold-start friction. The one hard wall this run was `market.refresh`, which is
**completely unusable** and leaks a raw exception.

I exercised live markets: bound `Will Jesus Christ return before GTA VI?` (gamma 540819)
and `Will China invades Taiwan before GTA VI?` (gamma 540843), forecast both, opened a
`no`-side paper position on the first, and skipped the second on resolution-ambiguity
grounds. All three sibling "before GTA VI" markets trade near 0.50, which does **not**
track the literal base rate of the named event — and no agent tool exposes the
resolution rule, so I could not confirm what YES means. Honest play was to skip / size
to a probe, recorded as a reflection.

## Fixes verified (drained beads, re-checked live — NOT re-reported)

| Bead | Was | Verified this run |
|------|-----|-------------------|
| trade-trace-ctvb (AX-008) | phantom PnL on `no`-side positions | New NO entry @0.52 vs YES mark 0.48 → NO mark 0.52 → `unrealized_pnl: 0` (flat). Pre-existing Iran NO @0.875 vs YES 0.12 → +0.50, not phantom ±75. ✓ |
| trade-trace-t9n5 (AX-013) | `report.coach` flagged forecasted-but-skipped market as unforecasted | `skip` with `forecast_id` accepted; `report.coach.top_mistakes` empty, no false "unforecasted" flag. ✓ |
| trade-trace-o1wr (AX-001) | cryptic bootstrap caveat codes, no gloss | `report.bootstrap` now embeds an inline `caveat_glossary`. ✓ |
| trade-trace-663l (AX-002) | no live market discovery surface | `market.search` ships; returns external_id/gamma_id/question/outcomes/close_at. ✓ |
| trade-trace-2cmb (AX-011) | snapshot/outcome.fetch advertised idempotency_key optional but required | `outcome.fetch` schema now lists `idempotency_key` required. ✓ (but see AX-015 — `market.refresh` was missed) |

## Friction found this run

| id | surface | severity | finding | disposition |
|----|---------|----------|---------|-------------|
| AX-014 | doc | low | AGENT_GUIDE `market.bind` example omitted now-required `state`+`mechanism`; a cold bot copying it gets `VALIDATION_ERROR` | **fixed** c9383ff (+regression test) |
| AX-015 | schema | med | `market.refresh` advertises only `market_id` required but rejects calls without `idempotency_key` (`auto_derivation_available:false`) — same class as AX-011, tool the 2cmb fix missed | **filed** trade-trace-4z2t |
| AX-016 | tool/envelope | **high** | `market.refresh` with `idempotency_key` throws a RAW (non-envelope) error: event_type `market.refreshed` not registered in `events_semantic_keys` (adapter_polymarket.py:281 emits it; semantic_keys.py registers only `market.bound`; log.py:224 raises a bare `KeyError`). Tool is 100% unusable. | **filed** trade-trace-4z2t |
| AX-017 | tool | med | No agent-surface way to retrieve a market's resolution_rule / description text (`market.search`/`market.bind`/`snapshot.fetch` don't expose it); can't responsibly forecast ambiguous markets | **filed** trade-trace-n33z |
| AX-018 | report | low | `report.work_queue` flags `resolve_due_forecast` on a still-live market (mid 0.505, accepting_orders:true) solely from `resolution_at_missing` (AX-012 inverse / possible over-correction) | **filed** trade-trace-fe2f (design Q) |

## Contract-firewall note

AX-016's root-cause fix (registering `market.refreshed` in the semantic-key registry +
typing the unregistered-event error) touches the append-only event log, idempotency-
comparison semantics, and the typed envelope contract — all firewall items — so it was
**filed, not fixed**. AX-015 is moot until the tool is functional and is folded into the
same bead.

## Journal artifacts written (run 2026-06-03-2000)

- Markets bound: `mkt_HAAfbWLPb5hV4HFZ` (540819 Jesus), `mkt_cr8zkY-XaBY6-6ID` (540843 Taiwan)
- Snapshots: `snp_avr4-PTM2OTkqLrh`, `snp_uFQr0rLsIs9JDedh` (+ `snp_JkUOFMkJilJHx6Rb` Rihanna)
- Forecasts: `fc_XPHkcpCvnyY5XTnS` (Jesus YES 0.05, conf low), `fc_Tjbs-FggeY9BDNEG` (Taiwan YES 0.04, conf low)
- Decisions: `dec_dyIM8DdaLbDYA5Is` (paper_enter NO, Jesus), `dec_jzXQyWgYy6ZP3Pao` (skip, Taiwan, links forecast_id)
- Reflection: `mem_6wWb9uY0nF3qaJ29` (GTA-VI markets resolution-ambiguity lesson)

No resolutions recorded: the one `resolve_due` forecast (fc_CheQKZ, Rihanna) is on a
live market; per the resolution-determination discipline I left it open rather than
fabricate an outcome.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2265 passed, 7 skipped** (skips are perf opt-ins) in 104s

## Commits

- `c9383ff` docs(agent-guide): market.bind example must include required state+mechanism

## Beads filed

- `trade-trace-4z2t` (bug, P1) — market.refresh unusable / raw error / unregistered event (AX-015+AX-016)
- `trade-trace-n33z` (feature, P2) — surface resolution_rule/description via agent surface (AX-017)
- `trade-trace-fe2f` (decision, P3) — work_queue resolve_due over-surfacing design Q (AX-018)

## Attempted-then-reverted

None.

## Handoff for next run

- **trade-trace-4z2t (P1)** is the headline: `market.refresh` is dead for every caller.
  The doc still presents it as usable (AGENT_GUIDE line 88 describes its Gamma lookup) —
  any agent that tries it hits a raw exception. Prioritize the semantic-key registration
  + envelope-typing fix.
- Two `no`-side paper positions on the Rihanna market (`mkt_pwuUfbLMcvhkia50`, fc_CheQKZ)
  and one new on Jesus remain open; the Iran NO position (`pos_R35Rl91Jz4FAkcS_`,
  resolves 2026-06-15) and Taiwan/Jesus forecasts (resolve 2026-07-31) are not yet due.
- The next run can re-verify trade-trace-4z2t's fix only after a **fresh server boot**
  (cron `run.sh` fire or restarted session), since the MCP server is long-lived.
