# AX dogfood run 2026-06-03-01

- **Run id:** 2026-06-03-1559 · **Actor:** agent:ax-dogfood (driven inline)
- **Home:** ~/.trade-trace-axloop · **Branch:** ax-dogfood
- **Surface caveat:** this first run was driven from an interactive session with
  **no MCP connected**, so it exercised the **`tt` CLI parity surface**, not the
  MCP `tools/list` surface. MCP-specific discoverability friction (tool
  namespacing, schema rendering in clients) is therefore NOT covered here — the
  headless cron runs (`run.sh`, MCP-connected, fresh/cold session) will cover it.
  The agent was also "warm" (built the harness this session), so cold-bot
  fidelity is lower than a fresh cron run.

## Summary

Drove one full bot lifecycle on a live Polymarket binary market
("New Rihanna Album before GTA VI?", id 540817): bind → snapshot → forecast →
paper_enter → review → reflect, plus a cold-start probe. The lifecycle works end
to end, but the **decision-entry path is rough**: identifier naming and
required-field errors repeatedly sent the bot guessing. Two error-message fixes
landed directly; five larger items filed to beads.

## Friction found

| # | Surface | Severity | Item | Disposition |
|---|---------|----------|------|-------------|
| 1 | report.bootstrap | low | Caveat codes are cryptic uppercase tokens with no inline gloss/glossary | filed `trade-trace-o1wr` |
| 2 | tool catalog | **med** | No live market discovery surface — bot must curl Gamma out-of-band to find markets to bind | filed `trade-trace-663l` |
| 3 | market.bind schema | low | `example_minimal` carries ~20 fields, obscuring the 4 required | filed `trade-trace-mpsu` |
| 4 | market.bind error | med | Missing `source` → bare "source is required", no allowed values | **fixed `4e4ea9c`** |
| 5 | decision.add error | med | Missing `type` → bare "type is required"; field is `type` not `decision_type`, no allowed values | **fixed `8e974a5`** |
| 6 | cross-tool | low | `market_id` (forecast/market) vs `instrument_id` (decision) naming inconsistency | filed `trade-trace-nqyv` (question) |
| 7 | forecast→decision | med | `paper_enter` needs `thesis_id`, but `forecast.add` hands back `forecast_id` | filed `trade-trace-4x1b` |

Positive notes: `report.bootstrap.suggested_process_calls` are clear and
actionable; `report.coach` gave a useful forecast-coverage callout;
`forecast.add` auto-creates the thesis; the present-but-invalid enum errors were
already helpful (the fixes bring the missing-field case up to parity).

## Fixes (direct, gate-verified)

- `4e4ea9c` — market.bind: surface allowed values when a required enum field
  (source/state/mechanism) is missing. +regression test.
- `8e974a5` — decision.add: list allowed decision types when `type` is missing.
  +regression test.

## Beads filed

`trade-trace-663l` (market discovery, P2 feature) · `trade-trace-4x1b`
(paper_enter accept forecast_id, P2 feature) · `trade-trace-nqyv` (id
consistency, P3 question) · `trade-trace-o1wr` (caveat glossary, P3) ·
`trade-trace-mpsu` (example_minimal, P3).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → All checks passed
- `mypy src` → Success: no issues found in 178 source files
- `pytest -q` → **2220 passed, 7 skipped** (perf opt-ins) in 99s

## Handoff for next run

- The decision-entry path is the friction hotspot; `trade-trace-4x1b` and
  `trade-trace-663l` are the highest-value follow-ups.
- First MCP-surface (headless) run still pending — watch whether the
  `mcp__trade-trace__*` tool names + schema rendering add their own friction.
- One live paper position is open on market 540817 (no real resolution yet); a
  future run should resolve it when GTA VI / the Rihanna album status is known.
