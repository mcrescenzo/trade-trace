# AX dogfood run — 2026-06-06-01

- **RUN_ID:** `2026-06-06-0000`
- **Branch:** `ax-dogfood`
- **Actor:** `agent:ax-dogfood`
- **Journal home:** `~/.trade-trace-axloop`
- **Disposition:** **One new friction fixed & gate-verified** — `AX-069`
  (`market.refresh` schema omitted `idempotency_key`), discovered cold in Phase B
  on the very first resolve-due call. Two standing design-Qs (fe2f, xqjv)
  reconfirmed with fresh evidence.

## Summary (as a bot)

Drove the full canonical loop on the live **BTC June month-window touch ladder**
(settle 2026-07-01T04:00Z, Binance BTC/USDT 1m High/Low). The very first action a
bot takes after bootstrap — `market.refresh` to check whether the 3 "due"
forecasts' markets had closed — **failed**: the advertised MCP schema said only
`market_id` was required, but the runtime rejected the call demanding
`idempotency_key`. That schema/runtime mismatch is the headline new friction
(AX-069), fixed this run. With keys supplied, all 3 "due" markets proved **live**
(Rihanna/GTA close 2026-07-31, US recession 2027-01-31, UK recession 2027-03-31),
so I fabricated nothing and left all 3 open — reconfirming the fe2f false-positive.
Today's BTC selloff (`reach-$70k` oneDayPriceChange −0.15, `dip-$52.5k` +0.124)
gave a coherent ladder ⇒ genuine edge ~0; I forecast 2 strikes near mid and
skipped both, which the calibration panel directly endorsed (both land in
empirically overconfident-YES bands).

## What was exercised

| step | tool | result |
|------|------|--------|
| orient | `report.bootstrap` | 3 standing `resolution_at_missing` obligations (fe2f); `active_ideas` 0/83 nuked `max_chars` (AX-038); `memory_context` not included by default |
| **resolve-due (NEW friction)** | `market.refresh` | **first call `VALIDATION_ERROR`** — schema advertised only `market_id`, runtime demanded `idempotency_key` (**AX-069**). Retried with key: all 3 `ok:true` |
| resolve-due verify | `market.refresh` | mkt_pwu (Rihanna/GTA) state=open close 2026-07-31; mkt_33h (US recession) state=open close 2027-01-31; mkt_VMo (UK recession) state=open close 2027-03-31 → all 3 "due" are live, **no fabricated resolutions** |
| scan | `market.search Bitcoin` | 15 live June ladder candidates, non-empty (AX-019/035) |
| bind ×2 | `market.bind` | reach-$70k `mkt_7tACn4aqhw6y8BwN`, dip-$52.5k `mkt_e86jIXc4vcxi7NZn`; `crypto_fees_v2` rate 0.07 |
| snapshot ×2 | `snapshot.fetch` | `price`=mid (0.185 / 0.2585, NOT lastTrade 0.19/0.239) (AX-027); numeric `liquidity_depth_json` 36559/38251 (AX-031) |
| recall | `memory.recall` | prior lessons surfaced (overconfident-YES bins, coherent-ladder, AX-068, stale market.refresh-broken note correctly identified as fixed) |
| forecast ×2 | `forecast.add` | reach-$70k YES 0.17 (shaded below mid 0.185), dip-$52.5k YES 0.27 (thin +1.2pt lean); `fc_Tvqw7AzmwRxQFOTn` / `fc_P44BEURN9AZ1r3M-`, both `resolution_at`=2026-07-01 |
| decide ×2 | `decision.add` | 2 forecast-linked skips, `material_non_action` category=skip reason=insufficient_edge (AX-013) |
| review | `report.calibration` / `report.coach` | N=33 Brier 0.109 skill 0.485 `sample_warning:null`; coach `top_mistakes/strengths` `[]` (AX-048) |
| remember | `memory.retain` + `memory.recall` | reflection `mem_zR811iYwRJIWeiPo`; round-trip re-confirmed (AX-024) |
| cold probe | throwaway `tt journal init` + bootstrap | read/continuity suggested calls only, obligations:0 (xqjv) |

## Calibration self-check (the point of the discipline)

N=33, Brier 0.109, skill 0.485, `sample_warning:null` (unchanged — month-window
forecasts settle July 1, won't score same-day). Standing **overconfident-YES**
signal is now visible across **three** bins, all `observed_frequency 0`:
bin 3 (0.12–0.2, n=4, gap 0.165), bin 4 (0.25–0.3, n=3, gap 0.28), bin 7
(0.57–0.85, n=3, gap 0.693; incl run-18 SPY 0.85 miss `fc_A8wbW92buXzfKgoA`
Brier 0.7225). My two new forecasts land in **bins 3 and 4** exactly — the
empirically overconfident bands. The shade-down (reach-$70k YES 0.17 vs mid 0.185)
and skips were the right call; the standing lesson sharpens to: **shade OTM
touch-YES even harder toward 0**, since these near-money/mid YES bands have
resolved NO every time so far.

## Friction found

| sev | surface | finding | disposition |
|-----|---------|---------|-------------|
| **med** | tool schema | **AX-069** `market.refresh` is a non-auto-derivable retryable write (`is_write=True`, not in `TOOL_PRIMARY_EVENT_TYPE`) so the caller must supply `idempotency_key`, but its schema was auto-derived from `example_minimal={market_id}` and advertised only `market_id` required. A schema-following bot's first call `VALIDATION_ERROR`s. Identical class to `trade-trace-2cmb` (snapshot.fetch/outcome.fetch). | **fixed `a501ca5`** (+regression; effective next fresh session) |
| low | report (work_queue) | **fe2f** reconfirmed: 3 forecasts flagged `resolve_due_forecast` urgency=due purely on `resolution_at_missing`; all 3 markets proven **live** (close 2026-07-31 / 2027-01-31 / 2027-03-31). | evidence appended to fe2f |
| low | onboarding (bootstrap) | **xqjv** reconfirmed via cold probe: empty-journal `suggested_process_calls` are all read/continuity tools; no first-run `market.search→bind→forecast` breadcrumb. | evidence appended to xqjv |

## Fixes made

- **`a501ca5`** `fix(adapter): advertise idempotency_key as required on
  market.refresh schema (AX-069)` — added `idempotency_key` to `market.refresh`'s
  `example_minimal` (so the auto-derived schema marks it required, matching
  dispatcher enforcement), folded it into the shared `trade-trace-2cmb` comment,
  and extended `tests/contracts/test_fetch_idempotency_schema_parity.py` to cover
  `market.refresh` alongside its sibling fetch writes. Effective next fresh
  session (long-lived MCP server still serves the old schema this session).

## Beads filed

**None new.** AX-069 fixed directly (small in-class schema fix, not a
contract-firewall item). Fresh evidence appended to existing **fe2f** and
**xqjv** rather than re-filing.

## Attempted-then-reverted

**None.**

## Standing re-confirms (no change, not re-filed)

- **AX-068** (`market.refresh` semantic key) — confirmed still live: all 3
  refresh calls returned `ok:true` once `idempotency_key` was supplied. The
  stale `mem_RZapluucuEm7R9rJ` "market.refresh COMPLETELY BROKEN" note is
  now historically-true-but-fixed; not re-reported.
- **AX-027** — `snapshot.fetch price`=mid, not lastTradePrice.
- **AX-031** — numeric `liquidity_depth_json`, no raw payload dump.
- **AX-013** — both skips forecast-linked + `material_non_action` tagged;
  `coach.unscored_forecasts.count:0`.
- **AX-048** — `coach.top_mistakes/top_strengths` `[]`.
- **AX-038 / dlup** — bootstrap `active_ideas` 0/83 nuked `max_chars`.
- **AX-024** — bm25 recall of unique token `PETREL-BTC-JUN6-0000` → rank 1 with
  `bm25:[1] temporal:[1]`; the same token in a fused multi-word query buried the
  just-written node out of top-2 (RRF behaving as designed — high-DF query terms
  dominate). Not friction.
- **s7nn** — `abstention_coverage` 2/93 (2.15%); considered-and-passed skips
  still uncounted in the survivorship denominator.

## Gate result

- `ruff check src tests` → All checks passed
- `mypy src` → Success: no issues found in 178 source files
- `pytest -q` → **2313 passed, 7 skipped** in 119.95s

## Handoff notes

- **AX-069 fix is effective next fresh session** — this session's long-lived MCP
  server still advertises the old `market.refresh` schema (only `market_id`).
  A cron `run.sh` fire or restarted session will pick up the corrected schema.
  Verify next run that `tool.schema market.refresh` lists `idempotency_key` in
  `required`.
- Two new open forecasts (reach-$70k YES 0.17 `fc_Tvqw7AzmwRxQFOTn`, dip-$52.5k
  YES 0.27 `fc_P44BEURN9AZ1r3M-`) settle 2026-07-01; both are forward tests of
  the "shade OTM touch-YES harder" calibration hypothesis. Calibration stays
  N=33 until same-day strikes are forecast+resolved within one cycle.
- The new-friction surface remains thin; AX-069 was a residual schema-parity gap
  in the same class as 2cmb (the fetch-write trio). Worth a one-off audit: are
  there *other* non-auto-derivable `is_write` tools whose `example_minimal` omits
  `idempotency_key`? (This run only confirmed the adapter trio is now consistent.)
- Standing design-Qs fe2f (work_queue false-positive due-gate) and xqjv (cold
  first-run entry-point) remain the highest-value unaddressed bot-ergonomics
  items; both now carry multi-run evidence.
