# AX dogfood run 2026-06-04-16

- **RUN_ID:** 2026-06-04-1500
- **Branch:** ax-dogfood (verified; never touched main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Phase A sync:** `git rebase origin/main` → already up to date.

## Summary (as a bot)

Drove the canonical journal loop on live point-in-time BTC strike markets that
close **today at 16:00Z** — chosen so the forecasts can actually be resolved and
scored by the next run, building the calibration sample. Orientation via
`report.bootstrap` worked but surfaced a genuinely **empty `active_ideas`
section** with internally-contradictory truncation metadata — the headline new
finding (AX-038). Bound two near-the-money strikes via the adapter path, which
**live-verified the AX-037 fix** (caller `resolution_rule_text` now preserved on
the adapter path). Both strikes are efficient/liquid, so I recorded honest
scoreable forecasts and **skipped both for `insufficient_edge`** (no exposure).
The system felt solid: every tool returned a typed envelope, the AX-027/031/037
fixes all held live, and the fe2f control pair was the cleanest yet.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-038** | report (bootstrap) | medium | `report.bootstrap` truncation reported `active_ideas` as `returned_count:0, available_count:51, reason:"max_items"` — a self-contradiction (a >0 max_items budget can't reduce a section to 0). Root cause: `_apply_budget` trims each bucket to `max_items`, then if the trimmed section still exceeds `max_chars` it nukes the **whole** section to `{}` but kept the stale `max_items` reason. | **fixed** (a0d28a2) + **filed** design half (trade-trace-dlup) |
| AX-037 (verify) | tool/adapter | — | Adapter-path `market.bind` with caller `resolution_rule_text` now **preserves** it (`provenance=caller_supplied`) on both BTC binds — fix e9892cd effective live this session. | verified live |
| AX-027 (verify) | tool/report | — | `snapshot.fetch price`=mid (0.3875) on mkt_cDjxbYAhIPlhUhdb, NOT `lastTradePrice` (0.438). | verified live |
| AX-031 (verify) | tool/adapter | — | `liquidity_depth_json` numeric (14495.0 / 24500.7), no raw Gamma dump. | verified live |
| AX-018 / fe2f | report | (filed) | Cleanest single-journal control pair: 3 `resolution_at_missing` forecasts flagged resolve_due (incl. 2027-close US/UK recession), 2 new forecasts with explicit `resolution_at`=16:00Z did NOT flag. | evidence appended to trade-trace-fe2f |
| AX-017 / n33z | tool/schema | (filed) | Supply-side now solved (AX-037 live); retrieval gap unchanged — Gamma `description` rule prose still not auto-mapped to `resolution_rule.text`. | evidence appended to trade-trace-n33z |
| AX-022 / xqjv | onboarding | (filed) | Cold throwaway-journal bootstrap surfaced only read/continuity `suggested_process_calls` (work_queue / next_actions / recall_receipts / strategy.show); no first-run entry-point. | re-confirmed (cold probe) |

## Fixes made (atomic commits)

- **a0d28a2** — `fix(report.bootstrap): emptied-by-char-cap section must report reason=max_chars not stale max_items (AX-038)`. One-line correctness fix in `_apply_budget` + unit regression test (`test_apply_budget_section_emptied_by_char_cap_reports_max_chars_not_stale_max_items`; reverted-fix confirmed repro: stale `max_items` → corrected `max_chars`).

## Beads filed

- **trade-trace-dlup** (P2, task, ax-dogfood) — `report.bootstrap` `active_ideas` is nuked to empty by the per-section char cap; a bot never sees its open ideas in orientation in a mature journal. The substantive design half of AX-038 (the metadata-honesty half is fixed in a0d28a2). Proposes trim-to-fit vs. nuke / larger budget / cursor; design-level because the budget enforcer affects all sections.

## Beads updated (evidence appended)

- **trade-trace-fe2f** — cleanest single-journal positive/negative control pair this run.
- **trade-trace-n33z** — supply-side workaround now verified live; retrieval gap remains.

## Attempted-then-reverted

- None. (The AX-038 fix passed gates first try; reverted only to confirm the regression test catches the bug, then restored.)

## Journal activity (live)

- Bound: mkt_1I__rxv9-y3mh65W (BTC>$60k, ext 2433925), mkt_cDjxbYAhIPlhUhdb (BTC>$64k, ext 2379483) — both adapter path, caller `resolution_rule_text` preserved.
- Snapshots: snp_cpXDu3QP8KZaEtqe ($60k, mid 0.9985), snp_WPhzvnPDpnceKpXt ($64k, mid 0.3875).
- Forecasts (scoreable, resolution_at 16:00Z): fc_LdbfXCZrqu8esJhw ($60k @ 0.99, very_high), fc_uhCM0iYQxlSAI7Zu ($64k @ 0.37, low).
- Decisions: skip both (insufficient_edge), forecast_id-linked — **no exposure taken**.
- Memory: retained mem_GBCV9Ze0jQovQgN2 (AX-037 verified-live, corrects mem_1PUaxr); round-trip recall confirmed.

## Gate results

- `ruff check src tests` → All checks passed.
- `mypy src` → Success, 178 source files.
- `pytest -q` → **2279 passed, 7 skipped** (was 2278; +1 new regression test). ~108s.

## Handoff notes for next run

- **Resolvable next run:** fc_LdbfXCZrqu8esJhw ($60k) and fc_uhCM0iYQxlSAI7Zu ($64k) close 2026-06-04T16:00:00Z. The 16:00+ run should determine the Binance noon-ET candle close and `resolution.add` each with `confidence>=0.9` + binary label so they auto-score (lifts the calibration N off 1). $60k is near-certain YES; $64k depends on whether BTC cleared $64k at noon ET.
- **trade-trace-dlup** is the meatiest open item from this run — the active_ideas cliff is a real orientation gap worth a design decision.
- **trade-trace-4z2t** (P1, market.refresh unusable) remains open and unfixed across many runs — a candidate for a dedicated fix session (the `market.refreshed` event_type is unregistered in events_semantic_keys + the call leaks a raw non-envelope error).
