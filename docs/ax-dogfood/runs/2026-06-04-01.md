# AX dogfood run — 2026-06-04-01

- **Run id:** `2026-06-04-0000` (UTC)
- **Branch:** `ax-dogfood`
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket enabled
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (as a bot)

Drove the full MCP journal loop on live Polymarket. Bootstrap → work_queue →
lifecycle oriented cleanly; the single flagged obligation was the known
false-positive resolve_due on a still-live market (AX-018). Resolved-what's-due
found nothing genuinely due (both candidate markets still `tradable:true`), so I
faithfully left them open rather than fabricate an outcome. Discovered markets
via `market.search`, bound two binary markets (a near-term BTC Binance-close band
and the June Fed "no change" market), forecast both, opened a small contrarian NO
paper position on the Fed market (3% tail vs the market's 1.65%) and skipped the
no-edge BTC market. Calibration/coach feedback was actionable (0/20 floor,
reflection hygiene). Reflected a lesson and confirmed it round-tripped through
recall. Two genuinely new friction items surfaced and both were fixed directly;
one cross-cutting follow-up was filed.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| AX-025 | report | medium | `report.open_positions`/`current_exposure` row-level `unrealized_pnl` stayed `null` and kept the `open_no_mark` caveat ("no current mark") even when `mark_state:"available"` + a populated `latest_mark` were on the same row — a contradictory exposure signal. Root cause: the positions projection only marks `unrealized_pnl` at rebuild time, so a position opened before its first snapshot is never re-marked when a later snapshot lands. | **fixed** `2315e62` |
| AX-026 | tool/schema | low | `market.search` query is matched conjunctively by Gamma `/public-search` (ALL terms must co-occur in one market): `"bitcoin ethereum price"` → 0 candidates while `"bitcoin"`→6 and `"bitcoin price"`→6. A bot's natural multi-keyword query silently dead-ends with no "narrow your query" signal. | **fixed** `7341ab9` |

### Re-confirmed (already tracked — not re-filed)

- **AX-018 / `trade-trace-fe2f`** — `resolve_due_forecast` flagged on still-live `mkt_pwuUfbLMcvhkia50` (mid 0.505, `accepting_orders:true`) solely from `resolution_at_missing`.
- **AX-022 / `trade-trace-xqjv`** — cold `report.bootstrap` (throwaway journal) surfaced only read/continuity `suggested_process_calls` (work_queue / next_actions / recall_receipts / strategy.show), no first-run entry-point.
- **AX-024 / `trade-trace-95ry`** — `memory.recall` BM25 conjunctivity confirmed again (cross-node multi-word queries return weak/zero lexical matches; a single-node query whose tokens co-occur — the new reflection — round-tripped at rank 1 with `bm25:[1]`). The `market.search`/Gamma conjunction (AX-026) is a *distinct* external mechanism, not the same FTS5 issue.
- **AX-008 / `trade-trace-ctvb`** — NO-side marking: bucket/event-level `unrealized_pnl` now computes ~0 (no phantom PnL). The residual row-level/null inconsistency is the new AX-025.
- **AX-023** — verified the fix is **live this session**: `forecast.add` succeeded on a freshly-bound adapter market with no prior `snapshot.fetch`.
- **AX-019 / AX-021** — `market.search` query routing and `market.bind` `gamma_market_id` path both worked correctly live.

## Fixes made

- **`2315e62`** `fix(report): re-mark open position from live mark so unrealized_pnl/open_no_mark stay consistent` — in `_position_row_payload`, when an open row has a live mark price but a null stored `unrealized_pnl`, recompute side-aware from the mark's YES-contract price via the canonical `_unrealized_pnl` helper and drop the now-false `open_no_mark` caveat. +regression test (reverted-fix confirms repro). Serves `report.open_positions` + `report.current_exposure`.
- **`7341ab9`** `feat(market.search): surface a search_hint when a multi-word query returns zero candidates` — adds a point-of-failure `search_hint` on zero-result multi-word queries and documents the conjunctive matching in the tool description. No change to matching (Gamma-side). +regression test.

## Beads filed

- **`trade-trace-pr2j`** (P2, task, ax-dogfood) — Design Q: open-position marking is inconsistent **across** PnL reports. `2315e62` fixed the exposure surfaces at the report layer, but `report.pnl` (`open_mark_coverage`, unrealized total — `reports/pnl.py:63`), `report.compare`, and `review_bundle` still read the stale projection `unrealized_pnl` directly. Decision needed: re-mark the projection on snapshot (single source of truth) vs. apply the shared marking helper uniformly across all PnL readers.

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2270 passed, 7 skipped** (skips are perf opt-ins) — +2 from this run's regression tests.

## Journal activity (live)

- Bound: `mkt_BxqIYenOQjh6ta1l` (BTC <$64k June 4), `mkt_bVxRurTN_6ibcQNo` (Fed no change June 2026).
- Forecasts: `fc_uZ-WmwfdrsEtISlE` (BTC, YES 0.49, low conf), `fc_O7R5yQTjMVcSG1pe` (Fed, YES 0.97, high conf).
- Decisions: `dec_ip5N-t5wpKt9fuPc` (paper_enter NO 100@0.0165 Fed, pos `pos_dIKpqkuqggcQfwKz`), `dec_i4DM0wwngKlZCxGJ` (skip BTC, linked forecast).
- Reflection: `mem_xnESWoh45AhC7laY` (tail-probe discipline on extreme-priced markets); recall round-trip confirmed.

## Handoff notes for next run

- New paper NO position on the Fed market (`pos_dIKpqkuqggcQfwKz`) and forecast `fc_O7R5yQTjMVcSG1pe` resolve after the June 16–17 FOMC; the BTC forecast `fc_uZ-WmwfdrsEtISlE` resolves **June 4 16:00Z** (Binance noon-ET close) — a near-term scoreable. Resolve them in a future run when closed.
- AX-025/AX-026 fixes are committed and gate-verified; they take effect in the live MCP tools on the **next fresh server boot** (cron `run.sh` / restarted session). AX-025's fix is report-layer only — `trade-trace-pr2j` tracks the systemic marking-consistency decision.
- `gh pr edit` still fails on this repo (GitHub "Projects (classic)" GraphQL deprecation); push updates PR #2 fine, but the PR body can't be edited via `gh`.
