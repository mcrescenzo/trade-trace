# AX dogfood run 2026-06-04-18

- **RUN_ID:** 2026-06-04-1700
- **Branch:** ax-dogfood (never touched main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma)
- **Disposition:** evidence-gathering run — exercised the full resolve→auto-score→calibration loop again; **no code shipped** (all findings re-confirm filed/firewall items); appended refining evidence to 2 beads.

## One-paragraph summary (how it felt as a bot)

Followed bootstrap's own suggested orientation sequence. As a cold bot, bootstrap
again showed me **zero** of my obligations and **zero** of my ideas — both the
`obligations` (0/5) and `active_ideas` (0/40) sections were nuked to empty by the
per-section char cap (AX-038/dlup). It routed me to `report.work_queue`, which
this run **succeeded** (5 obligations) — last run the same call failed at 13
obligations over the token cap. That contrast is the run's most useful datum: the
work_queue overflow is **item-count-proportional**, latent below a threshold and
re-triggering as obligations accumulate (refines AX-039/1y9s). From the queue I
resolved the two point-in-time strikes I deliberately left open last run — an hour
past their 16:00Z settle the orderbooks had fully converged (Bxq→0.9995 YES,
Soh→0.002 NO), so resolving at confidence 0.95 was faithful (the convergence is
ground truth, not inference). Both auto-scored, lifting calibration N 9→11, both
near-the-money and both scored poorly — exactly the band my own calibration says I
have no edge in. I then bound two fresh Solana month-window touch markets (a new
asset, and a **novel touch-below "dip to $X" structure**), forecast the efficient
deep tail, and skipped on insufficient_edge — no exposure. Memory retained and
round-tripped. The surfaces all behaved per prior fixes; nothing new to fix.

## What I exercised (the canonical loop, end to end)

- **Orient:** `report.bootstrap` → `report.work_queue` (5 resolve_due obligations).
- **Resolve due (2 markets, converged orderbook = ground truth):**
  - `mkt_BxqIYenOQjh6ta1l` → snapshot mid **0.9995** (was 0.616 ambiguous last run) →
    `resolution.add` **yes**, conf 0.95 → auto-scored `fc_uZ-WmwfdrsEtISlE`
    **Brier 0.2601** (p_yes 0.49).
  - `mkt_SohHCLs3qtCWRWbT` → snapshot mid **0.002** (strike unconfirmed last run;
    snapshot-identical to ETH>$1800 which settled NO at ETH≈$1778) → `resolution.add`
    **no**, conf 0.95 → auto-scored `fc_1yoUooDa7wbjasEk` **Brier 0.3136** (p_yes 0.56).
- **Review:** `report.calibration` N=11, Brier 0.163, skill **+0.343**, ECE 0.327.
  Both new scores landed in the near-the-money 0.49/0.56 bins with large gaps
  (−0.51/+0.56); tails (0.92/0.97) stay well-calibrated (gaps −0.08/−0.03).
- **Scan & forecast (fresh, no exposure):** bound SOL `mkt_1E2FEVMTTXg3fUIq`
  (reach $160, ext 2410600) + `mkt_DgqiOewVZWjWXC5f` (dip to $10, ext 2410615);
  snapshots clean (price=mid, numeric depth); `report.calibration_advisory(0.01)`
  (tail calibration good); `forecast.add` reach-$160 p_yes 0.01 (resolution_at
  2026-07-01); `decision.skip` insufficient_edge.
- **Remember:** retained the deferred-resolution lesson (`mem_sZQSpSChf3DVnRB-`);
  recall round-tripped it as the #1 BM25 hit.
- **Cold-start probe:** throwaway journal init+bootstrap → suggested only
  read/continuity tools, no first-run entry point (AX-022/xqjv), then destroyed.

## Friction found (all re-confirmations of filed items)

| ID | Surface | What | Disposition |
|---|---|---|---|
| AX-039 (refine) | report.work_queue | **N-proportionality proven:** succeeded at 5 obligations this run, failed at 13 last run. Overflow is item-count-proportional (triple-serialization × N), latent below threshold, re-triggers as queue grows. Refines the "unrecoverable" framing. | evidence → trade-trace-1y9s |
| AX-038 (dup) | report.bootstrap | Char cap nuked **both** obligations (0/5) and active_ideas (0/40) to empty again — a bootstrap-first bot sees zero of each at orientation. | evidence → trade-trace-dlup |
| AX-030 (re-verify) | resolution.add | Skip-reason fired live: first `resolution.add` without `confidence` → `auto_score_skipped_reason` named the cause; confidence-bearing retry scored both. Wart re-confirmed: leaves a non-scoring dead `resolved_final` row (out_WFz). | fixed (skip-reason verified live) |
| AX-017 (dup) | market.bind / snapshot | **Novel touch-BELOW structure + new asset (SOL):** "dip to $X" resolves on Binance 1m **Low** ≤ strike — distinct mechanic & probability model from all prior touch-above instances. Venue rule null; caller text preserved. | evidence → trade-trace-n33z |
| AX-037 (re-verify) | market.bind adapter | Adapter-path bind preserved caller `resolution_rule_text` (provenance caller_supplied) on both SOL binds. | fixed (prior SHA e9892cd verified) |
| AX-027/031 (re-verify) | snapshot.fetch | price=mid (0.0025, not lastTradePrice 0.005 on the dip market); numeric `liquidity_depth_json`, no raw dump. | fixed (prior SHAs verified) |
| AX-035 (re-verify) | market.search | Single-term zero-result `search_hint` fired ("temperature"→0). | fixed (prior SHA 583fe6f verified) |
| AX-013 (re-verify) | report.coach | skip+forecast_id link → coach reports 0 unscored false-flags. | fixed (verified) |
| AX-018 (dup) | report.work_queue | Positive control held: 3 `resolution_at_missing` forecasts flagged resolve_due (fc_CheQKZ live + US/UK recession 2027-close); the 2 `resolution_at`-bearing point-in-time forecasts resolved+scored cleanly and did not re-flag. | evidence → trade-trace-fe2f |
| AX-022 (re-verify) | bootstrap onboarding | Cold throwaway journal: bootstrap suggests only read/continuity tools, no first-run entry point. | filed → trade-trace-xqjv |

## Fixes made

None. No new fixable friction surfaced; all findings re-confirm filed or
contract-firewall items.

## Beads filed / updated

- **trade-trace-1y9s** — appended N-proportionality evidence (recovered at N=5).
- **trade-trace-n33z** — appended novel touch-below "dip to $X" SOL structure.
- No new beads (dedup discipline: nothing rose above existing coverage).

## Attempted-then-reverted

None.

## Gate result

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2279 passed, 7 skipped in 103.93s** (no code changed; baseline held)

## Handoff for next run

- **Calibration is N=11** (toward the ≥20 floor). The 3 remaining unresolved
  obligations are the `resolution_at_missing` set (fc_CheQKZ live; US/UK recession
  2027-close) — not resolvable. To keep feeding N, the clean pattern is still
  point-in-time strikes with explicit `resolution_at` resolved the next run — but
  note **timing**: at 17:00Z today's noon-ET (16:00Z) strikes had already settled
  and tomorrow's were not yet posted, so no fresh resolves-next-run inventory was
  available. Seeding works best in the window after the day's strikes post and
  before they settle.
- **New open SOL forecast** `fc_V2DgVI4ESX7q0f_q` (reach $160) resolves 2026-07-01
  (month-window) — will not feed calibration until July. The SOL dip-to-$10 market
  is bound (`mkt_DgqiOewVZWjWXC5f`) but only forecast on the reach-$160 leg.
- **AX-039/1y9s** is now the cleanest argument for the cross-report envelope
  rework (hv19 + lszg + 1y9s): the N-proportionality data shows a bootstrap-first
  bot works on a small journal and silently breaks as its own backlog grows. Good
  first PR whenever the human wants to land pagination + dedup + a serialized-size
  cap across work_queue / lifecycle / current_exposure.
- **AX-030 wart** (non-scoring dead `resolved_final` row left by the
  confidence-less first call) recurred again this run (out_WFz). Low severity but
  steadily accumulating dead outcome rows; worth a cleanup-on-supersede thought if
  it's ever promoted.
