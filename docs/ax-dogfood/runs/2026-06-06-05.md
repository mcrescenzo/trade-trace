# AX dogfood run — 2026-06-06-05

- **RUN_ID:** 2026-06-06-0400
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** Standing-confirmation run — **NO new friction**. But a genuine
  behavioral first: the loop drove its **first full forecast→resolution→auto-score
  settlement cycle on the intraday TOUCH ("reach $X on <date>") daily BTC ladder**
  — distinct from the noon-ET "above $X" point-in-time strikes settled back in
  run-16. Two real settlements recorded and auto-scored near-perfectly; ~8
  standing items re-confirmed live.

## Summary (as a bot)

Bootstrap oriented cleanly. `report.work_queue` surfaced **5** `resolve_due_forecast`
obligations, but only **2** were genuinely due: the June-5 intraday touch pair
**"Will Bitcoin reach $64,000 on June 5?"** (`mkt_Wxkna_mpchBYWIDh`, `fc_2jfW_utpl18Ps3om`)
and **"…$65,000…"** (`mkt_6Bpdgm1VCoNBURgx`, `fc_GJSlnFhzth-6H_16`), both
`resolution_at_due_without_score` with `due_at`=2026-06-06T04:00Z, which had just
passed. The other 3 are the standing **AX-018/fe2f** `resolution_at_missing`
false-positives on 2027-close recession markets — fabricated no resolution for
them. `report.unscored_forecasts` returned **exactly the 2** genuinely-due
forecasts, cleanly excluding the recession noise — the better surface for "what's
actually resolvable."

`market.refresh` on both touch markets (with `idempotency_key`, AX-068/069 path)
returned `ok:true`. Both showed unambiguous terminal pricing
`outcomePrices=["0.0005","0.9995"]` (Yes≈0, No≈1) — BTC did **not** touch
$64k/$65k during the June-5 ET day (consistent with the live June-6 ladder
placing spot ~$59–60k). Recorded both via `resolution.add` (status
`resolved_final`, `outcome_label=no`, `confidence=0.95` from the unambiguous
one-sided outcomePrices per the playbook's no-`winningOutcome` rule). Both
**auto-scored**: Brier **0.000625** (p_yes≈0.025) and **0.000036** (p_yes≈0.006)
— forecasting touch strikes well out of reach at a low YES is calibration-sound,
landing in deep-OTM bins 0/1.

Then scanned the live June-6 noon-ET "above $X" ladder, found one genuinely-new
(un-forecast) strike, forecast it at-mid, skipped it with `insufficient_edge`,
reviewed calibration/coach, retained the settlement lesson and round-tripped it,
and ran the cold-start probe. Friction-free: every surface behaved as designed
or as an already-filed standing item.

### Resolutions recorded (the headline)

| market | mkt_id | fc_id | outcomePrices | outcome | Brier | p_yes |
|---|---|---|---|---|---|---|
| Reach $64k on Jun 5 | mkt_Wxkna_mpchBYWIDh | fc_2jfW_utpl18Ps3om | ["0.0005","0.9995"] | **no** | **0.000625** | ≈0.025 |
| Reach $65k on Jun 5 | mkt_6Bpdgm1VCoNBURgx | fc_GJSlnFhzth-6H_16 | ["0.0005","0.9995"] | **no** | **0.000036** | ≈0.006 |

- Resolution mechanics (touch ladder): no `winningOutcome`, `umaResolutionStatuses=[]`,
  `resolved_at` null, `state` still `"open"` — but `outcomePrices` are terminal
  and one-sided, so the outcome is a settled historical fact (did BTC touch the
  strike on June 5 ET — it did not). Recorded `confidence=0.95`, `settlement_price=0.9995`.
- Auto-score gate (**intentional-design #4**) fired end-to-end: both
  `resolution.add` returned `auto_scoreable:true`, `auto_score_skipped_reason:null`,
  with a populated `auto_scored_forecasts` row.

### New forecast (scan & forecast)

The June-6 **noon-ET "above $X" point-in-time** ladder (settles 16:00Z today):

| market | mkt_id | YES mid | note |
|---|---|---|---|
| above $58k Jun 6 | mkt_Q-w3HIxGx4SgvobY | **0.9425** | **new bind** (`state_changed:true`) · 1d −0.038 |
| above $64k Jun 6 | mkt_mMrI9fVsyQDM0xrQ | 0.0085 | already bound+forecast in **run-18** (`state_changed:false`) — not re-forecast |
| above $66k Jun 6 | mkt_DaGR4nGM18wtBnBC | 0.0015 | already bound+forecast in **run-18** — not re-forecast |

- **Dedup discipline:** `memory.recall` surfaced ZEBRAFINCH-LADDER-J6 (run-18),
  which already forecast the $64k/$66k June-6 noon strikes. Both binds returned
  `state_changed:false` and they sit in `open`/`forecast_pending` (future
  `resolution_at`, correctly **not** flagged due — AX-018 positive control). Only
  the **$58k** strike was a fresh bind, so it was the only one forecast.
- **Forecast:** above-$58k YES **0.93** (`fc_LHIDb7fc29SHqHK8`), anchored to
  `snp_zbPGhtzejnfRSG39` (implied 0.9425), `resolution_at`=2026-06-06T16:00:00Z.
  Forecast a hair **below** mid (0.93 vs 0.9425), encoding the documented
  near-money/YES overconfidence + downside-momentum lesson rather than sitting
  above the market on a YES.
- **Decision:** **SKIPPED** with `insufficient_edge` (`dec_Je8nRXES_pj6xLDF`,
  `forecast_id` linked). Coherent ladder ⇒ edge ~0, and the 7% `crypto_fees_v2`
  taker fee swamps any thin edge on a deep-ITM strike. The linked `forecast_id`
  re-confirms the **AX-013** fix: `report.coach` did NOT false-flag the skip as
  unforecasted.

### Calibration

- N **33 → 35**, Brier **0.109 → 0.102**, skill **0.498**, ECE 0.154. The two new
  settlements landed in deep-OTM bins 0 (mean 0.0053, obs 0) and 1 (mean 0.0153,
  obs 0) — near-perfect.
- Standing weakness re-confirmed: **bin 7** (0.56–0.85, mean 0.66, n=4, obs_freq
  **0**, gap +0.66) and bin 4 (0.20–0.29, obs 0) — overconfident near-money YES.
  The well-calibrated bands are bin 6 (0.49–0.50, obs 0.67) and bins 8/9 (0.9+,
  obs 1.0). My new $58k forecast (0.93) will score into bin 8 next run.
- `report.coach`: `unscored_forecasts` count **0** (the resolved pair cleared the
  queue), no stale watches, no false-flags. Integrity diagnostics clean
  (unsupported/ambiguous/disputed/void all 0%).

## Friction found

**None new.** Standing-confirmation run; every observation maps to an
already-open bead or already-fixed item:

| surface | observation | disposition |
|---|---|---|
| report (`work_queue`) | 5 resolve_due, 3 are `resolution_at_missing` recession false-positives (close 2027) | **AX-018 / fe2f** (open) — re-confirmed; `report.unscored_forecasts` is the clean counter-surface (returned exactly the 2 truly-due) |
| tool (`memory.recall`) | 6-token query w/ hyphenated token didn't rank the just-written node (indexed: total_in_scope 58→59); single-token "SANDPIPER" → rank 1 (bm25:[1]) | **AX-024 / 95ry** (open) — re-confirmed (fused-mode conjunctive-AND degradation) |
| onboarding (`bootstrap`) | cold empty-journal probe suggests only read/continuity tools, no first-run entry point | **AX-022 / xqjv** (open) — re-confirmed |
| schema (`market.bind`) | touch-ladder + noon-ladder markets carry full Binance rule in `description` but `resolution_rule.text` null | **AX-017 / n33z** (open) — re-confirmed on the intraday touch category |

### Verified-live (fixes holding on this fresh server)

- **AX-068 / AX-069** — `market.refresh` with `idempotency_key` on stale touch
  markets returned `ok:true` (event_id 789/790).
- **AX-027 / AX-031** — `snapshot.fetch` `price`==`mid` (not lastTrade), numeric
  `liquidity_depth_json` (no raw-payload dump) on all 3 noon-ladder binds.
- **AX-013** — skip with linked `forecast_id`; coach did not false-flag.
- **Intentional-design #4** — auto-score gate fired correctly on both resolutions.

## Fixes made

None (no new friction; contract-clean run).

## Beads filed

None (no new findings; all observations map to open beads — no duplicates filed).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests`: **All checks passed!**
- `mypy src`: **Success: no issues found in 178 source files**
- `pytest -q`: **2313 passed, 7 skipped** (115.58s)
- No source files changed this run — only docs (run report + registry). Gate run
  for honesty/safety before the docs push; unchanged from prior runs.

## Handoff notes for next run

- **Resolvable next run:** the **June-6 noon-ET "above $X" ladder** settles today
  at **16:00Z** — `mkt_Q-w3HIxGx4SgvobY` ($58k, fc_LHIDb7fc29SHqHK8), plus the
  run-18 $64k (`mkt_mMrI9fVsyQDM0xrQ`) and $66k (`mkt_DaGR4nGM18wtBnBC`)
  forecasts. After 16:00Z: `market.refresh` each, read snapshot `mid` (≈0.999⇒yes,
  ≈0.001⇒no), `resolution.add status=resolved_final confidence≥0.9`. With BTC
  ~$59–60k, $58k likely **yes** (my 0.93), $64k/$66k likely **no**.
- **Settlement cadence is now two-track:** noon-ET "above $X" strikes settle
  ~16:00Z **same day**; midnight-ET "reach/touch $X on <date>" dailies settle
  ~04:00Z **next day**. The hourly loop should forecast each one cycle and resolve
  it the next — this is the engine that keeps calibration N climbing.
- **Longer-horizon inventory still pending:** 2 FOMC forecasts (after June-17
  statement), the NHL Stanley Cup Final pair (settles ~late June), the BTC/ETH
  June month-window ladder (settles July 1).
- Report-surface + write-vein friction catalogs remain exhausted; remaining
  novelty is behavioral re-confirmation across settlement cycles and new market
  categories. This run's novelty was the **first touch-ladder settlement**.
- Open ax-dogfood beads unchanged: v5va, 1y9s, lszg, n33z, s7nn, scx8, xqjv, fe2f.
