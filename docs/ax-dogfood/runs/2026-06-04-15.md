# AX dogfood run 2026-06-04-15

- **RUN_ID:** 2026-06-04-1400
- **Branch:** ax-dogfood (rebase: up to date with origin/main, no-op)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Mode:** one bounded lifecycle pass

## Summary (as a bot)

Oriented from `report.bootstrap` → the same three `resolve_due_forecast`
obligations were flagged, all from `resolution_at_missing` (the known fe2f
pattern): one still-live market and two 2027-close recession markets — none
genuinely resolvable, so nothing was resolved (the run-13/14 crypto
point-in-time markets resolve at 16:00Z, ~2h after this pass). Scanned a fresh
category (Beijing daily-high-temperature ladder) and a fresh structure
(ETH "$2,500 in June" **month-window** touch market), recalled the directly
applicable touch-vs-window lesson, forecast the ETH market then skipped it
(efficiently-priced far-OTM tail, no edge), reviewed via `report.coach`
(clean), and retained + round-tripped a lesson. The pass surfaced **one new,
direct-fixable bug (AX-037)** — `market.bind` silently drops a caller-supplied
`resolution_rule_text` on the adapter path — which I fixed and gate-verified.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-037** | tool/adapter | **med** | `market.bind` adapter path (`bound_via=adapter`) silently discards a caller-supplied `resolution_rule_text` / `resolution_rule.text`: `_polymarket_identity_metadata` builds `resolution_rule.text` only from Gamma keys (`resolutionCriteria`/`rules`), which Polymarket leaves empty (rule prose is in `description`), so text=null — and the caller arg is never consulted. The **manual** path preserves it. Verified live: bind mkt 2416222 with `resolution_rule_text` (adapter) → `text:null`; same on 2416223 with `bound_via=manual` → text preserved, provenance `caller_supplied`. Makes the n33z workaround ("agent can supply the rule text") ineffective on the path a live bot uses. | **fixed** (e9892cd, +2 regression tests) |
| AX-018 (dup) | report | — | fe2f re-confirmed: 3 resolve_due obligations all `resolution_at_missing`; `mkt_pwuUfbLMcvhkia50`/`fc_CheQKZ` verified **live** via snapshot.fetch (mid 0.505, accepting_orders:true). Positive control held: new forecast w/ explicit `resolution_at` did NOT flag. | evidence appended to fe2f |
| AX-017 (dup) | tool/schema | — | n33z re-confirmed on a **month-window touch** market (`mkt_JU-hFNkB7qNd-y4Z`, ETH $2,500 in June): `resolution_rule.text` null, window-touch rule only in description. Cross-linked AX-037. | evidence appended to n33z |
| AX-020 (dup) | tool/error | — | isqo re-confirmed: `outcome.fetch` → `CONFIG_REQUIRED` (no Polygon RPC); used `snapshot.fetch` (Gamma) as the unsignposted alternative. | already filed (isqo) |
| AX-022 (dup) | onboarding | — | cold-start probe re-confirmed: empty-journal bootstrap `suggested_process_calls` = `[report.work_queue, agent.next_actions, report.recall_receipts, strategy.show]` — no first-run entry-point. | already filed (xqjv) |
| AX-013 / AX-027 / AX-031 (re-verify) | report/tool | — | `report.coach` did NOT false-flag the forecast+skip as unforecasted; `snapshot.fetch` `price` anchored to **mid** (0.0225, not lastTradePrice 0.024); `liquidity_depth_json` numeric (79363.42, no raw dump). | fixed (prior SHAs hold) |
| (obs) market.search past-close | tool | low | `market.search closed:false` returned the Beijing temp markets whose `close_at` (12:00Z) was ~2h past, mixed with future-close markets, all `closed:false`/`accepting_orders:true` (Gamma keeps a market "open" until settlement). `close_at` IS present per-candidate, so a careful bot can do the date math — soft. Not filed (data is present; filing would be backlog noise). | observation only |

## Fixes made

- **e9892cd** — `fix(market.bind): preserve caller resolution_rule_text on adapter path (AX-037)`. New `_apply_caller_resolution_rule` helper fills a null/blank venue `resolution_rule.text` from the caller's `resolution_rule_text` (or nested `resolution_rule.text`), marking provenance `caller_supplied`; venue text still wins; refresh path untouched (no caller args). +2 regression tests (`test_adapter_market_payload_has_null_rule_text_for_description_only_market`, `test_adapter_bind_preserves_caller_resolution_rule_text_when_venue_has_none`). **Effective next fresh MCP session** (long-lived server this session still shows old behavior; verified by unit test).

## Beads filed

- None new. AX-037 had no existing bead (n33z is the *retrieval* gap; AX-037 was the *write-drop*) and was small + non-firewall, so fixed directly.

## Beads updated (evidence appended)

- **trade-trace-fe2f** — run-1400 re-confirm + live snapshot proof on mkt_pwuUfbLMcvhkia50 + positive control.
- **trade-trace-n33z** — month-window-touch instance + AX-037 supply-path-fix cross-link (retrieval gap remains open).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2278 passed, 7 skipped** (perf opt-ins) in ~104s. (prev 2275 → 2278; +3 incl. this run's 2 new tests.)

## Journal artifacts this run

- Bound: `mkt_JU-hFNkB7qNd-y4Z` (ETH $2,500/June, adapter), `mkt_qZZOm8m6JrRs0Vt-` (Beijing ≤25°C, adapter probe), `mkt_7caqZzOA10PAJcDw` (Beijing 26°C, manual probe).
- Forecast: `fc_Dl0Xp2CQf2e1EhDu` (ETH $2,500/June, p=0.02, low conf, resolution_at 2026-07-01T04:00Z).
- Decision: `dec_M6DGwjWSCijcsQHW` (skip, insufficient_edge, links forecast_id). **No paper exposure opened.**
- Memory: `mem_1PUaxr__S_pr95y9` (AX-037 lesson), round-tripped via recall (rank 1).

## Handoff for next run

- AX-037 fix is **effective next fresh session** — a later run can verify live by binding a Polymarket market with `resolution_rule_text` on the adapter path and confirming `resolution_rule.text` is now populated (`provenance:caller_supplied`).
- Open follow-up worth a future probe: the **refresh** path (`market.refresh`, blocked by AX-016/4z2t) and a re-bind both re-fetch venue truth and would overwrite a caller-supplied `resolution_rule.text`; AX-037 deliberately scoped to bind-time only. If n33z is implemented (retrieval of venue rule text), this becomes moot.
- The ETH "$2,500 in June" forecast (`fc_Dl0Xp2CQf2e1EhDu`) resolves end-of-June; it carries an explicit `resolution_at` so it will not false-flag as resolve_due in the interim (fe2f positive control).
- Open ax-dogfood beads unchanged: 4z2t (P1), lszg/hv19/pr2j/95ry/isqo/n33z (P2), scx8/xqjv/fe2f (P3).
