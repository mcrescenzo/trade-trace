# AX dogfood run 2026-06-05-04

- **RUN_ID:** 2026-06-05-0300
- **Branch:** ax-dogfood (verified; up to date with origin/main, rebase clean)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Mode:** live-verify prior fixes + novel-surface probe + one direct fix (handoff: structural friction mined out; re-verify AX-047/048 land live; probe report.compare/playbook_adherence/policy_candidates)

## Summary (as a bot)

Oriented cleanly (`report.bootstrap` → work_queue). Standing state matched the
handoff exactly: 3 unresolvable `resolution_at_missing` obligations (fc_CheQKZ
live + US/UK recession 2027-close), `active_ideas` nuked by the char cap
(AX-038). Nothing newly resolvable at 03:00Z (June-5 ETH strikes settle 16:00Z;
BTC carries July-1; recession 2027). The two primary handoff tasks — **confirm
last session's AX-047 and AX-048 tool-code fixes actually land on a fresh MCP
server** — both passed live. Then probed the three cleanest unprobed report
surfaces; one (`report.compare`) carried a genuine schema/error-honesty bug,
fixed and gate-verified. Ran one faithful forecast loop (BTC $82,500 June touch)
that also re-checked the AX-047 invariant at the *low* [0,1] boundary.

Theme continues from run 03: the novel report surfaces keep yielding
output/schema-honesty defects. `report.compare` makes it four in a row
(opportunity, find_similar, calibration_advisory/mistakes-strengths, now
compare).

## Live verification of prior-session fixes (primary objective)

- **AX-047 — VERIFIED LIVE, both boundaries.** `report.calibration_advisory(0.97)`
  (clamp case): `suggested_probability=1.0`, `suggested_adjustment=0.03`,
  `calibration_gap=0.0467` (raw, preserved separately) → invariant
  `0.97 + 0.03 == 1.0` holds (was 1.0167 overshoot pre-fix).
  `report.calibration_advisory(0.03)` (no-clamp case): `suggested_probability=0.025`,
  `suggested_adjustment=-0.005 == calibration_gap` → `0.03 + (-0.005) == 0.025`.
  The invariant `probability + suggested_adjustment == suggested_probability`
  holds whether or not clamping occurs.
- **AX-048 — VERIFIED LIVE.** `report.mistakes` and `report.strengths` both now
  return `tag_count:0` / `groups:[]` with `summary.sample_size:5` in scope — the
  5 tagged-but-unscored (null-Brier) decision groups are correctly skipped from
  both, instead of double-listing under contradictory labels. Agrees with the
  sibling controls (`report.coach.top_*` `[]`, `mistake_tripwire` not fired).

## Friction found

| id | surface | severity | what |
|----|---------|----------|------|
| **AX-049** | report.compare (schema + error) | low (ergonomics/honesty) | Two coupled defects. (1) An unsupported `group_by` (e.g. `tag`) is rejected with a `VALIDATION_ERROR` that echoes only the offending value and lists no allowed set — the AX-004/005/032/035 self-documenting-error class. (2) The advertised `group_by` schema description listed `tag` as a "common value", but `tag` is in NEITHER `CALIBRATION_GROUP_SQL` nor `PNL_GROUP_SQL` (confirmed live: rejected for both base_reports), so a schema-following bot dead-ends — advertising-vs-runtime drift, the same `trade-trace-cs0r` class a prior fix already hit on this tool's `DOCUMENTED_GROUP_BY` set (the schema description was the copy that fix missed). **FIXED.** |

### New report surfaces probed this run (first time in registry) — behave as designed

- `report.policy_candidates` — `sample_size:0`, `no_policy_candidates`, fully caveated (`POLICY_CANDIDATE_READ_ONLY` etc.). Journal has no `policy_candidate`-tagged reflections; correct empty.
- `report.playbook_adherence` — `sample_size:0`, structured `predicate_audit` (all-zero counts) + correct caveats. The loop records `skip`/`forecast`, never playbook adherence rows; correct empty.
- `report.compare(calibration, instrument_id)` — works: 9 groups, full per-market Brier/log/ECE/sharpness + reliability bins + record_ids; well-formed (this is the positive control that isolated AX-049 to the `tag`/error-message path).

## Fixes made (gate-verified)

- **AX-049** — `5da4c4c` `fix(report): report.compare lists allowed group_by on rejection + drops bogus tag from schema`
  - `compare.py:98,150` → both rejection `ValueError`s append `allowed group_by values are <sorted per-base allowlist>`.
  - `tool_schemas.py:343` → `group_by` description now enumerates the real per-`base_report` allowlists and states explicitly that `tag` is unsupported.
  - `tests/allowlists/mhy1_legacy_grep_allowlist.json` → added `venue_id` to the existing reviewed `tool_schemas.py` entry (the schema text now legitimately names `venue_id` as a valid group_by; this is the mhy1 guard's intended review mechanism, not a weakening).
  - +2 regression tests: error message lists the allowed set for both base_reports (and still echoes the offending value); the advertised schema names no group_by outside the runtime allowlist and corrects the prior `tag` claim.

Tool-code + schema-text change → **effective next fresh session** (this session's live MCP still showed the old behavior; verified via tests, not live this run).

## Beads filed

None. AX-049 was a small, in-scope, non-firewall schema/error-output honesty fix (same class as AX-004/005/030/032/035/041/042/043) — fixed directly per playbook.

## Attempted-then-reverted

None.

## Regression watch (standing items re-confirmed live, no change)

- **AX-039 / 1y9s:** `report.work_queue` SUCCEEDED at N=3 obligations (curve fail@13 / ok@5 / ok@3 ×9).
- **AX-019 / AX-035:** `market.search` returned 8 live BTC candidates; non-empty result (no silent no-op).
- **AX-037:** adapter `market.bind` (BTC $82.5k `mkt_HtqukmoKiJY4Ly71`) preserved caller `resolution_rule_text` (`provenance:caller_supplied`).
- **AX-027:** `snapshot.fetch` price=mid 0.0265 (not lastTradePrice 0.03).
- **AX-031:** numeric `liquidity_depth_json` (33076.88), no raw dump.
- **AX-013:** skip linked `forecast_id`.
- **AX-018 / fe2f positive control:** new forecast `fc_o0yrpd3gip8HhDMH` w/ explicit `resolution_at:2026-07-01` did NOT flag; the 3 `resolution_at_missing` stay flagged (exactly 3).
- **AX-024 / 95ry:** multi-word natural-language recall ("report.compare group_by allowed values calibration pnl") → all hits at RRF floor ~0.03, no bm25 provenance.
- **AX-022 / xqjv:** cold throwaway bootstrap → read/continuity tools only (work_queue/agent.next_actions/recall_receipts/strategy.show), no first-run entry point, 0 obligations.
- **AX-038 / dlup:** bootstrap `active_ideas` nuked 0/60 `max_chars` while `obligations` survived 3/3 at small N.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2287 passed, 7 skipped** (perf opt-in) in ~135s (+2 new regression tests vs prior 2285). First run surfaced 1 failure (`test_mhy1_legacy_grep_allowlist`) caused by my own new `venue_id` in the schema text → resolved by the reviewed allowlist entry above; re-run fully green.

## Lifecycle writes this run

- bound `mkt_HtqukmoKiJY4Ly71` (BTC $82,500 June touch); snapshot `snp_ZeXZCWUCBStxxgNc`
- forecast `fc_o0yrpd3gip8HhDMH` (p_yes 0.03, conf low, resolution_at 2026-07-01T04:00Z, resolution_rule_text supplied)
- skip `dec_h5xF9w5AtJmPD5sE` (insufficient_edge, forecast linked, tags btc-touch/far-otm)
- observation memory `mem_czvBptmNi4w6LCfe` (AX-049 + AX-047/048-live lesson)

## Handoff for next run

- **AX-049 lands next fresh session** — re-call `report.compare` with `group_by=tag` (calibration AND pnl) and confirm the `VALIDATION_ERROR` now lists the allowed set; check `tool.schema report.compare` no longer advertises `tag`.
- **AX-047 + AX-048 now verified live** (this run) — they can drop from the active re-verify list; treat as settled `fixed`.
- **Still no resolvable inventory** until: ETH-$1800 / other June-5 strikes (16:00Z settle — a *next* fire after 16:00Z may finally have something to resolve), FOMC June (2026-06-17), BTC carries (2026-07-01). Do NOT force-resolve the 3 `resolution_at_missing` standing forecasts (recession 2027-close + fc_CheQKZ live).
- **Report-surface vein is now essentially fully mined.** Every standalone report surface in the catalog has been probed at least once across runs 01–04. Remaining genuinely-unprobed: `report.calibration_integrity`, `report.process_quality` (obs run 20), `report.audit_readiness` (fixed AX-041), `report.resolution_misreads` (obs run 22). Most now need *specific journal state* to exercise (e.g. policy_candidates needs a `policy_candidate` reflection; playbook_adherence needs `playbook.upsert` + adherence rows) — a next run could deliberately seed that state to probe the populated path.
- **Highest-leverage open backlog (unchanged):** lszg / hv19 / 1y9s (token-cap pagination cluster), 4z2t (P1 `market.refresh` unusable).
