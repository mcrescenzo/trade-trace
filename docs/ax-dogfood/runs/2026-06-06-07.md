# AX dogfood run — 2026-06-06-07

- **RUN_ID:** 2026-06-06-0600
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** Standing-confirmation run — **NO new friction**. Genuine breadth
  first: the loop's **first full forecast loop on a real-world organizational /
  political ELECTION** — the **2026 Real Madrid presidential election** (vote
  June 7, market closes 2026-06-08). Structurally distinct from prior
  election-adjacent binds: subjective/conditional markets (Trump-insult, OpenAI
  IPO) and the geographic Trump–Putin grid were not "candidate wins the office"
  elections; this is. ~12 standing items re-confirmed live; nothing resolvable
  this cycle.

## Summary (as a bot)

Bootstrap oriented cleanly (contract `bootstrap.v0`, schema v29).
`report.work_queue`/bootstrap surfaced **3** `resolve_due_forecast` obligations —
all the standing **AX-018/fe2f** `resolution_at_missing` false-positives
(Rihanna/GTA `fc_CheQKZ`, UK-recession `fc_I0VaLn1`, US-recession `fc_uywJQC8`;
close 2026-07-31 / 2027). `report.unscored_forecasts` returned **count:0** — the
clean close-aware counter-surface confirming nothing is genuinely past
`resolution_at` yet. Fabricated no resolutions. (This run fired at 06:01Z; the
June-6 noon-ET "above $X" ladder settles 16:00Z today, ~10h out — not yet due.)

With nothing to resolve, scanned live markets for a fresh category. `temperature`
and `NBA` matched no live market (clean single-term `search_hint` — AX-035). The
`election` search surfaced the **2026 Real Madrid presidential election** — a
genuinely new category: a real-world organizational election with a near-term
(2-day) horizon, so it can actually settle in a future loop. Bound two legs,
snapshot-fetched, forecast both at mid (coherent pair, no edge), skipped both,
reviewed calibration/coach, retained + round-tripped the lesson, ran the
cold-start probe. Friction-free: every surface behaved as designed or as an
already-filed standing item.

### New forecasts (scan & forecast) — organizational election

| market | mkt_id | YES mid | fc_id | forecast | decision |
|---|---|---|---|---|---|
| **Florentino Pérez** win 2026 Real Madrid presidency? | mkt_2JyUlhTJWtADIOH0 | **0.965** | fc_6oac5wDvKvBN9e0o | YES 0.965 (at mid) | **skip** insufficient_edge (dec_CfJtjeeZPmDVydoW) |
| **Enrique Riquelme** win 2026 Real Madrid presidency? | mkt_jnjx8xc1DkIAgdGJ | **0.0355** | fc_qBq-mC4eU8KSAfSY | YES 0.0355 (at mid) | **skip** insufficient_edge (dec_YAR36hgsqQ5FS41-) |

- **Structure (new category):** a candidate-wins-the-office election as a negRisk
  grid (shared `negRiskMarketID 0xfd02…07200`, `negRisk:true`). Only **two**
  candidate legs are active (Pérez, Riquelme); the other listed legs
  (Candidate A/B, "someone else") are `active:false`, so this is effectively a
  coherent **complementary pair** — the head-to-head/organizational analogue of
  the NHL two-team pair and the crypto strike ladder.
- **Coherence cross-check:** Pérez YES 0.965 + Riquelme YES 0.0355 = **1.0005**
  (~0.05% vig). Internally coherent and reasonably deep
  ($25.7k / $28.8k vol, spreads 0.024 / 0.023, liq $7.5k / $11.4k).
- **No edge → forecast at mid.** Florentino Pérez is the long-dominant incumbent
  (Real Madrid president since 2009, repeatedly re-elected, often unopposed); no
  private information contradicts the deep coherent market ⇒ genuine edge ~0.
  Both anchored to their snapshots (`snp_B5AHhLy8G5KMNy83` / `snp_rm6D49a-10b8kvT9`),
  `resolution_at`=2026-06-08T00:00Z.
- **SKIPPED both** with `insufficient_edge` (forecast-linked): the incumbent leg
  is deep-ITM (risk ~0.965 to make ~0.035, poor R/R) and the challenger is a
  deep-OTM tail; `sports_fees_v2` (3% taker) swamps any thin edge. paper_enter
  not forced.
- **Calibration self-check:** Pérez 0.965 lands in well-calibrated **bin 9**
  (0.95–0.99, obs 1.0, gap −0.029); Riquelme 0.0355 in **bin 2** (0.035–0.1, obs
  0, gap +0.057) — both extreme well-calibrated bins, deliberately **not** the
  standing overconfident-YES bands (bin 7 0.56–0.85 gap +0.66; bins 3/4).
  Forecasting at mid on a coherent grid with no edge is the disciplined move.

### Calibration

- N **35** (unchanged — both new forecasts are pending; this election settles
  ~June 7–8), Brier **0.1025**, skill **0.498**, ECE 0.154, `sample_warning` null.
- Standing weakness re-confirmed: **bin 7** (0.56–0.85, mean 0.66, n=4, obs_freq
  **0**, gap +0.66), bin 4 (0.20–0.29, obs 0), bin 3 (0.10–0.17, obs 0) —
  overconfident near-money/mid YES. Well-calibrated bands: bin 6 (0.49–0.50, obs
  0.67), bins 8/9 (0.9+, obs 1.0), and the deep extreme bins 0/1/2 where this
  run's forecasts sit.
- `report.coach`: `top_mistakes`/`top_strengths` **[]** (AX-048),
  `unscored_forecasts` count **0** (both new skips forecast-linked — AX-013 holds,
  no false-flag), no stale watches. Integrity diagnostics clean
  (unsupported/ambiguous/disputed/void all 0%); `abstention_coverage` 2/105
  (1.9%) with the callout still undercounting the ~34 `decision.add(skip)` rows —
  **s7nn** standing.

## Friction found

**None new.** Standing-confirmation run; every observation maps to an
already-open bead or already-fixed item:

| surface | observation | disposition |
|---|---|---|
| report (`work_queue`) | 3 resolve_due, all `resolution_at_missing` (Rihanna/GTA + US/UK recession, close 2027) | **AX-018 / fe2f** (open) — re-confirmed; `report.unscored_forecasts` count:0 is the clean counter-surface |
| schema (`market.bind`) | both election legs carry full rule (winner by credible-reporting consensus, official Real Madrid sources fallback, "resolve to Other if undetermined") in `description` but `resolution_rule.text` **null**; source description even has a "June 31" typo | **AX-017 / n33z** (open) — re-confirmed on a new category (organizational election) |
| tool (`memory.recall`) | fused multi-word query w/ verbatim unique token `CAPYBARA-RMADRID-J6-0600` ranked none of the just-written node (top-5 older temporal/graph at RRF floor ~0.030, no bm25 provenance); bm25-only → rank 1 (bm25:[1]) | **AX-024 / 95ry** (open) — re-confirmed (fused-mode conjunctive-AND degradation) |
| report (`bootstrap`) | `active_ideas` nuked 0/100 `max_chars` while obligations survived 3/3 | **AX-038 / dlup** (open) — re-confirmed |
| onboarding (`bootstrap`) | cold empty-journal probe (schema v29) suggests only read/continuity tools, 0 obligations, no first-run entry point | **AX-022 / xqjv** (open) — re-confirmed |
| report (integrity) | `abstention_coverage` 2/105 counts only `abstention.record`, not the ~34 forecast-linked skips | **s7nn** (open) — re-confirmed |

### Verified-live (fixes holding on this fresh server)

- **AX-064** — `market.bind` advertises the source/state/mechanism enums; both
  binds used them and returned `bound_via:adapter`.
- **AX-027 / AX-031** — `snapshot.fetch` `price`==`mid` (Pérez 0.965 not lastTrade
  0.959; Riquelme 0.0355 not lastTrade 0.095), numeric `liquidity_depth_json`
  (7457.97 / 11376.94, no raw-payload dump) on both binds.
- **AX-037** — caller-supplied `resolution_rule_text` preserved on both adapter
  binds.
- **AX-013** — both skips with linked `forecast_id`; coach `unscored_forecasts` 0.
- **AX-035** — single-term zero-result searches (`temperature`, `NBA`) returned a
  distinct `search_hint` (search ran, term matched no live market).

## Fixes made

None (no new friction; contract-clean run).

## Beads filed

None (no new findings; the election-category `resolution_rule.text` null is a new
instance of the open **n33z** — captured in the registry row, no duplicate filed).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests`: **All checks passed!**
- `mypy src`: **Success: no issues found in 178 source files**
- `pytest -q`: **2313 passed, 7 skipped** (116.11s)
- No source files changed this run — only docs (run report + registry). Gate run
  for honesty/safety before the docs push; unchanged from prior runs.

## Handoff notes for next run

- **Resolvable next run(s):**
  - The **June-6 noon-ET "above $X" ladder** still settles at **16:00Z today** —
    run-05's $58k (`mkt_Q-w3HIxGx4SgvobY`, fc_LHIDb7fc29SHqHK8, YES 0.93) plus
    run-18's $64k (`mkt_mMrI9fVsyQDM0xrQ`) and $66k (`mkt_DaGR4nGM18wtBnBC`).
    After 16:00Z: `market.refresh` each, read snapshot `mid` (≈0.999⇒yes,
    ≈0.001⇒no), `resolution.add status=resolved_final confidence≥0.9`. With BTC
    ~$59–60k, $58k likely **yes**, $64k/$66k likely **no**.
  - **NEW — the Real Madrid election** (`fc_6oac5wDvKvBN9e0o` Pérez 0.965 /
    `fc_qBq-mC4eU8KSAfSY` Riquelme 0.0355, both `resolution_at`=2026-06-08)
    settles **~June 7–8**. The first run after the vote should `market.refresh`
    both legs, read snapshot mid (winner ~0.99 / loser ~0.01), `resolution.add`
    binary label confidence≥0.9. Near-term horizon (unlike the July-1
    crypto / June-30 grid / late-June NHL pending forecasts) → climbs calibration
    N past 35 within a day or two.
- **Two-track crypto settlement cadence** (unchanged): noon-ET "above $X" strikes
  settle ~16:00Z same day; midnight-ET "reach/touch" dailies ~04:00Z next day.
  Forecast one cycle, resolve the next.
- **Longer-horizon pending:** 2 FOMC (after June-17), NHL Stanley Cup Final pair
  (~late June), Trump–Putin geographic grid (June 30), BTC/ETH June month-window
  ladders (July 1), Real Madrid election (June 7–8).
- **Novelty budget:** report-surface + write-vein friction catalogs remain
  exhausted; remaining novelty is behavioral re-confirmation across settlement
  cycles and new market *categories*. This run's novelty was the **first
  organizational/political election** (a candidate-wins-the-office negRisk grid),
  reinforcing n33z's value across yet another rule space.
- Open ax-dogfood beads unchanged: v5va, 1y9s, lszg, n33z, s7nn, scx8, xqjv, fe2f.
