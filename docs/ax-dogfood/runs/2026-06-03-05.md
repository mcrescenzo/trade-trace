# AX dogfood run 2026-06-03-05

- **RUN_ID:** 2026-06-03-2200
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · **Journal home:** ~/.trade-trace-axloop (schema_v29, pkg 0.0.2)
- **Adapter:** polymarket enabled, gamma_base_url set, polygon_rpc_url **unset**

## Summary (as a bot)

Drove a clean end-to-end lifecycle pass on live Polymarket Bitcoin markets:
bootstrap → resolve-check → discover (`market.search`) → bind → snapshot →
recall → forecast → decide → review → reflect → recall round-trip. The loop is
feeling solid — prior-run fixes have visibly landed (search query routing, NO-side
PnL marking, skip-with-forecast coaching), and memory recall surfaced this run's
own new lesson as the top hit. The single sharp edge this run was `market.bind`:
following the AGENT_GUIDE's own canonical bind example (namespaced `external_id`
+ bare `gamma_market_id`) returns a 422, because the first-bind Gamma lookup was
built from `external_id` — the one path the AX-009 fix missed. Fixed directly.

## What I exercised

- `journal.status`, `report.bootstrap`, `report.work_queue` — orientation.
- `market.search query=bitcoin` — live discovery (validated AX-019 fix).
- `market.bind` ×2 (BTC→$70k `mkt_7tACn4aqhw6y8BwN`, BTC→$90k `mkt_Weg9U9Ul6g19DJuO`).
- `snapshot.fetch` ×3 (incl. the due-forecast market `mkt_pwuUfbLMcvhkia50`).
- `forecast.add` ×2 (`fc_9yWSj-d2qus9Jt3Z` @0.57 vs mkt 0.545; `fc_NJ0KvNsnPkZwH4sy` @0.012 vs mkt 0.0105).
- `decision.add` paper_enter (YES 100 @0.54 → `pos_EmNSXb2bDMWpFdub`) + skip (forecast-linked).
- `report.open_positions`, `report.coach` — review.
- `memory.reflect` (`mem_4MIojgkFHXh8GPHC`) + `memory.recall` round-trip.
- Cold-start probe: throwaway journal init + bootstrap + adapter-disabled error, then destroyed.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| FR-1 | tool/schema | **high** | `market.bind` (a) hits the Gamma network in adapter mode (`bound_via:adapter`) despite its schema saying "Manual/local only: no network", and (b) builds the first-bind `/markets/{id}` URL from `external_id`, ignoring the caller's `gamma_market_id` → the AGENT_GUIDE's own bind example (`external_id:"polymarket:<id>"` + bare `gamma_market_id`) 422s. AX-009 class, on the path that fix missed. | **fixed** (`f2313e1`) |
| FR-2 | report/onboarding | low | Cold `report.bootstrap` on an empty journal surfaces only read/continuity `suggested_process_calls` (work_queue, next_actions, recall_receipts, strategy.show) — no first-run entry-point hint (`market.search`→bind→forecast). A bot driving "bootstrap-first" hits empty queues with no forward action. | **filed** (`trade-trace-xqjv`) |

### Live regression confirmations (prior fixes verified, not new friction)

- **AX-019** (`market.search` query routing): `query=bitcoin` returns bitcoin-specific markets, not the old generic default list. ✔ live.
- **AX-008 / ctvb** (NO-side PnL marking): US-Iran NO position (entry 0.875, YES mark 0.12 → NO 0.88) reports **+$0.50**, not the old phantom +$75.50. ✔ live.
- **AX-013 / t9n5** (skip links forecast_id): `report.coach` `top_mistakes` empty; the forecast-linked skip is not false-flagged as unforecasted. ✔ live.
- **AX-018 / fe2f** (false `resolve_due_forecast`): `fc_CheQKZ` on `mkt_pwuUfbLMcvhkia50` is still flagged `resolve_due` while the market is live (mid 0.505, accepting_orders:true). Re-confirmed; left forecast open (did NOT fabricate a resolution).

## Resolution determination

The one `resolve_due_forecast` obligation (`fc_CheQKZ`) is the AX-018 false positive:
`snapshot.fetch` shows the market still actively trading (mid 0.505, tradable:true,
accepting_orders:true). No genuine resolution to record — forecast left open, per
the playbook's "do not invent an outcome" rule.

## Fixes made

- **`f2313e1`** — `fix(adapter): market.bind first-bind lookup uses gamma_market_id, not external_id` (FR-1). Touches `adapter_polymarket.py` (bind path prefers `gamma_market_id`), `market_bind.py` (corrected description + module docstring to disclose the dual-mode Gamma enrich), `test_market_fetch.py` (new regression: lookup uses bare id, persists namespaced external_id), `test_market_bind.py` (updated the catalog-description assertion that pinned the now-false "no network" text).

## Beads filed

- **`trade-trace-xqjv`** (P3, task, ax-dogfood) — FR-2 cold-bootstrap entry-point design question.

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests`: **All checks passed!**
- `mypy src`: **Success: no issues found in 178 source files**
- `pytest -q`: **2267 passed, 7 skipped** (skips are perf-baseline opt-ins).

## Handoff notes for next run

- FR-1 fix is committed + unit-verified; it is **effective next fresh session** (a
  new MCP server boot). The live MCP this session already reflected it because the
  session booted a fresh server — `market.bind` with bare numeric `external_id`
  worked, and the regression test pins the namespaced-id path.
- `polygon_rpc_url` remains unset → `outcome.fetch` will fail closed; for any market
  that genuinely closes, use `snapshot.fetch` (Gamma) for outcome evidence. This is
  the open `trade-trace-isqo` design question.
- Open forecasts now include 2 new BTC June touch-markets resolving 2026-07-01, plus
  `fc_rHrzE9` (US-Iran $70k-class) resolving 2026-06-04T00:30Z — may be resolvable next run.
- Open ax-dogfood queue after this run: 4z2t (P1), isqo, n33z, fe2f, **xqjv** (new).
