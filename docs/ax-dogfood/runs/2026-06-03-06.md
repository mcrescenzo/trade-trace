# AX dogfood run 2026-06-03-06

- **RUN_ID:** 2026-06-03-2300
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · **Journal:** ~/.trade-trace-axloop · schema_v29, pkg 0.0.2
- **Adapter:** polymarket enabled, gamma_base_url=true, polygon_rpc_url=**false** (no RPC)

## Summary (as a bot)

Drove the canonical loop end to end through the MCP surface only:
bootstrap → work_queue/lifecycle → resolve-due check → market.search →
market.bind → snapshot.fetch → memory.recall → forecast.add → decision.add →
calibration/coach → memory.retain/recall → cold-start probe. The loop is
coherent and the prior fixes hold up. Two prior fixes were **verified live this
fresh session** (market.search query routing, market.bind namespaced
external_id), and two **new** friction items surfaced — one a clear bug fixed
this run, one a recall-relevance defect filed for design.

Resolution discipline: the one `resolve_due_forecast` obligation
(`fc_CheQKZ`, `mkt_pwuUfbLMcvhkia50`) is the known AX-018 false flag — the
market is still live (snapshot mid 0.505, accepting_orders:true), so no outcome
was recorded. The soon-due `fc_rHrzE9` market is also still live (mid 0.365).
No outcomes fabricated.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| AX-023 | tool/error | medium | `market.bind` adapter path (`bound_via=adapter`) created only the `markets` row, not the compatibility `instruments` row, so `forecast.add`/`decision.add` on the freshly-returned `market_id` (which the bind docstring promises as a prerequisite) failed `NOT_FOUND "instrument_id not found"` until a `snapshot.fetch` lazily materialized it. Misleading error; contradicts docstring. | **fixed** (9b05e73) |
| AX-024 | tool | medium | `memory.recall` BM25/FTS5 multi-word queries are conjunctive (implicit AND): every token must co-occur in one node or BM25 returns nothing. Dotted/underscored tokens (`forecast.add`, `NOT_FOUND`) split oddly. Fused mode masks it via temporal/graph, so natural-language recall silently degrades to recency-only; LIKE fallback only fires on malformed MATCH, never on zero-result. | **filed** (trade-trace-95ry, P2) |

### Re-confirmed (already tracked — not re-filed)
- **AX-018 / fe2f** — `resolve_due_forecast` flagged on still-live `fc_CheQKZ` (mid 0.505, accepting_orders:true). Re-confirmed live.
- **AX-017 / n33z** — structured `resolution_rule.text` is `null`; rule text is only inside the raw `venue_metadata_json.description` blob. Re-confirmed on the Fed-25bps market bind.
- **AX-022 / xqjv** — cold `report.bootstrap` on an empty throwaway journal still surfaces only read/continuity `suggested_process_calls`, no first-run entry-point. Re-confirmed via cold-start probe (`fts5_available:true` in journal.init).

### Verified-fixed-live (prior runs' fixes, confirmed working in this fresh session)
- **AX-019** (782d4ec) — `market.search query="fed rate decision june"` returned relevant Fed markets (not a default list).
- **AX-021** (f2313e1) — `market.bind` with namespaced `external_id="polymarket:906973"` + bare `gamma_market_id="906973"` on the adapter path bound cleanly (`bound_via:adapter`), no 422.

## Lifecycle exercised this run

- Bound `mkt_M4lkGA2bEtaMx5jB` (Polymarket 906973, "Fed -25bps after June 2026 meeting", negRisk bracket).
- `forecast.add` → `fc_yetUV6UW2bD9gkZc` (P(yes)=0.02 vs market YES ~0.0085; thesis `th_OuPZ5MKU9qRn6uDE`).
- `decision.add` skip → `dec_idTMcUOTSLKVCFzs` (insufficient_edge; linked forecast_id per AX-013).
- `memory.retain` → `mem_Od0snvL0l1OyazO1` (the AX-023 ordering lesson); recall round-trip confirmed (and itself surfaced the AX-024 BM25 behavior).
- `report.calibration`: N=0 scored (all 10 open forecasts are future-dated; structurally below the N≥20 floor). `report.coach`: actionable hygiene next-actions (reflection coverage, "don't infer skill below floor").

## Fixes made

- **9b05e73** `fix(adapter): materialize instrument row at adapter bind time so forecast.add works without a prior snapshot` — calls the existing idempotent `_ensure_market_instrument` in `_upsert_market` after the market write. Regression test `test_adapter_bound_market_is_immediately_forecastable` (verified to fail with exact `NOT_FOUND "instrument_id not found"` when the fix is reverted, pass with it).

## Beads filed

- **trade-trace-95ry** (bug, P2, ax-dogfood) — AX-024 BM25 conjunctive recall.

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` — **All checks passed!**
- `mypy src` — **Success: no issues found in 178 source files**
- `pytest -q` — **2268 passed, 7 skipped** in ~103s (7 skipped = perf-baseline opt-ins).

## Handoff notes

- AX-023 fix is committed but the **live MCP server for this session still has
  the pre-fix behavior** (long-lived process); it takes effect on the next fresh
  session / cron fire. Verified via unit test only this run.
- AX-024 (trade-trace-95ry) is the highest-value open finding: it quietly
  weakens the core "recall prior lessons" capability. Needs a relevance decision
  (OR-combine-with-bm25-rank vs document keyword-only) — touches core recall
  semantics, so design review before any code change.
- Open `resolve_due_forecast` on `fc_CheQKZ` will keep showing until AX-018/fe2f
  is resolved or the underlying market closes; do not fabricate an outcome.
- `polygon_rpc_url` remains unset, so `outcome.fetch` fails closed (AX-020/isqo);
  use `snapshot.fetch` (Gamma) for resolution evidence when markets close.
