# AX dogfood run 2026-06-04-17

- **RUN_ID:** 2026-06-04-1600
- **Branch:** ax-dogfood (verified; never touched main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Phase A sync:** `git rebase origin/main` → already up to date.

## Summary (as a bot)

This was the first run to **exercise the full resolution → auto-score →
calibration loop at volume**. The point-in-time crypto strike forecasts that
prior runs (13–16) deliberately seeded with `resolution_at=16:00Z` came due this
run, so I resolved them and watched the calibration sample finally grow. Net: I
**resolved 6 markets and auto-scored 8 forecasts**, lifting `report.calibration`
from a near-empty N to **N=9** with real reliability bins and a genuinely useful
lesson (near-the-money strikes are where I add no edge).

Getting there surfaced the **headline new friction (AX-039)**: orienting via
`report.bootstrap` worked, but its `obligations` section was nuked to
`returned_count:0` (char cap), and its #1 suggested follow-up call —
`report.work_queue` — **failed at the MCP transport layer (~60.5KB, over the
token cap)**, as did its documented "safe alias" `agent.next_actions`. The single
most important orientation path is unrecoverable on a populated journal. This is
the same envelope-bloat disease as AX-029/AX-034, now on the primary surface, and
it disproves a stated assumption in `trade-trace-hv19` (that work_queue is
paginated — it is not).

The resolution path itself worked but re-confirmed two known frictions: the
AX-030 schema still omits `confidence`, so my first (schema-faithful)
`resolution.add` wrote a non-scoring resolution — **the live AX-030 fix's
`auto_score_skipped_reason` caught it and told me exactly how to recover** (retry
with explicit confidence, which scored both forecasts). And the markets were
still `accepting_orders:true` past their settle moment with no `winningOutcome`
on the snapshot surface (AX-020/isqo), so I ground-truthed BTC/ETH noon-ET prices
by web before recording, with per-strike calibrated confidence.

No code shipped this run — the headline finding is design-level (filed), and
forcing a hacky envelope edit would violate the firewall + AX-029/AX-034
precedent. The run's value is the calibration-loop exercise + the new finding.

## What I exercised

- **Resolved (6 markets, web-ground-truthed):** BTC noon-ET 2026-06-04 ≈ $63.6k,
  ETH ≈ $1,778 (confirmed via web; corroborated by the markets' own internally
  consistent pre-settle ladder). Per-strike calibrated confidence:
  - BTC>$60k → **yes** (conf 0.97) · BTC>$62k → **yes** (0.93) · BTC>$64k →
    **no** (0.95) · BTC>$70k → **no** (0.99)
  - ETH>$1700 → **yes** (0.96) · ETH>$1800 → **no** (0.92)
- **Auto-scored 8 forecasts** (Brier): fc_LdbfX 0.0001, fc_0K8m09 0.0064,
  fc_ton8d 0.000025, fc_zo3Tqu 0.0025, fc__FHwOm 0.09, fc_uhCM0i 0.1369,
  fc_GE5gU0 0.2601, fc_7p8h6j 0.3249.
- **Left open (honest non-resolution):** mkt_BxqIYenOQjh6ta1l (mid 0.616, wide
  spread 0.54/0.69 — genuinely uncertain) and mkt_SohHCLs3qtCWRWbT (strike
  identity not documented in prior run notes; snapshot identical to ETH>$1800 but
  not confirmed — did not resolve on inference).
- **Calibration review:** N=9, Brier 0.135, skill +0.452 vs baseline, ECE 0.28 —
  driven entirely by the 0.37–0.57 near-the-money band; tails well-calibrated.
- **Memory:** retained the near-the-money calibration lesson (mem_a5EmXTU);
  recall round-tripped (fused mode returned the sibling point-in-time reflections).

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-039** | tool (report.work_queue + agent.next_actions) | medium-high | `report.bootstrap`'s #1 suggested call `report.work_queue` (and its alias `agent.next_actions`) exceeds the MCP token cap (~60.5KB / 13 obligations); a `kind` filter does NOT shrink it (60.8KB — per-item bloat, not count). Root cause: every obligation serialized **3×** (`groups[]` — each also re-echoing the full `filter_view` — + `work_queue` + `next_actions`), no `limit`/`cursor`, `truncated:False` hardcoded. The primary orientation path is unrecoverable on a populated journal. Disproves hv19's premise that work_queue is paginated. | **filed** trade-trace-1y9s (P2); cross-linked hv19 + lszg |
| AX-038 / dlup (strengthened) | report (bootstrap) | medium | The per-section char cap nukes more than `active_ideas`: same bootstrap call also emptied the **obligations** section (`returned_count:0`, `available_count:13`, `reason:max_chars`). A bootstrap-first bot sees zero obligations AND zero ideas at orientation. | evidence appended to trade-trace-dlup |
| AX-030 (re-confirm) | tool/schema | (filed/working) | MCP-advertised `resolution.add` schema still omits `confidence`; a schema-faithful first call wrote a non-scoring `resolved_final` (out_D-Ae). The **live AX-030 fix worked**: `auto_score_skipped_reason` named the cause + recovery; retry with confidence auto-scored both forecasts. Wart: recovery leaves a duplicate dead outcome row. | AX-030 fix verified live; behavior intentional (skip-reason is the remediation) |
| AX-020 / isqo (re-confirm) | tool/error | (filed) | Point-in-time markets stay `accepting_orders:true` past their settle moment and `snapshot.fetch` exposes no `winningOutcome`; the surface alone can't confirm a market settled, so resolution needs out-of-band (web) evidence. | re-confirmed; evidence path is the isqo gap |
| AX-018 / fe2f (re-confirm) | report | (filed) | bootstrap/work_queue flagged the 3 `resolution_at_missing` forecasts (fc_CheQKZ live + US/UK recession) as resolve_due; this run's 8 newly-scored forecasts had explicit `resolution_at` and resolved cleanly. | evidence appended to trade-trace-fe2f |
| AX-022 / xqjv (re-confirm) | onboarding | (filed) | Cold throwaway-journal bootstrap surfaced only read/continuity `suggested_process_calls` (work_queue/next_actions/recall_receipts/strategy.show), and its #1 (work_queue) is the very call that overflows on a populated journal (AX-039). | re-confirmed (cold probe) |

## Fixes made (atomic commits)

- **None.** The headline finding (AX-039) is design-level (pagination + envelope
  de-serialization across reports), filed not hot-fixed, consistent with the
  hv19/lszg precedent and the contract firewall. ruff + mypy + pytest remained
  green (no code touched).

## Beads filed

- **trade-trace-1y9s** (P2, task, ax-dogfood) — `report.work_queue` +
  `agent.next_actions` exceed the MCP token cap: no `limit`/`cursor`, triple
  serialization, the #1 bootstrap-suggested orientation call is unrecoverable.
  Cross-linked to the sibling token-cap beads **trade-trace-hv19** (AX-029,
  report.lifecycle) and **trade-trace-lszg** (AX-034, current_exposure +
  open_positions); dep-linked to hv19 (shared fix).

## Beads updated (evidence appended)

- **trade-trace-dlup** — char cap also nukes the `obligations` section to 0, not
  just `active_ideas` (broadens the substantive harm).
- **trade-trace-hv19** — corrected its false premise that work_queue is paginated;
  noted 1y9s as the third sibling.
- **trade-trace-lszg** — noted 1y9s as the third sibling in the token-cap class.

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2279 passed, 7 skipped** (unchanged; no code touched this run).

## Handoff notes for the next run

- **Calibration sample is now N=9** (was near-empty). The remaining unscored
  forecasts are the 3 `resolution_at_missing` ones (fc_CheQKZ live, US/UK
  recession 2027-close — not resolvable) plus the 2 left-open markets above. To
  push N toward the ≥20 floor, keep seeding **point-in-time** forecasts with
  explicit `resolution_at` and resolve them the next run — that pattern works
  cleanly end-to-end.
- **mkt_BxqIYenOQjh6ta1l** (mid 0.616) and **mkt_SohHCLs3qtCWRWbT** (unconfirmed
  strike) were deliberately left open — resolve only if a future run confirms the
  outcome with real evidence; do not infer.
- **AX-039 (1y9s)** is the strongest argument yet for the cross-report pagination
  rework (hv19 + lszg + 1y9s): a bot literally cannot follow bootstrap's own
  suggested orientation sequence on a populated journal. Good first PR candidate
  whenever the human wants to land the envelope rework.
- When resolving point-in-time markets, remember the AX-030 dance: pass
  `confidence` explicitly (the schema hides it) to avoid the dead first-write.
