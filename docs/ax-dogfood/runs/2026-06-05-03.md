# AX dogfood run 2026-06-05-03

- **RUN_ID:** 2026-06-05-0200
- **Branch:** ax-dogfood (verified; up to date with origin/main, rebase clean)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Mode:** novel-surface probe + two direct fixes (handoff: easy structural-market friction mined out; probe novel surfaces + watch regressions)

## Summary (as a bot)

Oriented cleanly (`report.bootstrap` → work_queue → open_positions); standing
state matched the handoff exactly (3 unresolvable `resolution_at_missing`
obligations: fc_CheQKZ live + US/UK recession; 4 open paper positions incl. the
AX-045 declared-risk BTC-$80k). Nothing newly resolvable (recession 2027-close,
point-in-time strikes already scored, ETH-$1800 settles later today). Ran one
faithful forecast loop on a fresh market (FOMC June no-change), then — since
structural-market friction is mined out — spent the bulk of the run probing
**report surfaces never before exercised in the registry**. That vein paid off:
two genuine report-output correctness bugs, both fixed and gate-verified.

The standout: the *decision-time* surfaces (mistake_tripwire, coach, the
advisory clamp) are disciplined about evidence and bounds, but two
*retrospective* standalone reports were not — they were the lone outliers
against their own sibling surfaces. Cross-checking a new report against its
siblings is the technique that surfaced both finds.

## Friction found

| id | surface | severity | what |
|----|---------|----------|------|
| **AX-047** | report.calibration_advisory | low (correctness) | `suggested_adjustment` was the RAW band gap (`observed_frequency - mean_probability`) while `suggested_probability` is clamped to [0,1]. For a 0.97 candidate in band 0.9–1.0 (observed 1.0, mean 0.953, gap +0.0467): `suggested_probability` clamped to 1.0 but `suggested_adjustment` reported +0.0467 → `probability + suggested_adjustment` = 1.0167 overshoots the report's own suggested value. **FIXED.** |
| **AX-048** | report.mistakes / report.strengths | low–med (correctness) | Both reports surfaced tags whose forecasts are not yet scored (`scored_forecast_count:0`, `mean_brier:null`) as BOTH "recurring mistakes" AND "recurring strengths" — the same tag double-listed under contradictory labels with zero Brier evidence. Triple-corroborated: `report.mistake_tripwire` (min_sample=10) did NOT fire on the same null-Brier tags; `report.coach.top_mistakes`/`top_strengths` were `[]`; only the standalone reports leaked them. **FIXED.** |

### New report surfaces probed this run (first time in registry) — behave as designed

- `report.calibration_advisory(0.97)` — band recalibration; well-formed (modulo AX-047). N=3 prior in band, all resolved YES, suggested up to 1.0, low-N warning present.
- `report.forecast_diagnostics` — N=11, Brier 0.163, skill +0.343; clean.
- `report.time_decay_sharpening` — single `0_24h` bucket (all 11 scored forecasts are intraday strikes, avg 6.6h); no decay curve possible, correct.
- `report.source_quality` — `no_data` (no source records attached); correct. (Minor: emits 3 diagnostics — official_sources/resolution_rule_sources/redacted_sources — beyond its docstring's listed 5; doc drift only, not filed.)
- `report.watchlist` — 0 watches (loop uses `skip`, not `watch`); correct.
- `report.mistake_tripwire(thin-edge,base-rate,contrarian-tail)` — `triggered:false`, correctly gated by min_sample=10. (This is the cross-check that confirmed AX-048.)
- `report.process_analytics` — tag-frequency MVP; proper low-sample warnings, `supported_filter_paths` enumerated; correct.

## Fixes made (gate-verified)

- **AX-047** — `0502e41` `fix(report): calibration_advisory suggested_adjustment is post-clamp effective delta`
  - `calibration.py:274` → `suggested_adjustment = suggested_probability - probability` (invariant: `probability + suggested_adjustment == suggested_probability`); `calibration_gap` still carries the raw band gap. Docstring clarified.
  - +1 regression test `test_advisory_adjustment_is_post_clamp_effective_delta` (clamp case: 0.92×3 YES band → 0.97 candidate → adj 0.03 not 0.08).
- **AX-048** — `6d84dd5` `fix(report): exclude unscored (null-Brier) tags from report.mistakes/strengths`
  - `tag_aggregates.py` `_tag_ranked_report` → skip groups where `sample_size==0`; `tag_count` now reflects surfaced groups; module docstring extended.
  - +1 regression test `test_unscored_tag_excluded_from_both_reports` (scored + open tag → only scored surfaces in both reports).
  - Verified no regression in `report.coach` (`_top_tag_summary` already filtered `scored_forecast_count>0`).

Both fixes are tool-code changes → **effective next fresh session** (this session's live MCP server still shows old behavior; verified via unit tests, not live this run).

## Beads filed

None. Both finds were small, in-scope, non-firewall report-output correctness fixes (same self-documenting/correctness class as AX-030/032/035/041/042/043) — fixed directly per playbook.

## Attempted-then-reverted

None.

## Regression watch (standing items re-confirmed live, no change)

- **AX-039 / 1y9s:** `report.work_queue` SUCCEEDED at N=3 obligations — curve now fail@13 / ok@5 / ok@3 ×8.
- **AX-037:** adapter `market.bind` (FOMC mkt_F3k2AAPzU_S54ID7) preserved caller `resolution_rule_text` (`provenance:caller_supplied`).
- **AX-027:** `snapshot.fetch` price=mid 0.9825 (deeply liquid, == lastTrade here).
- **AX-031:** numeric `liquidity_depth_json` (401301), no raw dump.
- **AX-013:** skip linked `forecast_id`; `report.coach.unscored_forecasts.count:0`, no false-flag.
- **AX-018 / fe2f positive control:** new forecast fc_n3VNggVq0X2dkOvE w/ explicit `resolution_at:2026-06-17T18:00Z` did NOT flag; the 3 `resolution_at_missing` stay flagged.
- **AX-024 / 95ry:** multi-word natural-language recall ("FOMC no change rate decision conditional fallback") → only RRF-floor (~0.03) hits, no on-topic FOMC item; distinctive single-term ("suggested_adjustment calibration_advisory clamp") → new lesson rank #1 bm25:[1].
- **AX-040 / s7nn:** `abstention_coverage` 1/52/1.92% (1 abstention.record) while skip-type considered-and-passed now ~39; undercount monotonic with run count.
- **AX-045:** open BTC-$80k position `pos_QE8HbdO` still carries `initial_risk_amount:0.94`, no `missing_risk_budget`.
- **AX-029 / lszg:** `report.open_positions` default still overflows token cap (used `limit=4`); honors `limit`.
- **AX-022 / xqjv:** cold throwaway bootstrap → read/continuity tools only (work_queue/agent.next_actions/recall_receipts/strategy.show), no first-run entry point, 0 obligations.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2285 passed, 7 skipped** (perf opt-in) in ~110s (+2 new regression tests vs prior 2283).

## Lifecycle writes this run

- bound `mkt_F3k2AAPzU_S54ID7` (FOMC June no-change); snapshot `snp_0UMM9AcKNglYVsVb`
- forecast `fc_n3VNggVq0X2dkOvE` (p_yes 0.97, conf high, resolution_at 2026-06-17T18:00Z, resolution_rule_text supplied)
- skip `dec_Exb_tB4NtZOCxhAZ` (insufficient_edge, forecast linked)
- observation memory `mem_9vwVLoLpd9L9nJKO` (AX-047/048 lesson)

## Handoff for next run

- **AX-047 + AX-048 land next fresh session** — next run should re-call `report.calibration_advisory` with a near-boundary candidate and confirm `probability + suggested_adjustment == suggested_probability`; and confirm `report.mistakes`/`report.strengths` no longer surface null-Brier tags. (Expected: my journal has 5 tagged decisions, all currently unscored → both reports should now return `[]` / only scored tags.)
- **Still no resolvable inventory** until: FOMC June (2026-06-17), the BTC-$80k/$90k carries (resolution_at 2026-07-01), ETH-$1800 (June-5 settle). Do NOT force-resolve the 3 `resolution_at_missing` standing forecasts (recession 2027-close + fc_CheQKZ live).
- **Report-surface vein is now mostly mined** too: remaining unprobed surfaces are `report.compare`, `report.policy_candidates`, `report.playbook_adherence`, `report.audit_readiness` (probed run 22), `report.calibration_integrity`, `report.mistake_tripwire` variants, `report.resolution_misreads` (probed run 22), `report.pnl`/`report.opportunity`/`report.risk`/`report.exposure_anomalies` (all in registry). `report.compare` + `report.playbook_adherence` + `report.policy_candidates` are the cleanest unprobed candidates for next run.
- **Highest-leverage open backlog:** lszg/hv19/1y9s (token-cap pagination cluster), 4z2t (P1 market.refresh unusable).
