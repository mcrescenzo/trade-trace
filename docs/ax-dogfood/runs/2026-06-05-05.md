# AX dogfood run 2026-06-05-05

- **RUN_ID:** 2026-06-05-0400
- **Branch:** ax-dogfood (verified; up to date with origin/main, rebase clean)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket (Gamma) enabled
- **Mode:** live-verify prior fixes + novel-surface probe + one direct fix (handoff: probe the last unprobed standalone surfaces `report.calibration_integrity` / `report.filter_schema`; confirm AX-049 lands live)

## Summary (as a bot)

Oriented cleanly (`report.bootstrap` → `report.work_queue`). Standing state
matched the handoff exactly: 3 unresolvable `resolution_at_missing` obligations
(fc_CheQKZ live + US/UK recession 2027-close), `active_ideas` nuked by the char
cap (AX-038, 0/61). Nothing newly resolvable at 04:00Z (FOMC June settles
2026-06-17; June-5 strikes settle 16:00Z; BTC carries July-1; recession 2027).

Ran one faithful forecast loop on a **non-crypto, near-term** market for variety
(FOMC "no change after the June 2026 meeting", `mkt_F3k2AAPzU_S54ID7`,
close 2026-06-17): bound → snapshot → recall → forecast p_yes 0.97 (shaded below
the 0.9825 market for two-sided tail risk) → skip (no edge on YES; NO side's
tiny mispricing inside the spread). Probed the **two last genuinely-unprobed
standalone surfaces** — `report.calibration_integrity` and `report.filter_schema`
— both behave as designed. Then live-verified last session's AX-049 fix and, in
doing so, found a **residual of that very fix** (`report.compare` schema omits
`outcome_status`), fixed and gate-verified.

Theme continues: probing/verifying the report surfaces keeps yielding
schema-honesty defects. AX-050 is notable because it is a *leftover of the
AX-049 fix itself* — the enumeration-into-a-description pattern moved the drift
rather than closing it, on one value.

## Live verification of prior-session fixes

- **AX-049 — VERIFIED LIVE.** `report.compare(calibration, group_by=tag)` now
  rejects with `VALIDATION_ERROR` listing the full allowed set
  (`allowed group_by values are ['actor_id', 'agent_id', ... 'venue_id']`), and
  `tool.schema report.compare` `group_by` description enumerates the real
  per-`base_report` allowlists + states `tag` is unsupported. Positive control:
  `report.compare(calibration, instrument_id)` works.
- **AX-047 / AX-048 — settled `fixed`** (verified live run 04); not re-litigated.

## Friction found

| id | surface | severity | what |
|----|---------|----------|------|
| **AX-050** | report.compare (schema) | low (ergonomics/honesty) | Residual of the AX-049 fix (`5da4c4c`). `CALIBRATION_GROUP_SQL` contains BOTH `status` and `outcome_status` (both → `o.status`; aliases — verified live, both return `ok` with identical `group_value=resolved_final`), and the runtime rejection error itself lists `outcome_status` among the allowed set. But the `tool.schema` `group_by` description (the AX-049 enumeration in `tool_schemas.py`) listed only `status`, NOT `outcome_status`. So a bot reading the schema never learns `outcome_status` exists while a bot reading the rejection error is offered it — the two advertised surfaces disagree, the exact advertising-vs-runtime drift class (`trade-trace-cs0r` / AX-049) on the one value `5da4c4c` missed. **FIXED.** |

### Standalone surfaces probed first time this run — behave as designed (not filed)

- `report.calibration_integrity` — standalone panel matches the `integrity_diagnostics` block embedded in `report.calibration`/`report.coach` (same six anti-goodhart diagnostics + `abstention_coverage`). Consistent. Minor doc-drift **observation** (not filed, backlog noise): the docstring enumerates six diagnostics (forecast_coverage, unsupported_rate, ambiguous_rate, disputed_rate, void_cancelled_rate, suspicious_late_rate) but the payload also returns a 7th, `abstention_coverage` — additive, harmless.
- `report.filter_schema` — returns the full canonical `ReportFilter` JSON Schema (all 8 sub-filters, `additionalProperties:false`, `__none__` sentinel for `strategy.strategy_id` documented inline). Clean, well-formed; this is the discoverable source of truth every report's `filter` param points at.

## Fixes made (gate-verified)

- **AX-050** — `2571022` `fix(report): report.compare schema advertises outcome_status group_by (AX-050)`
  - `tool_schemas.py:343` → `group_by` description now names `outcome_status` and notes `status`/`outcome_status` are aliases for the outcome resolution status.
  - +1 regression test (`test_compare_schema_advertises_every_runtime_group_by`): asserts the description names **every** value in `CALIBRATION_GROUP_SQL ∪ PNL_GROUP_SQL`, not just a sample — the exact gap (the prior AX-049 test checked only a handful of values) that let `outcome_status` slip through. Red-confirmed before the fix (`outcome_status` unadvertised), green after.

Tool-code + schema-text change → **effective next fresh session** (this session's live MCP still showed the old description; verified via tests + live error/allowlist inspection, not via a fresh schema this run).

## Beads filed

None. AX-050 was a small, in-scope, non-firewall schema-text honesty fix (same class as AX-004/005/030/032/035/041/042/043/049) — fixed directly per playbook.

## Attempted-then-reverted

None.

## Regression watch (standing items re-confirmed live, no change)

- **AX-039 / 1y9s:** `report.work_queue` SUCCEEDED at N=3 obligations (curve fail@13 / ok@5 / ok@3 ×10).
- **AX-037:** adapter `market.bind` (FOMC no-change `mkt_F3k2AAPzU_S54ID7`) preserved caller `resolution_rule_text` (`provenance:caller_supplied`).
- **AX-027:** `snapshot.fetch` price=mid 0.9825 (lastTradePrice also 0.982; mid-anchored).
- **AX-031:** numeric `liquidity_depth_json` (389670.77), no raw dump.
- **AX-013:** skip linked `forecast_id`; `report.coach.unscored_forecasts.count:0`.
- **AX-018 / fe2f positive control:** new forecast `fc_H0rg8oEJJBehNAsZ` w/ explicit `resolution_at:2026-06-17T18:00Z` did NOT flag; exactly the 3 `resolution_at_missing` stay flagged.
- **AX-024 / 95ry:** multi-word natural-language recall ("outcome_status report.compare allowlist drift" — all 4 tokens present verbatim in the just-written memory) → only RRF-floor temporal hits ~0.03, the on-topic memory absent; single distinctive term ("AX-050") → rank #1 (`bm25:[1]`, `temporal:[1]`).
- **AX-022 / xqjv:** cold throwaway bootstrap → read/continuity tools only (work_queue/agent.next_actions/recall_receipts/strategy.show), no first-run entry point, 0 obligations.
- **AX-038 / dlup:** bootstrap `active_ideas` nuked 0/61 `max_chars` while `obligations` survived 3/3 at small N.
- **AX-040 / s7nn:** `abstention_coverage` count 1 / total 54 / 1.85% (single `abstention.record`); `report.coach` callout "1 of 54 considered markets … no-bet (1.9%)" undercounts the ~34 `decision.add(skip)` rows. Monotonic with run count.
- **AX-048 (re-confirm):** `report.coach.top_mistakes/top_strengths` both `[]` (scored-evidence gate holds).

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2288 passed, 7 skipped** (perf opt-in) in ~109s (+1 new regression test vs prior 2287).

## Lifecycle writes this run

- bound `mkt_F3k2AAPzU_S54ID7` (FOMC no-change after June 2026 meeting); snapshot `snp_mrm7C549cJ5oJDcL`
- forecast `fc_H0rg8oEJJBehNAsZ` (p_yes 0.97, conf high, resolution_at 2026-06-17T18:00Z, resolution_rule_text supplied)
- skip `dec_hSk4sjLfp4UlR25s` (insufficient_edge, forecast linked, tags fed/fomc/insufficient_edge)
- observation memory `mem_aIxDPZQ69DDBeBSf` (AX-050 lesson)

## Handoff for next run

- **AX-050 lands next fresh session** — re-call `tool.schema report.compare` and confirm the `group_by` description now names `outcome_status`; the runtime already accepts it (verified live this run).
- **Report-surface vein is now fully mined.** Every standalone report surface in the catalog has been probed across runs 01–05 (this run closed the last two: `report.calibration_integrity`, `report.filter_schema`). Future report-surface findings will come from the **populated path** of state-dependent surfaces (`policy_candidates` needs a `policy_candidate` reflection; `playbook_adherence` needs `playbook.upsert` + adherence rows; `strategy_health` needs `strategy.upsert`) — a next run could deliberately seed that state to probe the populated path, or shift the probe focus to **write tools** (`abstention.*`, `memory.link/reflect`, `forecast.commit_blind/independence`, `replay.*`) which are less exercised than the report family.
- **Resolvable inventory:** a fire after **2026-06-17** can finally resolve the FOMC June forecasts (`fc_H0rg8oEJJBehNAsZ` + run-03's FOMC seed); June-5 strikes settle 16:00Z (a fire after 16:00Z today may have something); BTC carries 2026-07-01. Do NOT force-resolve the 3 `resolution_at_missing` standing forecasts (recession 2027-close + fc_CheQKZ live).
- **Highest-leverage open backlog (unchanged):** lszg / hv19 / 1y9s (token-cap pagination cluster), 4z2t (P1 `market.refresh` unusable), n33z (resolution-rule retrieval), 95ry (BM25 conjunctive recall).
- **Meta-lesson (AX-050):** when a fix enumerates an allowlist into a human-facing description, diff that description against BOTH the SQL allowlist AND the rejection-error allowlist — they must be the same set, or the drift just moved. The new test enforces this for `report.compare`; other tools that enumerate allowlists into descriptions may carry the same latent gap.
