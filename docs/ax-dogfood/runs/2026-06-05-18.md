# AX dogfood run — 2026-06-05-18

- **RUN_ID:** `2026-06-05-2001`
- **Branch:** `ax-dogfood` (never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket
- **Mode:** standing-confirmation pass + one genuine live resolution event

## Summary (as a bot)

Drove the full canonical loop on live Polymarket during an active BTC/equity
selloff. The headline this run was a **genuine same-day equity resolution**:
bootstrap's resolve_due queue contained, alongside the 3 standing
`resolution_at_missing` false-positives (fe2f), a real past-due forecast
`fc_A8wbW92` on **"S&P 500 (SPY) closes above $750 on June 5?"**
(`resolution_at_due_without_score`, due_at 20:00Z — its 4pm-ET Pyth close had
just passed). Resolving it faithfully exercised three friction surfaces at once
and produced a real, costly calibration miss. The rest of the loop
(scan → bind → snapshot → recall → forecast → skip → review → remember →
cold-probe) re-confirmed the now-large body of standing findings with no new
fixable bug — the expected asymptote after 17 prior runs hardened the surface.

## The SPY $750 resolution (depth item)

1. **Rule retrieval gap (n33z), live on a real resolution.** The resolve_due
   obligation handed me `forecast_id` + `instrument_id` but **no readable market
   question/rule**. To resolve faithfully I had to re-`market.bind` idempotently
   to read the `description` (Pyth "Close" of the final-minute 1m candle; 50-50
   fallback on a non-trading day). The retrieval gap is most acute exactly when
   it matters — at settlement.
2. **Evidence conflict (web vs venue).** Light web research (per playbook)
   returned an LLM summary claiming SPY ~$754 on June 5 ⇒ would be **YES**. But
   the live Polymarket orderbook priced YES at **0.0005** (`bestAsk 0.001`,
   NO 0.9995) eight minutes before the same-day close. A liquid market
   (liq $2.1k) cannot leave a ~1000× arbitrage open if SPY were truly above
   $750, so the **arbitrage-free near-settlement orderbook is authoritative**
   and the web summary was confabulated (it even hedged on June-5 data
   availability and cited a June-2 article). Determination: **NO**.
3. **Faithful record, not fabricated.** Recorded `resolution.add`
   (`status=resolved_final`, `outcome_label=no`, **confidence 0.9** — the
   playbook's documented "unambiguous outcomePrices ⇒ ≥0.9" path; not 0.99
   since no official `winningOutcome`/UMA post yet, `umaResolutionStatuses []`).
   Auto-scored `fc_A8wbW92` → **Brier 0.7225** (my June-4 forecast was
   `p_yes=0.85` → a textbook overconfident-YES miss). Outcome `out_d93OU0…`,
   clean single write (no AX-030 dup-row wart).

## Friction found (all standing; surface · severity)

| item | surface | this run | disposition |
|------|---------|----------|-------------|
| n33z / AX-017 | tool/schema | re-bind required to read SPY rule at settlement; `resolution_rule.text` null, rule in `description` | filed (P2) |
| isqo / AX-020 (adjacent) | tool/error | no in-surface authoritative settlement signal; web research **conflicted** with the venue orderbook (new wrinkle — venue is more authoritative) | filed; noted, not re-filed |
| fe2f / AX-018 | report | 3 standing `resolution_at_missing` false-positives (fc_CheQKZ live, US/UK recession 2027-close); positive control held — the genuinely-due `fc_A8wbW92` flagged via `resolution_at_due_without_score`, and my 2 new forecasts with explicit `resolution_at`=2026-07-01 did NOT flag | filed (P3) |
| AX-024 / 95ry | tool (memory.recall) | re-confirmed: bm25-only on unique token `PANGOLIN-SPY750-J5-2001` → rank 1 w/ bm25 provenance; the **fused** query *containing that verbatim token* buried the node entirely out of top-5 (RRF temporal/graph swamp) — same as run-17, not re-filed (backlog-noise) | filed (P2, in_progress) |
| AX-022 / xqjv | onboarding | cold throwaway bootstrap → only read/continuity tools, 0 obligations, no first-run entry point | filed (P3) |
| s7nn / AX-040 | report/integrity | abstention_coverage 2/82 (2.44%); my 2 new `decision.add(skip)` rows uncounted (~38 skips total uncounted) | filed (P3) |
| AX-038 / dlup | report | bootstrap `active_ideas` nuked 0/68 `max_chars` (per-section nuke); obligations survived 4/4 | filed (P2) |
| AX-037 / AX-027 / AX-031 / AX-013 / AX-048 | tool/adapter/report | all re-confirmed (see below) | fixed (prior SHAs) |

### Standing re-confirms (no change)
- **AX-037:** both BTC binds ($70k reach `mkt_7tACn4aqhw6y8BwN`, $55k dip
  `mkt_NEPHuqH1YQ2Wq-8F`) preserved caller `resolution_rule_text`
  (`provenance=caller_supplied`); venue structured rule null (n33z).
- **AX-027:** `snapshot.fetch price`=mid on all ($70k 0.165, $55k 0.4755, SPY
  0.01) — not lastTradePrice.
- **AX-031:** numeric `liquidity_depth_json` on all (52177 / 32105 / 2115).
- **AX-013:** both skips linked `forecast_id`; `coach.unscored_forecasts.count:0`,
  not false-flagged.
- **AX-048:** `coach.top_mistakes`/`top_strengths` `[]` (scored-evidence gate).
- **AX-067 / v5va:** the `resolution_source` coarse adapter default
  (`market_contract`) re-observed on all 3 binds.

## Fixes made (direct commits)

**None.** No new fixable bug surfaced — every friction item is already filed or
fixed by prior runs. The SPY episode is a usage/calibration finding (the product
behaved correctly: it has no web surface, and the snapshot/outcomePrices path
worked). Forcing a code change would violate the honesty rule.

## Verification of a prior fix (fresh-server, this run)

- **AX-067 (`81345c8`) verified live.** This session is a fresh cron-booted MCP
  server, so the run-17 fix is now effective. `report.resolution_misreads` now
  carries per-group `actual_source_provenance: "adapter"`, summary
  `contract_misread_adapter_bound_count: 1`, and the adapter-default coarseness
  caveat — so the `fc_EDl7QETV9uG7ORu9` `oracle_feed`-vs-`market_contract`
  misread is now correctly discountable. Design half remains `trade-trace-v5va`.

## Beads filed / updated

**None filed.** Dedup against the 8 open `ax-dogfood` beads (v5va, 1y9s, lszg,
n33z, s7nn, scx8, xqjv, fe2f) + the registry: every observation this run maps to
an existing bead with evidence already recorded at equal-or-greater specificity
(the 95ry fused-query-buries-token pattern was already appended in run-17; the
SPY web/venue conflict is an isqo-adjacent usage lesson, captured in memory
`mem_rzdYmGk2Hoi8B48K`). Filing again would be backlog noise.

## Loop activity (journal writes, axloop home — not git)

- Resolved 1 (SPY $750 → NO, conf 0.9; auto-scored `fc_A8wbW92` Brier 0.7225).
- Bound 2 (BTC $70k reach, $55k dip), 3 snapshots (incl. SPY read).
- Forecast 2: `fc_vvGRvuIa` $70k-reach YES **0.12** (shaded below mid 0.165 per
  the overconfident-tail-YES lesson), `fc_EH6-O0x` $55k-dip YES 0.47 (≈ mid,
  near-money coinflip, no edge).
- 2 skips (already_exposed on $70k per scx8; insufficient_edge on $55k), both
  forecast-linked.
- Retained `mem_rzdYmGk2Hoi8B48K` (SPY resolution + web/venue lesson);
  round-tripped via recall.

## Calibration

N **32 → 33** (the SPY resolution scored `fc_A8wbW92`). The single miss
dominates a small panel: **Brier 0.089 → 0.109, skill +0.583 → +0.485, ECE
0.141 → 0.131**. The SPY 0.85-YES forecast landed in **bin 0.57–0.85 (mean
0.693, observed 0, gap +0.693)** — the sharpest overconfident-YES band now, a
real, costly confirmation of the standing near-money/tail-YES overconfidence
signal. **Acted on it** this run: shaded $70k-reach to YES 0.12 below mid 0.165.

## Gate result

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2310 passed, 7 skipped** in 98.10s (the 7 skips are perf
  opt-ins, `TRADE_TRACE_RUN_PERF_TESTS=1`). Identical to the last green
  baseline — no `src`/`tests` changes this run.

## Handoff notes for next run

- **Stashes:** `stash@{0}` / `stash@{1}` (human-owned axloop-debris 1500/1600)
  remain untouched. Working tree was otherwise clean; this run touched only
  `src`-free docs.
- **Forecasts pending:** the 2 new June month-window forecasts
  (`fc_vvGRvuIa` $70k, `fc_EH6-O0x` $55k) close **2026-07-01** → they will NOT
  score this cycle. N-growth still depends on same-day strikes (none ripe this
  run; the SPY one was the only same-day item due, now scored).
- **SPY/Pyth equity markets** are a fertile same-day-resolution lane (settle at
  20:00Z = 4pm ET, distinct from the 16:00Z crypto noon strikes) — but expect
  the n33z rule-retrieval gap + the web/venue evidence conflict each time;
  **trust the arbitrage-free near-settlement orderbook over LLM web summaries.**
- **Surface friction has asymptoted:** runs 01–18 fixed the ergonomic surface
  bugs; recent runs are increasingly standing-confirmation. The remaining open
  beads are design-level (1y9s/lszg envelope bloat, n33z/v5va resolution
  semantics, fe2f/s7nn/scx8/xqjv design questions) requiring human/product
  decisions, not direct loop fixes.
