# AX dogfood run — 2026-06-05-21

- **RUN_ID:** `2026-06-05-2300`
- **Branch:** `ax-dogfood`
- **Actor:** `agent:ax-dogfood`
- **Journal home:** `~/.trade-trace-axloop`
- **Disposition:** Standing-confirmation run, **NO new friction**. Headline:
  **AX-068 fix (`4cb1307`) VERIFIED LIVE** on this fresh server — `market.refresh`
  works end-to-end again.

## Summary (as a bot)

Drove the full canonical loop on the live **BTC June month-window touch ladder**
(settle 2026-07-01T04:00Z, Binance BTC/USDT 1m High/Low). Intraday BTC selloff:
`reach-$70k` YES mid 0.22 (oneDayPriceChange −0.105), `dip-$57.5k` YES 0.561
(+0.16), `dip-$52.5k` YES 0.2355 (+0.09) — a coherent ladder ⇒ genuine edge ~0.
Bound 3 markets, recorded 3 forecasts, 2 forecast-linked skips, and one thin
15-unit YES paper position on the near-money `dip-$57.5k` entered **at mid**
(calibration discipline). The loop felt smooth; the one thing a bot would still
want — a usable `market.refresh` for open→resolved detection — is now actually
present (AX-068 fix landed and is live), which is the meaningful change since
prior runs.

## What was exercised

| step | tool | result |
|------|------|--------|
| orient | `report.bootstrap` | 3 standing `resolution_at_missing` obligations (fe2f false-positives); `active_ideas` nuked 0/78 `max_chars` (AX-038); `memory_context` not included by default |
| resolve-due verify | `snapshot.fetch` / `market.refresh` | mkt_pwu (fc_CheQKZ) live mid 0.505 accepting_orders; mkt_33h (US recession, fc_uywJQC8) live, outcomePrices [0.185,0.815], state=open close 2027-01-31 → all 3 "due" are live, **no fabricated resolutions** |
| **AX-068 verify** | `market.refresh` | **`ok:true`** on stale mkt_33h (event_id 708, `state_changed:false`, row re-synced from Gamma) — the run-20 fix is LIVE (was a hard KeyError on unregistered `market.refreshed`) |
| scan | `market.search Bitcoin` | 12 live June ladder candidates, non-empty (AX-019/035) |
| bind ×3 | `market.bind` | reach-$70k `mkt_7tACn4aqhw6y8BwN`, dip-$57.5k `mkt_2T1L59jjnoYXgS39`, dip-$52.5k `mkt_e86jIXc4vcxi7NZn` — all preserved caller `resolution_rule_text` provenance=`caller_supplied` (AX-037); venue structured rule null (n33z) |
| snapshot ×3 | `snapshot.fetch` | `price`=mid on all (0.22 / 0.561 / 0.2355, NOT lastTrade 0.23/0.564/0.23) (AX-027); numeric `liquidity_depth_json` 20297/18299/27138 (AX-031) |
| recall | `memory.recall` | prior lessons surfaced (overconfident-YES, coherent-ladder, AX-068) |
| forecast ×3 | `forecast.add` | reach-$70k YES 0.20, dip-$57.5k YES 0.56 (at mid), dip-$52.5k YES 0.23; `fc_Lu7c10Od5pmD72wN` / `fc_pels_RDu1otsyfXH` / `fc_LSGKvxbVjh6kFGdy`, all `resolution_at`=2026-07-01 |
| decide ×3 | `decision.add` | 2 forecast-linked skips (insufficient_edge); thin 15u YES `paper_enter` dip-$57.5k @0.561, `declared_risk_amount=8.42`, `pos_WyFm4hU95R--dsYz`, no `missing_risk_budget`, marked side-aware unrealized ~0 (=entered at mid) (AX-045/025/028) |
| review | `report.calibration` / `report.coach` / `report.open_positions` / `report.unscored_forecasts` | N=33 Brier 0.109 skill 0.485 `sample_warning:null`; coach `top_mistakes/strengths` `[]` (AX-048); unscored 0 (fe2f positive control); skips not false-flagged (AX-013) |
| remember | `memory.retain` + `memory.recall` | reflection `mem_ZW4HANU7XzH63GPm`; AX-024 round-trip re-confirmed |
| cold probe | throwaway `tt journal init` + bootstrap | read/continuity suggested calls only (AX-022/xqjv) |

## Calibration self-check (the point of the discipline)

N=33, Brier 0.109, skill 0.485, `sample_warning:null` (unchanged — month-window
forecasts settle July 1, won't score same-day). The standing **overconfident-YES**
signal is reliability bin 7 (0.57–0.85, mean 0.693, **observed_frequency 0**, gap
0.693). I deliberately forecast `dip-$57.5k` YES at **0.56**, landing it in the
*well-calibrated* bin 6 (0.49–0.56, observed 0.5, gap 0.01) rather than shading up
into the overconfident band. The +0pt-over-mid restraint is the right call given
the standing miss history (run-18 SPY 0.85 → Brier 0.7225 still in scope).

## Standing re-confirms (no change, not re-filed)

- **AX-068** (`market.refresh`) — **fix `4cb1307` VERIFIED LIVE** (see above). Was
  the run-20 headline bug; now confirmed effective on a fresh server.
- **AX-037** — 3 adapter binds preserved caller `resolution_rule_text`.
- **AX-027** — `snapshot.fetch price`=mid, not lastTradePrice.
- **AX-031** — numeric `liquidity_depth_json`, no raw payload dump.
- **AX-045/025/028** — `paper_enter` carried `declared_risk_amount`; position
  marked side-aware against the latest mark (unrealized ~0 at mid entry).
- **AX-013** — both skips forecast-linked; `coach.unscored_forecasts.count:0`.
- **fe2f / AX-018** — exactly 3 `resolution_at_missing` false-positives, markets
  proven **live** (not fabricated); 3 new forecasts with explicit `resolution_at`
  did NOT flag (positive control); `report.unscored_forecasts` count 0.
- **AX-024 / 95ry** — bm25-only recall of unique token `PLATYPUS-DIP575-J5-2300`
  → rank 1 with bm25 provenance; the same token inside a fused multi-word query
  buried the just-written node OUT of top-5 (RRF floor ~0.017–0.031, no bm25
  provenance). In_progress under 95ry.
- **AX-048** — `coach.top_mistakes/top_strengths` `[]`.
- **AX-038 / dlup** — bootstrap `active_ideas` 0/78 nuked `max_chars`.
- **AX-022 / xqjv** — cold throwaway bootstrap surfaces read/continuity tools only.
- **scx8** — re-confirmed: the `dip-$57.5k` YES `paper_enter` opened a **2nd** row
  (`pos_WyFm4hU95R--dsYz` 15@0.561) while a prior June-4 YES (`pos_CwabrU-E9BKf00jk`
  20@0.434) was already open on the **same instrument + same side**, with no
  `already_exposed` signal at decision time. Exposure nets correctly (35 YES,
  side-aware) so this is the autonomous-feeder fragmentation ergonomics gap, not a
  marking bug. Design Q already filed.
- **s7nn** — `abstention_coverage` 2/91 (2.2%); ~34 considered-and-passed skips
  still uncounted in the survivorship denominator.

## Friction found

**None new.** Every observed behavior is an already-registered item (standing,
filed, or intentional) or a positive verification. Depth-over-breadth: one
coherent ladder, 3 forecasts, faithful resolve-due verification, AX-068 live check.

## Fixes made

**None.** No code changed this run (no new friction to fix).

## Beads filed

**None.** No genuinely-new finding; re-filing/re-appending standing items
(fe2f/scx8/95ry/xqjv/s7nn/n33z/AX-038) would be backlog noise.

## Attempted-then-reverted

**None.**

## Gate result

Full suite, green (docs-only change this run):

- `ruff check src tests` → All checks passed
- `mypy src` → Success: no issues found in 178 source files
- `pytest -q` → **2313 passed, 7 skipped** in 115.88s

Branch HEAD was green at `01f4c03` (run-20) and remains green.

## Handoff notes

- Two human-owned debris stashes remain untouched: `stash@{0}`
  (axloop-debris-20260605T160001Z), `stash@{1}` (axloop-debris-20260605T150001Z).
  Not this run's; left for the human to land/discard.
- `market.refresh` is now a live, working route for open→resolved lifecycle
  detection (AX-068 closed-and-verified). Future runs can use it alongside
  `snapshot.fetch` for the no-RPC resolution path.
- Open paper exposure on `dip-$57.5k` (`mkt_2T1L59jjnoYXgS39`) is now 35 YES across
  2 rows (scx8 fragmentation across runs) — a legit standing-state artifact, not a
  defect; settles 2026-07-01.
- No new forecasts will score until same-day strikes are forecast+resolved within
  one cycle (the month-window ladder settles July 1). Calibration stays N=33.
- The new-friction surface is genuinely thin now (report catalog + write-vein
  catalog both exhausted over runs 01–20; AX-068 was the last novel-surface bug).
  Future runs are mostly standing-confirmation + live fix-verification + acting on
  the calibration signal. Consider probing a genuinely new market *category*
  (equities/macro same-day) for fresh resolution-path friction.
