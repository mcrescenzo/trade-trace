# AX dogfood run — 2026-06-06-09

- **RUN_ID:** `2026-06-06-0800`
- **Branch:** `ax-dogfood` (rebase: up to date with `origin/main`)
- **Actor:** `agent:ax-dogfood` · journal `~/.trade-trace-axloop` · live Polymarket
- **Disposition:** standing-confirmation pass, **NO new friction** (8th consecutive). Genuine breadth FIRST: first forecast on an **awards / film-nominations** domain, paired with an **IPO-valuation cap ladder** (SpaceX).

## Summary (as a bot)

Bootstrap was clean and legible: 3 derived obligations, all `resolve_due_forecast`
flagged purely on `resolution_at_missing` — the known standing false-positives
(fe2f: Rihanna/GTA `fc_CheQKZ` + US/UK recession `fc_uywJQC8os4NsVqIP` /
`fc_I0VaLn1GHFFYpFYZ`). `report.unscored_forecasts` returned `count:0`, the clean
counter-surface confirming nothing genuinely closed (run at 08:00Z; June-6 noon
ladder settles 16:00Z, ~8h out; Real Madrid election June 7-8; tariff June 30).
I fabricated no resolution.

I again reached for **new market categories** now that the report/write-vein
catalogs are exhausted. Two domains surfaced and bound cleanly, one brand-new:

1. **Awards / film-nominations** (NEW domain) — "Will Project Hail Mary get the
   MOST Oscar nominations at the 99th Academy Awards?" (`mkt_DXksFdGDgBIPXL8T`,
   **negRisk grid leg**). mid **0.0605** YES (bid 0.051/ask 0.07, lastTrade 0.073,
   `outcomePrices ["0.0605","0.9395"]`), thin ($847 CLOB / $2.4k vol), **5%
   culture_fees**, `oneMonthPriceChange -0.1225` (faded from ~0.18). Deep-tail leg
   in a multi-film grid ~8 months before nominations ⇒ no private edge; forecast
   at mid. **Resolution_at 2027-02-28 — long horizon.**

2. **IPO-valuation cap ladder** — "SpaceX IPO closing market cap above $1T?"
   (`mkt_DbZN6yzQSWXoi0x6`, **independent binary**, `negRisk=false`). mid **0.9915**
   YES (bid 0.99/ask 0.993, spread 0.003), deeply liquid ($62k CLOB / $552k vol),
   **4% finance_prices_fees**. Lowest rung of the cap ladder ⇒ market prices it
   near-certain; no independent edge over a deeply liquid consensus; forecast at
   mid. **Resolution_at 2027-12-31 — long horizon.**

Both forecasts anchored to fresh snapshots; both **SKIPPED `insufficient_edge`**
(forecast-linked so coach's unscored count stays 0). Calibration self-check: the
two land in well-separated reliability bins — 0.0605 → bin 2, and 0.9915 → bin 9
(currently observed_freq 1.0, gap -0.029, the best-calibrated high band) — both
away from the standing overconfident-near-money-YES band (bin 7, 0.56-0.85, gap
0.66). Same coherent-liquid-market-no-edge discipline as the crypto ladders /
tariff / box-office, now anchoring both a deep-OTM tail and a deep-ITM near-cert
to the mid.

## Lifecycle exercised

| Step | Result |
|---|---|
| `report.bootstrap` | 3 obligations, all standing `resolution_at_missing` false-positives (fe2f) |
| `report.unscored_forecasts` | `count:0` — clean, nothing genuinely closed |
| `report.calibration` | N=35 Brier 0.1025 skill 0.498 ECE 0.154; bin 7 overconfidence persists |
| `market.search` | `Oscar`→10, `SpaceX`→10 (single-term, clean) |
| `market.bind` ×2 | Oscars `mkt_DXksFdGDgBIPXL8T` (negRisk true) + SpaceX `mkt_DbZN6yzQSWXoi0x6` (negRisk false), `bound_via:adapter` |
| `snapshot.fetch` ×2 | Oscars `snp_mRBHeoydUjioS0vd` ip 0.0605; SpaceX `snp_nmx-jrZOK7aPRd1p` ip 0.9915; numeric `liquidity_depth_json` (AX-031) |
| `memory.recall` | no awards/IPO memory yet (new domains); returned transferable crypto-ladder/box-office no-edge lessons; fabricated no signal |
| `forecast.add` ×2 | Oscars `fc_8z58I6-HwcgNB8IW` (0.0605) + SpaceX `fc_71GDDR9PVjyp5m6g` (0.9915) |
| `decision.add` (skip) ×2 | `dec_AEQfMMjqrJ3orznW` + `dec_8GWNL8N5OiZd-rrY`, both `insufficient_edge`, forecast-linked |
| `report.coach` | `unscored_forecasts` count 0 (AX-013 ✓); abstention 2/109 (s7nn); top lists `[]` (AX-048) |
| `memory.reflect` + recall | `mem_w4d8T8lMJz0V6rAk` (TARSIER token) round-trips rank #1 bm25:[1], in_scope 62→63 |
| cold-start probe | empty journal schema v29; suggested calls all read/continuity tools, no first-run entry-point (xqjv) |

## Friction found

**None new.** Standing items re-verified live this run:

- **AX-027** — `snapshot.price`/`mid` = market mid, not `lastTrade` (Oscars 0.0605 vs lastTrade 0.073).
- **AX-031** — `liquidity_depth_json` numeric (847.167 / 62334.788), not an object.
- **AX-013** — forecast-linked skips ⇒ `report.coach` unscored count 0.
- **AX-017 / n33z** — `resolution_rule.text` null on both binds; full rules live only in
  `description`. Oscars: greatest-nomination-count + **alphabetical-order tiebreak** +
  "Other" if no noms by 2027-06-30 + credible-reporting-consensus. SpaceX: first-day-close
  cap > $1T + **"no IPO by 2027-12-31 → No"** conditional fallback (same conditional-outcome
  class as the OpenAI-IPO/FOMC instances already on n33z). Two new structural instances.
- **AX-048** — `report.coach` `top_mistakes`/`top_strengths` `[]`.
- **AX-035** — single-term `search_hint` behavior (clean: both single-term searches returned candidates).
- **AX-024 / 95ry** — bm25-only single-token recall ranks the probe #1; fused buries it.
- **AX-022 / xqjv** — cold bootstrap surfaces no first-run entry-point.
- **s7nn** — `abstention_coverage` 2/109 (1.83%) still undercounts the ~36 forecast-linked skips.
- **fe2f** — 3 `resolve_due_forecast` obligations flagged solely on `resolution_at_missing`.

## Fixes made

None (no new friction; no code touched this run).

## Beads filed / updated

- **trade-trace-n33z** — appended two new structural instances of the `resolution_rule.text=null`
  surface gap (awards/film-nominations negRisk grid with alphabetical tiebreak; SpaceX IPO-cap
  ladder with conditional "no IPO → No" fallback). No new bead filed — dedup against the 8 open
  `ax-dogfood` beads (v5va, 1y9s, lszg, n33z, s7nn, scx8, xqjv, fe2f) found nothing else new.

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` — **All checks passed!**
- `mypy src` — **Success: no issues found in 178 source files**
- `pytest -q` — **2313 passed, 7 skipped** in 115.06s

## Handoff notes for next run

Settlement opportunities to climb N past 35 (priority order by time):

1. **June-6 NOON "above $X" ladder settles 16:00Z.** The first cron fire after 16:00Z
   should resolve: run-05's $58k (`mkt_Q-w3HIxGx4SgvobY`, `fc_LHIDb7fc29SHqHK8`, YES 0.93)
   + run-18's $64k (`mkt_mMrI9fVsyQDM0xrQ`) + $66k (`mkt_DaGR4nGM18wtBnBC`). Procedure:
   `market.refresh` each, snapshot mid (≈0.999 ⇒ yes, ≈0.001 ⇒ no), `resolution.add`
   conf≥0.9. BTC ~$59-60k ⇒ $58k likely YES, $64k/$66k likely NO. These auto-score and climb N.

2. **Real Madrid election** (`fc_6oac5wDvKvBN9e0o` Pérez 0.965 / `fc_qBq-mC4eU8KSAfSY`
   Riquelme 0.0355, resolution_at 2026-06-08) settles **~June 7-8**. After the vote:
   `market.refresh` both, snapshot mid (winner ~0.99 / loser ~0.01), `resolution.add`
   binary conf≥0.9.

3. **US×China tariff** (`fc_-6WE3NqCmMuBEzaD`, YES 0.08, resolution_at 2026-06-30) settles
   ~June 30. After deadline: `market.refresh`, snapshot mid, `resolution.add` conf≥0.9.

Longer-horizon pending (do not force): 2 FOMC (after June-17), NHL Final pair (~late June),
Trump-Putin grid (June 30), BTC/ETH June month-window (July 1), **box-office Wicked**
(`fc_9iuBZBPGA1YqU1A1`, Dec 31), and now **Oscars Project Hail Mary**
(`fc_8z58I6-HwcgNB8IW`, 2027-02-28) + **SpaceX IPO >$1T** (`fc_71GDDR9PVjyp5m6g`, 2027-12-31)
— both multi-month/year horizons, won't reach the calibration floor soon.

Report-surface + write-vein friction catalogs remain EXHAUSTED — novelty now = settlement
cycles + new market CATEGORIES (this run added one genuinely new: awards/film-nominations).
Open beads unchanged: v5va / 1y9s / lszg / n33z / s7nn / scx8 / xqjv / fe2f.
