# AX dogfood run — 2026-06-04-14

- **RUN_ID:** 2026-06-04-1300
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** **no code change** — clean re-confirmation run. Every recent
  fix held live (AX-027 mid-anchoring, AX-031 clean depth, AX-013 skip/coach no
  false-flag). Two beads strengthened with fresh evidence (n33z ETH point-in-time
  ladder; fe2f positive control re-confirmed). Gates green (2276 passed). No new
  code-worthy friction surfaced.

## Summary (as a bot)

Drove the full journal loop on the **ETH daily point-in-time strike ladder**
("Will the price of Ethereum be above $X on June 4?", noon-ET Binance ETH/USDT
single-candle close, settling **today at 16:00Z**). The loop was smooth and
nothing surprised me: bootstrap → recall → forecast → skip → reflect all behaved
exactly as a bot would want. Memory recall was again the decisive step — it
surfaced my prior **window-touch** lessons (`mem_KrD5guOtsKXrhkGE`,
`mem_4MIojgkFHXh8GPHC`) which must NOT be applied to a point-in-time settle (the
noon-candle model is much lower than any-candle-touch for the same strike).

ETH sat right at the $1,800 strike: `>$1,700` traded YES 0.94 (deep ITM),
`>$1,800` traded YES 0.3055 with a **wide 0.049 spread** (the only live strike),
and `>$2,000` was settled-No at 0.0005. I had no genuine edge over the market
mid on a ~3h crypto move, so I forecasted at the market-implied probabilities
(with explicit `resolution_at=close_at` so a later loop can auto-score them) and
**skipped both, no exposure**. The one durable trading lesson — recorded to
memory — is that per-strike spread tracks moneyness: the at-the-money strike is
simultaneously the only one with real uncertainty and the widest/least liquid,
so taker entry there pays the most spread exactly where edge is hardest to find.

## What was exercised

- `report.bootstrap` → 3 obligations, all `resolve_due_forecast` /
  `resolution_at_missing` (US-recession `fc_uywJQC8os4NsVqIP`, UK-recession
  `fc_I0VaLn1GHFFYpFYZ`, `fc_CheQKZ-UOTWPVUPs`). Bootstrap now carries an inline
  `caveat_glossary` (the AX-001 area, resolved) — codes are self-glossing.
- `market.search` (`ethereum`) → intraday strike ladder, `search_hint:null`,
  populated (correct single-term behavior, AX-035 path).
- `market.bind` ×3 (2379374 → `mkt_UsoYE8KN0eFkrdE6` $2,000; 2379366 →
  `mkt_lDGbhDtTiS5l9RDa` $1,800; 2379363 → `mkt_BAtc1d8DOVp4G3hw` $1,700), all
  `bound_via:adapter`.
- `snapshot.fetch` on `mkt_lDGbhDtTiS5l9RDa` → `price=0.3055` anchored to **mid**
  (not lastTradePrice 0.329) and numeric `liquidity_depth_json` (8805.65, no raw
  dump) → **AX-027 + AX-031 confirmed live**.
- `memory.recall` → surfaced the window-touch lessons + the auto-score trap note,
  which shaped how I priced the point-in-time strikes.
- `forecast.add` ×2 (`fc__FHwOmnhZmK7FEcR` $1,800 p_yes 0.30;
  `fc_zo3TquA7-zhMMubX` $1,700 p_yes 0.95), each with explicit
  `resolution_at=2026-06-04T16:00:00Z` + self-supplied `resolution_rule_text`.
- `decision.add(skip)` ×2 (`dec_wj7psCDhHoTo1mhu`, `dec_PKCVIyoCYORMDqf_`),
  linked `forecast_id`, materiality `insufficient_edge`.
- `report.calibration` (N=1, below floor — honest low-sample warning) /
  `report.coach` (clean; my two skip+forecast decisions were **not** false-flagged
  as unforecasted → AX-013 holds live).
- `memory.reflect` (`mem_7CmZu75_NXliqMfs`, spread-vs-moneyness) +
  `memory.recall` round-trip (top hit).
- Cold-start probe: throwaway `tt journal init` + `tt report bootstrap`.

## Friction found

| id | surface | severity | disposition |
|----|---------|----------|-------------|
| AX-017 (dup) | tool/schema | P2 | filed (n33z) — ETH point-in-time ladder, `resolution_rule.text` null across all 3 strikes; rule only in `venue_metadata_json.description`. Evidence appended. |
| AX-018 (dup) | report | P3 | filed (fe2f) — positive control: 3 `resolution_at_missing` forecasts flagged resolve_due; 2 new forecasts with future `resolution_at` did NOT flag, same journal. Evidence appended. |
| AX-022 (dup) | onboarding | P3 | filed (xqjv) — cold bootstrap on empty journal surfaces only the 4 read/continuity `suggested_process_calls`, no first-run `market.search→bind→forecast` entry point. Re-confirmed. |

No new code-worthy friction. The bot loop ran clean end to end.

## Fixes made

None this run (no new fixable friction). Re-confirmed live: **AX-027** (snapshot
`price` anchors to mid), **AX-031** (clean numeric `liquidity_depth_json`),
**AX-013** (skip links forecast_id; coach no longer false-flags).

## Beads filed / updated

- No new beads filed (dedup: all friction matched existing open beads).
- `trade-trace-n33z` — appended ETH point-in-time ladder evidence (broadens the
  asset coverage of the resolution-rule discovery gap beyond the run-13 BTC
  instance).
- `trade-trace-fe2f` — appended positive-control evidence (3 missing-resolution_at
  forecasts flagged; 2 future-resolution_at forecasts suppressed, same journal).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2276 passed, 7 skipped** in ~104s (7 skips are perf-baseline
  opt-ins, unchanged).

## Handoff notes for next run

- **Same-day scoreable forecasts pending:** `fc__FHwOmnhZmK7FEcR` (ETH >$1,800)
  and `fc_zo3TquA7-zhMMubX` (ETH >$1,700) both resolve at the **16:00Z noon-ET
  Binance candle today**. A loop firing after 16:00Z can resolve these via
  `resolution.add` (binary label, `confidence>=0.9`) and push N toward the
  `report.calibration` N≥20 floor — the only forecasts in the journal that can
  actually be scored within hours. ETH was ~$1,775 at 13:02Z; the $1,800 outcome
  is a genuine toss-up, the $1,700 outcome near-certain.
- **Standing positive-control pair** for fe2f remains in the journal: 3
  `resolution_at_missing` forecasts (recession/long-horizon) keep flagging
  resolve_due while future-`resolution_at` forecasts correctly suppress.
- No environment issues; main untouched.
