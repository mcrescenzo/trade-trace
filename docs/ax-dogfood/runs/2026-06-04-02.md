# AX dogfood run 2026-06-04-02

- **RUN_ID:** `2026-06-04-0100`
- **Branch:** `ax-dogfood` (verified; never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma)
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (how it felt as a bot)

Drove the full MCP loop on live Polymarket. Orientation via `report.bootstrap`
→ `report.work_queue`/`report.lifecycle` was coherent and the two due
obligations were legible. Resolution of due forecasts was **blocked honestly**:
both `resolve_due` items are on markets that are still actively trading, so I
left them open rather than invent outcomes. Scanned ETH markets, bound one,
forecast it, opened a calibration probe, reviewed calibration/coach, and
retained a lesson that round-tripped cleanly through `memory.recall`. The
single biggest positive this run: `memory.recall` surfaced a directly
applicable prior lesson (touch-market asymmetry) that materially shaped the
forecast — the substrate did its job. The single real bug found this run was a
marking error (off-book last-trade mark) that inverted a position's PnL sign.

## What I exercised

`report.bootstrap` → `report.work_queue` → `report.lifecycle` →
`snapshot.fetch` (×2) → `market.search` → `tool.schema` → `market.bind`
(adapter) → `snapshot.fetch` → `memory.recall` → `forecast.add` (folded public
path, snapshot-anchored) → `decision.add` (`paper_enter`) →
`report.open_positions` → `report.calibration` → `report.coach` →
`memory.reflect` → `memory.recall` (round-trip) → cold-start probe (throwaway
journal init + bootstrap, destroyed).

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **F3 / AX-027** | tool/report | **med-high (correctness)** | `snapshot.fetch` derived `snapshots.price` (the canonical YES mark for the positions projection + PnL reports) from Gamma `lastTradePrice` in preference to the book mid. Live ETH market printed lastTrade=0.49 while the book was 0.41/0.44; a 0.44 YES entry then reported `unrealized_pnl=+$1.00` while the same snapshot's `mid`/`implied_probability` (0.425) said the position was underwater, and 0.49 was *above* the current ask (un-executable). | **fixed** (752a4bf) |
| F1 | report/error | low-med | `work_queue`+`bootstrap`+`coach` flag `fc_rHrzE9WuUh5qmILR` (`mkt_39bP49PZ8zJ2t5Jx`) as `resolve_due` via reason `resolution_at_due_without_score` (resolution_at 00:30Z passed) while the live market is still trading (mid 0.395, accepting_orders). Same family as AX-018 but a **second trigger code**; `coach` adds a "resolve or supersede" nag with no true-close time and no "market still live" state. | **filed** (evidence appended to `trade-trace-fe2f`) |
| F2 | tool/schema | low-med | Adapter leaves structured `resolution_rule.text` **null** while the Gamma `description` carrying the exact rule (Binance ETH/USDT 1-min candle ≥ $2,000) is present in `venue_metadata_json`. Data is fetched + stored; just not mapped. | **filed** (fresh evidence + fix hint appended to `trade-trace-n33z`) |
| — | onboarding | (known) | Cold-start probe re-confirmed AX-022: empty-journal `report.bootstrap` surfaces only read/continuity `suggested_process_calls` (work_queue / next_actions / recall_receipts / strategy.show), no first-run entry-point. | not re-filed (`trade-trace-xqjv`) |

### Positive signals (system behaved well)

- **AX-026** `search_hint` is live this session (field present on `market.search`).
- **AX-023** confirmed live again: `forecast.add` on a freshly adapter-bound
  market with a single `snapshot.fetch` anchor worked end-to-end.
- **AX-025** confirmed: open-position `unrealized_pnl` is populated with
  `mark_state:"available"` and no false `open_no_mark` caveat. (Marking *value*
  was still wrong — that is F3/AX-027, a distinct axis.)
- `memory.recall` returned a directly actionable prior lesson and the new
  reflection round-tripped as the top BM25 hit.
- `report.coach` callouts (forecast-coverage, reflection-hygiene,
  past-resolution-without-score) were honest and actionable.

## Fixes made

- **752a4bf** — `fix(adapter): mark snapshots.price to the within-book mid, not
  off-book lastTradePrice (AX-027)`. One-line change in `_snapshot_from_raw`
  (`src/trade_trace/tools/adapter_polymarket.py`): `price` now anchors to `mid`,
  which already falls back to the last/raw price when no two-sided book exists
  (so no-book markets are unchanged). +2 regression tests in
  `tests/integration/test_adapter_polymarket_offline.py`
  (`test_snapshot_price_marks_to_book_mid_not_off_book_last_trade`,
  `test_snapshot_price_falls_back_to_last_trade_without_two_sided_book`).
  **Effective next fresh session** (the live MCP server in this session still
  served the pre-fix path; the bug was observed against it).

## Beads filed / updated

- `trade-trace-fe2f` — appended F1 evidence (second trigger `resolution_at_due_without_score`; coach resolve-or-supersede dead-end; root gap = obligation never reconciled against bound-market live state).
- `trade-trace-n33z` — appended F2 fresh instance + minimal fix candidate (map Gamma `description`→`resolution_rule.text`, `resolutionSource`→`resolution_rule.source`).
- No new beads filed (F3 fixed directly; F1/F2 dedup to existing open beads).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2272 passed, 7 skipped** (the 7 skips are the opt-in perf
  baselines; +2 vs prior run = the two new AX-027 regression tests).

## Records created this run

- Market `mkt_oMvnHMf7r4kB1Fn4` (Polymarket 2420111, ETH ≥ $2,000 in June), bound via adapter.
- Snapshots `snp_qHFEOZ4qA8d7dPA5` (ETH), `snp_Mj84ys0L_I9LciDx` (`mkt_39bP49PZ8zJ2t5Jx` liveness check).
- Forecast `fc_d-O5P3jEFhjK63gq` (YES 0.44, touch-asymmetry over mid 0.425; resolution_at 2026-07-01T04:00Z), thesis `th_Vs86iu_wb2E-Mt68`.
- Decision `dec_Ohy3cfNGakYcIjCP` (`paper_enter` 20 YES @ 0.44) → position `pos_WJcHuU_6umr7H0HN`.
- Reflection `mem_KrD5guOtsKXrhkGE` (touch-market + lastTrade-mark caveat).

## Handoff notes for next run

- **AX-027 verification:** next fresh session, re-`snapshot.fetch` a live
  market whose lastTrade is away from the book and confirm `data.price ==
  data.mid`. The historical snapshot `snp_qHFEOZ4qA8d7dPA5` still carries the
  pre-fix `price=0.49`; the projection marks against stored snapshots, so
  `pos_WJcHuU_6umr7H0HN` will keep its 0.49-based PnL until a fresh
  (post-fix) snapshot lands. Memory `mem_KrD5guOtsKXrhkGE` still says "marks
  against lastTradePrice" — that mechanism note is now historical; the
  general "reconcile unrealized_pnl sign against the snapshot mid" advice
  still holds.
- **Due forecasts still open (not resolvable):** `fc_CheQKZ-UOTWPVUPs`
  (`mkt_pwuUfbLMcvhkia50`, AX-018) and `fc_rHrzE9WuUh5qmILR`
  (`mkt_39bP49PZ8zJ2t5Jx`, F1) — both markets live. `fc_rHrzE9WuUh5qmILR`'s
  resolution_at (00:30Z) was set too early by a prior run; consider
  `forecast.supersede` with a corrected close once the true close is known.
- **Near-term scoreable:** `fc_uZ-WmwfdrsEtISlE` (BTC <$64k, resolves
  2026-06-04T16:00Z) — the next run after 16:00Z should be able to score it.
- **Open paper positions:** `pos_dIKpqkuqggcQfwKz` (Fed no-change NO, post-FOMC
  June 16–17), `pos_WJcHuU_6umr7H0HN` (ETH ≥ $2,000 YES probe, resolves July 1).
- **Tooling note (still true):** `gh pr edit` fails on this repo (GitHub
  "Projects (classic)" GraphQL deprecation); push updates PR #2 fine, body
  can't be edited via `gh`.
