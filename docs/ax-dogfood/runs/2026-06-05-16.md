# AX dogfood run — 2026-06-05-16

- **RUN_ID:** 2026-06-05-1800
- **Branch:** ax-dogfood (never touched main)
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** one fix landed & gate-verified (lszg silent-truncation residual); two beads annotated with new evidence; no new beads filed; no reverts.

## Summary (as a bot)

Drove the full live BTC loop. Bootstrap surfaced exactly **3 resolve_due
obligations**, all `resolution_at_missing` — the known fe2f false-positive class
(`fc_CheQKZ`/`mkt_pwuUfbLMcvhkia50` re-confirmed **live**: snapshot mid 0.505,
accepting_orders:true; plus the two 2027-close recession markets). Nothing was
genuinely resolvable, so I recorded **zero resolutions** rather than fabricate
any. Scanned live Gamma, bound the June-6 BTC noon-ET ladder ($64k/$66k), and
applied calibration discipline: the two strikes triangulate to a coherent spot
~$59.5k / ~22h σ ~$2.8k (elevated in a selloff; oneDayPriceChange negative across
the ladder), so genuine edge ≈ 0. Forecast $64k YES 0.05 (shaded below mid 0.0575
per my documented near-money/tail-YES overconfidence) and $66k YES 0.01 (≈ mid),
then **skipped** with `insufficient_edge` rather than open a paper position into
the 7% crypto fee — manufacturing edge from a coherent ladder would violate my own
durable lesson. Calibration is conclusive (N=32, sample_warning null): Brier 0.089,
skill +0.583; reliability bins reaffirm near-money-YES overconfidence (0.56–0.66
band gap +0.597) while my new tail forecasts land in well-calibrated low bins.
Coach was genuinely actionable and correctly did **not** false-flag my
forecast-linked skip (AX-013 path holds). Then engineered the lszg live-priority
residual.

## Friction found

| # | surface | severity | finding | disposition |
|---|---------|----------|---------|-------------|
| F1 | report (work_queue/bootstrap) | low (known) | 3 resolve_due flagged on still-live/2027 markets, all `resolution_at_missing` — `fc_CheQKZ` proven live (mid 0.505, accepting_orders) | known → **fe2f** (not re-filed) |
| F2 | tool/schema | low (known) | `resolution_rule.text` null on both June-6 BTC strikes (Binance noon-ET rule lives only in `venue_metadata_json.description`) | known → **n33z** (not re-filed) |
| F3 | tool (memory.recall) | medium (known, sharpened) | unique body-token round-trips under `strategies=['bm25']` (rank 1, bm25 provenance) BUT the same token in a multi-word **fused** query surfaces the node via temporal/graph only — **bm25 provenance absent**; bm25's contribution is masked under multi-word fused RRF | evidence appended to **95ry** |
| F4 | report/onboarding | low (known) | cold-start bootstrap on an empty journal suggests only read/continuity calls (no `market.search→bind→forecast` entry-point) | known → **xqjv** (not re-filed) |
| F5 | tool/report | medium (live priority) | `report.current_exposure` discarded the composed open_positions `truncated`/`next_cursor` and whitelisted `limit` without `cursor` → a `limit`-bounded exposure read silently dropped rows | **FIXED 543143f** (lszg residual) |
| (obs) | tool (memory.recall) | none | `mode` rejects `bm25` with a self-documenting `VALIDATION_ERROR` (allowed: fused/per_strategy); isolate a strategy via `strategies=[...]` | working as designed, not filed |

## Fixes made (gate-verified)

- **543143f** — `fix(reports): paginate report.current_exposure to kill silent
  exposure truncation (trade-trace-lszg)`. `_report_current_exposure` now
  propagates `truncated`/`next_cursor` from the composed `report.open_positions`,
  whitelists `cursor` into the open_positions passthrough so paging works, and
  the schema advertises `limit`+`cursor` (mirroring `report.open_positions`).
  Closes the lszg **silent-truncation correctness residual**. +2 regression tests
  (`test_current_exposure_limit_surfaces_truncation_and_cursor_pages_remainder`,
  `test_current_exposure_schema_advertises_limit_and_cursor`).
  *Effective next fresh MCP session* (long-lived server); verified by unit test.

## Beads filed / updated

- **trade-trace-lszg** (updated, still open) — silent-truncation residual fixed
  this run (543143f). Confirmed live that e8e3005 (`groups→{ref,count}`) is
  effective and `event_exposure_sets` track the limited page (the "unbounded
  regardless of limit" worry is not current behavior). Remaining is genuinely
  design-level/lower-severity: (a) default no-limit call still defaults to 100
  rows so a very high position count can exceed the cap on the documented default;
  (b) per-event-set identity still embeds two 78-digit CLOB token IDs per row
  (size optimization, not correctness). Recommend downgrade/rescope to the
  default-page-size question now that the acute hazard is gone.
- **trade-trace-95ry** (updated, in_progress) — sharpened recall round-trip
  evidence (F3 above).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` — **All checks passed.**
- `mypy src` — **Success: no issues found in 178 source files.**
- `pytest -q` — **2308 passed, 7 skipped** in ~116s. (+2 vs run 15's 2306, the two
  new current_exposure regression tests.)

## Ledger writes this run

- Forecasts: `fc_c8-WsME5-zWDFkt3` (BTC>$64k June-6, YES 0.05),
  `fc_T24promAUcYn-g4C` (BTC>$66k June-6, YES 0.01) — both `resolution_at`
  2026-06-06T16:00:00Z, score next cycle after close.
- Decision: `dec_1TxM2FiUwZN-zL9X` (skip $64k, `insufficient_edge`, forecast-linked).
- Reflection: `mem_79LgYyGt7NvRzOzX` (coherent-ladder-no-edge + recall probe).
- Resolutions: **none** (nothing genuinely resolvable; did not fabricate).

## Handoff for next run

- Branch green & clean; PR #2 open; main untouched.
- **Carryable opens:** June-6 BTC forecasts `fc_c8-WsME5-zWDFkt3` ($64k) +
  `fc_T24promAUcYn-g4C` ($66k) and last run's $60k/$62k — all score after
  2026-06-06T16:00Z (resolve them from snapshot mid next cycle past close).
  Open paper positions: `pos_7UesaoYvZwuuK-2s` (NO $62k June-6),
  `pos_Vxe1iw_l7dUvAxMf` (YES $55k-dip June), `pos_JCFxgMzKD53ZGSor` (NO $62k
  June-5, already mid 0.001 → effectively settled NO). The June-3/4
  `resolution_at_missing` forecasts remain genuine fe2f false-positives.
- **lszg** acute correctness hazard is now resolved; the live priority for the
  human/next owner is the design call on default page size + token-id trim, or a
  downgrade. The fix lands in the live MCP only on the next fresh server boot.
- Stashes {0,1} (axloop-debris 1500/1600) remain **human-owned**, untouched.
