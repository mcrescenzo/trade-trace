# AX dogfood run 2026-06-04-05

- **RUN_ID:** 2026-06-04-0400
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home `~/.trade-trace-axloop` · live Polymarket
- **Mode:** bounded single lifecycle pass (cold bot → informed engineer)

## Summary (as a bot)

The loop felt *productive* this run: for the first time it produced a genuinely
**scored** forecast (Knicks/Spurs NBA market resolved live; my YES@0.37 → Brier
0.3969), which exposed the headline trap. Orientation was smooth —
`report.bootstrap` → `work_queue` surfaced two `resolve_due` obligations, one
genuinely closed (NBA) and one a known false-flag (still-live BTC market). The
big friction: resolving a closed forecast through `resolution.add` **silently
failed to score** because the field that gates auto-scoring (`confidence`) is
absent from the tool's advertised MCP schema. I only recovered by passing the
unadvertised field on a hunch. Search/bind/snapshot/forecast/decision/memory all
behaved; prior PnL + search fixes held live.

## Friction found

| id | surface | severity | what happened |
|----|---------|----------|----------------|
| **AX-030** | tool/schema/correctness | **high** | `resolution.add` (outcome.add) auto-derived MCP schema omits `confidence` (and `settlement_price`/`metadata_json`/`resolution_source_url` from `example_rich`). Auto-scoring **requires** `confidence>=0.9` + a binary label, so a bot calling with only the advertised fields gets `auto_scoreable:false`, **no `forecast_score`, and no error** — the run-fatal silent trap. Confirmed live: 3 `resolved_final` rows on mkt_39bP49PZ8zJ2t5Jx, only the one with `confidence=0.95` scored fc_rHrzE9WuUh5qmILR. |
| **AX-031** | tool/adapter/data-quality | medium | `snapshot.fetch` on a market lacking `book`/`liquidity`/`orderBook`/`depth` (sports + closed/resolved markets) dumped the **entire ~5KB raw Gamma payload** into `liquidity_depth_json` via an `or raw` fallback. Live ETH/BTC markets stored a clean numeric `"21232.26239"`; the resolved Knicks market stored the whole object (conditionId, clobTokenIds, full description…). Pollutes a depth column + bloats snapshot-embedding reports. |
| AX-017 (re-confirmed) | tool/schema | — | Two fresh binds (mkt_SohHCLs3qtCWRWbT ETH>$1,800; mkt_Vh36actmypkVNBiq BTC reach $77.5k) carry the full Binance rule in `venue_metadata_json.description` but `resolution_rule.text` is null. Evidence appended to `trade-trace-n33z`. |
| AX-018 (re-confirmed) | report | — | mkt_pwuUfbLMcvhkia50 / fc_CheQKZ flagged `resolve_due` (`resolution_at_missing`) while live (mid 0.505, accepting_orders:true). Evidence appended to `trade-trace-fe2f`. |
| AX-022 (re-confirmed) | onboarding | — | Cold-probe bootstrap on a fresh journal still suggests only read/continuity tools (work_queue/next_actions/recall_receipts/strategy.show); no first-run market.search→bind→forecast entry point. Already filed `trade-trace-xqjv`. |

### Fixes verified holding live
- **AX-019/026** — `market.search` query works (bitcoin→8, ethereum→6; no false no-op).
- **AX-013** — `skip` links `forecast_id` (fc_viRadR7vKtpYsYN9 on the BTC skip); coach didn't false-flag.
- **AX-025/028** — open BTC-dip position pos_CwabrU-E9BKf00jk marked −$0.01 vs live mid 0.4335 (side-aware, fresh, non-null re-marked).
- **AX-027** — `snapshot.price == mid` on every fetch (ETH 0.579, BTC 0.095, NBA 0.9995).

## Fixes made (atomic commits)

| commit | item | what |
|--------|------|------|
| `d03fd22` | AX-030 | Add `auto_score_block_reason()` (`_finality.py`) and surface it as `auto_score_skipped_reason` on the `outcome.add` result (success + idempotent-replay paths), so a non-scoring `resolved_final` write explains itself (missing/low confidence, or non-binary label). Document `confidence` in `outcome.add`'s `example_rich` so `tool.schema` advertises it. +3 regression assertions in `test_polymarket_resolution_finality`. No contract change. |
| `9e10fc2` | AX-031 | Drop the `or raw` fallback in `_snapshot_from_raw`; when no depth/liquidity field exists, store `{}` not the whole market. +regression test (`test_snapshot_from_raw_without_depth_fields_does_not_dump_whole_payload`). |

Both effective **next fresh MCP session** (long-lived server won't hot-reload).

## Beads filed / updated

- **Filed:** none (both new findings fixed directly).
- **Updated with fresh evidence:** `trade-trace-n33z` (AX-017), `trade-trace-fe2f` (AX-018).

## Attempted-then-reverted

- None.

## Live journal writes this run

- Resolved fc_rHrzE9WuUh5qmILR (NBA Knicks win, YES, Brier 0.3969) — first scored forecast in the calibration panel (N=1).
- Bound mkt_SohHCLs3qtCWRWbT (ETH>$1,800 Jun 4) + mkt_Vh36actmypkVNBiq (BTC reach $77.5k Jun); snapshots snp_-8VlotZt7gDU1nSe / snp_12NS-Mbp7lDfhsx1.
- Forecast fc_1yoUooDa7wbjasEk (ETH YES 0.56, resolves today 16:00Z → scoreable next run) + paper probe pos_yEG4IYrG-y7tz55U (10u YES @0.58).
- Forecast fc_viRadR7vKtpYsYN9 (BTC YES 0.08) + skip (insufficient_edge).
- Reflection mem_Y58o--t2Cgp_5YXv (the confidence trap) — round-trips as top recall hit.

## Gate results

- `ruff check src tests` — **pass** (all checks passed).
- `mypy src` — **pass** (178 source files, no issues).
- `pytest -q` — **2274 passed, 7 skipped** (skips are perf opt-ins) in 102s.

## Handoff for next run

- **Score-due next run:** fc_1yoUooDa7wbjasEk (ETH>$1,800) resolves 2026-06-04T16:00:00Z — fetch the Binance ETH/USDT noon-ET close and resolve it. **Pass `confidence` explicitly** (the AX-030 fix is server-code only; the live tool schema still won't advertise it until the next fresh server boot, but the value is accepted).
- **Open positions accumulating (8 → 9):** pos_CwabrU (BTC dip $57.5k YES) + new pos_yEG4IYrG (ETH>$1,800 YES). Exit/resolve as their markets close; the ETH probe resolves today.
- **Three forecasts will re-flag `resolve_due`:** fc_CheQKZ (still-live AX-018), fc_viRadR7vKtpYsYN9 (BTC ~07-01), fc_1yoUooDa7wbjasEk (ETH today). Don't fabricate outcomes.
- **Data-hygiene note from the AX-030 trap:** mkt_39bP49PZ8zJ2t5Jx now has 3 `resolved_final` outcome rows (2 unscored from the retry hunt + 1 scored). Harmless but a reminder the trap leaves litter; the new `auto_score_skipped_reason` should prevent repeat fumbling.
- **Highest-priority untouched bead:** `trade-trace-4z2t` (market.refresh unusable, P1).
- Beads remain embedded-Dolt local-only (no remote). PR #2 stays open, not merged. `main` untouched.
