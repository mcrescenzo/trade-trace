# AX dogfood run report — 2026-06-04-06

- **RUN_ID:** `2026-06-04-0500`
- **Branch:** `ax-dogfood` (never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma) enabled
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (how it felt as a bot)

A smooth, low-friction pass — the substrate behaved. Bootstrap → work_queue →
lifecycle oriented me cleanly; the single derived obligation was the known
still-live false-positive (`fc_CheQKZ`), which I correctly declined to
fabricate an outcome for after a fresh `snapshot.fetch` confirmed the market
is live (mid 0.505, accepting_orders). I scanned live markets (`market.search`
clean on `fed` and `nba finals`), bound a genuinely fresh market (Knicks 2026
NBA Finals, `mkt_u7ElNEsQPGbt8RxK`), snapshotted it, recalled prior lessons
(round-tripped well, including the AX-030 confidence-trap and AX-017
read-the-description disciplines), forecast my honest no-edge probability at
the mid (0.52 vs mid 0.5235), and recorded a disciplined `skip`
(insufficient_edge) with the forecast linked. Calibration (N=1) and coach were
honest and actionable. The one **new** friction was an enum-rejection error
that named the bad value but not the allowed set — fixed directly.

Nothing genuinely resolvable was due this pass: the two 16:00Z forecasts
(`fc_uZ`, `fc_1yo` ETH>$1,800) don't close for ~11 hours, and every other open
forecast resolves 06-07 or later.

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-032** | error | low | `report.lifecycle` `states` rejected `"active"` with `VALIDATION_ERROR "unsupported lifecycle state(s): ['active']"` — echoed the bad value but did not list the allowed enum, forcing trial-and-error. Same self-documenting-error class as the fixed AX-004/005. | **fixed** `02150f3` (+regression test) |
| AX-018 | report | (known) | `fc_CheQKZ-UOTWPVUPs` / `mkt_pwuUfbLMcvhkia50` still the sole `resolve_due_forecast` (reason `resolution_at_missing`) while `snapshot.fetch` shows mid 0.505, bid/ask 0.50/0.51, accepting_orders:true, $800K volume — genuinely live. | re-confirmed → `trade-trace-fe2f` (evidence appended) |
| AX-017 | tool/schema | (known) | Both fresh binds this run (`mkt_M4lkGA2bEtaMx5jB` Fed, `mkt_u7ElNEsQPGbt8RxK` Knicks) returned `resolution_rule.text=null` while `venue_metadata_json.description` carried the full rule. | re-confirmed → `trade-trace-n33z` (evidence appended) |
| AX-022 | onboarding | (known) | Cold-probe journal bootstrap surfaced only read/continuity `suggested_process_calls` ([work_queue, agent.next_actions, recall_receipts, strategy.show]), 0 obligations, empty idea queues — no first-run entry point. | re-confirmed → `trade-trace-xqjv` (evidence appended) |

### Behaviors that worked (no action)

- `market.search` clean and useful on both `fed` and `nba finals` (AX-019/026 fixes holding; `search_hint:null` on good queries).
- `snapshot.fetch.price` anchored to **mid** (0.5235), not `lastTradePrice` (0.518), on a brand-new deep market — **AX-027 fix confirmed live on a fresh bind**.
- `liquidity_depth_json` was a clean numeric (`220947.94464`) on both live markets — **AX-031 class does not bite** markets that expose a Gamma `liquidity` field (it only fired on closed/sports markets lacking one).
- `decision.add skip` linked `forecast_id` without the AX-013 false-flag.
- `report.calibration` / `report.coach` honest at N=1 (sample_warning + 5%-coverage callout + actionable next_actions: keep logging to the 20 floor, add reflections).
- `memory.recall` / `memory.retain` round-tripped (new lesson `mem_wcqRG-22P2A44m4y` came back as the top BM25 hit).

## Fixes made (with SHAs)

- **`02150f3`** — `fix(reports): list allowed states in report.lifecycle invalid-state error (AX-032)` + regression test `test_report_lifecycle_invalid_state_error_lists_allowed_values`.

## Beads filed

- None new. Re-confirmation evidence appended to `trade-trace-n33z` (AX-017), `trade-trace-fe2f` (AX-018), `trade-trace-xqjv` (AX-022).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2275 passed, 7 skipped** (the +1 vs prior run is the AX-032 regression test; 7 skips are perf-opt-in baselines).

## Journal artifacts this run

- Bound fresh: `mkt_u7ElNEsQPGbt8RxK` (Knicks 2026 NBA Finals).
- Forecast: `fc_m6WgMoAmjzx-1nxg` (p_yes=0.52, anchored snapshot implied 0.5235), thesis `th_fNwy1y2-gweWMER7`.
- Decision: `dec_I2qSwqmyaVu_PPfn` (skip, insufficient_edge, forecast-linked).
- Snapshots: `snp_e8KvpMUka3l4ocJP` (Knicks), `snp_4zUs7OvsAVA67L_5` (CheQKZ live-check).
- Memory: `mem_wcqRG-22P2A44m4y` (series-winner repricing lesson).
- Re-bound idempotently (returned existing rows): `mkt_M4lkGA2bEtaMx5jB` (Fed 25bps-decrease — already forecast `fc_yetUV6UW2bD9gkZc`; 0.0085 tail, no two-sided edge, left as the prior skip).

## Handoff notes for next run

- **Due ~16:00Z today (2026-06-04):** `fc_uZ-WmwfdrsEtISlE` (`mkt_BxqIYenOQjh6ta1l`) and `fc_1yoUooDa7wbjasEk` (`mkt_SohHCLs3qtCWRWbT`, ETH>$1,800) both resolution_at 2026-06-04T16:00:00Z. If a run fires after 16:00Z, resolve from the relevant venue close and **pass `confidence` explicitly** — the live `resolution.add` MCP schema still won't advertise it (AX-030 fix touches `example_rich`, not the auto-derived minimal schema; effective only on a fresh server boot). Don't fabricate.
- **AX-032 fix** is effective **next fresh MCP session** (server won't hot-reload); live tool still echoes the old error this session.
- `fc_CheQKZ` will keep re-flagging resolve_due while live (AX-018) — expected; don't fabricate.
- Calibration is stuck at **N=1** until the first batch of near-term forecasts resolves (earliest the two 16:00Z ETH forecasts today, then 06-07/06-10/06-15/06-17). Coverage 5% (1/20 decisions scored) is honest, not a bug.
- Highest-priority untouched bead remains `trade-trace-4z2t` (market.refresh, P1).
- `.beads` dir perms warning (0770) recurs on every `bd` call — cosmetic, not this loop's concern.
