# AX dogfood run 2026-06-04-04

- **RUN_ID:** `2026-06-04-0300`
- **Branch:** `ax-dogfood` (confirmed; never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket enabled
- **Mode:** one bounded lifecycle pass (verification-heavy; 2 forecasts, 1 paper probe)

## Summary (as a bot)

A smooth pass. Orientation (`report.bootstrap` → `report.work_queue`) worked
cleanly and the previously-fixed surfaces held up live: `market.search`
returned clean hits with no spurious `search_hint` (AX-019/026), `snapshot.fetch`
anchored `price` to the within-book **mid** (AX-027), and the open-position
PnL marking was **side-aware and fresh** against the current mid (AX-025/027/028).
The canonical loop — bind → snapshot → recall → forecast → decision → review →
retain → recall — ran end-to-end without a single validation dead-end.

The one genuinely new friction was an orientation report that is unusable at the
MCP surface: `report.lifecycle` returned a ~51KB result that **exceeded the MCP
token cap** and failed at the transport layer, with no partial result and no
narrowing hint — because it has no pagination and double-serializes every case.

## What I exercised

| Step | Detail |
|------|--------|
| Orient | `report.bootstrap` (truncated/partial, fine), `report.work_queue` (2 resolve_due obligations) |
| Resolve-due | Both flagged forecasts' markets are **still live** (mids 0.505 / 0.575, `accepting_orders:true`) — did NOT fabricate resolutions; forecasts stay open. Re-confirms AX-018/fe2f. |
| Scan | `market.search bitcoin` / `fed` → clean hits, `search_hint:null` |
| Bind | Fed "no change" (`mkt_F3k2AAPzU_S54ID7`), BTC reach \$90k (`mkt_Weg9U9Ul6g19DJuO`), BTC dip \$57.5k (`mkt_2T1L59jjnoYXgS39`) |
| Forecast | `fc_sENzjU_szu8wUQ02` Fed YES 0.96 (calibration-only, tail); `fc_S4PzoUssLBRUPsi-` BTC-dip YES 0.47 (touch-window lean) |
| Decision | `dec_M2ydQqBrJL2EHbJ4` paper_enter BTC-dip YES @0.434 ×20 (`pos_CwabrU-E9BKf00jk`); `dec_mAf0bW3vXIH21_Fa` skip Fed (insufficient_edge, linked `forecast_id` — AX-013 path) |
| Review | `report.open_positions` row: YES ×20, `unrealized_pnl -0.01`, `mark_state:available` (entered 0.434 vs mid 0.4335 → side-aware, fresh). `report.coach` actionable. |
| Retain | `mem_4qoPE859q5bgq5T-` (PnL-sign now trustworthy on a fresh probe; supersedes the caveat in `mem_KrD5guOtsKXrhkGE`) — round-tripped via `memory.recall` (BM25 rank 1) |
| Cold probe | throwaway journal init + bootstrap → only read/continuity `suggested_process_calls`; re-confirms AX-022/xqjv |

## Friction found

| id | surface | severity | item | disposition |
|----|---------|----------|------|-------------|
| AX-029 | tool (`report.lifecycle`) | medium | Full output (~51KB at 22 cases) exceeds the MCP token cap and fails at the transport layer; no pagination (only `filter`/`states`), `truncated:False` hardcoded, and each case is serialized **twice** (`groups` + `lifecycle_cases`). No in-surface narrowing hint; a bot following the orientation sequence hits a hard wall. | **filed** `trade-trace-hv19` |
| AX-018 (re) | report/error | low | Both pending forecasts still false-flag `resolve_due` while their markets are live; `report.coach` nags "resolve or supersede" with no "market still live" state. | re-confirmed → evidence appended to `fe2f` |
| AX-017 (re) | tool/schema | low | `resolution_rule.text` null on two fresh markets (Fed, BTC-dip) while `description` carries the full rule. Adapter (`adapter_polymarket.py:92-96`) maps text only from `resolutionCriteria`/`rules` (absent in Gamma), never falls back to `description`. | re-confirmed → evidence + minimal-fix candidate appended to `n33z` |

## Verified-fixed-and-holding (live this session)

- **AX-027** — `snapshot.fetch` `price` == within-book `mid` (0.4335), not off-book `lastTradePrice` (0.431).
- **AX-025 / AX-028** — `report.open_positions` re-marks the open row side-aware from the latest mark (`unrealized_pnl -0.01`, not a stale/phantom value).
- **AX-019 / AX-026** — `market.search` returns clean hits; `search_hint:null` on multi/single-term hits.
- **AX-013** — `skip` decision accepts and links `forecast_id` (Fed skip linked `fc_sENzjU_szu8wUQ02`).

## Fixes made

None. No `src/` or `tests/` changes this run — the new finding (AX-029) has no
honest small fix (the result is too large to return at all, so an in-result
caveat can never surface; the real fix is pagination/de-duplication, a
design-level envelope-pattern rework → filed).

## Beads filed / updated

- **Filed:** `trade-trace-hv19` (AX-029, P2 task) — `report.lifecycle` pagination + double-serialization.
- **Updated:** `trade-trace-fe2f` (AX-018 re-confirm), `trade-trace-n33z` (AX-017 re-confirm + minimal-fix candidate).

## Attempted-then-reverted

None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2273 passed, 7 skipped** (7 are perf-baseline opt-ins) in ~103s

## Handoff notes for next run

- **`pos_CwabrU-E9BKf00jk`** (BTC dip \$57.5k YES ×20 @0.434) is open; the dip
  market resolves on any Binance 1-min Low ≤ \$57,500 through 2026-06-30 — check
  for a touch and `paper_exit` / `resolution.add` when it resolves.
- Three forecasts now sit open and will be `resolve_due`-flagged next run:
  `fc_CheQKZ-UOTWPVUPs`, `fc_rHrzE9WuUh5qmILR` (both still-live, AX-018), and the
  two new ones (Fed resolves ~2026-06-17, BTC-dip ~2026-07-01). Do NOT fabricate
  outcomes — resolve only on real closure.
- `trade-trace-hv19` is the freshest open AX bead; `4z2t` (market.refresh, P1)
  remains the highest-priority untouched item.
