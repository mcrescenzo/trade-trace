# AX dogfood run — 2026-06-06-02

- **RUN_ID:** 2026-06-06-0100
- **Branch:** ax-dogfood
- **Actor:** agent:ax-dogfood · journal home ~/.trade-trace-axloop · live Polymarket
- **Disposition:** Standing-confirmation run — **NO new friction**. Three run-08→01 fixes verified live on this fresh server; ~12 standing items re-confirmed.

## Summary (as a bot)

Drove the full canonical loop on the live **BTC June month-window touch
ladder** (settle 2026-07-01T04:00Z, Binance BTC/USDT 1m High/Low). Bootstrap
oriented cleanly to 3 obligations (all the standing `resolution_at_missing`
false-positives), 1 active strategy, `active_ideas` nuked. Resolved nothing
fabricated: the 3 "due" markets were each proven **live** via `market.refresh`
(Rihanna/GTA close 2026-07-31, UK-recession 2027-03-31, US-recession
2027-01-31) and left open. Bound a coherent 3-strike ladder, snapshotted each,
recalled prior lessons, forecast all 3, took ONE thin disciplined position and
skipped the rest, reviewed calibration/coach/exposure, retained a reflection
and round-tripped it. It felt like a mature, friction-free session: every
surface behaved as designed or as an already-filed standing item.

### Market read

Ladder at 01:03Z triangulated BTC spot to **~$59-60k with downward momentum**:

| market | mkt_id | YES mid | note |
|---|---|---|---|
| reach $77.5k | mkt_Vh36actmypkVNBiq | 0.044 | deep OTM upside tail |
| dip $55k | mkt_NEPHuqH1YQ2Wq-8F | 0.3985 | oneDayPriceChange **+0.175** (sharp intraday selloff repriced downside-touch up) |
| dip $50k | mkt_78SJcfosqQz28xTt | 0.1465 | downside tail |

Coherent monotonic ladder ⇒ genuine edge ~0.

- **Forecasts:** dip-55k YES **0.40** (`fc_8cA1LaYQ9y74Wd77`, at mid — selloff
  already repriced, refused to chase); reach-77.5k YES **0.035**
  (`fc_MPZ0_f7qOTwiPe1b`, below mid — tail overpricing); dip-50k YES **0.12**
  (`fc_40oESG-ZmBF8zKsQ`, below mid).
- **Decision:** ONE thin position — 15u **NO on dip-50k @0.8535**
  (`pos_7Cvu2i8Qd_eFs_5r`, `declared_risk_amount=12.80`) selling the
  overpriced downside tail; skipped dip-55k and reach-77.5k with
  `insufficient_edge` into the 7% crypto fee.
- **Self-check:** dip-50k 0.12 lands in reliability **bin 3 (0.12-0.20,
  observed 0, gap +0.165)** — the standing overconfident-YES band; shading
  below mid AND taking the NO side both align with the evidence that these
  downside touches settle NO. Calibration N=33 Brier 0.1087 skill 0.485.

## Friction found

**None new.** This is the fourth standing-confirmation run (cf. runs 11, 19,
21). Every observation maps to an already-open bead or an already-fixed item.

## Fixes made

**None.** No code changed (deliberately — no new friction to fix; manufacturing
a change would be backlog noise).

## Beads filed

**None.** All Phase B observations dedup against the open queue
(fe2f/xqjv/s7nn/95ry/n33z/scx8/1y9s/lszg).

## Attempted-then-reverted

**None.**

## Verified live this run (fresh cron-booted server)

- **AX-068** (`4cb1307`) — `market.refresh` functional on **stale/aged** rows:
  3× `ok:true` with `idempotency_key` on the 3 due markets; each re-synced from
  live Gamma, `state_changed:false`, all confirmed `open`. No `KeyError`.
- **AX-069** (`a501ca5`) — `tool.schema market.refresh` advertises
  `required:["market_id","idempotency_key"]`; my first schema-following call
  succeeded (no `VALIDATION_ERROR` on the missing key that bit the cold bot
  last run).
- **AX-064** (`db45fb6`) — `market.bind` schema advertises the
  `source`/`state`/`mechanism`/`resolution_source`/`ambiguity_kind` enums.

## Standing items re-confirmed (no change)

- **AX-027** — `snapshot.fetch price`=mid on all 3 (0.044 / 0.3985 / 0.1465).
- **AX-031** — numeric `liquidity_depth_json` (37941 / 25931 / 35887).
- **AX-037 / n33z** — all 3 adapter binds preserved caller
  `resolution_rule_text` (provenance=caller_supplied); venue
  `resolution_rule.text` null, Binance window rule description-only.
- **AX-045 / 025 / 028** — dip-50k NO position carries
  `initial_risk_amount:12.8`, no `missing_risk_budget`; side-aware NO mark
  (entry 0.8535 vs mark mid 0.1465 → `unrealized_pnl:0`, entered at complement).
- **AX-066 / e8e3005** — `report.open_positions groups:{ref:"event_exposure_sets",count:1}` (no full duplicate).
- **AX-013** — both skips linked `forecast_id`; `coach.unscored_forecasts.count:0`.
- **AX-018 / fe2f** — exactly 3 `resolution_at_missing` flagged resolve_due
  (all proven live via `market.refresh`, fabricated nothing); 3 new forecasts
  with explicit `resolution_at`=2026-07-01 did NOT flag.
- **AX-048** — `coach.top_mistakes/top_strengths` `[]`.
- **AX-040 / s7nn** — `abstention_coverage` 2/96 (2.08%); the 2 new skips this
  run remain uncounted (only first-class `abstention.record` credited).
- **AX-024 / 95ry** — bm25-only recall of unique token
  `WOMBAT-DIP50K-J6-0100` → rank 1 with `bm25:[1]` provenance; the fused
  multi-word query *containing the verbatim token* surfaced it at #2 via
  `temporal[1]/graph[9]` only, **no bm25 provenance** — bm25 contributes
  nothing to the fused RRF ranking even on a verbatim unique-token match.
- **AX-038 / dlup** — bootstrap `active_ideas` nuked 0/87 `max_chars`;
  obligations survived 3/3 at small N.
- **AX-022 / xqjv** — cold throwaway-journal bootstrap → only read/continuity
  `suggested_process_calls` (`report.work_queue`/`agent.next_actions`/
  `report.recall_receipts`/`strategy.show`), `obligations:0`, no first-run
  `market.search→bind→forecast` breadcrumb.

## Gate result

`ruff check src tests` clean · `mypy src` clean (178 files) ·
`pytest -q` → **2313 passed, 7 skipped** (120.64s). Identical to the prior
baseline — confirms no code change and a healthy branch.

## Handoff for next run

- Branch HEAD healthy at 2313p/7s; no stranded WIP, no env anomaly.
- This run's 3 new forecasts (dip-55k/reach-77.5k/dip-50k, all
  `resolution_at`=2026-07-01) and the dip-50k NO position settle at the June
  month-window close (July 1); they will become resolvable inventory then. The
  `outcome.fetch` no-RPC path means resolution will come from `snapshot.fetch`
  mid / `market.refresh` after close — not before.
- Open design-Q queue unchanged (8 ax-dogfood beads). The report-surface and
  write-vein catalogs are exhausted; remaining novelty is mostly behavioral
  re-confirmation + waiting on month-window settlements.
- Calibration N=33 stable (month-window forecasts don't settle until July 1);
  overconfident-YES (bin 7 gap +0.693) standing.
