# AX dogfood run report — 2026-06-04-07

- **RUN_ID:** `2026-06-04-0600`
- **Branch:** `ax-dogfood` (never touched `main`)
- **Actor:** `agent:ax-dogfood` · journal home `~/.trade-trace-axloop` · live Polymarket (Gamma) enabled
- **Mode:** one bounded lifecycle pass (bot → engineer)

## Summary (how it felt as a bot)

Another smooth, low-friction pass — the substrate behaved. Bootstrap →
work_queue → lifecycle oriented me cleanly; the single derived obligation was
the known still-live false-positive (`fc_CheQKZ`), which I declined to fabricate
an outcome for after a fresh `snapshot.fetch` confirmed the market is live (mid
0.505, accepting_orders:true, $800K volume). I scanned live markets
(`market.search` clean on `bitcoin` and `fed`, `search_hint:null`), worked the
BTC June "reach $X" touch ladder, and ran two honest forecasts: a deep-tail
calibration-only forecast on **$90k** (YES 0.0105 → p_yes 0.02, paired with a
disciplined `skip(insufficient_edge)` per my own no-manufactured-edge lesson),
and a genuine small probe on **$70k** (YES 0.36, freshly crashed −0.325 on the
day) where I applied the durable touch-probability-lower-bound lesson
(`mem_4MIojgkFHXh8GPHC`) to lift to p_yes 0.43 and size a 20-unit YES probe.
`memory.recall` round-tripped well and surfaced exactly the right priors.
Calibration (N=1) and coach were honest and actionable, and coach correctly did
**not** false-flag the linked skip (AX-013 holding).

The one **new** finding was a design-level ergonomics/safety gap (filed, not
fixed): `decision.add(paper_enter)` opened a *second* position row on a market
where a prior run already had an open position on the same side, with no
`already_exposed` caveat at decision time. Exposure nets correctly in reports,
so it is not a correctness bug — but an autonomous hourly feeder silently
accumulates parallel position rows. Nothing genuinely resolvable was due this
pass (the two ETH 16:00Z forecasts don't close for ~10 hours).

## Friction found

| id | surface | severity | description | disposition |
|----|---------|----------|-------------|-------------|
| **AX-033** | tool/report | low | `decision.add(paper_enter)` always mints a fresh `position_id` (`decision.py:217-231`) and never looks up an existing open position on the same instrument+side; there is no paper `add` that increases an existing paper position. Live: a 20-unit YES probe (`pos_dTqU6DR07HnbZHvd` @ 0.37) was opened on `mkt_7tACn4aqhw6y8BwN` while a prior-run 100-unit YES position (`pos_EmNSXb2bDMWpFdub` @ 0.54) was already open, with **no** `already_exposed` signal — even though the decision matrix defines `already_exposed` as a materiality reason. Exposure nets in reports (120 YES, side-aware to mid), so not a marking bug (distinct from AX-025/027/028). | **filed** → `trade-trace-scx8` (design Q, P3) |
| AX-018 | report | (known) | `fc_CheQKZ-UOTWPVUPs` / `mkt_pwuUfbLMcvhkia50` still the sole `resolve_due_forecast` (reason `resolution_at_missing`) while `snapshot.fetch` shows mid 0.505, bid/ask 0.50/0.51, accepting_orders:true, $800K vol — genuinely live. Declined to fabricate. | re-confirmed → `trade-trace-fe2f` |
| AX-017 | tool/schema | (known) | All three fresh BTC binds (`mkt_Weg9U9Ul6g19DJuO` $90k, `mkt_Vh36actmypkVNBiq` $77.5k, `mkt_7tACn4aqhw6y8BwN` $70k) returned `resolution_rule.text=null` while `venue_metadata_json.description` carried the full Binance touch rule. | re-confirmed → `trade-trace-n33z` |
| AX-022 | onboarding | (known) | Cold-probe journal bootstrap surfaced only read/continuity `suggested_process_calls` ([work_queue, agent.next_actions, recall_receipts, strategy.show]), 0 obligations, no active strategies — no first-run entry point. | re-confirmed → `trade-trace-xqjv` |

### Behaviors that worked (no action)

- `market.search` clean and useful on `bitcoin` and `fed` (AX-019/026 holding; `search_hint:null` on good queries; proper binary candidates).
- `snapshot.fetch.price` anchored to **mid** (0.36 on $70k, not `lastTradePrice` 0.35; 0.505 on CheQKZ) — **AX-027 fix confirmed live this session**.
- `report.open_positions` marked **both** rows side-aware to mid: fresh probe (entry 0.37, mark 0.36 → −$0.20) and the prior-run row (entry 0.54, mark 0.36 → −$18.00) — **AX-025/028 fixes holding live**, including a non-null stale-mark refresh on the carried position.
- `liquidity_depth_json` clean numeric on all three live binds (79893.77518 / 57003.1592 / 31483.4663) — **AX-031 class does not bite** markets exposing a Gamma `liquidity` field.
- `decision.add skip` linked `forecast_id` without the AX-013 false-flag; `report.coach` honest at N=1 (4.5% coverage callout + actionable next_actions).
- `memory.recall` / `memory.reflect` round-tripped (new lesson `mem_4zRTSDdMFuzcfAJs` came back at rank 2).

## Fixes made (with SHAs)

- None. No new direct-fix friction surfaced this run.

## Beads filed

- **`trade-trace-scx8`** (AX-033, P3 design Q) — `decision.add(paper_enter)` opens a new position row / no `already_exposed` caveat at decision time.
- Re-confirmation evidence noted (already-filed): `trade-trace-n33z` (AX-017), `trade-trace-fe2f` (AX-018), `trade-trace-xqjv` (AX-022).

## Attempted-then-reverted

- None.

## Gate results

- `ruff check src tests` → **All checks passed!**
- `mypy src` → **Success: no issues found in 178 source files**
- `pytest -q` → **2275 passed, 7 skipped** (unchanged from prior run — no source code changed this pass; 7 skips are perf-opt-in baselines).

## Journal artifacts this run

- Bound fresh: `mkt_Weg9U9Ul6g19DJuO` (BTC $90k June), `mkt_Vh36actmypkVNBiq` (BTC $77.5k June, scouted only), `mkt_7tACn4aqhw6y8BwN` (BTC $70k June).
- Forecasts: `fc_WCWuNyYn6NWRyYzI` (BTC $90k, p_yes 0.02, tail calibration-only; thesis `th_DLsPB8fwOpE76Zrf`), `fc_OkXaSOXxG80Xu5Dl` (BTC $70k, p_yes 0.43; thesis `th_5jIeovMZ8h8izs2w`).
- Decisions: `dec_qHzNfRvzP9dY1kvv` (skip, insufficient_edge, forecast-linked, $90k); `dec_RnrT3tPpjnJGDybZ` (paper_enter YES 20 @ 0.37, $70k → `pos_dTqU6DR07HnbZHvd`).
- Snapshots: `snp_mRS77zuILvzztypZ` ($90k), `snp_c-h1tKwXENKBmn6k` ($70k), `snp_D_0DNSjpedMnKCYC` (CheQKZ live-check).
- Memory: `mem_4zRTSDdMFuzcfAJs` (cross-run exposure-accumulation lesson, about `dec_RnrT3tPpjnJGDybZ`).

## Handoff notes for next run

- **Due ~16:00Z today (2026-06-04):** `fc_uZ-WmwfdrsEtISlE` (`mkt_BxqIYenOQjh6ta1l`) and `fc_1yoUooDa7wbjasEk` (`mkt_SohHCLs3qtCWRWbT`, ETH>$1,800) both resolution_at 2026-06-04T16:00:00Z. If a run fires after 16:00Z, resolve from the venue close and **pass `confidence` explicitly** — the live `resolution.add` MCP schema still won't advertise it (AX-030 fix touches `example_rich`, effective only on a fresh server boot). Don't fabricate.
- **New exposure carried:** now **120 YES on `mkt_7tACn4aqhw6y8BwN` (BTC $70k June)** across two position rows (`pos_EmNSXb2bDMWpFdub` 100 @ 0.54 + `pos_dTqU6DR07HnbZHvd` 20 @ 0.37), marked −$18.20 against the 0.36 mid. Per AX-033/`mem_4zRTSDdMFuzcfAJs`: **check `report.open_positions` before re-entering this market** — don't pile on a third row. Both resolve 2026-07-01T04:00:00Z (touch-of-$70k by June 30).
- `fc_CheQKZ` will keep re-flagging resolve_due while live (AX-018) — expected; don't fabricate.
- Calibration stuck at **N=1** until the first near-term forecasts resolve (earliest the two ETH 16:00Z today, then the June reach-ladder on 07-01). Coverage 4.5% (1/22 decisions scored) is honest, not a bug.
- Highest-priority untouched bead remains `trade-trace-4z2t` (market.refresh, P1).
- `.beads` dir perms warning (0770) recurs on every `bd` call — cosmetic, not this loop's concern.
