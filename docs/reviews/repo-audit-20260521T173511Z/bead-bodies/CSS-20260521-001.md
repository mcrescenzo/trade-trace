## Audit candidate
- Candidate ID: `CSS-20260521-001`
- Audit run: `repo-audit-20260521T173511Z`
- Scope fingerprint: `1901b8097953e720`
- Source lane: `core-storage-security`
- Source skill/lens: `repo-bughunt`
- Matrix row: `docs/reviews/repo-audit-20260521T173511Z/candidate-matrix.json`
- Lane artifact(s): /home/hermes/code/trade-trace/docs/reviews/repo-audit-20260521T173511Z/candidate-matrix.json

## Problem
Fix SQLite read-only URI escaping for DB paths containing URI-reserved characters

## Evidence
database.py:116-149; storage/paths.py:16-30; parent execute_code probe output: ReadOnlyDatabaseError unsupported_schema and files [db, db?x.sqlite].

## Impact / carrying cost
Read-only DB open can inspect/create wrong sibling path for valid POSIX names.

## Duplicate / prior-backlog disposition
- Prior match status: `new`
- Duplicate of: `['trade-trace-l24k', 'trade-trace-d2jv', 'trade-trace-71zq']`
- Related IDs: none
- Coordinator disposition: `accept` — New exact bug; related closed path/security items do not cover SQLite URI escaping.

## Acceptance criteria
- open_database_readonly addresses the exact requested Path for ?, #, spaces, and URI-reserved characters.
- Regression asserts PRAGMA database_list points at requested DB and no truncated sibling is created.
- Existing readonly/security tests pass.

## Suggested validation
`python -m pytest tests/security/test_readonly_database.py -q`

## Boundaries
Fix only this audited root cause / coverage gap. Do not broaden into unrelated cleanup or product expansion without a separate bead.
## Steps to Reproduce
1. Create a valid Trade Trace DB at a path whose filename contains a URI-reserved delimiter such as `?` or `#`.
2. Call `open_database_readonly()` with that exact Path.
3. Observe that the current URI interpolation can make SQLite inspect/create a truncated sibling path instead of the requested DB.
