Context:
Domain: adapter-market-security. Affected surface: see evidence paths.

Current complexity:
Duplicated security-sensitive retry paths can diverge in endpoint/scrubbing/retry semantics.

Evidence:
- src/trade_trace/adapters/polymarket/client.py:get_json lines 113-147 and polygon_rpc lines 161-199 implement similar bounded retry/error/status/JSON parsing/logging/backoff loops with local GET/POST and JSON-RPC differences.

Why simplification is safe/desirable:
One private HTTP JSON helper makes security behavior easier to audit. Reduces duplicated control-flow while retaining explicit RPC specifics.

Target simplification:
Introduce _request_json helper parameterized by send function/error labels; keep JSON-RPC payload error handling in polygon_rpc after parsed payload; do not introduce new dependencies.

Non-goals:
Do not change public CLI/MCP/tool/report/API behavior.; Do not broaden the refactor beyond cited files.; Do not change storage schema, event semantics, security policy, or execution boundaries unless explicitly stated.

Behavior preservation:
Disabled-by-default behavior, HTTPS/host/suffix endpoint validation before client use, retry bounds/backoff/Retry-After, timeout vs transport vs HTTP error codes/messages, invalid JSON handling, JSON-RPC error handling, scrubbed endpoint details, and no body logging must remain unchanged. Characterize current behavior before editing. Preserve current response/envelope keys, error codes/messages/details, ordering, idempotency, and security constraints for the cited surface.

Risks / intentional complexity check:
Preserve explicitness where it encodes compatibility, public contract, security, audit, idempotency, or historical data semantics.

Validation:
- python -m pytest tests/security/test_adapter_endpoint_policy.py tests/security/test_adapter_url_scrubbing.py tests/integration/test_adapter_polymarket_offline.py -q

Acceptance criteria:
- Current behavior is characterized before refactor.
- Simplification is limited to the cited surface and no unrelated behavior changes are made.
- Listed validation passes or any gap is resolved before close.
- Implementation notes document why intentional complexity was preserved.

Provenance:
Discovered by repo-simplification-review candidate AMS-002 in domain adapter-market-security. Matrix: docs/audits/simplification-20260525T173157Z/candidate-matrix.json
