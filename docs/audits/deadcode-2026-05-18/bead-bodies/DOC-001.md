Context:
Domain: packaging-ci-docs
Affected surface: local markdown links
Candidate: DOC-001 from exhaustive deadcode hunt epic trade-trace-5lx.

Dead-code claim:
Fix broken local markdown links after docs path moves

Evidence:
- Paths: README.md, docs/PRD.md, docs/architecture/*.md
- markdown-link-check.json found missing local targets: README links ./VISION.md/./PRD.md though actual files are docs/VISION.md/docs/PRD.md; docs/PRD.md links ./docs/architecture/... resolving to docs/docs/...; docs/architecture links ../../PRD.md/../../VISION.md resolving to repo root.
- Evidence artifacts: docs/audits/deadcode-2026-05-18/candidate-matrix.json, lane-packets.md, advisor-review.md.

Reference search scope:
tracked README.md and docs/**/*.md

Reference search commands / output summary:
python3 markdown local-link checker saved to markdown-link-check.json

Entrypoint / public / dynamic checks:
- Dynamic-loading caveats: docs-only; no runtime dynamic caveat
- Public/API surface: public docs contract
- Generated/reflection risk: none

Why it may be falsely alive:
If rendered by a docs host with nonstandard path rewriting, local checker may overstate; raw GitHub/local markdown links are broken.

Impact / risk of keeping:
Misleads users/agents and breaks navigation through product contract docs.

Recommended action:
remove

Safe-removal validation:
rerun markdown local-link checker and verify missing_count for edited links drops to zero or only intentional anchors remain.

Duplicate check:
Compared against open Beads snapshot and duplicate scan captured in docs/audits/deadcode-2026-05-18/pre-mutation-snapshot.txt. Duplicate/related notes: duplicate_of=None; related_to=[]. This bead is materialized because coordinator disposition is confirmed and advisor review approved this class of materialization.

Acceptance criteria:
- README and docs local links resolve from their file locations.
- Link checker artifact updated or equivalent verification passes.

Provenance:
Discovered by repo-deadcode-hunt candidate DOC-001 in domain packaging-ci-docs. Labels expected: dead-code, deadcode-hunt, deadcode:exhaustive-20260518, domain:docs, bug, docs-truth, risk:stale-contract.

## Steps to Reproduce
1. Inspect the cited files/artifacts above.
2. Re-run the validation/readback command(s): rerun markdown local-link checker and verify missing_count for edited links drops to zero or only intentional anchors remain.
3. Observe the stale/missing docs/tool/package contract described in the evidence section.

## Acceptance Criteria
- README and docs local links resolve from their file locations.
- Link checker artifact updated or equivalent verification passes.
