# Lane packet: docs-packaging-ci-ops

Inspected README, docs, pyproject, CI, config, CLI catalog. Commands: link checker, pytest collect-only, CLI registry inspection. Candidates: missing SECRET_PATTERNS blocks CI; broken local links; non-existent tt config set docs; non-existent tt init/tt mcp docs.

Side effects: no intentional repo edits; probes used temporary directories. See primary_evidence.txt for coordinator proof snippets.
